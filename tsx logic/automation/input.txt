```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene51PassiveTrap',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#E8823A';
const BG2 = '#8F4310';
const ORG = '#FF9B51';

const Scene51PassiveTrap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.07], { easing: Easing.inOut(Easing.ease) });
  const seesaw = Math.sin(frame * 0.035) * 5;
  const strike = interpolate(frame, [55, 80], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const shake = frame > 80 && frame < 90 ? Math.sin(frame * 2.8) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: BG2, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at 50% 30%, ${BG1} 0%, ${BG2} 78%)` }} />

      {/* Shape layers — slow drifting transparent geometry */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 4);
        return (
          <div key={i} style={{
            position: 'absolute', left: r * 1500 - 150, top: seededRandom(i + 14) * 800 - 100,
            width: 320 + r * 340, height: 320 + r * 340,
            border: `4px solid ${INK}`, opacity: 0.06,
            borderRadius: i % 2 ? '50%' : 0,
            transform: `rotate(${frame * 0.12 * (i % 2 ? 1 : -1) + r * 90}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.05})`,
          }} />
        );
      })}

      {/* Motion texture — diagonal punk stripes */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(45deg, ${INK} 0 2px, transparent 2px 52px)`, opacity: 0.06, transform: `translateX(${(frame * 0.4) % 52}px)` }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const speed = 0.5 + seededRandom(i + 60) * 0.9;
        const y = 1140 - ((seededRandom(i + 90) * 1080 + frame * speed) % 1200);
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 7) * 1920, top: y, width: 5 + seededRandom(i + 30) * 7, height: 5 + seededRandom(i + 30) * 7, background: i % 3 === 0 ? YEL : INK, opacity: 0.25, transform: `rotate(${frame * 2 + i * 30}deg)` }} />;
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)` }}>

        {/* HERO — PASSIVE struck out, TRAP stamped */}
        <div style={{ position: 'absolute', top: 90, left: 120, transform: `translateY(${(1 - sp(8)) * -140}px)`, opacity: Math.min(1, sp(8) * 1.4) }}>
          <div style={{ position: 'relative', display: 'inline-block', background: INK, padding: '10px 44px', boxShadow: '16px 16px 0 rgba(22,33,42,0.85)', transform: `rotate(-2deg) translateY(${Math.sin(frame * 0.05) * 3}px)` }}>
            <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 158, color: ORG, letterSpacing: -4 }}>PASSIVE</span>
            <div style={{ position: 'absolute', top: '52%', left: 24, height: 14, width: `${strike}%`, background: YEL, transform: 'rotate(-3deg)' }} />
          </div>
          <div style={{ marginTop: 26, transform: `rotate(1.5deg) scale(${sp(85)})`, display: 'inline-block', background: YEL, padding: '6px 36px', boxShadow: `12px 12px 0 ${INK}` }}>
            <span style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 110, color: INK }}>= TRAP</span>
          </div>
          <div style={{ marginTop: 30, opacity: sp(120), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 46, fontWeight: 700, color: '#FFE9CF', maxWidth: 760 }}>
            सिर्फ EMI भरना + RATE CUT का इंतज़ार = UNCERTAINTY का शिकार
          </div>
        </div>

        {/* SEESAW infographic — RBI equilibrium */}
        <div style={{ position: 'absolute', right: 120, top: 300, width: 720, transform: `scale(${sp(150)})`, transformOrigin: 'center bottom' }}>
          <div style={{ transform: `rotate(${seesaw}deg)`, transformOrigin: '50% 100%' }}>
            <div style={{ width: 700, height: 16, background: INK, boxShadow: `8px 8px 0 rgba(22,33,42,0.7)` }} />
            <div style={{ position: 'absolute', left: -10, top: -108, background: YEL, border: `5px solid ${INK}`, padding: '16px 22px', boxShadow: `8px 8px 0 ${INK}`, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 34, color: INK, opacity: sp(185) }}>CREDIT GROWTH</div>
            <div style={{ position: 'absolute', right: -10, top: -108, background: '#FFE9CF', border: `5px solid ${INK}`, padding: '16px 22px', boxShadow: `8px 8px 0 ${INK}`, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 34, color: INK, opacity: sp(205) }}>LIQUIDITY</div>
          </div>
          <svg width="140" height="120" viewBox="0 0 140 120" style={{ display: 'block', margin: '0 auto' }}>
            <polygon points="70,0 130,110 10,110" fill={INK} />
            <text x="70" y="92" textAnchor="middle" fontFamily="Inter, sans-serif" fontWeight="900" fontSize="30" fill={YEL}>RBI</text>
          </svg>
          <div style={{ marginTop: 24, textAlign: 'center', opacity: sp(250), background: INK, padding: '18px 26px', transform: 'rotate(-1deg)', boxShadow: `10px 10px 0 rgba(22,33,42,0.7)` }}>
            <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 800, color: YEL }}>GOAL = EQUILIBRIUM + INFLATION CONTROL</span>
          </div>
        </div>

        {/* Bottom ticker — micro marquee */}
        <div style={{ position: 'absolute', bottom: 90, left: 0, width: '100%', height: 74, background: INK, overflow: 'hidden', transform: 'rotate(-0.8deg)', opacity: interpolate(frame, [300, 330], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <div style={{ whiteSpace: 'nowrap', transform: `translateX(${-((frame * 3.4) % 1400)}px)`, fontFamily: 'JetBrains Mono, monospace', fontSize: 40, fontWeight: 700, color: ORG, lineHeight: '74px' }}>
            {Array.from({ length: 8 }).map((_, i) => (
              <span key={i}>SUSTAINABLE LENDING <span style={{ color: YEL }}>///</span> INFLATION CONTROL <span style={{ color: YEL }}>///</span> </span>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(24,13,3,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 90], [0.7, 0.3], { extrapolateRight: 'clamp' }) }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene51PassiveTrap;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene52PriorityList',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#E0script7A32';
const ORG = '#FF9B51';

const Scene52PriorityList: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camX = interpolate(frame, [0, durationInFrames], [-30, 30], { easing: Easing.inOut(Easing.ease) });
  const stampShake = frame > 170 && frame < 180 ? Math.sin(frame * 3) * 7 : 0;
  const arrowY = (frame * 2.2) % 44;

  const items = ['01 MACRO STABILITY', '02 INFLATION CONTROL', '03 SUSTAINABLE LENDING', '04 LIQUIDITY BALANCE'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#8F4310', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill style={{ background: `linear-gradient(160deg, #E8823A 0%, #A5551B 60%, #7C3B0D 100%)` }} />

      {/* Shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 9);
        return <div key={i} style={{ position: 'absolute', left: r * 1600 - 200, top: seededRandom(i + 21) * 900 - 150, width: 420 + r * 300, height: 420 + r * 300, background: INK, opacity: 0.05, transform: `rotate(${45 + frame * 0.08 * (i % 2 ? 1 : -1)}deg) scale(${1 + Math.sin(frame * 0.018 + i * 2) * 0.06})` }} />;
      })}

      {/* Motion texture — halftone dot matrix drifting */}
      <AbsoluteFill style={{ backgroundImage: `radial-gradient(${INK} 1.8px, transparent 1.8px)`, backgroundSize: '36px 36px', opacity: 0.08, transform: `translate(${(frame * 0.25) % 36}px, ${(frame * 0.18) % 36}px)` }} />

      {/* Particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const speed = 0.4 + seededRandom(i + 44) * 0.8;
        const y = 1140 - ((seededRandom(i + 77) * 1080 + frame * speed) % 1200);
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 11) * 1920, top: y, width: 10, height: 3, background: i % 2 ? YEL : '#FFE9CF', opacity: 0.3, transform: `rotate(${45 + frame}deg)` }} />;
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>

        {/* LEFT — RBI priority list card */}
        <div style={{ position: 'absolute', left: 130, top: 110, width: 760, background: '#FFE9CF', border: `7px solid ${INK}`, boxShadow: `20px 20px 0 ${INK}`, transform: `rotate(-1.4deg) translateY(${(1 - sp(6)) * 400}px)` }}>
          <div style={{ background: INK, padding: '20px 34px' }}>
            <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 56, color: YEL, letterSpacing: 2 }}>RBI PRIORITY LIST</span>
          </div>
          {items.map((t, i) => (
            <div key={i} style={{ display: 'flex', alignItems: 'center', gap: 22, padding: '20px 34px', borderBottom: `4px solid ${INK}`, opacity: sp(45 + i * 18), transform: `translateX(${(1 - sp(45 + i * 18)) * -180}px)` }}>
              <div style={{ width: 22, height: 22, background: INK, transform: `rotate(${frame * 1.5}deg)` }} />
              <span style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 42, color: INK }}>{t}</span>
            </div>
          ))}
          <div style={{ padding: '18px 34px', fontFamily: 'JetBrains Mono, monospace', fontSize: 40, color: INK, opacity: 0.45 }}>. . . . . . . . . . . . . . .</div>
          <div style={{ margin: '4px 26px 30px', background: ORG, border: `6px solid ${INK}`, padding: '18px 28px', transform: `rotate(${-2 + stampShake * 0.3}deg) scale(${sp(160, { stiffness: 200 })}) translateX(${stampShake}px)`, boxShadow: `10px 10px 0 ${INK}` }}>
            <span style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 58, color: INK }}>#99 — आपकी EMI</span>
          </div>
        </div>

        {/* RIGHT — ACTION 01 download card */}
        <div style={{ position: 'absolute', right: 120, top: 190, width: 620, transform: `scale(${sp(230)})`, transformOrigin: 'center' }}>
          <div style={{ position: 'absolute', top: -26, left: 40, width: 190, height: 46, background: YEL, opacity: 0.92, transform: 'rotate(-7deg)', border: `3px solid ${INK}` }} />
          <div style={{ background: INK, border: `7px solid ${INK}`, boxShadow: `18px 18px 0 rgba(22,33,42,0.75)`, padding: '40px 44px', transform: `rotate(1.6deg) translateY(${Math.sin(frame * 0.06) * 4}px)` }}>
            <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 40, color: ORG, letterSpacing: 3 }}>WEEKEND ACTION 01</div>
            <div style={{ marginTop: 14, fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 52, color: YEL, lineHeight: 1.2 }}>AMORTIZATION SCHEDULE DOWNLOAD करो</div>
            <svg width="180" height="200" viewBox="0 0 180 200" style={{ marginTop: 26 }}>
              <rect x="30" y="10" width="120" height="150" fill="#FFE9CF" stroke={INK} strokeWidth="6" />
              <line x1="50" y1="45" x2="130" y2="45" stroke={INK} strokeWidth="8" />
              <line x1="50" y1="75" x2="130" y2="75" stroke={INK} strokeWidth="8" />
              <line x1="50" y1="105" x2="110" y2="105" stroke={INK} strokeWidth="8" />
              <g transform={`translate(0, ${arrowY * 0.5})`}>
                <polygon points="90,150 120,150 120,120 60,120 60,150" fill={YEL} opacity="0" />
                <polygon points="70,140 110,140 110,165 130,165 90,195 50,165 70,165" fill={YEL} stroke={INK} strokeWidth="5" />
              </g>
            </svg>
            <div style={{ marginTop: 20, fontFamily: 'JetBrains Mono, monospace', fontSize: 33, fontWeight: 700, color: '#FFE9CF' }}>NET BANKING / BANK APP</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(24,13,3,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 90], [0.7, 0.3], { extrapolateRight: 'clamp' }) }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene52PriorityList;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene53SurplusFormula',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const CREAM = '#FFE9CF';

const Scene53SurplusFormula: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const barFill = interpolate(frame, [40, 130], [0, 72], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const alarmPulse = frame > 140 ? 1 + Math.sin(frame * 0.35) * 0.06 : 0;
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1], { easing: Easing.inOut(Easing.ease) });
  const dashOff = -(frame * 3) % 40;

  const formulaParts = [
    { t: 'INCOME', bg: YEL, d: 210 },
    { t: '−', bg: 'none', d: 230 },
    { t: 'EXPENSES', bg: CREAM, d: 250 },
    { t: '−', bg: 'none', d: 270 },
    { t: 'EMERGENCY FUND', bg: CREAM, d: 290 },
    { t: '=', bg: 'none', d: 320 },
    { t: 'SURPLUS', bg: ORG, d: 340 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#8F4310', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at 20% 20%, #E8823A 0%, #99501A 65%, #7C3B0D 100%)` }} />

      {/* Shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 6);
        return <div key={i} style={{ position: 'absolute', right: r * 1400 - 200, top: seededRandom(i + 33) * 900 - 200, width: 380 + r * 380, height: 380 + r * 380, border: `4px dashed ${INK}`, borderRadius: '50%', opacity: 0.07, transform: `rotate(${frame * 0.2 * (i % 2 ? 1 : -1)}deg)` }} />;
      })}

      {/* Motion texture — vertical scanlines */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(90deg, ${INK} 0 2px, transparent 2px 70px)`, opacity: 0.05, transform: `translateX(${(frame * 0.5) % 70}px)` }} />

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const speed = 0.5 + seededRandom(i + 24) * 0.7;
        const y = 1140 - ((seededRandom(i + 55) * 1080 + frame * speed) % 1200);
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 3) * 1920, top: y, width: 6, height: 6, borderRadius: i % 2 ? '50%' : 0, background: i % 3 === 0 ? YEL : INK, opacity: 0.28 }} />;
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* TOP — interest vs principal bar */}
        <div style={{ position: 'absolute', top: 100, left: 140, width: 1640, transform: `translateY(${(1 - sp(5)) * -200}px)` }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 60, color: CREAM, marginBottom: 24 }}>
            हर महीने चेक करो — <span style={{ background: INK, color: YEL, padding: '2px 20px' }}>INTEREST vs PRINCIPAL</span>
          </div>
          <div style={{ width: '100%', height: 110, background: CREAM, border: `7px solid ${INK}`, boxShadow: `14px 14px 0 ${INK}`, position: 'relative', overflow: 'hidden' }}>
            <div style={{ width: `${barFill}%`, height: '100%', background: `repeating-linear-gradient(45deg, ${INK} 0 24px, #31434F 24px 48px)`, display: 'flex', alignItems: 'center' }}>
              <span style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 52, color: YEL, paddingLeft: 30, whiteSpace: 'nowrap' }}>INTEREST {Math.round(barFill)}%</span>
            </div>
            <span style={{ position: 'absolute', right: 26, top: 26, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 44, color: INK }}>PRINCIPAL</span>
          </div>
          <div style={{ marginTop: 22, display: 'inline-block', background: YEL, border: `6px solid ${INK}`, padding: '12px 30px', transform: `rotate(-2deg) scale(${frame > 140 ? alarmPulse : sp(140)})`, boxShadow: `10px 10px 0 ${INK}`, opacity: frame > 130 ? 1 : 0 }}>
            <span style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 62, color: INK }}>70%+ = ALARM BELL</span>
          </div>
        </div>

        {/* MIDDLE — surplus formula typography */}
        <div style={{ position: 'absolute', top: 560, left: 140, width: 1640, display: 'flex', flexWrap: 'wrap', gap: 26, alignItems: 'center' }}>
          {formulaParts.map((p, i) => (
            <div key={i} style={{ transform: `scale(${sp(p.d, { stiffness: 180 })}) rotate(${(i % 2 ? 1 : -1) * 1.2}deg)`, background: p.bg === 'none' ? 'transparent' : p.bg, border: p.bg === 'none' ? 'none' : `6px solid ${INK}`, boxShadow: p.bg === 'none' ? 'none' : `10px 10px 0 ${INK}`, padding: p.bg === 'none' ? 0 : '14px 30px' }}>
              <span style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: p.bg === 'none' ? 90 : 56, color: p.bg === 'none' ? YEL : INK }}>{p.t}</span>
            </div>
          ))}
        </div>

        {/* BOTTOM — 20-30% earmark arrow */}
        <div style={{ position: 'absolute', bottom: 130, left: 140, display: 'flex', alignItems: 'center', gap: 30, opacity: interpolate(frame, [400, 430], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <div style={{ background: INK, padding: '16px 34px', boxShadow: `12px 12px 0 rgba(22,33,42,0.7)` }}>
            <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 64, color: ORG }}>SURPLUS का 20–30%</span>
          </div>
          <svg width="220" height="60" viewBox="0 0 220 60">
            <line x1="0" y1="30" x2="170" y2="30" stroke={YEL} strokeWidth="10" strokeDasharray="22 18" strokeDashoffset={dashOff} />
            <polygon points="170,10 220,30 170,50" fill={YEL} />
          </svg>
          <div style={{ background: YEL, border: `6px solid ${INK}`, padding: '16px 34px', boxShadow: `12px 12px 0 ${INK}`, transform: `rotate(1.5deg) translateY(${Math.sin(frame * 0.07) * 4}px)` }}>
            <span style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 64, color: INK }}>PREPAYMENT</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(24,13,3,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 90], [0.7, 0.3], { extrapolateRight: 'clamp' }) }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene53SurplusFormula;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene54CibilGauge',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const CREAM = '#FFE9CF';

const Scene54CibilGauge: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const needleDeg = interpolate(sp(70, { damping: 9, stiffness: 70 }), [0, 1], [-86, 52]);
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08], { easing: Easing.inOut(Easing.ease) });
  const warnBlink = frame > 420 ? (Math.sin(frame * 0.4) > 0 ? 1 : 0.35) : 1;

  const zones = [
    { label: '<700', sub: 'REJECTION RISK', bg: INK, fg: ORG, d: 200 },
    { label: '725', sub: 'MINIMUM ENTRY', bg: CREAM, fg: INK, d: 225 },
    { label: '750+', sub: 'STRONG POSITION', bg: YEL, fg: INK, d: 250 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#8F4310', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill style={{ background: `linear-gradient(200deg, #E8823A 0%, #A5551B 55%, #77380C 100%)` }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 17);
        return <div key={i} style={{ position: 'absolute', left: r * 1700 - 180, top: seededRandom(i + 41) * 950 - 180, width: 300 + r * 320, height: 300 + r * 320, border: `5px solid ${i % 2 ? YEL : INK}`, opacity: 0.05, transform: `rotate(${frame * 0.1 + i * 45}deg)`, borderRadius: i === 2 ? '50%' : 0 }} />;
      })}

      {/* Motion texture — plus-sign grid */}
      <AbsoluteFill style={{ backgroundImage: `radial-gradient(circle, transparent 0, transparent 100%), repeating-linear-gradient(0deg, ${INK} 0 2px, transparent 2px 60px), repeating-linear-gradient(90deg, ${INK} 0 2px, transparent 2px 60px)`, opacity: 0.05, transform: `translate(${(frame * 0.2) % 60}px, ${(frame * 0.3) % 60}px)` }} />

      {/* Particles */}
      {Array.from({ length: 13 }).map((_, i) => {
        const speed = 0.45 + seededRandom(i + 66) * 0.85;
        const y = 1140 - ((seededRandom(i + 88) * 1080 + frame * speed) % 1200);
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 13) * 1920, top: y, width: 7, height: 7, background: i % 4 === 0 ? YEL : CREAM, opacity: 0.25, transform: `rotate(${frame * 1.2}deg)` }} />;
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Title */}
        <div style={{ position: 'absolute', top: 80, left: 130, transform: `translateX(${(1 - sp(5)) * -400}px)` }}>
          <div style={{ display: 'inline-block', background: INK, padding: '12px 40px', boxShadow: `16px 16px 0 rgba(22,33,42,0.8)`, transform: 'rotate(-1.5deg)' }}>
            <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 108, color: YEL, letterSpacing: -2 }}>CIBIL SCOREBOARD</span>
          </div>
        </div>

        {/* Gauge SVG */}
        <div style={{ position: 'absolute', left: 170, top: 290, transform: `scale(${sp(40)})`, transformOrigin: 'center' }}>
          <svg width="760" height="470" viewBox="0 0 760 470">
            <path d="M 80 420 A 300 300 0 0 1 268 141" fill="none" stroke={INK} strokeWidth="58" />
            <path d="M 268 141 A 300 300 0 0 1 500 143" fill="none" stroke={CREAM} strokeWidth="58" />
            <path d="M 500 143 A 300 300 0 0 1 680 420" fill="none" stroke={YEL} strokeWidth="58" />
            <text x="120" y="200" fontFamily="JetBrains Mono, monospace" fontWeight="800" fontSize="42" fill={CREAM}>700</text>
            <text x="352" y="80" fontFamily="JetBrains Mono, monospace" fontWeight="800" fontSize="42" fill={CREAM}>725</text>
            <text x="580" y="200" fontFamily="JetBrains Mono, monospace" fontWeight="800" fontSize="42" fill={CREAM}>750+</text>
            <g transform={`rotate(${needleDeg} 380 420)`}>
              <polygon points="372,420 388,420 380,150" fill={INK} />
              <circle cx="380" cy="420" r="34" fill={INK} stroke={YEL} strokeWidth="6" />
            </g>
          </svg>
        </div>

        {/* Zone cards */}
        <div style={{ position: 'absolute', right: 120, top: 300, width: 640, display: 'flex', flexDirection: 'column', gap: 30 }}>
          {zones.map((z, i) => (
            <div key={i} style={{ background: z.bg, border: `6px solid ${INK}`, boxShadow: `14px 14px 0 ${INK}`, padding: '22px 32px', transform: `rotate(${(i % 2 ? 1 : -1) * 1.2}deg) translateX(${(1 - sp(z.d)) * 500}px)`, display: 'flex', alignItems: 'center', gap: 28 }}>
              <span style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 72, color: z.fg }}>{z.label}</span>
              <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 800, fontSize: 38, color: z.fg }}>{z.sub}</span>
            </div>
          ))}
        </div>

        {/* Bottom warning strip */}
        <div style={{ position: 'absolute', bottom: 100, left: 130, right: 130, background: YEL, border: `7px solid ${INK}`, boxShadow: `16px 16px 0 ${INK}`, padding: '22px 40px', transform: `rotate(-0.8deg) scale(${sp(400, { stiffness: 200 })})`, opacity: warnBlink }}>
          <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 56, color: INK }}>
            ACTION 04: RATE CONFIRM करो — <span style={{ background: INK, color: YEL, padding: '0 18px' }}>8.5%+ = WARNING</span> — MARKET से COMPARE करो
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(24,13,3,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 90], [0.7, 0.3], { extrapolateRight: 'clamp' }) }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene54CibilGauge;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene55TenureVsEmi',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const CREAM = '#FFE9CF';

const Scene55TenureVsEmi: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [20, -20], { easing: Easing.inOut(Easing.ease) });
  const stampIn = sp(430, { damping: 8, stiffness: 240 });
  const vsWobble = Math.sin(frame * 0.09) * 4;

  return (
    <AbsoluteFill style={{ backgroundColor: '#8F4310', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at 80% 15%, #E8823A 0%, #9E5219 60%, #7C3B0D 100%)` }} />

      {/* Shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 27);
        return <div key={i} style={{ position: 'absolute', left: r * 1500, top: seededRandom(i + 51) * 800, width: 0, height: 0, borderLeft: `${180 + r * 160}px solid transparent`, borderRight: `${180 + r * 160}px solid transparent`, borderBottom: `${300 + r * 260}px solid ${INK}`, opacity: 0.05, transform: `rotate(${frame * 0.09 * (i % 2 ? 1 : -1) + r * 120}deg)` }} />;
      })}

      {/* Motion texture — concentric ring blueprint */}
      <AbsoluteFill style={{ backgroundImage: `repeating-radial-gradient(circle at 50% 120%, ${INK} 0 2px, transparent 2px 90px)`, opacity: 0.06, transform: `scale(${1 + Math.sin(frame * 0.01) * 0.03})` }} />

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const speed = 0.4 + seededRandom(i + 35) * 0.9;
        const y = 1140 - ((seededRandom(i + 71) * 1080 + frame * speed) % 1200);
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 19) * 1920, top: y, width: 5 + seededRandom(i + 5) * 6, height: 5 + seededRandom(i + 5) * 6, background: i % 3 ? INK : YEL, opacity: 0.26, transform: `rotate(${frame * 1.8 + i * 24}deg)` }} />;
      })}

      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>

        {/* TOP — emergency fund 6 blocks */}
        <div style={{ position: 'absolute', top: 80, left: 140, right: 140 }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 54, color: CREAM, marginBottom: 20, opacity: sp(5) }}>
            RULE ZERO — पहले <span style={{ background: YEL, color: INK, padding: '0 16px', border: `4px solid ${INK}` }}>6 MONTHS EMERGENCY FUND</span> LIQUID
          </div>
          <div style={{ display: 'flex', gap: 20 }}>
            {Array.from({ length: 6 }).map((_, i) => (
              <div key={i} style={{ width: 240, height: 76, background: sp(40 + i * 14) > 0.5 ? YEL : CREAM, border: `5px solid ${INK}`, boxShadow: `8px 8px 0 ${INK}`, transform: `scale(${sp(40 + i * 14, { stiffness: 200 })}) rotate(${(i % 2 ? 1 : -1) * 1.5}deg)`, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                <span style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 40, color: INK }}>M{i + 1}</span>
              </div>
            ))}
          </div>
        </div>

        {/* MIDDLE — two brutalist cards VS */}
        <div style={{ position: 'absolute', top: 340, left: 140, width: 740, background: YEL, border: `8px solid ${INK}`, boxShadow: `22px 22px 0 ${INK}`, padding: '36px 40px', transform: `rotate(-1.6deg) translateY(${(1 - sp(160)) * 600}px)` }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 74, color: INK }}>TENURE REDUCTION</div>
          <svg width="90" height="90" viewBox="0 0 90 90" style={{ marginTop: 16 }}>
            <rect x="4" y="4" width="82" height="82" fill={INK} />
            <path d="M 20 48 L 40 68 L 72 26" stroke={YEL} strokeWidth="12" fill="none" strokeDasharray="120" strokeDashoffset={interpolate(frame, [220, 260], [120, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} />
          </svg>
          <div style={{ marginTop: 18, fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 700, fontSize: 44, color: INK, lineHeight: 1.35 }}>
            PRINCIPAL FASTER घटेगा<br />TOTAL INTEREST ज्यादा बचेगा
          </div>
          <div style={{ position: 'absolute', top: -46, right: -36, background: ORG, border: `6px solid ${INK}`, padding: '10px 26px', transform: `rotate(8deg) scale(${stampIn})`, boxShadow: `8px 8px 0 ${INK}` }}>
            <span style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 54, color: INK }}>चुनो यह</span>
          </div>
        </div>

        <div style={{ position: 'absolute', top: 560, left: 905, transform: `rotate(${vsWobble}deg) scale(${sp(280, { stiffness: 240 })})`, background: INK, width: 130, height: 130, borderRadius: '50%', display: 'flex', alignItems: 'center', justifyContent: 'center', border: `6px solid ${YEL}`, zIndex: 5 }}>
          <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 56, color: YEL }}>VS</span>
        </div>

        <div style={{ position: 'absolute', top: 340, right: 140, width: 700, background: CREAM, border: `8px solid ${INK}`, boxShadow: `22px 22px 0 ${INK}`, padding: '36px 40px', transform: `rotate(1.6deg) translateY(${(1 - sp(200)) * 600}px)`, opacity: 0.92 }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 74, color: INK, textDecoration: 'line-through', textDecorationThickness: 8 }}>EMI REDUCTION</div>
          <svg width="90" height="90" viewBox="0 0 90 90" style={{ marginTop: 16 }}>
            <rect x="4" y="4" width="82" height="82" fill={INK} />
            <line x1="24" y1="24" x2="66" y2="66" stroke={ORG} strokeWidth="12" strokeDasharray="60" strokeDashoffset={interpolate(frame, [300, 330], [60, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} />
            <line x1="66" y1="24" x2="24" y2="66" stroke={ORG} strokeWidth="12" strokeDasharray="60" strokeDashoffset={interpolate(frame, [330, 360], [60, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} />
          </svg>
          <div style={{ marginTop: 18, fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 700, fontSize: 44, color: INK, lineHeight: 1.35 }}>
            CASH FLOW थोड़ा improve<br />लेकिन TOTAL INTEREST ज्यादा PAY
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(24,13,3,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 90], [0.7, 0.3], { extrapolateRight: 'clamp' }) }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene55TenureVsEmi;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene56ReverseCompounding',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const CREAM = '#FFE9CF';

const Scene56ReverseCompounding: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const counter = Math.round(interpolate(frame, [150, 240], [0, 300000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1], { easing: Easing.inOut(Easing.ease) });
  const flowDash = -(frame * 4) % 48;
  const badgeSpin = Math.sin(frame * 0.06) * 8;
  const slam = sp(430, { damping: 9, stiffness: 260 });
  const slamShake = frame > 438 && frame < 448 ? Math.sin(frame * 3) * 7 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#8F4310', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at 50% 80%, #E8823A 0%, #9A5018 60%, #733609 100%)` }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 8);
        return <div key={i} style={{ position: 'absolute', left: r * 1700 - 150, top: seededRandom(i + 28) * 900 - 150, width: 260 + r * 420, height: 260 + r * 420, border: `4px solid ${INK}`, borderRadius: i % 2 ? '50%' : 0, opacity: 0.06, transform: `rotate(${frame * 0.14 * (i % 2 ? -1 : 1)}deg) scale(${1 + Math.sin(frame * 0.022 + i) * 0.05})` }} />;
      })}

      {/* Motion texture — rupee-flow diagonal lines */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(-30deg, ${INK} 0 3px, transparent 3px 80px)`, opacity: 0.05, transform: `translateY(${(frame * 0.6) % 80}px)` }} />

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const speed = 0.5 + seededRandom(i + 42) * 1;
        const y = 1140 - ((seededRandom(i + 63) * 1080 + frame * speed) % 1200);
        return <span key={i} style={{ position: 'absolute', left: seededRandom(i + 2) * 1920, top: y, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 22 + seededRandom(i + 9) * 14, color: i % 3 === 0 ? YEL : INK, opacity: 0.22, transform: `rotate(${(seededRandom(i) - 0.5) * 40}deg)` }}>₹</span>;
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${slamShake}px)` }}>

        {/* 1L card -> arrow x3 -> 3L card */}
        <div style={{ position: 'absolute', top: 250, left: 130, display: 'flex', alignItems: 'center', gap: 44 }}>
          <div style={{ background: CREAM, border: `7px solid ${INK}`, boxShadow: `16px 16px 0 ${INK}`, padding: '46px 44px', transform: `rotate(-2deg) scale(${sp(15)})` }}>
            <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 800, fontSize: 36, color: INK, letterSpacing: 3 }}>AAJ PREPAY</div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 96, color: INK }}>₹1,00,000</div>
          </div>

          <div style={{ position: 'relative', transform: `scale(${sp(80)})` }}>
            <svg width="300" height="120" viewBox="0 0 300 120">
              <line x1="0" y1="60" x2="230" y2="60" stroke={YEL} strokeWidth="14" strokeDasharray="26 22" strokeDashoffset={flowDash} />
              <polygon points="230,26 300,60 230,94" fill={YEL} />
            </svg>
            <div style={{ position: 'absolute', top: -74, left: 90, background: INK, borderRadius: '50%', width: 120, height: 120, display: 'flex', alignItems: 'center', justifyContent: 'center', border: `6px solid ${YEL}`, transform: `rotate(${badgeSpin}deg) scale(${sp(110, { stiffness: 220 })})` }}>
              <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 52, color: YEL }}>×3</span>
            </div>
          </div>

          <div style={{ background: YEL, border: `8px solid ${INK}`, boxShadow: `20px 20px 0 ${INK}`, padding: '54px 52px', transform: `rotate(2deg) scale(${sp(140)}) translateY(${Math.sin(frame * 0.05) * 5}px)` }}>
            <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 800, fontSize: 36, color: INK, letterSpacing: 3 }}>FUTURE INTEREST SAVED</div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 118, color: INK }}>₹{counter.toLocaleString('en-IN')}</div>
          </div>
        </div>

        {/* Sub-line */}
        <div style={{ position: 'absolute', top: 640, left: 130, opacity: sp(280), transform: `translateY(${(1 - sp(280)) * 60}px)` }}>
          <div style={{ display: 'inline-block', background: INK, padding: '16px 36px', transform: 'rotate(-1deg)', boxShadow: `12px 12px 0 rgba(22,33,42,0.75)` }}>
            <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 56, color: ORG }}>REVERSE COMPOUNDING — इस बार <span style={{ color: YEL }}>आपके FAVOUR में</span></span>
          </div>
        </div>

        {/* Final slam */}
        <div style={{ position: 'absolute', bottom: 110, left: 130, right: 130, textAlign: 'center', transform: `scale(${slam})`, opacity: Math.min(1, slam * 1.3) }}>
          <div style={{ display: 'inline-block', background: ORG, border: `8px solid ${INK}`, boxShadow: `18px 18px 0 ${INK}`, padding: '20px 50px', transform: 'rotate(-1.2deg)' }}>
            <span style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 76, color: INK }}>सबसे बड़ा WEALTH DESTROYER = FINANCIAL LITERACY की कमी</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(24,13,3,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 90], [0.7, 0.3], { extrapolateRight: 'clamp' }) }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene56ReverseCompounding;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene57SpendBars',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const CREAM = '#FFE9CF';

const Scene57SpendBars: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.09], { easing: Easing.inOut(Easing.ease) });
  const zeroBlink = frame > 260 ? (Math.sin(frame * 0.32) > -0.3 ? 1 : 0.3) : 0;

  const bars = [
    { label: 'EDUCATION', tag: 'लाखों ₹', h: 420, d: 70, bg: CREAM },
    { label: 'HEALTH', tag: 'हज़ारों ₹', h: 300, d: 105, bg: CREAM },
    { label: 'ENTERTAINMENT', tag: 'हज़ारों ₹', h: 250, d: 140, bg: CREAM },
    { label: 'FINANCIAL EDUCATION', tag: 'ZERO', h: 16, d: 190, bg: YEL },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#8F4310', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill style={{ background: `linear-gradient(180deg, #D97831 0%, #A5551B 55%, #6F3308 100%)` }} />

      {/* Shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 12);
        return <div key={i} style={{ position: 'absolute', left: r * 1600 - 100, top: seededRandom(i + 48) * 800 - 100, width: 340 + r * 360, height: 340 + r * 360, border: `4px solid ${YEL}`, opacity: 0.05, transform: `rotate(${frame * 0.11 + i * 60}deg)` }} />;
      })}

      {/* Motion texture — graph-paper grid */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(0deg, ${INK} 0 2px, transparent 2px 64px), repeating-linear-gradient(90deg, ${INK} 0 2px, transparent 2px 64px)`, opacity: 0.06, transform: `translateY(${(frame * 0.35) % 64}px)` }} />

      {/* Particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const speed = 0.45 + seededRandom(i + 53) * 0.8;
        const y = 1140 - ((seededRandom(i + 82) * 1080 + frame * speed) % 1200);
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 16) * 1920, top: y, width: 8, height: 8, borderRadius: '50%', border: `2px solid ${i % 2 ? YEL : CREAM}`, opacity: 0.3 }} />;
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Title */}
        <div style={{ position: 'absolute', top: 70, left: 130, transform: `translateY(${(1 - sp(5)) * -160}px)` }}>
          <div style={{ display: 'inline-block', background: INK, padding: '12px 40px', boxShadow: `16px 16px 0 rgba(22,33,42,0.8)`, transform: 'rotate(-1.2deg)' }}>
            <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 900, fontSize: 84, color: YEL }}>खर्च कहाँ, सीख कहाँ?</span>
          </div>
        </div>

        {/* Bar chart */}
        <div style={{ position: 'absolute', bottom: 300, left: 170, display: 'flex', alignItems: 'flex-end', gap: 90 }}>
          {bars.map((b, i) => {
            const grow = sp(b.d, { damping: 11 });
            return (
              <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', width: 320 }}>
                <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: i === 3 ? 62 : 44, color: i === 3 ? YEL : CREAM, marginBottom: 14, opacity: i === 3 ? zeroBlink : grow }}>{b.tag}</div>
                <div style={{ width: 240, height: Math.max(10, b.h * grow), background: i === 3 ? YEL : `repeating-linear-gradient(45deg, ${INK} 0 26px, #33454F 26px 52px)`, border: `6px solid ${INK}`, boxShadow: `12px 12px 0 rgba(22,33,42,0.75)` }} />
                <div style={{ marginTop: 18, fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 800, fontSize: 32, color: CREAM, textAlign: 'center', opacity: grow, background: i === 3 ? INK : 'transparent', padding: i === 3 ? '6px 14px' : 0 }}>{b.label}</div>
              </div>
            );
          })}
        </div>

        {/* Baseline */}
        <div style={{ position: 'absolute', bottom: 280, left: 140, width: 1560, height: 10, background: INK }} />

        {/* Consequence strip */}
        <div style={{ position: 'absolute', bottom: 120, left: 140, right: 140, background: CREAM, border: `7px solid ${INK}`, boxShadow: `16px 16px 0 ${INK}`, padding: '20px 36px', transform: `rotate(0.8deg) translateY(${(1 - sp(330)) * 300}px)` }}>
          <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 46, color: INK }}>
            RESULT: ₹60 लाख LOAN बिना AMORTIZATION समझे + TENURE EXTENSION ACCEPT + RATE CUT का इंतज़ार = <span style={{ background: INK, color: YEL, padding: '0 14px' }}>पैसा बर्बाद</span>
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(24,13,3,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 90], [0.7, 0.3], { extrapolateRight: 'clamp' }) }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene57SpendBars;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene58MissionCTA',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const CREAM = '#FFE9CF';

const Scene58MissionCTA: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1], { easing: Easing.inOut(Easing.ease) });
  const buttons = [
    { t: 'SUBSCRIBE', bg: YEL, d: 190 },
    { t: 'SHARE', bg: CREAM, d: 220 },
    { t: 'COMMENT', bg: ORG, d: 250 },
  ];
  const polls = [
    { t: 'PREPAYMENT', d: 330 },
    { t: 'BALANCE TRANSFER', d: 365 },
    { t: 'WAIT & WATCH', d: 400 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#8F4310', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at 30% 70%, #E8823A 0%, #A0531A 60%, #773A0C 100%)` }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 22);
        return <div key={i} style={{ position: 'absolute', left: r * 1700 - 200, top: seededRandom(i + 61) * 950 - 200, width: 280 + r * 380, height: 280 + r * 380, background: i % 2 ? YEL : INK, opacity: 0.04, borderRadius: i === 1 ? '50%' : 0, transform: `rotate(${frame * 0.1 * (i % 2 ? 1 : -1) + r * 70}deg)` }} />;
      })}

      {/* Motion texture — horizontal punk stripes */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(0deg, ${INK} 0 3px, transparent 3px 88px)`, opacity: 0.05, transform: `translateY(${(frame * 0.45) % 88}px)` }} />

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const speed = 0.5 + seededRandom(i + 38) * 0.9;
        const y = 1140 - ((seededRandom(i + 74) * 1080 + frame * speed) % 1200);
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 21) * 1920, top: y, width: 12, height: 4, background: i % 3 === 0 ? YEL : CREAM, opacity: 0.3, transform: `rotate(${-30 + frame * 0.8}deg)` }} />;
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Mission typography */}
        <div style={{ position: 'absolute', top: 90, left: 130 }}>
          <div style={{ display: 'inline-block', background: INK, padding: '10px 34px', transform: `rotate(-1.5deg) translateX(${(1 - sp(5)) * -500}px)`, boxShadow: `14px 14px 0 rgba(22,33,42,0.8)` }}>
            <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 96, color: CREAM, letterSpacing: -2 }}>COMPLEX FINANCE</span>
          </div>
          <div style={{ marginTop: 22, marginLeft: 90, display: 'flex', alignItems: 'center', gap: 26 }}>
            <svg width="120" height="70" viewBox="0 0 120 70" style={{ opacity: sp(60) }}>
              <line x1="0" y1="35" x2="80" y2="35" stroke={YEL} strokeWidth="12" strokeDasharray="20 14" strokeDashoffset={-(frame * 3) % 34} />
              <polygon points="80,10 120,35 80,60" fill={YEL} />
            </svg>
            <div style={{ display: 'inline-block', background: YEL, border: `6px solid ${INK}`, padding: '8px 34px', transform: `rotate(1.4deg) scale(${sp(85)})`, boxShadow: `12px 12px 0 ${INK}` }}>
              <span style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 100, color: INK }}>SIMPLE भाषा</span>
            </div>
          </div>
        </div>

        {/* Brutalist CTA buttons */}
        <div style={{ position: 'absolute', top: 470, left: 130, display: 'flex', gap: 46 }}>
          {buttons.map((b, i) => (
            <div key={i} style={{ background: b.bg, border: `7px solid ${INK}`, boxShadow: `14px 14px 0 ${INK}`, padding: '24px 52px', transform: `rotate(${(i % 2 ? 1 : -1) * 2}deg) scale(${sp(b.d, { stiffness: 220 })}) translateY(${Math.sin(frame * 0.08 + i * 2) * 5}px)` }}>
              <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 62, color: INK }}>{b.t}</span>
            </div>
          ))}
        </div>

        {/* Poll card */}
        <div style={{ position: 'absolute', bottom: 110, left: 130, width: 1100, background: CREAM, border: `7px solid ${INK}`, boxShadow: `18px 18px 0 ${INK}`, padding: '30px 40px', transform: `rotate(-0.8deg) translateY(${(1 - sp(300)) * 400}px)` }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 900, fontSize: 54, color: INK, marginBottom: 22 }}>COMMENT में बताओ — आपकी STRATEGY?</div>
          {polls.map((p, i) => {
            const tick = interpolate(frame, [p.d + 20, p.d + 50], [40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <div key={i} style={{ display: 'flex', alignItems: 'center', gap: 24, marginBottom: 16, opacity: sp(p.d), transform: `translateX(${(1 - sp(p.d)) * -200}px)` }}>
                <svg width="46" height="46" viewBox="0 0 46 46">
                  <rect x="3" y="3" width="40" height="40" fill="none" stroke={INK} strokeWidth="6" />
                  <path d="M 10 24 L 20 34 L 37 12" stroke={ORG} strokeWidth="7" fill="none" strokeDasharray="40" strokeDashoffset={tick} />
                </svg>
                <span style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 44, color: INK }}>{p.t}</span>
              </div>
            );
          })}
        </div>

        {/* Side sticker */}
        <div style={{ position: 'absolute', right: 130, bottom: 200, background: INK, padding: '26px 34px', transform: `rotate(4deg) scale(${sp(430, { stiffness: 240 })})`, boxShadow: `12px 12px 0 rgba(22,33,42,0.7)` }}>
          <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 44, color: YEL, display: 'block', maxWidth: 420, lineHeight: 1.35 }}>HOME-LOAN वाले दोस्तों को SHARE करो — उन्हें सबसे ज्यादा जरूरत है</span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(24,13,3,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 90], [0.7, 0.3], { extrapolateRight: 'clamp' }) }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene58MissionCTA;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene59GrowthVsDrain',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const CREAM = '#FFE9CF';

const Scene59GrowthVsDrain: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const lineDraw = interpolate(frame, [40, 160], [900, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.07], { easing: Easing.inOut(Easing.ease) });
  const dripY = (frame * 3) % 90;
  const xDraw = interpolate(frame, [380, 420], [80, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#8F4310', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill style={{ background: `linear-gradient(140deg, #E8823A 0%, #9E5219 55%, #6F3308 100%)` }} />

      {/* Shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 31);
        return <div key={i} style={{ position: 'absolute', left: r * 1600 - 150, top: seededRandom(i + 58) * 900 - 150, width: 360 + r * 340, height: 360 + r * 340, border: `4px solid ${INK}`, opacity: 0.06, transform: `rotate(${frame * 0.12 * (i % 2 ? 1 : -1) + 30}deg) scale(${1 + Math.sin(frame * 0.02 + i * 3) * 0.05})` }} />;
      })}

      {/* Motion texture — rising diagonal grid */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(60deg, ${INK} 0 2px, transparent 2px 56px)`, opacity: 0.06, transform: `translateY(${-((frame * 0.5) % 56)}px)` }} />

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const speed = 0.5 + seededRandom(i + 47) * 0.9;
        const y = 1140 - ((seededRandom(i + 93) * 1080 + frame * speed) % 1200);
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 27) * 1920, top: y, width: 6, height: 6, background: i % 3 === 0 ? YEL : INK, opacity: 0.26, borderRadius: i % 2 ? '50%' : 0 }} />;
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Growth chart panel */}
        <div style={{ position: 'absolute', left: 130, top: 110, width: 900, transform: `translateY(${(1 - sp(6)) * -300}px)` }}>
          <div style={{ display: 'inline-block', background: INK, padding: '10px 32px', transform: 'rotate(-1.2deg)', boxShadow: `14px 14px 0 rgba(22,33,42,0.8)`, marginBottom: 26 }}>
            <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 66, color: YEL }}>INDIA = FASTEST GROWING</span>
          </div>
          <div style={{ background: CREAM, border: `7px solid ${INK}`, boxShadow: `18px 18px 0 ${INK}`, padding: 24 }}>
            <svg width="820" height="360" viewBox="0 0 820 360">
              <line x1="30" y1="330" x2="790" y2="330" stroke={INK} strokeWidth="6" />
              <line x1="30" y1="330" x2="30" y2="20" stroke={INK} strokeWidth="6" />
              <polyline points="30,300 190,270 350,285 510,190 670,140 790,60" fill="none" stroke={INK} strokeWidth="14" strokeDasharray="900" strokeDashoffset={lineDraw} />
              <polygon points="790,30 820,60 780,86" fill={YEL} stroke={INK} strokeWidth="4" opacity={frame > 150 ? 1 : 0} />
            </svg>
          </div>
          <div style={{ marginTop: 24, overflow: 'hidden', background: INK, height: 64, transform: 'rotate(0.6deg)' }}>
            <div style={{ whiteSpace: 'nowrap', transform: `translateX(${-((frame * 3.2) % 1200)}px)`, fontFamily: 'JetBrains Mono, monospace', fontWeight: 700, fontSize: 36, color: ORG, lineHeight: '64px' }}>
              {Array.from({ length: 6 }).map((_, i) => (
                <span key={i}>GROWTH UP <span style={{ color: YEL }}>//</span> OPPORTUNITIES UP <span style={{ color: YEL }}>//</span> INCOMES UP <span style={{ color: YEL }}>//</span> </span>
              ))}
            </div>
          </div>
        </div>

        {/* Drain infographic */}
        <div style={{ position: 'absolute', right: 120, top: 200, width: 640, transform: `scale(${sp(220)})`, transformOrigin: 'top center' }}>
          <div style={{ background: YEL, border: `7px solid ${INK}`, boxShadow: `14px 14px 0 ${INK}`, padding: '22px 30px', transform: 'rotate(1.4deg)' }}>
            <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 58, color: INK }}>आपकी INCOME</span>
          </div>
          <svg width="640" height="330" viewBox="0 0 640 330">
            <rect x="290" y="0" width="60" height="180" fill={INK} />
            {[0, 1, 2].map((i) => (
              <rect key={i} x="305" y={60 + ((dripY + i * 30) % 90)} width="30" height="20" fill={ORG} />
            ))}
            <g opacity={frame > 370 ? 1 : 0}>
              <line x1="240" y1="80" x2="400" y2="200" stroke={YEL} strokeWidth="18" strokeDasharray="80" strokeDashoffset={xDraw} />
              <line x1="400" y1="80" x2="240" y2="200" stroke={YEL} strokeWidth="18" strokeDasharray="80" strokeDashoffset={xDraw} />
            </g>
            <rect x="170" y="210" width="300" height="100" fill="none" stroke={INK} strokeWidth="7" />
            <text x="320" y="277" textAnchor="middle" fontFamily="Inter, sans-serif" fontWeight="900" fontSize="46" fill={INK}>INTEREST DRAIN</text>
          </svg>
          <div style={{ background: INK, padding: '18px 28px', transform: 'rotate(-1.2deg)', boxShadow: `12px 12px 0 rgba(22,33,42,0.7)`, opacity: sp(400) }}>
            <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 46, color: YEL }}>फायदा तभी, जब DRAIN बंद हो</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(24,13,3,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 90], [0.7, 0.3], { extrapolateRight: 'clamp' }) }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene59GrowthVsDrain;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene60FinalChoice',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const CREAM = '#FFE9CF';

const Scene60FinalChoice: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1], { easing: Easing.inOut(Easing.ease) });
  const slam = sp(560, { damping: 9, stiffness: 280 });
  const slamShake = frame > 568 && frame < 580 ? Math.sin(frame * 3.2) * 8 : 0;
  const activePulse = 1 + Math.sin(frame * 0.09) * 0.02;
  const tickDraw = interpolate(frame, [500, 535], [60, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#8F4310', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at 50% 50%, #DD7B2E 0%, #99501A 62%, #6B3107 100%)` }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 36);
        return <div key={i} style={{ position: 'absolute', left: r * 1700 - 180, top: seededRandom(i + 67) * 950 - 180, width: 300 + r * 400, height: 300 + r * 400, border: `5px solid ${i % 2 ? YEL : INK}`, borderRadius: i % 2 ? '50%' : 0, opacity: 0.05, transform: `rotate(${frame * 0.1 * (i % 2 ? -1 : 1)}deg) scale(${1 + Math.sin(frame * 0.02 + i * 2) * 0.06})` }} />;
      })}

      {/* Motion texture — cross-hatch */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(45deg, ${INK} 0 2px, transparent 2px 74px), repeating-linear-gradient(-45deg, ${INK} 0 2px, transparent 2px 74px)`, opacity: 0.05, transform: `translateX(${(frame * 0.3) % 74}px)` }} />

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const speed = 0.45 + seededRandom(i + 52) * 0.95;
        const y = 1140 - ((seededRandom(i + 84) * 1080 + frame * speed) % 1200);
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 29) * 1920, top: y, width: 5 + seededRandom(i + 6) * 8, height: 5 + seededRandom(i + 6) * 8, background: i % 4 === 0 ? YEL : i % 4 === 1 ? CREAM : INK, opacity: 0.24, transform: `rotate(${frame * 1.4 + i * 20}deg)` }} />;
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${slamShake}px)` }}>

        {/* Top — TOOL या DESTROYER */}
        <div style={{ position: 'absolute', top: 70, left: 0, width: '100%', textAlign: 'center', transform: `translateY(${(1 - sp(5)) * -180}px)` }}>
          <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 900, fontSize: 76, color: CREAM }}>
            HOME LOAN = <span style={{ background: YEL, color: INK, padding: '2px 22px', border: `5px solid ${INK}` }}>WEALTH TOOL</span> या <span style={{ background: INK, color: ORG, padding: '2px 22px' }}>DESTROYER?</span>
          </span>
        </div>

        {/* Split choice panels */}
        <div style={{ position: 'absolute', top: 250, left: 130, width: 760, height: 420, background: INK, border: `8px solid ${INK}`, boxShadow: `20px 20px 0 rgba(22,33,42,0.7)`, padding: '40px 44px', transform: `rotate(-1.4deg) translateX(${(1 - sp(90)) * -800}px)`, opacity: 0.85 }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 92, color: '#8A9AA6', textDecoration: 'line-through', textDecorationThickness: 10, textDecorationColor: ORG }}>PASSIVE</div>
          <div style={{ marginTop: 24, fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 700, fontSize: 46, color: CREAM, lineHeight: 1.4, transform: `translateY(${Math.sin(frame * 0.04) * 3}px)` }}>
            SYSTEM DECIDE करेगा<br />EMI भरते रहो, इंतज़ार करते रहो
          </div>
        </div>

        <div style={{ position: 'absolute', top: 250, right: 130, width: 760, height: 420, background: YEL, border: `8px solid ${INK}`, boxShadow: `20px 20px 0 ${INK}`, padding: '40px 44px', transform: `rotate(1.4deg) translateX(${(1 - sp(140)) * 800}px) scale(${activePulse})` }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 24 }}>
            <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 92, color: INK }}>ACTIVE</div>
            <svg width="70" height="70" viewBox="0 0 70 70">
              <rect x="4" y="4" width="62" height="62" fill={INK} />
              <path d="M 16 38 L 30 52 L 56 18" stroke={YEL} strokeWidth="10" fill="none" strokeDasharray="60" strokeDashoffset={tickDraw} />
            </svg>
          </div>
          <div style={{ marginTop: 24, fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 46, color: INK, lineHeight: 1.4 }}>
            PREPAY + STEP-UP + TRANSFER<br />FINANCIAL DESTINY आपके CONTROL में
          </div>
        </div>

        {/* Final hero slam */}
        <div style={{ position: 'absolute', bottom: 120, left: 0, width: '100%', textAlign: 'center', transform: `scale(${slam})`, opacity: Math.min(1, slam * 1.3) }}>
          <div style={{ display: 'inline-block', background: ORG, border: `8px solid ${INK}`, boxShadow: `20px 20px 0 ${INK}`, padding: '24px 60px', transform: 'rotate(-1deg)' }}>
            <span style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 92, color: INK }}>INFORMATION मिल गई — DISCIPLINE आपका काम</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(24,13,3,0.65) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 90], [0.7, 0.3], { extrapolateRight: 'clamp' }) }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene60FinalChoice;
```

