```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene1_VahanIntro',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene1_VahanIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.08, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const camX = interpolate(frame, [0, durationInFrames], [-20, 10], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{ transform: `scale(${camScale}) translateX(${camX}px)`, width: '100%', height: '100%', position: 'absolute' }}>

        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: `radial-gradient(ellipse at 30% 60%, #2D2D2D 0%, #1C1C1C 50%, #0a0a0a 100%)`,
        }} />

        {/* Animated shape layers — hexagons and diamonds */}
        {[0, 1, 2, 3, 4].map(i => {
          const s = seededRandom(i + 61);
          const size = 120 + s * 300;
          const x = seededRandom(i + 62) * 1920;
          const y = seededRandom(i + 63) * 1080;
          const rot = frame * (0.15 + s * 0.2);
          const scaleB = 1 + Math.sin(frame * 0.02 + i) * 0.06;
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y,
              width: size, height: size,
              transform: `rotate(${rot}deg) scale(${scaleB})`,
              opacity: 0.04 + s * 0.03,
            }}>
              <svg viewBox="0 0 100 100" width="100%" height="100%">
                <polygon points="50,2 95,25 95,75 50,98 5,75 5,25" fill="none" stroke="#FFC107" strokeWidth="1" />
              </svg>
            </div>
          );
        })}

        {/* Motion texture — diagonal road lines */}
        {Array.from({ length: 12 }).map((_, i) => {
          const lineY = -50 + i * 110;
          const dashOffset = frame * 3;
          return (
            <div key={`line-${i}`} style={{
              position: 'absolute',
              left: -100, top: lineY,
              width: 2200, height: 2,
              transform: 'rotate(-8deg)',
              opacity: 0.06,
            }}>
              <svg width="2200" height="2">
                <line x1="0" y1="1" x2="2200" y2="1" stroke="#9E9E9E" strokeWidth="2" strokeDasharray="30 50" strokeDashoffset={dashOffset} />
              </svg>
            </div>
          );
        })}

        {/* Floating particles */}
        {Array.from({ length: 25 }).map((_, i) => {
          const s = seededRandom(i + 70);
          const px = seededRandom(i + 71) * 1920;
          const py = seededRandom(i + 72) * 1080;
          const drift = Math.sin(frame * 0.03 + i * 2) * 15;
          const driftY = Math.cos(frame * 0.02 + i * 3) * 10;
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: px + drift, top: py + driftY,
              width: 3 + s * 4, height: 3 + s * 4,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#FFC107' : '#F5F5F5',
              opacity: 0.15 + s * 0.2,
            }} />
          );
        })}

        {/* Main content */}
        {/* Chapter number badge */}
        <div style={{
          position: 'absolute', left: 100, top: 80,
          transform: `scale(${sp(5)}) translateY(${interpolate(sp(5), [0, 1], [40, 0])}px)`,
          opacity: sp(5),
        }}>
          <div style={{
            background: '#e9d700',
            color: '#1C1C1C',
            fontFamily: 'Inter, Poppins',
            fontSize: 28,
            fontWeight: 800,
            padding: '10px 28px',
            borderRadius: 6,
            letterSpacing: 3,
          }}>
            चोथी बात
          </div>
        </div>

        {/* Main headline */}
        <div style={{
          position: 'absolute', left: 100, top: 170,
          transform: `translateY(${interpolate(sp(15), [0, 1], [60, 0])}px)`,
          opacity: sp(15),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 110,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
            maxWidth: 900,
          }}>
            वो <span style={{ color: '#e9d700' }}>वाहन</span>
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 70,
            fontWeight: 700,
            color: '#9E9E9E',
            marginTop: 5,
          }}>
            जिसकी अब दरकार नहीं
          </div>
        </div>

        {/* Car SVG icon — large, fading in from right */}
        <div style={{
          position: 'absolute', right: 80, top: 120,
          transform: `translateX(${interpolate(sp(25), [0, 1], [200, 0])}px) scale(${sp(25)})`,
          opacity: sp(25),
        }}>
          <svg width="480" height="280" viewBox="0 0 480 280">
            {/* Car body */}
            <rect x="60" y="120" width="360" height="100" rx="20" fill="#2D2D2D" stroke="#FFC107" strokeWidth="3" />
            <path d="M120 120 L170 50 L310 50 L360 120" fill="#2D2D2D" stroke="#FFC107" strokeWidth="3" />
            {/* Windows */}
            <path d="M175 55 L195 95 L285 95 L305 55 Z" fill="#1C1C1C" stroke="#e9d700" strokeWidth="1.5" opacity="0.7" />
            <line x1="240" y1="55" x2="240" y2="95" stroke="#e9d700" strokeWidth="1.5" opacity="0.5" />
            {/* Wheels */}
            <circle cx="150" cy="220" r="35" fill="#1C1C1C" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="150" cy="220" r="15" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
            <circle cx="330" cy="220" r="35" fill="#1C1C1C" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="330" cy="220" r="15" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
            {/* Headlight */}
            <rect x="400" y="140" width="25" height="20" rx="5" fill="#e9d700" opacity="0.9" />
            {/* Dust cloud — animated */}
            <circle cx={30 - frame * 0.3} cy="210" r={12 + Math.sin(frame * 0.1) * 4} fill="#9E9E9E" opacity={0.15} />
            <circle cx={10 - frame * 0.2} cy="230" r={8 + Math.cos(frame * 0.08) * 3} fill="#9E9E9E" opacity={0.1} />
          </svg>
        </div>

        {/* Bottom context line — stagger */}
        <div style={{
          position: 'absolute', left: 100, bottom: 160,
          transform: `translateY(${interpolate(sp(45), [0, 1], [30, 0])}px)`,
          opacity: sp(45),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 44,
            fontWeight: 600,
            color: '#F5F5F5',
            maxWidth: 1100,
            lineHeight: 1.5,
          }}>
            नौकरी में गाड़ी <span style={{ color: '#e9d700' }}>जरूरत</span> थी — दफतर, बाजार, बच्चों का विद्यालय...
          </div>
        </div>

        {/* Small icons row — office, school, market */}
        <div style={{
          position: 'absolute', left: 100, bottom: 80,
          display: 'flex', gap: 40,
          transform: `translateY(${interpolate(sp(60), [0, 1], [20, 0])}px)`,
          opacity: sp(60),
        }}>
          {/* Office icon */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
              <rect x="3" y="7" width="18" height="14" rx="2" stroke="#FFC107" strokeWidth="2" />
              <path d="M7 7V5a2 2 0 012-2h6a2 2 0 012 2v2" stroke="#FFC107" strokeWidth="2" />
              <line x1="3" y1="13" x2="21" y2="13" stroke="#FFC107" strokeWidth="1.5" />
            </svg>
            <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#9E9E9E' }}>दफ्तर</span>
          </div>
          {/* School icon */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
              <path d="M12 3L2 9l10 6 10-6-10-6z" stroke="#FFC107" strokeWidth="2" />
              <path d="M20 9v7" stroke="#FFC107" strokeWidth="2" />
              <path d="M6 12v5c0 2 6 4 6 4s6-2 6-4v-5" stroke="#FFC107" strokeWidth="2" />
            </svg>
            <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#9E9E9E' }}>विद्यालय</span>
          </div>
          {/* Market icon */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
              <path d="M3 9h18l-2 11H5L3 9z" stroke="#FFC107" strokeWidth="2" />
              <path d="M8 9V5a4 4 0 018 0v4" stroke="#FFC107" strokeWidth="2" />
            </svg>
            <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#9E9E9E' }}>बाजार</span>
          </div>
        </div>

        {/* Stick figure — thinking pose */}
        <div style={{
          position: 'absolute', right: 120, bottom: 50,
          transform: `scale(${sp(40)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: sp(40),
        }}>
          <svg width="90" height="160" viewBox="0 0 90 160">
            {/* Glow */}
            <circle cx="45" cy="30" r="35" fill="#FFC107" opacity="0.08" />
            {/* Head */}
            <circle cx="45" cy="30" r="18" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            {/* Golden glasses */}
            <circle cx="38" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
            <circle cx="52" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="44" y1="28" x2="46" y2="28" stroke="#FFC107" strokeWidth="2" />
            {/* Body */}
            <line x1="45" y1="48" x2="45" y2="100" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arms — one hand on chin thinking */}
            <line x1="45" y1="65" x2="20" y2="85" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="45" y1="65" x2="65" y2="55" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="65" y1="55" x2="52" y2="35" stroke="#F5F5F5" strokeWidth="3" />
            {/* Legs */}
            <line x1="45" y1="100" x2="25" y2="145" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="45" y1="100" x2="65" y2="145" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        </div>

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Ch6Scene1_VahanIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene2_GaadiKharcha',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene2_GaadiKharcha: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const costItems = [
    { label: 'बीमा', amount: '₹15,000', delay: 30, color: '#FFC107' },
    { label: 'देखरेख', amount: '₹20,000', delay: 50, color: '#e9d700' },
    { label: 'इंधन', amount: '₹30,000', delay: 70, color: '#FFC107' },
    { label: 'खड़ी रखना', amount: '₹6,000', delay: 90, color: '#e9d700' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{ transform: `scale(${camScale})`, width: '100%', height: '100%', position: 'absolute' }}>

        {/* Dynamic gradient background — deep red tint warning */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: `linear-gradient(135deg, #1C1C1C 0%, #2a1515 40%, #1C1C1C 100%)`,
        }} />

        {/* Animated shape layers — rectangles rotating */}
        {[0, 1, 2, 3].map(i => {
          const s = seededRandom(i + 80);
          const size = 200 + s * 250;
          const x = seededRandom(i + 81) * 1800;
          const y = seededRandom(i + 82) * 900;
          const rot = frame * (0.1 + s * 0.15);
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y,
              width: size, height: size * 0.6,
              border: '1px solid rgba(255,0,0,0.06)',
              borderRadius: 8,
              transform: `rotate(${rot}deg) scale(${1 + Math.sin(frame * 0.015 + i) * 0.04})`,
              opacity: 0.05,
            }} />
          );
        })}

        {/* Motion texture — concentric circles from center */}
        {Array.from({ length: 6 }).map((_, i) => {
          const radius = 150 + i * 120 + Math.sin(frame * 0.02) * 10;
          return (
            <div key={`c-${i}`} style={{
              position: 'absolute',
              left: '50%', top: '50%',
              width: radius * 2, height: radius * 2,
              marginLeft: -radius, marginTop: -radius,
              borderRadius: '50%',
              border: '1px solid rgba(255,0,0,0.04)',
            }} />
          );
        })}

        {/* Floating particles */}
        {Array.from({ length: 20 }).map((_, i) => {
          const s = seededRandom(i + 90);
          const px = seededRandom(i + 91) * 1920;
          const py = seededRandom(i + 92) * 1080;
          const drift = Math.sin(frame * 0.025 + i) * 12;
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: px + drift, top: py + Math.cos(frame * 0.02 + i) * 8,
              width: 3 + s * 3, height: 3 + s * 3,
              borderRadius: '50%',
              backgroundColor: i % 4 === 0 ? '#ff0000' : '#9E9E9E',
              opacity: 0.12 + s * 0.15,
            }} />
          );
        })}

        {/* Main content — split layout */}
        {/* Left: Garage car illustration */}
        <div style={{
          position: 'absolute', left: 80, top: 100,
          width: 700, height: 600,
          transform: `scale(${sp(10)})`,
          opacity: sp(10),
        }}>
          {/* Garage frame */}
          <svg width="700" height="500" viewBox="0 0 700 500">
            {/* Garage roof */}
            <path d="M50 200 L350 50 L650 200" fill="none" stroke="#9E9E9E" strokeWidth="3" />
            {/* Garage walls */}
            <rect x="80" y="200" width="540" height="280" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            {/* Garage door lines */}
            {[0, 1, 2, 3, 4].map(j => (
              <line key={j} x1="80" y1={220 + j * 52} x2="620" y2={220 + j * 52} stroke="#9E9E9E" strokeWidth="1" opacity="0.3" />
            ))}
            {/* Car inside — parked, dusty */}
            <rect x="160" y="320" width="380" height="110" rx="18" fill="#2D2D2D" stroke="#ff0000" strokeWidth="2" opacity="0.8" />
            <path d="M220 320 L260 260 L440 260 L480 320" fill="#2D2D2D" stroke="#ff0000" strokeWidth="2" opacity="0.8" />
            {/* Dust particles on car */}
            {Array.from({ length: 8 }).map((_, d) => (
              <circle key={d} cx={180 + d * 50} cy={310 + seededRandom(d + 100) * 20} r="2" fill="#9E9E9E" opacity="0.4" />
            ))}
            {/* Wheels */}
            <circle cx="250" cy="430" r="28" fill="#1C1C1C" stroke="#9E9E9E" strokeWidth="2" />
            <circle cx="450" cy="430" r="28" fill="#1C1C1C" stroke="#9E9E9E" strokeWidth="2" />
            {/* Cobwebs */}
            <path d="M85 205 Q120 220 110 250" fill="none" stroke="#9E9E9E" strokeWidth="0.5" opacity="0.4" />
            <path d="M85 205 Q100 230 130 230" fill="none" stroke="#9E9E9E" strokeWidth="0.5" opacity="0.3" />
            {/* "खड़ी है" text */}
            <text x="350" y="390" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="26" fill="#ff0000" opacity={sp(30)}>
              गैराज में खड़ी है
            </text>
          </svg>
        </div>

        {/* Right: Cost breakdown bars */}
        <div style={{
          position: 'absolute', right: 80, top: 100, width: 520,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 52,
            fontWeight: 800,
            color: '#F5F5F5',
            marginBottom: 30,
            transform: `translateX(${interpolate(sp(15), [0, 1], [80, 0])}px)`,
            opacity: sp(15),
          }}>
            सालाना <span style={{ color: '#ff0000' }}>हिसाब</span>
          </div>

          {costItems.map((item, idx) => {
            const barWidth = interpolate(sp(item.delay), [0, 1], [0, 100]);
            return (
              <div key={idx} style={{
                marginBottom: 22,
                opacity: sp(item.delay),
                transform: `translateX(${interpolate(sp(item.delay), [0, 1], [60, 0])}px)`,
              }}>
                <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6 }}>
                  <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 36, fontWeight: 600, color: '#F5F5F5' }}>
                    {item.label}
                  </span>
                  <span style={{ fontFamily: 'JetBrains Mono', fontSize: 36, fontWeight: 700, color: item.color }}>
                    {item.amount}
                  </span>
                </div>
                <div style={{ width: '100%', height: 10, backgroundColor: '#2D2D2D', borderRadius: 5 }}>
                  <div style={{
                    width: `${barWidth}%`, height: '100%',
                    backgroundColor: item.color, borderRadius: 5,
                  }} />
                </div>
              </div>
            );
          })}

          {/* Total */}
          <div style={{
            marginTop: 30,
            borderTop: '3px solid #ff0000',
            paddingTop: 20,
            display: 'flex', justifyContent: 'space-between',
            opacity: sp(115),
            transform: `scale(${sp(115)})`,
          }}>
            <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 48, fontWeight: 900, color: '#F5F5F5' }}>
              कुल
            </span>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 56, fontWeight: 900, color: '#ff0000' }}>
              ₹71,000
            </span>
          </div>
        </div>

        {/* Screen shake on total reveal */}
        {frame > 115 * 30 / 30 && frame < 120 * 30 / 30 ? null : null}

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Ch6Scene2_GaadiKharcha;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene3_Comparison',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene3_Comparison: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [15, -15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{ transform: `translateY(${camPan}px)`, width: '100%', height: '100%', position: 'absolute' }}>

        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: `linear-gradient(180deg, #1a1a2e 0%, #1C1C1C 50%, #16213e 100%)`,
        }} />

        {/* Shape layers — triangles */}
        {[0, 1, 2, 3, 4, 5].map(i => {
          const s = seededRandom(i + 110);
          const x = seededRandom(i + 111) * 1800;
          const y = seededRandom(i + 112) * 900;
          const size = 100 + s * 200;
          const rot = frame * 0.12 * (i % 2 === 0 ? 1 : -1);
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y,
              transform: `rotate(${rot}deg)`,
              opacity: 0.04 + s * 0.03,
            }}>
              <svg width={size} height={size} viewBox="0 0 100 100">
                <polygon points="50,5 95,95 5,95" fill="none" stroke="#e9d700" strokeWidth="1.5" />
              </svg>
            </div>
          );
        })}

        {/* Motion texture — vertical dashed lines */}
        {Array.from({ length: 15 }).map((_, i) => (
          <div key={`vl-${i}`} style={{
            position: 'absolute',
            left: 60 + i * 130, top: 0,
            width: 1, height: '100%',
            opacity: 0.04,
          }}>
            <svg width="1" height="1080">
              <line x1="0" y1="0" x2="0" y2="1080" stroke="#F5F5F5" strokeWidth="1" strokeDasharray="8 20" strokeDashoffset={frame * 2} />
            </svg>
          </div>
        ))}

        {/* Particles */}
        {Array.from({ length: 18 }).map((_, i) => {
          const s = seededRandom(i + 120);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: seededRandom(i + 121) * 1920 + Math.sin(frame * 0.03 + i) * 10,
              top: seededRandom(i + 122) * 1080 + Math.cos(frame * 0.025 + i) * 8,
              width: 2 + s * 4, height: 2 + s * 4,
              borderRadius: '50%',
              backgroundColor: '#e9d700',
              opacity: 0.1 + s * 0.15,
            }} />
          );
        })}

        {/* VS split layout */}
        {/* Divider line in center */}
        <div style={{
          position: 'absolute',
          left: '50%', top: 80, width: 3, height: 920,
          marginLeft: -1.5,
          background: `linear-gradient(to bottom, transparent, #e9d700, transparent)`,
          opacity: interpolate(sp(10), [0, 1], [0, 0.5]),
        }} />

        {/* VS badge */}
        <div style={{
          position: 'absolute',
          left: '50%', top: '50%',
          transform: `translate(-50%, -50%) scale(${sp(20)})`,
          opacity: sp(20),
          zIndex: 10,
        }}>
          <div style={{
            width: 90, height: 90,
            borderRadius: '50%',
            background: '#e9d700',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            boxShadow: '0 0 40px rgba(233,215,0,0.3)',
          }}>
            <span style={{ fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 900, color: '#1C1C1C' }}>VS</span>
          </div>
        </div>

        {/* LEFT — Own car */}
        <div style={{
          position: 'absolute', left: 60, top: 80, width: 850,
          transform: `translateX(${interpolate(sp(15), [0, 1], [-100, 0])}px)`,
          opacity: sp(15),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 50, fontWeight: 800,
            color: '#ff0000', marginBottom: 25, textAlign: 'center',
          }}>
            🚗 अपनी गाड़ी
          </div>
          <div style={{
            background: 'rgba(255,0,0,0.08)',
            borderRadius: 16, padding: '30px 40px',
            border: '2px solid rgba(255,0,0,0.2)',
          }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 80, fontWeight: 900, color: '#ff0000', textAlign: 'center' }}>
              ₹71,000
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 30, color: '#9E9E9E', textAlign: 'center', marginTop: 8 }}>
              सालाना खर्च
            </div>
          </div>

          {/* Depreciation warning */}
          <div style={{
            marginTop: 25,
            opacity: sp(60),
            transform: `translateY(${interpolate(sp(60), [0, 1], [20, 0])}px)`,
          }}>
            <div style={{
              background: 'rgba(255,0,0,0.12)',
              borderRadius: 10, padding: '15px 25px',
              display: 'flex', alignItems: 'center', gap: 15,
              border: '1px solid rgba(255,0,0,0.25)',
            }}>
              <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
                <path d="M12 2L2 22h20L12 2z" stroke="#ff0000" strokeWidth="2" />
                <line x1="12" y1="9" x2="12" y2="15" stroke="#ff0000" strokeWidth="2" />
                <circle cx="12" cy="18" r="1" fill="#ff0000" />
              </svg>
              <div>
                <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, fontWeight: 700, color: '#ff0000' }}>
                  मूल्य गिरना: 5 साल में आधा
                </div>
                <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 22, color: '#9E9E9E' }}>
                  + ₹35,000 फालतू हर साल
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* RIGHT — Hire rides */}
        <div style={{
          position: 'absolute', right: 60, top: 80, width: 850,
          transform: `translateX(${interpolate(sp(25), [0, 1], [100, 0])}px)`,
          opacity: sp(25),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 50, fontWeight: 800,
            color: '#e9d700', marginBottom: 25, textAlign: 'center',
          }}>
            🚕 किराय की सवारी
          </div>
          <div style={{
            background: 'rgba(233,215,0,0.06)',
            borderRadius: 16, padding: '30px 40px',
            border: '2px solid rgba(233,215,0,0.2)',
          }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 80, fontWeight: 900, color: '#e9d700', textAlign: 'center' }}>
              ₹36,000
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 30, color: '#9E9E9E', textAlign: 'center', marginTop: 8 }}>
              20 × ₹150 × 12 महीने
            </div>
          </div>

          {/* Savings highlight */}
          <div style={{
            marginTop: 25,
            opacity: sp(75),
            transform: `scale(${sp(75)})`,
          }}>
            <div style={{
              background: 'rgba(233,215,0,0.1)',
              borderRadius: 10, padding: '15px 25px',
              display: 'flex', alignItems: 'center', gap: 15,
              border: '1px solid rgba(233,215,0,0.25)',
            }}>
              <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
                <circle cx="12" cy="12" r="10" stroke="#e9d700" strokeWidth="2" />
                <path d="M8 12l3 3 5-6" stroke="#e9d700" strokeWidth="2.5" />
              </svg>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 30, fontWeight: 700, color: '#e9d700' }}>
                बचत: ₹35,000+ सालाना
              </div>
            </div>
          </div>
        </div>

        {/* Bottom punchline */}
        <div style={{
          position: 'absolute', bottom: 50, left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(sp(90), [0, 1], [30, 0])}px)`,
          opacity: sp(90),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 38, fontWeight: 700,
            color: '#F5F5F5', textAlign: 'center',
            background: 'rgba(45,45,45,0.8)', padding: '12px 40px', borderRadius: 10,
          }}>
            हिसाब साफ है — गाड़ी <span style={{ color: '#ff0000' }}>बोझ</span> है, सवारी <span style={{ color: '#e9d700' }}>समझदारी</span>
          </div>
        </div>

        {/* Vignette */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Ch6Scene3_Comparison;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene4_GwaliorStory',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene4_GwaliorStory: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.04, 0.98], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{ transform: `scale(${camZoom})`, width: '100%', height: '100%', position: 'absolute' }}>

        {/* Dynamic gradient */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: `radial-gradient(circle at 70% 30%, #1e3a1e 0%, #1C1C1C 60%, #0d0d0d 100%)`,
        }} />

        {/* Shape layers — circles */}
        {[0, 1, 2, 3].map(i => {
          const s = seededRandom(i + 140);
          const size = 180 + s * 350;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: seededRandom(i + 141) * 1700,
              top: seededRandom(i + 142) * 800,
              width: size, height: size,
              borderRadius: '50%',
              border: '1.5px solid rgba(233,215,0,0.05)',
              transform: `scale(${1 + Math.sin(frame * 0.02 + i * 1.5) * 0.05})`,
            }} />
          );
        })}

        {/* Motion texture — grid dots */}
        {Array.from({ length: 80 }).map((_, i) => {
          const col = i % 10;
          const row = Math.floor(i / 10);
          return (
            <div key={`dot-${i}`} style={{
              position: 'absolute',
              left: 100 + col * 190,
              top: 60 + row * 140,
              width: 3, height: 3,
              borderRadius: '50%',
              backgroundColor: '#9E9E9E',
              opacity: 0.06,
            }} />
          );
        })}

        {/* Particles */}
        {Array.from({ length: 20 }).map((_, i) => {
          const s = seededRandom(i + 150);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: seededRandom(i + 151) * 1920 + Math.sin(frame * 0.03 + i) * 12,
              top: seededRandom(i + 152) * 1080 + Math.cos(frame * 0.02 + i) * 10,
              width: 3 + s * 3, height: 3 + s * 3,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#FFC107' : '#F5F5F5',
              opacity: 0.12 + s * 0.18,
            }} />
          );
        })}

        {/* Location badge */}
        <div style={{
          position: 'absolute', left: 100, top: 60,
          display: 'flex', alignItems: 'center', gap: 12,
          opacity: sp(5),
          transform: `translateY(${interpolate(sp(5), [0, 1], [30, 0])}px)`,
        }}>
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
            <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7z" stroke="#FFC107" strokeWidth="2" />
            <circle cx="12" cy="9" r="2.5" stroke="#FFC107" strokeWidth="2" />
          </svg>
          <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, fontWeight: 700, color: '#FFC107' }}>
            ग्वालियर
          </span>
        </div>

        {/* Story card */}
        <div style={{
          position: 'absolute', left: 100, top: 130, width: 800,
          opacity: sp(15),
          transform: `translateX(${interpolate(sp(15), [0, 1], [-80, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 48, fontWeight: 800,
            color: '#F5F5F5', lineHeight: 1.4, marginBottom: 15,
          }}>
            सेवा निवृत्त <span style={{ color: '#e9d700' }}>अध्यापक</span> ने
            <br />यही हिसाब लगाया
          </div>
        </div>

        {/* Flow: Car → Money → FD → Interest */}
        {/* Step 1: Car sold */}
        <div style={{
          position: 'absolute', left: 100, top: 350,
          opacity: sp(30),
          transform: `scale(${sp(30)})`,
        }}>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 20,
            background: 'rgba(45,45,45,0.7)', borderRadius: 14, padding: '18px 30px',
            border: '1px solid rgba(255,193,7,0.2)',
          }}>
            <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
              <rect x="2" y="10" width="20" height="8" rx="3" stroke="#ff0000" strokeWidth="2" />
              <circle cx="7" cy="18" r="2" stroke="#ff0000" strokeWidth="1.5" />
              <circle cx="17" cy="18" r="2" stroke="#ff0000" strokeWidth="1.5" />
              <line x1="1" y1="8" x2="23" y2="20" stroke="#ff0000" strokeWidth="2.5" />
            </svg>
            <div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, fontWeight: 700, color: '#F5F5F5' }}>गाड़ी बेची</div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 38, fontWeight: 900, color: '#e9d700' }}>₹5,00,000</div>
            </div>
          </div>
        </div>

        {/* Arrow */}
        <div style={{
          position: 'absolute', left: 530, top: 390,
          opacity: sp(45),
        }}>
          <svg width="80" height="40" viewBox="0 0 80 40">
            <path d="M0 20 L60 20 M50 10 L65 20 L50 30" stroke="#FFC107" strokeWidth="3" fill="none" />
          </svg>
        </div>

        {/* Step 2: FD */}
        <div style={{
          position: 'absolute', left: 640, top: 350,
          opacity: sp(55),
          transform: `scale(${sp(55)})`,
        }}>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 20,
            background: 'rgba(45,45,45,0.7)', borderRadius: 14, padding: '18px 30px',
            border: '1px solid rgba(233,215,0,0.2)',
          }}>
            <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
              <rect x="3" y="6" width="18" height="14" rx="2" stroke="#e9d700" strokeWidth="2" />
              <line x1="3" y1="10" x2="21" y2="10" stroke="#e9d700" strokeWidth="1.5" />
              <circle cx="12" cy="16" r="2" stroke="#e9d700" strokeWidth="1.5" />
            </svg>
            <div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, fontWeight: 700, color: '#F5F5F5' }}>सावधी जमा</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 24, color: '#9E9E9E' }}>Fixed Deposit</div>
            </div>
          </div>
        </div>

        {/* Results row */}
        <div style={{
          position: 'absolute', left: 100, top: 530, width: 1700,
          display: 'flex', gap: 40,
        }}>
          {/* Savings card */}
          <div style={{
            flex: 1,
            background: 'rgba(233,215,0,0.06)',
            borderRadius: 16, padding: '25px 30px',
            border: '2px solid rgba(233,215,0,0.15)',
            textAlign: 'center',
            opacity: sp(70),
            transform: `translateY(${interpolate(sp(70), [0, 1], [40, 0])}px)`,
          }}>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#9E9E9E', marginBottom: 8 }}>सालाना बचत</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 52, fontWeight: 900, color: '#e9d700' }}>₹40,000</div>
          </div>

          {/* Plus sign */}
          <div style={{
            display: 'flex', alignItems: 'center',
            opacity: sp(80),
          }}>
            <span style={{ fontFamily: 'Inter', fontSize: 60, fontWeight: 900, color: '#FFC107' }}>+</span>
          </div>

          {/* Interest card */}
          <div style={{
            flex: 1,
            background: 'rgba(233,215,0,0.06)',
            borderRadius: 16, padding: '25px 30px',
            border: '2px solid rgba(233,215,0,0.15)',
            textAlign: 'center',
            opacity: sp(90),
            transform: `translateY(${interpolate(sp(90), [0, 1], [40, 0])}px)`,
          }}>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#9E9E9E', marginBottom: 8 }}>हर साल ब्याज</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 52, fontWeight: 900, color: '#FFC107' }}>₹35,000</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 22, color: '#9E9E9E', marginTop: 5 }}>बिना कुछ किये</div>
          </div>

          {/* Equals */}
          <div style={{
            display: 'flex', alignItems: 'center',
            opacity: sp(100),
          }}>
            <span style={{ fontFamily: 'Inter', fontSize: 60, fontWeight: 900, color: '#FFC107' }}>=</span>
          </div>

          {/* Total freedom card */}
          <div style={{
            flex: 1.2,
            background: 'linear-gradient(135deg, rgba(233,215,0,0.12), rgba(255,193,7,0.08))',
            borderRadius: 16, padding: '25px 30px',
            border: '2px solid rgba(255,193,7,0.3)',
            textAlign: 'center',
            opacity: sp(110),
            transform: `scale(${sp(110)})`,
          }}>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#FFC107', marginBottom: 8 }}>कुल सालाना लाभ</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 60, fontWeight: 900, color: '#F5F5F5' }}>₹75,000</div>
          </div>
        </div>

        {/* Stick figure — happy teacher */}
        <div style={{
          position: 'absolute', right: 80, top: 100,
          transform: `scale(${sp(50)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: sp(50),
        }}>
          <svg width="100" height="170" viewBox="0 0 100 170">
            <circle cx="50" cy="30" r="38" fill="#FFC107" opacity="0.06" />
            <circle cx="50" cy="30" r="18" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            {/* Glasses */}
            <circle cx="43" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
            <circle cx="57" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="49" y1="28" x2="51" y2="28" stroke="#FFC107" strokeWidth="2" />
            {/* Smile */}
            <path d="M42 34 Q50 42 58 34" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            {/* Body */}
            <line x1="50" y1="48" x2="50" y2="105" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arms up celebrating */}
            <line x1="50" y1="68" x2="20" y2="45" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="50" y1="68" x2="80" y2="45" stroke="#F5F5F5" strokeWidth="3" />
            {/* Legs */}
            <line x1="50" y1="105" x2="30" y2="155" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="50" y1="105" x2="70" y2="155" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        </div>

        {/* Vignette */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Ch6Scene4_GwaliorStory;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene5_MentalBojh',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene5_MentalBojh: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camTilt = interpolate(frame, [0, durationInFrames], [0.5, -0.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{ transform: `rotate(${camTilt}deg)`, width: '100%', height: '100%', position: 'absolute' }}>

        {/* Dynamic gradient background — deep thoughtful tones */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: `radial-gradient(ellipse at 50% 40%, #2a2040 0%, #1C1C1C 55%, #0a0a0a 100%)`,
        }} />

        {/* Shape layers — pentagon and star shapes */}
        {[0, 1, 2, 3, 4].map(i => {
          const s = seededRandom(i + 170);
          const size = 140 + s * 280;
          const x = seededRandom(i + 171) * 1700;
          const y = seededRandom(i + 172) * 900;
          const rot = frame * 0.08 * (i % 2 === 0 ? 1 : -1);
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y,
              width: size, height: size,
              transform: `rotate(${rot}deg) scale(${1 + Math.sin(frame * 0.018 + i) * 0.05})`,
              opacity: 0.035,
            }}>
              <svg viewBox="0 0 100 100" width="100%" height="100%">
                <polygon points="50,5 61,35 95,35 68,57 79,90 50,70 21,90 32,57 5,35 39,35" fill="none" stroke="#FFC107" strokeWidth="1" />
              </svg>
            </div>
          );
        })}

        {/* Motion texture — brain-like neural lines */}
        {Array.from({ length: 10 }).map((_, i) => {
          const startX = seededRandom(i + 180) * 1920;
          const startY = seededRandom(i + 181) * 1080;
          const endX = startX + (seededRandom(i + 182) - 0.5) * 400;
          const endY = startY + (seededRandom(i + 183) - 0.5) * 400;
          const cpX = (startX + endX) / 2 + (seededRandom(i + 184) - 0.5) * 200;
          const cpY = (startY + endY) / 2 + (seededRandom(i + 185) - 0.5) * 200;
          return (
            <svg key={`n-${i}`} style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.05 }}>
              <path d={`M${startX} ${startY} Q${cpX} ${cpY} ${endX} ${endY}`} fill="none" stroke="#e9d700" strokeWidth="1" />
              <circle cx={startX} cy={startY} r="3" fill="#e9d700" opacity="0.3" />
              <circle cx={endX} cy={endY} r="3" fill="#e9d700" opacity="0.3" />
            </svg>
          );
        })}

        {/* Particles */}
        {Array.from({ length: 22 }).map((_, i) => {
          const s = seededRandom(i + 190);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: seededRandom(i + 191) * 1920 + Math.sin(frame * 0.028 + i) * 14,
              top: seededRandom(i + 192) * 1080 + Math.cos(frame * 0.022 + i) * 10,
              width: 2 + s * 4, height: 2 + s * 4,
              borderRadius: '50%',
              backgroundColor: i % 5 === 0 ? '#FFC107' : '#F5F5F5',
              opacity: 0.1 + s * 0.15,
            }} />
          );
        })}

        {/* Quote card — main visual */}
        <div style={{
          position: 'absolute', left: '50%', top: 60,
          transform: `translateX(-50%) translateY(${interpolate(sp(10), [0, 1], [60, 0])}px)`,
          opacity: sp(10),
          width: 1100,
        }}>
          <div style={{
            background: 'rgba(45,45,45,0.6)',
            borderRadius: 20, padding: '35px 50px',
            borderLeft: '6px solid #e9d700',
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 50, fontWeight: 800,
              color: '#F5F5F5', lineHeight: 1.4,
            }}>
              "गाड़ी छोड़ना सबसे <span style={{ color: '#e9d700' }}>समझदार फैसला</span> था,
              <br /><span style={{ color: '#9E9E9E', fontSize: 44 }}>काश पहले किया होता"</span>
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#FFC107',
              marginTop: 12, fontStyle: 'italic',
            }}>
              — ग्वालियर के सेवा निवृत्त अध्यापक
            </div>
          </div>
        </div>

        {/* Brain illustration with weight blocks */}
        <div style={{
          position: 'absolute', left: 120, top: 380,
          opacity: sp(40),
          transform: `scale(${sp(40)})`,
        }}>
          <svg width="380" height="350" viewBox="0 0 380 350">
            {/* Brain outline */}
            <ellipse cx="190" cy="130" rx="140" ry="110" fill="none" stroke="#F5F5F5" strokeWidth="2.5" opacity="0.6" />
            <path d="M120 130 Q130 80 190 70 Q250 80 260 130" fill="none" stroke="#F5F5F5" strokeWidth="1.5" opacity="0.4" />
            <path d="M140 100 Q190 60 240 100" fill="none" stroke="#F5F5F5" strokeWidth="1" opacity="0.3" />
            {/* Weight blocks hanging from brain */}
            {['फिक्र', 'देखभाल', 'फैसले'].map((text, idx) => {
              const bx = 100 + idx * 90;
              const by = 240 + Math.sin(frame * 0.04 + idx * 2) * 5;
              const delay2 = 60 + idx * 20;
              return (
                <g key={idx} opacity={sp(delay2)}>
                  <line x1={bx + 30} y1="200" x2={bx + 30} y2={by} stroke="#9E9E9E" strokeWidth="1.5" strokeDasharray="4 3" />
                  <rect x={bx} y={by} width="60" height="40" rx="6" fill="#2D2D2D" stroke="#ff0000" strokeWidth="1.5" />
                  <text x={bx + 30} y={by + 26} textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="16" fill="#ff0000" fontWeight="700">
                    {text}
                  </text>
                </g>
              );
            })}
            {/* "बोझ" label */}
            <text x="190" y="330" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="28" fill="#ff0000" fontWeight="800" opacity={sp(110)}>
              मानसिक बोझ
            </text>
          </svg>
        </div>

        {/* Right side — key insight boxes */}
        <div style={{
          position: 'absolute', right: 100, top: 400, width: 650,
        }}>
          {[
            { text: 'हर अतिरिक्त चीज सिर्फ पैसा नहीं लेती', delay: 50 },
            { text: 'दिमाग की जगह भी लेती है', delay: 70 },
            { text: 'फिक्र • देखभाल • फैसले = मानसिक बोझ', delay: 90 },
          ].map((item, idx) => (
            <div key={idx} style={{
              marginBottom: 20,
              opacity: sp(item.delay),
              transform: `translateX(${interpolate(sp(item.delay), [0, 1], [60, 0])}px)`,
            }}>
              <div style={{
                display: 'flex', alignItems: 'center', gap: 18,
                background: 'rgba(45,45,45,0.5)',
                borderRadius: 12, padding: '16px 24px',
                borderLeft: `4px solid ${idx === 2 ? '#ff0000' : '#FFC107'}`,
              }}>
                <svg width="24" height="24" viewBox="0 0 24 24">
                  <path d={idx === 2 ? "M12 2L2 22h20L12 2z" : "M9 12l2 2 4-4"} stroke={idx === 2 ? '#ff0000' : '#FFC107'} strokeWidth="2.5" fill="none" />
                </svg>
                <span style={{
                  fontFamily: 'Noto Sans Devanagari', fontSize: 34, fontWeight: 700,
                  color: idx === 2 ? '#ff0000' : '#F5F5F5',
                }}>
                  {item.text}
                </span>
              </div>
            </div>
          ))}
        </div>

        {/* Bottom punchline */}
        <div style={{
          position: 'absolute', bottom: 80, left: '50%',
          transform: `translateX(-50%) scale(${sp(120)})`,
          opacity: sp(120),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 46, fontWeight: 900,
            color: '#e9d700', textAlign: 'center',
            textShadow: '0 0 30px rgba(233,215,0,0.2)',
          }}>
            रिटायर्मेंट में कम बोझ चाहिए, ज्यादा नहीं
          </div>
        </div>

        {/* Stick figure — burdened */}
        <div style={{
          position: 'absolute', left: 550, top: 430,
          transform: `scale(${sp(55)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: sp(55) * 0.4,
        }}>
          <svg width="70" height="120" viewBox="0 0 70 120">
            <circle cx="35" cy="20" r="14" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            <circle cx="29" cy="18" r="4" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <circle cx="41" cy="18" r="4" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="35" y1="34" x2="35" y2="75" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="35" y1="50" x2="15" y2="65" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="35" y1="50" x2="55" y2="65" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="35" y1="75" x2="20" y2="110" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="35" y1="75" x2="50" y2="110" stroke="#9E9E9E" strokeWidth="2" />
          </svg>
        </div>

        {/* Vignette */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Ch6Scene5_MentalBojh;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene6_StatusSymbols',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene6_StatusSymbols: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camOrbit = interpolate(frame, [0, durationInFrames], [-8, 8], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const statusItems = [
    { icon: 'land', label: 'जमीन', delay: 35 },
    { icon: 'jewelry', label: 'जेवर', delay: 55 },
    { icon: 'luxury', label: 'महँगा सामान', delay: 75 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{ transform: `translateX(${camOrbit}px)`, width: '100%', height: '100%', position: 'absolute' }}>

        {/* Gradient background — luxurious dark gold */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: `radial-gradient(ellipse at 60% 50%, #2a2510 0%, #1C1C1C 50%, #0d0d0d 100%)`,
        }} />

        {/* Shape layers — diamond shapes */}
        {[0, 1, 2, 3, 4, 5].map(i => {
          const s = seededRandom(i + 200);
          const size = 100 + s * 220;
          const x = seededRandom(i + 201) * 1800;
          const y = seededRandom(i + 202) * 900;
          const rot = 45 + frame * 0.1 * (i % 2 === 0 ? 1 : -1);
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y,
              width: size, height: size,
              border: '1px solid rgba(255,193,7,0.06)',
              transform: `rotate(${rot}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.04})`,
              opacity: 0.05,
            }} />
          );
        })}

        {/* Motion texture — radiating lines from center */}
        {Array.from({ length: 16 }).map((_, i) => {
          const angle = (i / 16) * Math.PI * 2;
          const length = 600;
          const x2 = 960 + Math.cos(angle) * length;
          const y2 = 540 + Math.sin(angle) * length;
          return (
            <svg key={`ray-${i}`} style={{ position: 'absolute', width: '100%', height: '100%' }}>
              <line x1="960" y1="540" x2={x2} y2={y2} stroke="#FFC107" strokeWidth="0.5" opacity="0.04" />
            </svg>
          );
        })}

        {/* Particles */}
        {Array.from({ length: 25 }).map((_, i) => {
          const s = seededRandom(i + 210);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: seededRandom(i + 211) * 1920 + Math.sin(frame * 0.025 + i) * 12,
              top: seededRandom(i + 212) * 1080 + Math.cos(frame * 0.02 + i) * 9,
              width: 2 + s * 5, height: 2 + s * 5,
              borderRadius: '50%',
              backgroundColor: '#FFC107',
              opacity: 0.08 + s * 0.15,
            }} />
          );
        })}

        {/* Chapter badge */}
        <div style={{
          position: 'absolute', left: 100, top: 70,
          opacity: sp(5),
          transform: `scale(${sp(5)})`,
        }}>
          <div style={{
            background: 'linear-gradient(135deg, #FFC107, #e9d700)',
            color: '#1C1C1C',
            fontFamily: 'Inter, Poppins',
            fontSize: 26, fontWeight: 800,
            padding: '8px 26px',
            borderRadius: 6,
          }}>
            पाँचवी बात
          </div>
        </div>

        {/* Main headline */}
        <div style={{
          position: 'absolute', left: 100, top: 140,
          opacity: sp(12),
          transform: `translateY(${interpolate(sp(12), [0, 1], [50, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 90, fontWeight: 900,
            color: '#F5F5F5', lineHeight: 1.15,
          }}>
            <span style={{ color: '#FFC107' }}>उपलब्धि</span> का प्रतीक
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 60, fontWeight: 700,
            color: '#ff0000', marginTop: 8,
          }}>
            या चुपचाप जेब काटने वाले?
          </div>
        </div>

        {/* Status items — 3 cards with icons */}
        <div style={{
          position: 'absolute', left: 100, top: 450,
          display: 'flex', gap: 50,
        }}>
          {statusItems.map((item, idx) => (
            <div key={idx} style={{
              width: 340, height: 350,
              background: 'rgba(45,45,45,0.5)',
              borderRadius: 20,
              border: '2px solid rgba(255,193,7,0.15)',
              display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
              padding: 30,
              opacity: sp(item.delay),
              transform: `translateY(${interpolate(sp(item.delay), [0, 1], [50, 0])}px) scale(${sp(item.delay)})`,
              position: 'relative',
              overflow: 'hidden',
            }}>
              {/* Shine sweep */}
              <div style={{
                position: 'absolute',
                left: interpolate(frame, [item.delay, item.delay + 40], [-100, 500], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                top: 0, width: 80, height: '100%',
                background: 'linear-gradient(90deg, transparent, rgba(255,193,7,0.08), transparent)',
                transform: 'skewX(-15deg)',
              }} />

              {/* Icon */}
              <svg width="100" height="100" viewBox="0 0 24 24" fill="none">
                {item.icon === 'land' && (
                  <>
                    <path d="M3 21h18" stroke="#FFC107" strokeWidth="2" />
                    <path d="M5 21V7l7-4 7 4v14" stroke="#FFC107" strokeWidth="2" />
                    <rect x="9" y="13" width="6" height="8" stroke="#FFC107" strokeWidth="1.5" />
                  </>
                )}
                {item.icon === 'jewelry' && (
                  <>
                    <circle cx="12" cy="12" r="8" stroke="#FFC107" strokeWidth="2" />
                    <path d="M8 8l4 4 4-4" stroke="#FFC107" strokeWidth="2" />
                    <circle cx="12" cy="12" r="3" stroke="#FFC107" strokeWidth="1.5" />
                  </>
                )}
                {item.icon === 'luxury' && (
                  <>
                    <rect x="4" y="8" width="16" height="12" rx="2" stroke="#FFC107" strokeWidth="2" />
                    <path d="M8 8V6a4 4 0 018 0v2" stroke="#FFC107" strokeWidth="2" />
                    <line x1="12" y1="12" x2="12" y2="16" stroke="#FFC107" strokeWidth="2" />
                    <circle cx="12" cy="16" r="1" fill="#FFC107" />
                  </>
                )}
              </svg>

              <div style={{
                fontFamily: 'Noto Sans Devanagari', fontSize: 40, fontWeight: 800,
                color: '#FFC107', marginTop: 20,
              }}>
                {item.label}
              </div>

              {/* Scissors cutting — animated */}
              <div style={{
                marginTop: 15,
                opacity: interpolate(frame, [item.delay + 30, item.delay + 50], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}>
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
                  <circle cx="6" cy="6" r="3" stroke="#ff0000" strokeWidth="1.5" />
                  <circle cx="6" cy="18" r="3" stroke="#ff0000" strokeWidth="1.5" />
                  <line x1="8.5" y1="7.5" x2="20" y2="18" stroke="#ff0000" strokeWidth="2" />
                  <line x1="8.5" y1="16.5" x2="20" y2="6" stroke="#ff0000" strokeWidth="2" />
                </svg>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari', fontSize: 22, color: '#ff0000', fontWeight: 600, marginTop: 4,
                }}>
                  जेब काट रहा
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Right side — stick figure with gold items weighing down */}
        <div style={{
          position: 'absolute', right: 100, top: 420,
          transform: `scale(${sp(45)}) translateY(${Math.sin(frame * 0.05) * 3}px)`,
          opacity: sp(45),
        }}>
          <svg width="120" height="200" viewBox="0 0 120 200">
            <circle cx="60" cy="35" r="40" fill="#FFC107" opacity="0.06" />
            <circle cx="60" cy="35" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="52" cy="33" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
            <circle cx="68" cy="33" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="58" y1="33" x2="62" y2="33" stroke="#FFC107" strokeWidth="2" />
            {/* Worried mouth */}
            <path d="M52 42 Q60 38 68 42" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            <line x1="60" y1="55" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arms holding heavy bags */}
            <line x1="60" y1="75" x2="25" y2="95" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="60" y1="75" x2="95" y2="95" stroke="#F5F5F5" strokeWidth="3" />
            {/* Heavy bags */}
            <rect x="10" y="95" width="30" height="25" rx="4" fill="#2D2D2D" stroke="#FFC107" strokeWidth="1.5" />
            <rect x="80" y="95" width="30" height="25" rx="4" fill="#2D2D2D" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="60" y1="120" x2="40" y2="180" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="60" y1="120" x2="80" y2="180" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        </div>

        {/* Vignette */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Ch6Scene6_StatusSymbols;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene7_TeenZameen',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene7_TeenZameen: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake at frame 90
  const shakeX = frame > 85 && frame < 100 ? Math.sin(frame * 2.5) * 6 * interpolate(frame, [85, 100], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) : 0;
  const shakeY = frame > 85 && frame < 100 ? Math.cos(frame * 3) * 4 * interpolate(frame, [85, 100], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) : 0;

  const plots = [
    { label: 'जमीन 1', status: 'विवाद में', statusColor: '#ff0000', icon: '⚠️', delay: 25 },
    { label: 'जमीन 2', status: 'नगर निगम नोटिस', statusColor: '#ff0000', icon: '📄', delay: 45 },
    { label: 'जमीन 3', status: 'खरीदार ही नहीं', statusColor: '#ff0000', icon: '🚫', delay: 65 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{ transform: `translate(${shakeX}px, ${shakeY}px)`, width: '100%', height: '100%', position: 'absolute' }}>

        {/* Gradient */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: `linear-gradient(150deg, #1C1C1C 0%, #1a0e0e 40%, #2D2D2D 100%)`,
        }} />

        {/* Shape layers — irregular polygons */}
        {[0, 1, 2, 3].map(i => {
          const s = seededRandom(i + 230);
          const size = 200 + s * 300;
          const x = seededRandom(i + 231) * 1600;
          const y = seededRandom(i + 232) * 800;
          const rot = frame * 0.06 * (i % 2 === 0 ? 1 : -1);
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y,
              width: size, height: size,
              transform: `rotate(${rot}deg)`,
              opacity: 0.04,
            }}>
              <svg viewBox="0 0 100 100" width="100%" height="100%">
                <polygon points="20,10 80,5 95,50 70,95 10,80" fill="none" stroke="#ff0000" strokeWidth="1" />
              </svg>
            </div>
          );
        })}

        {/* Texture — crack lines */}
        {Array.from({ length: 8 }).map((_, i) => {
          const x1 = seededRandom(i + 240) * 1920;
          const y1 = seededRandom(i + 241) * 1080;
          const x2 = x1 + (seededRandom(i + 242) - 0.5) * 300;
          const y2 = y1 + (seededRandom(i + 243) - 0.5) * 200;
          return (
            <svg key={`crack-${i}`} style={{ position: 'absolute', width: '100%', height: '100%' }}>
              <line x1={x1} y1={y1} x2={x2} y2={y2} stroke="#ff0000" strokeWidth="0.8" opacity="0.06" />
            </svg>
          );
        })}

        {/* Particles */}
        {Array.from({ length: 18 }).map((_, i) => {
          const s = seededRandom(i + 250);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: seededRandom(i + 251) * 1920 + Math.sin(frame * 0.03 + i) * 10,
              top: seededRandom(i + 252) * 1080 + Math.cos(frame * 0.025 + i) * 8,
              width: 2 + s * 4, height: 2 + s * 4,
              borderRadius: '50%',
              backgroundColor: i % 4 === 0 ? '#ff0000' : '#9E9E9E',
              opacity: 0.1 + s * 0.12,
            }} />
          );
        })}

        {/* Top context */}
        <div style={{
          position: 'absolute', left: 100, top: 70,
          opacity: sp(8),
          transform: `translateY(${interpolate(sp(8), [0, 1], [40, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 44, fontWeight: 600, color: '#9E9E9E',
          }}>
            मेहनत का फल भोगने का <span style={{ color: '#e9d700' }}>हक है</span>...
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 62, fontWeight: 900, color: '#F5F5F5', marginTop: 10,
          }}>
            लेकिन एक <span style={{ color: '#ff0000' }}>मोड</span> आता है
          </div>
        </div>

        {/* Three land plots — visual cards */}
        <div style={{
          position: 'absolute', left: 80, top: 280,
          display: 'flex', gap: 45,
        }}>
          {plots.map((plot, idx) => (
            <div key={idx} style={{
              width: 520, height: 480,
              background: 'rgba(45,45,45,0.5)',
              borderRadius: 18,
              border: `2px solid rgba(255,0,0,${0.15 + idx * 0.1})`,
              padding: '30px',
              display: 'flex', flexDirection: 'column',
              opacity: sp(plot.delay),
              transform: `translateY(${interpolate(sp(plot.delay), [0, 1], [60, 0])}px) scale(${sp(plot.delay)})`,
              position: 'relative',
              overflow: 'hidden',
            }}>
              {/* Land plot SVG */}
              <svg width="460" height="200" viewBox="0 0 460 200">
                {/* Ground */}
                <rect x="30" y="130" width="400" height="60" rx="5" fill="none" stroke="#9E9E9E" strokeWidth="2" strokeDasharray="8 4" />
                {/* Fence posts */}
                {Array.from({ length: 6 }).map((_, f) => (
                  <g key={f}>
                    <line x1={50 + f * 80} y1="80" x2={50 + f * 80} y2="130" stroke="#9E9E9E" strokeWidth="2" />
                    <circle cx={50 + f * 80} cy="78" r="4" fill="#9E9E9E" opacity="0.5" />
                  </g>
                ))}
                {/* Fence wire */}
                <line x1="50" y1="95" x2="450" y2="95" stroke="#9E9E9E" strokeWidth="1" opacity="0.5" />
                <line x1="50" y1="115" x2="450" y2="115" stroke="#9E9E9E" strokeWidth="1" opacity="0.5" />

                {/* Problem indicator */}
                {idx === 1 && (
                  <g transform={`translate(200, 40) rotate(${Math.sin(frame * 0.06) * 5})`}>
                    <rect x="-50" y="-20" width="100" height="40" rx="4" fill="#ff0000" opacity="0.2" />
                    <text x="0" y="6" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="14" fill="#ff0000" fontWeight="700">
                      नोटिस
                    </text>
                  </g>
                )}
                {idx === 2 && (
                  <text x="230" y="110" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="18" fill="#ff0000" opacity="0.8">
                    कोई खरीदार नहीं
                  </text>
                )}
              </svg>

              {/* Label */}
              <div style={{
                fontFamily: 'Noto Sans Devanagari', fontSize: 38, fontWeight: 800,
                color: '#F5F5F5', marginTop: 15,
              }}>
                {plot.label}
              </div>

              {/* Status badge */}
              <div style={{
                marginTop: 12,
                background: 'rgba(255,0,0,0.1)',
                borderRadius: 8, padding: '8px 18px',
                display: 'inline-flex', alignItems: 'center', gap: 10,
                border: '1px solid rgba(255,0,0,0.3)',
                alignSelf: 'flex-start',
              }}>
                <svg width="22" height="22" viewBox="0 0 24 24">
                  <path d="M12 2L2 22h20L12 2z" stroke="#ff0000" strokeWidth="2" fill="none" />
                  <line x1="12" y1="9" x2="12" y2="15" stroke="#ff0000" strokeWidth="2" />
                  <circle cx="12" cy="18" r="1" fill="#ff0000" />
                </svg>
                <span style={{
                  fontFamily: 'Noto Sans Devanagari', fontSize: 28, fontWeight: 700, color: '#ff0000',
                }}>
                  {plot.status}
                </span>
              </div>
            </div>
          ))}
        </div>

        {/* Vignette */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Ch6Scene7_TeenZameen;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene8_DerhLakh',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene8_DerhLakh: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big number count-up animation
  const countUp = interpolate(frame, [60, 140], [0, 150000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const displayAmount = `₹${Math.floor(countUp).toLocaleString('en-IN')}+`;

  // Screen shake on number complete
  const shakePhase = frame > 135 && frame < 150;
  const sx = shakePhase ? Math.sin(frame * 3) * 5 * interpolate(frame, [135, 150], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) : 0;

  const expenses = [
    { label: 'सम्पत्ति कर', delay: 30 },
    { label: 'देखभाल', delay: 45 },
    { label: 'कानूनी लड़ाई', delay: 60 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{ transform: `translateX(${sx}px)`, width: '100%', height: '100%', position: 'absolute' }}>

        {/* Gradient */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: `radial-gradient(circle at 50% 50%, #2a0a0a 0%, #1C1C1C 55%, #0a0a0a 100%)`,
        }} />

        {/* Shape layers — rings */}
        {[0, 1, 2, 3].map(i => {
          const size = 200 + i * 180;
          const pulse = 1 + Math.sin(frame * 0.015 + i * 0.8) * 0.03;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: '50%', top: '50%',
              width: size, height: size,
              marginLeft: -size / 2, marginTop: -size / 2,
              borderRadius: '50%',
              border: '1.5px solid rgba(255,0,0,0.04)',
              transform: `scale(${pulse})`,
            }} />
          );
        })}

        {/* Particles */}
        {Array.from({ length: 20 }).map((_, i) => {
          const s = seededRandom(i + 260);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: seededRandom(i + 261) * 1920 + Math.sin(frame * 0.03 + i) * 10,
              top: seededRandom(i + 262) * 1080 + Math.cos(frame * 0.025 + i) * 8,
              width: 2 + s * 4, height: 2 + s * 4,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#ff0000' : '#F5F5F5',
              opacity: 0.1 + s * 0.12,
            }} />
          );
        })}

        {/* Center — big number */}
        <div style={{
          position: 'absolute',
          left: '50%', top: 80,
          transform: `translateX(-50%) scale(${sp(10)})`,
          opacity: sp(10),
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 42, fontWeight: 600, color: '#9E9E9E',
          }}>
            तीनों का सालाना खर्च
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 160, fontWeight: 900,
            color: '#ff0000', marginTop: 10,
            textShadow: '0 0 60px rgba(255,0,0,0.3)',
          }}>
            {displayAmount}
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 36, color: '#9E9E9E', marginTop: -10,
          }}>
            सालाना — बिना कोई रिटर्न
          </div>
        </div>

        {/* Expense breakdown — 3 columns */}
        <div style={{
          position: 'absolute', left: '50%', top: 480,
          transform: 'translateX(-50%)',
          display: 'flex', gap: 60,
        }}>
          {expenses.map((exp, idx) => (
            <div key={idx} style={{
              textAlign: 'center',
              opacity: sp(exp.delay),
              transform: `translateY(${interpolate(sp(exp.delay), [0, 1], [40, 0])}px)`,
            }}>
              <div style={{
                width: 100, height: 100,
                borderRadius: '50%',
                border: '3px solid #ff0000',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                margin: '0 auto 15px',
                background: 'rgba(255,0,0,0.06)',
              }}>
                <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                  {idx === 0 && <><rect x="3" y="3" width="18" height="18" rx="2" stroke="#ff0000" strokeWidth="2" /><path d="M9 7v10M15 7v10M3 12h18" stroke="#ff0000" strokeWidth="1.5" /></>}
                  {idx === 1 && <><path d="M14.7 6.3a1 1 0 000 1.4l1.6 1.6a1 1 0 001.4 0l3.77-3.77a6 6 0 01-7.94 7.94l-6.91 6.91a2.12 2.12 0 01-3-3l6.91-6.91a6 6 0 017.94-7.94l-3.76 3.76z" stroke="#ff0000" strokeWidth="2" /></>}
                  {idx === 2 && <><path d="M12 2L2 7l10 5 10-5-10-5z" stroke="#ff0000" strokeWidth="2" /><path d="M2 17l10 5 10-5M2 12l10 5 10-5" stroke="#ff0000" strokeWidth="2" /></>}
                </svg>
              </div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari', fontSize: 34, fontWeight: 700, color: '#F5F5F5',
              }}>
                {exp.label}
              </div>
            </div>
          ))}
        </div>

        {/* Bottom — "बदले में" result */}
        <div style={{
          position: 'absolute', bottom: 80, left: '50%',
          transform: `translateX(-50%) scale(${sp(100)})`,
          opacity: sp(100),
        }}>
          <div style={{
            display: 'flex', gap: 80, alignItems: 'center',
          }}>
            <div style={{
              background: 'rgba(255,0,0,0.1)',
              borderRadius: 14, padding: '14px 35px',
              border: '2px solid rgba(255,0,0,0.3)',
              display: 'flex', alignItems: 'center', gap: 15,
            }}>
              <svg width="36" height="36" viewBox="0 0 24 24">
                <circle cx="12" cy="12" r="10" stroke="#ff0000" strokeWidth="2" fill="none" />
                <line x1="8" y1="8" x2="16" y2="16" stroke="#ff0000" strokeWidth="2.5" />
                <line x1="16" y1="8" x2="8" y2="16" stroke="#ff0000" strokeWidth="2.5" />
              </svg>
              <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 38, fontWeight: 800, color: '#ff0000' }}>
                ना आमदनी
              </span>
            </div>
            <div style={{
              background: 'rgba(255,0,0,0.1)',
              borderRadius: 14, padding: '14px 35px',
              border: '2px solid rgba(255,0,0,0.3)',
              display: 'flex', alignItems: 'center', gap: 15,
            }}>
              <svg width="36" height="36" viewBox="0 0 24 24">
                <circle cx="12" cy="12" r="10" stroke="#ff0000" strokeWidth="2" fill="none" />
                <line x1="8" y1="8" x2="16" y2="16" stroke="#ff0000" strokeWidth="2.5" />
                <line x1="16" y1="8" x2="8" y2="16" stroke="#ff0000" strokeWidth="2.5" />
              </svg>
              <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 38, fontWeight: 800, color: '#ff0000' }}>
                ना सुकून
              </span>
            </div>
          </div>
        </div>

        {/* Vignette */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.7) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Ch6Scene8_DerhLakh;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene9_BojhKeMalik',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene9_BojhKeMalik: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Transformation animation — heavy to light
  const transformPhase = interpolate(frame, [100, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Background transitions from dark red to green */}
      <div style={{
        position: 'absolute', width: '100%', height: '100%',
        background: interpolate(transformPhase, [0, 1], [0, 1]) > 0.5
          ? `radial-gradient(ellipse at 50% 50%, #0d1f0d 0%, #1C1C1C 60%)`
          : `radial-gradient(ellipse at 50% 50%, #1f0d0d 0%, #1C1C1C 60%)`,
        opacity: 1,
      }} />

      {/* Shape layers — crosses (burden) transitioning to open circles (freedom) */}
      {[0, 1, 2, 3, 4].map(i => {
        const s = seededRandom(i + 280);
        const size = 120 + s * 250;
        const x = seededRandom(i + 281) * 1700;
        const y = seededRandom(i + 282) * 900;
        const rot = frame * 0.1 * (i % 2 === 0 ? 1 : -1);
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: size, height: size,
            borderRadius: transformPhase > 0.5 ? '50%' : '0%',
            border: `1px solid ${transformPhase > 0.5 ? 'rgba(233,215,0,0.06)' : 'rgba(255,0,0,0.05)'}`,
            transform: `rotate(${rot}deg)`,
            transition: 'border-radius 0.5s',
          }} />
        );
      })}

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const s = seededRandom(i + 290);
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute',
            left: seededRandom(i + 291) * 1920 + Math.sin(frame * 0.03 + i) * 12,
            top: seededRandom(i + 292) * 1080 + Math.cos(frame * 0.025 + i) * 10,
            width: 2 + s * 4, height: 2 + s * 4,
            borderRadius: '50%',
            backgroundColor: transformPhase > 0.5 ? '#e9d700' : '#ff0000',
            opacity: 0.1 + s * 0.15,
          }} />
        );
      })}

      {/* Before/After split */}
      {/* BEFORE — left side */}
      <div style={{
        position: 'absolute', left: 80, top: 100, width: 800,
        opacity: interpolate(transformPhase, [0, 0.8], [1, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 36, fontWeight: 600, color: '#ff0000',
          marginBottom: 15, opacity: sp(8),
        }}>
          पहले
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 68, fontWeight: 900, color: '#F5F5F5',
          lineHeight: 1.3, opacity: sp(15),
          transform: `translateY(${interpolate(sp(15), [0, 1], [40, 0])}px)`,
        }}>
          सालों से <span style={{ color: '#ff0000' }}>संपत्ती के नहीं</span>,
          <br /><span style={{ color: '#ff0000', fontSize: 80 }}>बोझ के मालिक</span> थे
        </div>

        {/* Heavy chain visual */}
        <div style={{
          marginTop: 30, opacity: sp(30),
          transform: `scale(${sp(30)})`,
        }}>
          <svg width="500" height="120" viewBox="0 0 500 120">
            {Array.from({ length: 8 }).map((_, c) => (
              <g key={c}>
                <ellipse cx={30 + c * 60} cy="60" rx="25" ry="35" fill="none" stroke="#9E9E9E" strokeWidth="3" opacity="0.6" />
              </g>
            ))}
            {/* Weight at end */}
            <rect x="460" y="30" width="40" height="60" rx="5" fill="#2D2D2D" stroke="#ff0000" strokeWidth="2" />
            <text x="480" y="68" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="16" fill="#ff0000" fontWeight="700">
              बोझ
            </text>
          </svg>
        </div>
      </div>

      {/* Transition arrow */}
      <div style={{
        position: 'absolute', left: '50%', top: '50%',
        transform: `translate(-50%, -50%) scale(${sp(80)})`,
        opacity: sp(80),
      }}>
        <svg width="120" height="80" viewBox="0 0 120 80">
          <path d="M10 40 L90 40 M75 20 L95 40 L75 60" stroke="#e9d700" strokeWidth="4" fill="none" />
        </svg>
      </div>

      {/* AFTER — right side */}
      <div style={{
        position: 'absolute', right: 80, top: 100, width: 750,
        opacity: sp(90),
        transform: `translateX(${interpolate(sp(90), [0, 1], [80, 0])}px)`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 36, fontWeight: 600, color: '#e9d700',
          marginBottom: 15,
        }}>
          बाद
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 68, fontWeight: 900, color: '#F5F5F5',
          lineHeight: 1.3,
        }}>
          दो <span style={{ color: '#e9d700' }}>बेचीं</span> — एहसास हुआ
          <br /><span style={{ fontSize: 50, color: '#e9d700' }}>आजादी मिली</span>
        </div>

        {/* Broken chain */}
        <div style={{
          marginTop: 30, opacity: sp(110),
          transform: `scale(${sp(110)})`,
        }}>
          <svg width="500" height="120" viewBox="0 0 500 120">
            {Array.from({ length: 3 }).map((_, c) => (
              <ellipse key={c} cx={30 + c * 60} cy="60" rx="25" ry="35" fill="none" stroke="#9E9E9E" strokeWidth="2" opacity="0.3" strokeDasharray="5 5" />
            ))}
            {/* Break mark */}
            <text x="220" y="70" fontFamily="Inter" fontSize="40" fill="#e9d700" fontWeight="900">✂️</text>
            {/* Free bird */}
            <path d="M300 50 Q320 30 340 50 Q360 30 380 50" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <path d="M350 45 Q370 25 390 45 Q410 25 430 45" fill="none" stroke="#e9d700" strokeWidth="2" opacity="0.6" />
          </svg>
        </div>
      </div>

      {/* Bottom insight */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: `translateX(-50%) translateY(${interpolate(sp(130), [0, 1], [30, 0])}px)`,
        opacity: sp(130),
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 40, fontWeight: 700,
          color: '#9E9E9E', textAlign: 'center',
          background: 'rgba(45,45,45,0.6)', padding: '14px 40px', borderRadius: 12,
        }}>
          सिर्फ जमीन ही नहीं — <span style={{ color: '#e9d700' }}>पुराना सामान जो बरसों से बंद है</span> — वो भी बोझ है
        </div>
      </div>

      {/* Stick figure — freed, arms up */}
      <div style={{
        position: 'absolute', right: 140, bottom: 160,
        transform: `scale(${sp(100)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
        opacity: sp(100),
      }}>
        <svg width="80" height="140" viewBox="0 0 80 140">
          <circle cx="40" cy="25" r="30" fill="#e9d700" opacity="0.06" />
          <circle cx="40" cy="25" r="16" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <circle cx="34" cy="23" r="5" fill="none" stroke="#FFC107" strokeWidth="2" />
          <circle cx="46" cy="23" r="5" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="39" y1="23" x2="41" y2="23" stroke="#FFC107" strokeWidth="2" />
          <path d="M34 30 Q40 36 46 30" fill="none" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="40" y1="41" x2="40" y2="88" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="40" y1="58" x2="15" y2="40" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="40" y1="58" x2="65" y2="40" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="40" y1="88" x2="22" y2="130" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="40" y1="88" x2="58" y2="130" stroke="#F5F5F5" strokeWidth="3" />
        </svg>
      </div>

      {/* Vignette */}
      <div style={{
        position: 'absolute', width: '100%', height: '100%',
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch6Scene9_BojhKeMalik;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene10_PuraniPehchaan',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene10_PuraniPehchaan: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slow zoom in
  const camScale = interpolate(frame, [0, durationInFrames], [0.96, 1.04], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Mirror crack animation
  const crackProgress = interpolate(frame, [80, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{ transform: `scale(${camScale})`, width: '100%', height: '100%', position: 'absolute' }}>

        {/* Gradient — introspective deep blue */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: `radial-gradient(ellipse at 40% 40%, #0d1425 0%, #1C1C1C 55%, #0a0a0a 100%)`,
        }} />

        {/* Shape layers — mirrors/rectangles */}
        {[0, 1, 2, 3, 4].map(i => {
          const s = seededRandom(i + 300);
          const w = 100 + s * 200;
          const h = 150 + s * 250;
          const x = seededRandom(i + 301) * 1600;
          const y = seededRandom(i + 302) * 800;
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y,
              width: w, height: h,
              border: '1px solid rgba(245,245,245,0.03)',
              borderRadius: 4,
              transform: `rotate(${frame * 0.05 * (i % 2 === 0 ? 1 : -1)}deg)`,
            }} />
          );
        })}

        {/* Motion texture — fading horizontal stripes */}
        {Array.from({ length: 20 }).map((_, i) => (
          <div key={`stripe-${i}`} style={{
            position: 'absolute',
            left: 0, top: i * 56,
            width: '100%', height: 1,
            backgroundColor: '#F5F5F5',
            opacity: 0.02 + Math.sin(frame * 0.02 + i * 0.5) * 0.01,
          }} />
        ))}

        {/* Particles */}
        {Array.from({ length: 20 }).map((_, i) => {
          const s = seededRandom(i + 310);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: seededRandom(i + 311) * 1920 + Math.sin(frame * 0.025 + i) * 10,
              top: seededRandom(i + 312) * 1080 + Math.cos(frame * 0.02 + i) * 8,
              width: 2 + s * 3, height: 2 + s * 3,
              borderRadius: '50%',
              backgroundColor: '#F5F5F5',
              opacity: 0.08 + s * 0.1,
            }} />
          );
        })}

        {/* Chapter badge */}
        <div style={{
          position: 'absolute', left: 100, top: 60,
          opacity: sp(5),
          transform: `scale(${sp(5)})`,
        }}>
          <div style={{
            background: 'rgba(245,245,245,0.1)',
            border: '2px solid rgba(245,245,245,0.2)',
            color: '#F5F5F5',
            fontFamily: 'Inter, Poppins',
            fontSize: 26, fontWeight: 800,
            padding: '8px 26px',
            borderRadius: 6,
          }}>
            छठवीं बात — सबसे गहरी
          </div>
        </div>

        {/* Center — cracking mirror with identity */}
        <div style={{
          position: 'absolute', left: '50%', top: '50%',
          transform: `translate(-50%, -50%) scale(${sp(15)})`,
          opacity: sp(15),
        }}>
          <svg width="600" height="500" viewBox="0 0 600 500">
            {/* Mirror frame */}
            <rect x="100" y="30" width="400" height="440" rx="20" fill="none" stroke="#FFC107" strokeWidth="3" />
            <rect x="110" y="40" width="380" height="420" rx="16" fill="rgba(45,45,45,0.4)" />

            {/* Reflection — old identity titles */}
            <text x="300" y="130" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="32" fill="#FFC107" opacity={0.7}>
              अधिकारी
            </text>
            <text x="300" y="180" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="28" fill="#9E9E9E" opacity={0.5}>
              प्राध्यापक
            </text>
            <text x="300" y="225" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="28" fill="#9E9E9E" opacity={0.5}>
              अभियंता
            </text>

            {/* Face silhouette */}
            <ellipse cx="300" cy="310" rx="70" ry="85" fill="none" stroke="#F5F5F5" strokeWidth="2" opacity="0.4" />
            <circle cx="280" cy="290" r="8" fill="#F5F5F5" opacity="0.3" />
            <circle cx="320" cy="290" r="8" fill="#F5F5F5" opacity="0.3" />
            <path d="M285 330 Q300 345 315 330" fill="none" stroke="#F5F5F5" strokeWidth="1.5" opacity="0.3" />

            {/* Cracks — animated */}
            <path
              d={`M300 40 L${290 + crackProgress * 20} ${40 + crackProgress * 200} L${270 + crackProgress * 10} ${240 + crackProgress * 150}`}
              fill="none" stroke="#ff0000" strokeWidth="2"
              opacity={crackProgress * 0.8}
              strokeDasharray={crackProgress * 500}
            />
            <path
              d={`M300 40 L${315 + crackProgress * 15} ${40 + crackProgress * 180} L${340 + crackProgress * 20} ${220 + crackProgress * 200}`}
              fill="none" stroke="#ff0000" strokeWidth="1.5"
              opacity={crackProgress * 0.6}
              strokeDasharray={crackProgress * 500}
            />
            <path
              d={`M${290 + crackProgress * 20} ${140 + crackProgress * 100} L${240 + crackProgress * 5} ${200 + crackProgress * 80}`}
              fill="none" stroke="#ff0000" strokeWidth="1"
              opacity={crackProgress * 0.5}
            />

            {/* "?" on forehead area */}
            <text x="300" y="420" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="48" fill="#e9d700" fontWeight="900"
              opacity={interpolate(frame, [120, 150], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}>
              अब मैं कौन?
            </text>
          </svg>
        </div>

        {/* Left text */}
        <div style={{
          position: 'absolute', left: 80, top: 200, width: 350,
          opacity: sp(35),
          transform: `translateX(${interpolate(sp(35), [0, 1], [-50, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 42, fontWeight: 800,
            color: '#F5F5F5', lineHeight: 1.4,
          }}>
            वो <span style={{ color: '#e9d700' }}>पुरानी पहचान</span>
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 34, fontWeight: 600,
            color: '#9E9E9E', marginTop: 10, lineHeight: 1.5,
          }}>
            जो अब आपकी नहीं रही...
          </div>
        </div>

        {/* Right text */}
        <div style={{
          position: 'absolute', right: 80, top: 200, width: 350,
          opacity: sp(55),
          transform: `translateX(${interpolate(sp(55), [0, 1], [50, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 42, fontWeight: 800,
            color: '#ff0000', lineHeight: 1.4,
          }}>
            जबर्दस्ती जिन्दा
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 34, fontWeight: 600,
            color: '#9E9E9E', marginTop: 10, lineHeight: 1.5,
          }}>
            रखे हुए हैं...
          </div>
        </div>

        {/* Bottom — deep line */}
        <div style={{
          position: 'absolute', bottom: 70, left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(sp(100), [0, 1], [30, 0])}px)`,
          opacity: sp(100),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 48, fontWeight: 900,
            color: '#e9d700', textAlign: 'center',
            textShadow: '0 0 40px rgba(233,215,0,0.15)',
          }}>
            ये सिर्फ पद नहीं था — ये परिचय था
          </div>
        </div>

        {/* Stick figure — looking in mirror */}
        <div style={{
          position: 'absolute', left: 140, bottom: 120,
          transform: `scale(${sp(40)}) translateY(${Math.sin(frame * 0.05) * 2}px) rotate(${Math.sin(frame * 0.04) * 3}deg)`,
          opacity: sp(40) * 0.5,
        }}>
          <svg width="70" height="120" viewBox="0 0 70 120">
            <circle cx="35" cy="20" r="14" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            <circle cx="29" cy="18" r="4" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <circle cx="41" cy="18" r="4" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="35" y1="34" x2="35" y2="72" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="35" y1="48" x2="55" y2="40" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="35" y1="48" x2="15" y2="60" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="35" y1="72" x2="20" y2="110" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="35" y1="72" x2="50" y2="110" stroke="#9E9E9E" strokeWidth="2" />
          </svg>
        </div>

        {/* Vignette */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 42%, rgba(0,0,0,0.7) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Ch6Scene10_PuraniPehchaan;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene11_PadParichay',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene11_PadParichay: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Orbit movement
  const orbitX = Math.sin(frame * 0.008) * 20;
  const orbitY = Math.cos(frame * 0.006) * 12;

  const roles = [
    { title: 'अधिकारी', years: '30 बरस', delay: 20 },
    { title: 'प्राध्यापक', years: '30 बरस', delay: 45 },
    { title: 'अभियंता', years: '30 बरस', delay: 70 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{ transform: `translate(${orbitX}px, ${orbitY}px)`, width: '100%', height: '100%', position: 'absolute' }}>

        {/* Gradient — warm nostalgic */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: `radial-gradient(ellipse at 50% 60%, #1f1a10 0%, #1C1C1C 50%, #0a0a0a 100%)`,
        }} />

        {/* Shape layers — overlapping ovals */}
        {[0, 1, 2, 3, 4, 5].map(i => {
          const s = seededRandom(i + 330);
          const w = 150 + s * 250;
          const h = 100 + s * 180;
          const x = seededRandom(i + 331) * 1700;
          const y = seededRandom(i + 332) * 900;
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y,
              width: w, height: h,
              borderRadius: '50%',
              border: '1px solid rgba(255,193,7,0.04)',
              transform: `rotate(${frame * 0.08 * (i % 2 === 0 ? 1 : -1)}deg) scale(${1 + Math.sin(frame * 0.015 + i) * 0.04})`,
            }} />
          );
        })}

        {/* Motion texture — timeline dots */}
        <div style={{ position: 'absolute', left: 0, top: 520, width: '100%', height: 4 }}>
          <svg width="1920" height="4">
            <line x1="0" y1="2" x2="1920" y2="2" stroke="#FFC107" strokeWidth="2" opacity="0.15" strokeDasharray="4 12" strokeDashoffset={frame * 1.5} />
          </svg>
        </div>

        {/* Particles */}
        {Array.from({ length: 22 }).map((_, i) => {
          const s = seededRandom(i + 340);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: seededRandom(i + 341) * 1920 + Math.sin(frame * 0.025 + i) * 10,
              top: seededRandom(i + 342) * 1080 + Math.cos(frame * 0.02 + i) * 8,
              width: 2 + s * 4, height: 2 + s * 4,
              borderRadius: '50%',
              backgroundColor: '#FFC107',
              opacity: 0.08 + s * 0.12,
            }} />
          );
        })}

        {/* Main content — 3 role cards in timeline */}
        <div style={{
          position: 'absolute', left: '50%', top: 100,
          transform: 'translateX(-50%)',
          display: 'flex', gap: 80, alignItems: 'flex-start',
        }}>
          {roles.map((role, idx) => (
            <div key={idx} style={{
              width: 420,
              opacity: sp(role.delay),
              transform: `translateY(${interpolate(sp(role.delay), [0, 1], [80, 0])}px) scale(${sp(role.delay)})`,
              textAlign: 'center',
            }}>
              {/* ID card style */}
              <div style={{
                background: 'rgba(45,45,45,0.6)',
                borderRadius: 18,
                border: '2px solid rgba(255,193,7,0.2)',
                padding: '30px 25px',
                position: 'relative',
                overflow: 'hidden',
              }}>
                {/* Top bar */}
                <div style={{
                  position: 'absolute', top: 0, left: 0, right: 0, height: 6,
                  background: 'linear-gradient(90deg, #FFC107, #e9d700)',
                }} />

                {/* Avatar circle */}
                <div style={{
                  width: 100, height: 100,
                  borderRadius: '50%',
                  border: '3px solid #FFC107',
                  margin: '15px auto 20px',
                  display: 'flex', alignItems: 'center', justifyContent: 'center',
                  background: 'rgba(255,193,7,0.06)',
                }}>
                  <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
                    <circle cx="12" cy="8" r="4" stroke="#FFC107" strokeWidth="2" />
                    <path d="M4 20c0-4 4-7 8-7s8 3 8 7" stroke="#FFC107" strokeWidth="2" />
                  </svg>
                </div>

                {/* Title */}
                <div style={{
                  fontFamily: 'Noto Sans Devanagari', fontSize: 50, fontWeight: 900,
                  color: '#FFC107',
                }}>
                  {role.title}
                </div>

                {/* Years badge */}
                <div style={{
                  marginTop: 12,
                  background: 'rgba(255,193,7,0.1)',
                  borderRadius: 8, padding: '6px 18px',
                  display: 'inline-block',
                }}>
                  <span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700, color: '#e9d700' }}>
                    {role.years}
                  </span>
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Bottom insight — what it really was */}
        <div style={{
          position: 'absolute', bottom: 100, left: '50%',
          transform: `translateX(-50%)`,
          width: 1400,
        }}>
          <div style={{
            display: 'flex', justifyContent: 'center', gap: 60,
          }}>
            {[
              { word: 'परिचय', delay: 95 },
              { word: 'भाषा', delay: 110 },
              { word: 'दुनिया से जुड़ने का तरीका', delay: 125 },
            ].map((item, idx) => (
              <div key={idx} style={{
                opacity: sp(item.delay),
                transform: `translateY(${interpolate(sp(item.delay), [0, 1], [25, 0])}px)`,
              }}>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari', fontSize: 40, fontWeight: 800,
                  color: '#F5F5F5',
                  borderBottom: '3px solid #e9d700',
                  paddingBottom: 8,
                }}>
                  {item.word}
                </div>
              </div>
            ))}
          </div>

          <div style={{
            textAlign: 'center', marginTop: 25,
            opacity: sp(140),
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 36, fontWeight: 600,
              color: '#9E9E9E',
            }}>
              ये सिर्फ <span style={{ color: '#e9d700' }}>पद</span> नहीं था...
            </div>
          </div>
        </div>

        {/* Stick figure — proud pose with name badge */}
        <div style={{
          position: 'absolute', left: 80, bottom: 50,
          transform: `scale(${sp(50)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: sp(50),
        }}>
          <svg width="90" height="150" viewBox="0 0 90 150">
            <circle cx="45" cy="28" r="35" fill="#FFC107" opacity="0.06" />
            <circle cx="45" cy="28" r="17" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="38" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <circle cx="52" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="43" y1="26" x2="47" y2="26" stroke="#FFC107" strokeWidth="2" />
            {/* Proud stance */}
            <line x1="45" y1="45" x2="45" y2="98" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="45" y1="62" x2="18" y2="78" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="45" y1="62" x2="72" y2="78" stroke="#F5F5F5" strokeWidth="3" />
            {/* Name badge on chest */}
            <rect x="32" y="68" width="26" height="14" rx="2" fill="#FFC107" opacity="0.4" />
            <line x1="45" y1="98" x2="28" y2="142" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="45" y1="98" x2="62" y2="142" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        </div>

        {/* Vignette */}
        <div style={{
          position: 'absolute', width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Ch6Scene11_PadParichay;
```

