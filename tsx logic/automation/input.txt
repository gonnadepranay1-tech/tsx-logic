```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene51PartageValeur',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene51PartageValeur: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title reveal
  const titleY = interpolate(sp(10), [0, 1], [60, 0]);
  const titleOp = sp(10);

  // Law badge
  const badgeScale = sp(25);

  // Two pillars
  const pillar1 = sp(60);
  const pillar2 = sp(85);

  // Plus sign
  const plusScale = sp(110);
  const plusRotate = interpolate(frame, [110, 140], [0, 360], { extrapolateRight: 'clamp' });

  // Result bar
  const resultWidth = interpolate(sp(150), [0, 1], [0, 800]);
  const resultOp = sp(150);

  // Salary line (unchanged)
  const salaryLineDash = interpolate(frame, [180, 240], [0, 1000], { extrapolateRight: 'clamp' });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.05]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 20%, rgba(246,36,64,0.08) 0%, transparent 55%), radial-gradient(circle at 75% 80%, rgba(0,0,0,0.06) 0%, transparent 60%)',
        }}
      />

      {/* Animated grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid51" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid51)" />
      </svg>

      {/* Drifting transparent shapes */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          borderRadius: '50%',
          border: '2px solid #000000',
          opacity: 0.04,
          top: -200,
          right: -150,
          transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 400,
          height: 400,
          background: '#F62440',
          opacity: 0.05,
          bottom: -100,
          left: -100,
          transform: `rotate(${-frame * 0.15}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 300,
          height: 300,
          border: '3px solid #F62440',
          opacity: 0.06,
          top: '40%',
          left: '5%',
          transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
        }}
      />

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = seededRandom(i + 51);
        const x = seed * 1920;
        const y = (seededRandom(i + 99) * 1080 + frame * 0.4) % 1080;
        const size = 2 + seededRandom(i + 7) * 4;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: size,
              height: size,
              borderRadius: '50%',
              background: i % 3 === 0 ? '#F62440' : '#000000',
              opacity: 0.25,
            }}
          />
        );
      })}

      {/* Main camera container */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top badge - LOI */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: '50%',
            transform: `translateX(-50%) scale(${badgeScale})`,
            display: 'flex',
            alignItems: 'center',
            gap: 16,
            background: '#000000',
            padding: '14px 32px',
            borderRadius: 50,
          }}
        >
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
            <path d="M12 2L4 6V12C4 17 7.5 21 12 22C16.5 21 20 17 20 12V6L12 2Z" stroke="#F62440" strokeWidth="2" />
            <path d="M9 12L11 14L15 10" stroke="#F62440" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <span
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 22,
              color: '#DDDDDD',
              letterSpacing: 4,
              fontWeight: 700,
            }}
          >
            LOI PARTAGE DE LA VALEUR
          </span>
        </div>

        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 170,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `translateY(${titleY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 110,
              fontWeight: 800,
              color: '#000000',
              lineHeight: 1,
              letterSpacing: -2,
            }}
          >
            Augmentez votre revenu
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 110,
              fontWeight: 800,
              color: '#F62440',
              lineHeight: 1,
              letterSpacing: -2,
              marginTop: 8,
              fontStyle: 'italic',
            }}
          >
            sans toucher au salaire.
          </div>
        </div>

        {/* Two pillars */}
        <div
          style={{
            position: 'absolute',
            top: 470,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            alignItems: 'center',
            gap: 60,
          }}
        >
          {/* Pillar 1 - Intéressement */}
          <div
            style={{
              opacity: pillar1,
              transform: `translateY(${interpolate(pillar1, [0, 1], [40, 0])}px)`,
              background: '#000000',
              padding: '32px 40px',
              borderRadius: 20,
              minWidth: 380,
              textAlign: 'center',
              border: '3px solid #F62440',
            }}
          >
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 12 }}>
              <circle cx="12" cy="12" r="10" stroke="#F62440" strokeWidth="2" />
              <path d="M8 12H16M12 8V16" stroke="#F62440" strokeWidth="2.5" strokeLinecap="round" />
            </svg>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 60,
                color: '#DDDDDD',
                lineHeight: 1,
                letterSpacing: 1,
              }}
            >
              INTÉRESSEMENT
            </div>
            <div
              style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 26,
                color: '#9E9E9E',
                marginTop: 10,
                fontWeight: 500,
              }}
            >
              Prime liée à la performance
            </div>
          </div>

          {/* Plus sign rotating */}
          <div
            style={{
              transform: `scale(${plusScale}) rotate(${plusRotate}deg)`,
              width: 70,
              height: 70,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <svg width="70" height="70" viewBox="0 0 24 24" fill="none">
              <path d="M12 4V20M4 12H20" stroke="#F62440" strokeWidth="4" strokeLinecap="round" />
            </svg>
          </div>

          {/* Pillar 2 - Participation */}
          <div
            style={{
              opacity: pillar2,
              transform: `translateY(${interpolate(pillar2, [0, 1], [40, 0])}px)`,
              background: '#F62440',
              padding: '32px 40px',
              borderRadius: 20,
              minWidth: 380,
              textAlign: 'center',
              border: '3px solid #000000',
            }}
          >
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 12 }}>
              <path d="M12 2L2 7L12 12L22 7L12 2Z" stroke="#000000" strokeWidth="2" strokeLinejoin="round" />
              <path d="M2 17L12 22L22 17" stroke="#000000" strokeWidth="2" strokeLinejoin="round" />
              <path d="M2 12L12 17L22 12" stroke="#000000" strokeWidth="2" strokeLinejoin="round" />
            </svg>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 60,
                color: '#000000',
                lineHeight: 1,
                letterSpacing: 1,
              }}
            >
              PARTICIPATION
            </div>
            <div
              style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 26,
                color: '#000000',
                marginTop: 10,
                fontWeight: 600,
              }}
            >
              Part des bénéfices entreprise
            </div>
          </div>
        </div>

        {/* Result equation - Revenu augmente */}
        <div
          style={{
            position: 'absolute',
            top: 760,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            opacity: resultOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 38,
              color: '#000000',
              fontWeight: 600,
              marginBottom: 14,
              letterSpacing: 1,
            }}
          >
            REVENU ANNUEL
          </div>
          <div style={{ position: 'relative', width: 820, height: 50 }}>
            {/* Track */}
            <div
              style={{
                position: 'absolute',
                width: '100%',
                height: 50,
                background: '#000000',
                opacity: 0.1,
                borderRadius: 25,
              }}
            />
            {/* Fill */}
            <div
              style={{
                position: 'absolute',
                width: resultWidth,
                height: 50,
                background: 'linear-gradient(90deg, #000000 0%, #F62440 100%)',
                borderRadius: 25,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'flex-end',
                paddingRight: 20,
              }}
            >
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
                <path d="M7 17L17 7M17 7H8M17 7V16" stroke="#DDDDDD" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </div>
          </div>
        </div>

        {/* Bottom - Salary unchanged line */}
        <div
          style={{
            position: 'absolute',
            top: 900,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            opacity: interpolate(frame, [180, 210], [0, 1], { extrapolateRight: 'clamp' }),
          }}
        >
          <svg width="34" height="34" viewBox="0 0 24 24" fill="none">
            <rect x="3" y="6" width="18" height="12" rx="2" stroke="#000000" strokeWidth="2" />
            <line x1="3" y1="11" x2="21" y2="11" stroke="#000000" strokeWidth="2" />
          </svg>
          <span
            style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 34,
              color: '#000000',
              fontWeight: 700,
            }}
          >
            Salaire de base
          </span>
          <svg width="220" height="20">
            <line
              x1="0"
              y1="10"
              x2="220"
              y2="10"
              stroke="#000000"
              strokeWidth="3"
              strokeDasharray="1000"
              strokeDashoffset={1000 - salaryLineDash * 0.22}
            />
          </svg>
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 36,
              color: '#F62440',
              letterSpacing: 2,
            }}
          >
            INCHANGÉ
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene51PartageValeur;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene52NuanceGeo',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene52NuanceGeo: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Eye reveal
  const eyeScale = sp(10);
  const eyePulse = 1 + Math.sin(frame * 0.08) * 0.05;

  // Headline words
  const word1 = sp(35);
  const word2 = sp(55);
  const word3 = sp(75);

  // Map outline
  const mapDraw = interpolate(frame, [100, 180], [0, 1], { extrapolateRight: 'clamp' });
  const mapOp = sp(100);

  // Pin pulses
  const pinPulse = (delay: number) => 1 + Math.sin((frame - delay) * 0.15) * 0.15;

  // Bottom strip
  const stripOp = sp(170);

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -20]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at top right, rgba(246,36,64,0.1) 0%, transparent 50%), radial-gradient(ellipse at bottom left, rgba(0,0,0,0.08) 0%, transparent 60%)',
        }}
      />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="diag52" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag52)" />
      </svg>

      {/* Drifting shape */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: '50%',
          background: '#F62440',
          opacity: 0.05,
          top: -250,
          left: -200,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '180px solid transparent',
          borderRight: '180px solid transparent',
          borderBottom: '320px solid #000000',
          opacity: 0.05,
          bottom: 100,
          right: 80,
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 52);
        const x = (seed * 1920 + frame * 0.3) % 1920;
        const y = seededRandom(i + 110) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: i % 4 === 0 ? '#F62440' : '#000000',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Eye icon top */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: '50%',
            transform: `translateX(-50%) scale(${eyeScale * eyePulse})`,
          }}
        >
          <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
            <path
              d="M2 12C2 12 5 5 12 5C19 5 22 12 22 12C22 12 19 19 12 19C5 19 2 12 2 12Z"
              stroke="#F62440"
              strokeWidth="2"
            />
            <circle cx="12" cy="12" r="3" fill="#000000" />
            <line x1="3" y1="3" x2="21" y2="21" stroke="#F62440" strokeWidth="2.5" />
          </svg>
        </div>

        {/* Big headline */}
        <div
          style={{
            position: 'absolute',
            top: 220,
            left: 0,
            right: 0,
            textAlign: 'center',
            fontFamily: 'Clash Display, sans-serif',
            fontWeight: 800,
            lineHeight: 1,
            letterSpacing: -3,
          }}
        >
          <div
            style={{
              fontSize: 140,
              color: '#000000',
              opacity: word1,
              transform: `translateY(${interpolate(word1, [0, 1], [40, 0])}px)`,
            }}
          >
            La nuance
          </div>
          <div
            style={{
              fontSize: 140,
              color: '#F62440',
              opacity: word2,
              transform: `translateY(${interpolate(word2, [0, 1], [40, 0])}px)`,
              fontStyle: 'italic',
            }}
          >
            géographique
          </div>
          <div
            style={{
              fontSize: 60,
              color: '#000000',
              opacity: word3,
              marginTop: 16,
              fontFamily: 'Bricolage Grotesque, sans-serif',
              letterSpacing: 0,
            }}
          >
            que tout le monde <span style={{ color: '#F62440', fontWeight: 700 }}>oublie</span>.
          </div>
        </div>

        {/* France map silhouette */}
        <svg
          width="500"
          height="500"
          viewBox="0 0 200 200"
          style={{
            position: 'absolute',
            bottom: 100,
            left: 200,
            opacity: mapOp * 0.9,
          }}
        >
          <path
            d="M100 20 L130 35 L145 55 L150 80 L145 105 L150 130 L135 155 L110 175 L80 175 L55 160 L45 130 L50 100 L40 75 L55 50 L75 30 Z"
            stroke="#000000"
            strokeWidth="2"
            fill="none"
            strokeDasharray="600"
            strokeDashoffset={600 - mapDraw * 600}
          />
          <path
            d="M100 20 L130 35 L145 55 L150 80 L145 105 L150 130 L135 155 L110 175 L80 175 L55 160 L45 130 L50 100 L40 75 L55 50 L75 30 Z"
            fill="#F62440"
            opacity={mapDraw * 0.08}
          />
        </svg>

        {/* Pin 1 - Big city */}
        {frame > 150 && (
          <div
            style={{
              position: 'absolute',
              bottom: 460,
              left: 380,
              transform: `scale(${pinPulse(150)})`,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
            }}
          >
            <div
              style={{
                background: '#000000',
                color: '#DDDDDD',
                padding: '8px 18px',
                borderRadius: 8,
                fontFamily: 'Space Mono, monospace',
                fontSize: 22,
                fontWeight: 700,
                marginBottom: 8,
              }}
            >
              MÉTROPOLE
            </div>
            <svg width="36" height="48" viewBox="0 0 24 32" fill="none">
              <path d="M12 0C5.4 0 0 5.4 0 12C0 21 12 32 12 32C12 32 24 21 24 12C24 5.4 18.6 0 12 0Z" fill="#F62440" />
              <circle cx="12" cy="12" r="5" fill="#DDDDDD" />
            </svg>
          </div>
        )}

        {/* Pin 2 - Medium city */}
        {frame > 165 && (
          <div
            style={{
              position: 'absolute',
              bottom: 320,
              left: 540,
              transform: `scale(${pinPulse(165)})`,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
            }}
          >
            <div
              style={{
                background: '#F62440',
                color: '#000000',
                padding: '8px 18px',
                borderRadius: 8,
                fontFamily: 'Space Mono, monospace',
                fontSize: 22,
                fontWeight: 700,
                marginBottom: 8,
              }}
            >
              VILLE MOYENNE
            </div>
            <svg width="36" height="48" viewBox="0 0 24 32" fill="none">
              <path d="M12 0C5.4 0 0 5.4 0 12C0 21 12 32 12 32C12 32 24 21 24 12C24 5.4 18.6 0 12 0Z" fill="#000000" />
              <circle cx="12" cy="12" r="5" fill="#F62440" />
            </svg>
          </div>
        )}

        {/* Right side - comparison cards */}
        <div
          style={{
            position: 'absolute',
            right: 120,
            top: 600,
            display: 'flex',
            flexDirection: 'column',
            gap: 24,
            opacity: stripOp,
            transform: `translateX(${interpolate(stripOp, [0, 1], [50, 0])}px)`,
          }}
        >
          <div
            style={{
              background: '#000000',
              padding: '24px 32px',
              borderRadius: 16,
              minWidth: 460,
              borderLeft: '6px solid #F62440',
            }}
          >
            <div
              style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 24,
                color: '#9E9E9E',
                marginBottom: 6,
                letterSpacing: 2,
              }}
            >
              MÊME SALAIRE
            </div>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 56,
                color: '#DDDDDD',
                lineHeight: 1,
              }}
            >
              POUVOIR D'ACHAT
            </div>
            <div
              style={{
                fontFamily: 'Clash Display, sans-serif',
                fontSize: 64,
                color: '#F62440',
                fontWeight: 800,
                marginTop: 6,
                fontStyle: 'italic',
              }}
            >
              ≠ Égal
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene52NuanceGeo;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene53ValenceLyon',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene53ValenceLyon: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header
  const headerOp = sp(10);

  // Left card - Valence
  const leftCard = sp(35);
  const leftCounter = Math.floor(interpolate(frame, [40, 90], [0, 2500], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // VS divider
  const vsScale = sp(95);
  const vsRot = Math.sin(frame * 0.1) * 5;

  // Right card - Lyon
  const rightCard = sp(115);
  const rightCounter = Math.floor(interpolate(frame, [120, 180], [0, 3200], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Arrow connecting
  const arrowDraw = interpolate(frame, [185, 230], [0, 1], { extrapolateRight: 'clamp' });

  // Bottom result
  const resultOp = sp(220);
  const resultPulse = 1 + Math.sin(frame * 0.1) * 0.03;

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(0,0,0,0.05) 0%, transparent 40%, rgba(246,36,64,0.08) 100%)',
        }}
      />

      {/* Dot grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots53" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots53)" />
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          borderRadius: 30,
          border: '3px solid #000000',
          opacity: 0.04,
          top: 100,
          left: 200,
          transform: `rotate(${frame * 0.15}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 380,
          height: 380,
          background: '#F62440',
          borderRadius: '50%',
          opacity: 0.06,
          bottom: 50,
          right: 150,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const seed = seededRandom(i + 53);
        const x = seed * 1920;
        const y = (seededRandom(i + 130) * 1080 + frame * 0.3) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: i % 3 === 0 ? '#F62440' : '#000000',
              opacity: 0.28,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(headerOp, [0, 1], [-30, 0])}px)`,
            opacity: headerOp,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 24,
              color: '#F62440',
              letterSpacing: 6,
              fontWeight: 700,
              marginBottom: 12,
            }}
          >
            COMPARAISON RÉELLE
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 90,
              color: '#000000',
              fontWeight: 800,
              lineHeight: 1,
              letterSpacing: -2,
            }}
          >
            Même salaire. <span style={{ color: '#F62440', fontStyle: 'italic' }}>Vie différente.</span>
          </div>
        </div>

        {/* Left card - Valence */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            left: 180,
            opacity: leftCard,
            transform: `translateY(${interpolate(leftCard, [0, 1], [60, 0])}px)`,
            background: '#000000',
            padding: '40px 50px',
            borderRadius: 24,
            width: 520,
            border: '4px solid #F62440',
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginBottom: 24 }}>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
              <path d="M12 2L4 6V12C4 17 7.5 21 12 22C16.5 21 20 17 20 12V6L12 2Z" fill="#F62440" />
              <circle cx="12" cy="11" r="3" fill="#000000" />
            </svg>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 56,
                color: '#DDDDDD',
                letterSpacing: 2,
              }}
            >
              VALENCE
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 26,
              color: '#9E9E9E',
              marginBottom: 8,
              fontWeight: 500,
            }}
          >
            Salaire net mensuel
          </div>
          <div
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 110,
              color: '#DDDDDD',
              fontWeight: 700,
              lineHeight: 1,
            }}
          >
            {leftCounter.toLocaleString('fr-FR')} €
          </div>
          <div
            style={{
              marginTop: 18,
              padding: '12px 20px',
              background: 'rgba(246,36,64,0.15)',
              borderRadius: 10,
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 26,
              color: '#F62440',
              fontWeight: 600,
            }}
          >
            Coût de la vie modéré
          </div>
        </div>

        {/* VS */}
        <div
          style={{
            position: 'absolute',
            top: 510,
            left: '50%',
            transform: `translateX(-50%) scale(${vsScale}) rotate(${vsRot}deg)`,
            width: 130,
            height: 130,
            borderRadius: '50%',
            background: '#F62440',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            border: '5px solid #000000',
            boxShadow: '0 0 0 8px rgba(246,36,64,0.15)',
          }}
        >
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 64,
              color: '#000000',
              letterSpacing: 2,
            }}
          >
            VS
          </span>
        </div>

        {/* Right card - Lyon */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            right: 180,
            opacity: rightCard,
            transform: `translateY(${interpolate(rightCard, [0, 1], [60, 0])}px)`,
            background: '#F62440',
            padding: '40px 50px',
            borderRadius: 24,
            width: 520,
            border: '4px solid #000000',
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginBottom: 24 }}>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
              <path d="M3 21H21M5 21V7L12 3L19 7V21M9 9H10M14 9H15M9 13H10M14 13H15M9 17H10M14 17H15" stroke="#000000" strokeWidth="2" strokeLinecap="round" />
            </svg>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 56,
                color: '#000000',
                letterSpacing: 2,
              }}
            >
              LYON
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 26,
              color: '#000000',
              marginBottom: 8,
              fontWeight: 600,
              opacity: 0.75,
            }}
          >
            Pouvoir d'achat équivalent
          </div>
          <div
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 110,
              color: '#000000',
              fontWeight: 700,
              lineHeight: 1,
            }}
          >
            {rightCounter.toLocaleString('fr-FR')} €
          </div>
          <div
            style={{
              marginTop: 18,
              padding: '12px 20px',
              background: 'rgba(0,0,0,0.15)',
              borderRadius: 10,
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 26,
              color: '#000000',
              fontWeight: 700,
            }}
          >
            Métropole tendue
          </div>
        </div>

        {/* Bottom result */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: '50%',
            transform: `translateX(-50%) scale(${resultPulse})`,
            opacity: resultOp,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            background: '#000000',
            padding: '22px 50px',
            borderRadius: 60,
          }}
        >
          <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
            <path
              d="M3 12L9 6M3 12L9 18M3 12H15C18 12 21 14 21 18"
              stroke="#F62440"
              strokeWidth="2.5"
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeDasharray="60"
              strokeDashoffset={60 - arrowDraw * 60}
            />
          </svg>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 50,
              color: '#DDDDDD',
              letterSpacing: 2,
            }}
          >
            ÉCART INVISIBLE :
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 60,
              color: '#F62440',
              fontWeight: 800,
              fontStyle: 'italic',
            }}
          >
            +700 € / mois
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene53ValenceLyon;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene54GelEmbauches',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene54GelEmbauches: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Warning band
  const warningSlide = interpolate(sp(10), [0, 1], [-100, 0]);

  // Title
  const titleOp = sp(30);

  // Frozen ice effect on big text
  const freezeProgress = interpolate(frame, [60, 140], [0, 1], { extrapolateRight: 'clamp' });

  // Frozen positions stagger
  const items = [
    { label: 'RECRUTEMENTS', delay: 80 },
    { label: 'AUGMENTATIONS', delay: 105 },
    { label: 'PROMOTIONS', delay: 130 },
    { label: 'BUDGETS RH', delay: 155 },
  ];

  // Thermometer
  const tempFill = interpolate(frame, [180, 240], [180, 30], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const tempOp = sp(180);

  // Camera shake at impact
  const shakeX = frame > 60 && frame < 75 ? Math.sin(frame * 2) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(246,36,64,0.08) 0%, transparent 70%)',
        }}
      />

      {/* Crosshatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="cross54" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 0 30 L 60 30 M 30 0 L 30 60" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross54)" />
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          border: '4px solid #F62440',
          opacity: 0.05,
          top: -200,
          right: -150,
          transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '200px solid transparent',
          borderRight: '200px solid transparent',
          borderBottom: '350px solid #000000',
          opacity: 0.05,
          bottom: -100,
          left: 100,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Snowflake particles - drifting */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = seededRandom(i + 54);
        const x = seed * 1920;
        const y = (seededRandom(i + 200) * 1080 + frame * 0.6) % 1080;
        const size = 4 + seededRandom(i + 33) * 6;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: size,
              height: size,
              opacity: 0.3,
              transform: `rotate(${frame * 0.5 + i * 30}deg)`,
            }}
          >
            <svg width={size} height={size} viewBox="0 0 24 24" fill="none">
              <path d="M12 2V22M2 12H22M5 5L19 19M5 19L19 5" stroke="#F62440" strokeWidth="1.5" />
            </svg>
          </div>
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${shakeX}px)` }}>
        {/* Top warning band */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 0,
            right: 0,
            transform: `translateY(${warningSlide}px)`,
            background: '#F62440',
            padding: '14px 0',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            gap: 20,
            borderTop: '3px solid #000000',
            borderBottom: '3px solid #000000',
          }}
        >
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <path d="M12 9V13M12 17H12.01M10.29 3.86L1.82 18C1.64537 18.3024 1.55296 18.6453 1.55198 18.9945C1.551 19.3437 1.64148 19.6871 1.81441 19.9905C1.98734 20.2939 2.23672 20.5467 2.53771 20.7239C2.83869 20.901 3.18079 20.9962 3.53 21H20.47C20.8192 20.9962 21.1613 20.901 21.4623 20.7239C21.7633 20.5467 22.0127 20.2939 22.1856 19.9905C22.3585 19.6871 22.449 19.3437 22.448 18.9945C22.447 18.6453 22.3546 18.3024 22.18 18L13.71 3.86C13.5318 3.56611 13.2807 3.32312 12.9812 3.15448C12.6817 2.98585 12.3438 2.89725 12 2.89725C11.6562 2.89725 11.3183 2.98585 11.0188 3.15448C10.7193 3.32312 10.4682 3.56611 10.29 3.86Z" stroke="#000000" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 38,
              color: '#000000',
              letterSpacing: 6,
            }}
          >
            ALERTE — ENTREPRISES SOUS PRESSION FISCALE
          </span>
        </div>

        {/* Main title - frozen effect */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 200,
              fontWeight: 800,
              color: '#000000',
              lineHeight: 1,
              letterSpacing: -5,
              position: 'relative',
              display: 'inline-block',
            }}
          >
            GEL
            {/* Ice overlay */}
            <div
              style={{
                position: 'absolute',
                top: 0,
                left: 0,
                width: '100%',
                height: '100%',
                background: `linear-gradient(180deg, rgba(221,221,221,${freezeProgress * 0.7}) 0%, transparent 100%)`,
                mixBlendMode: 'overlay',
              }}
            />
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 80,
              fontWeight: 700,
              color: '#F62440',
              lineHeight: 1,
              marginTop: -10,
              fontStyle: 'italic',
              letterSpacing: -2,
            }}
          >
            des embauches.
          </div>
        </div>

        {/* Frozen items list */}
        <div
          style={{
            position: 'absolute',
            bottom: 220,
            left: '8%',
            display: 'flex',
            flexDirection: 'column',
            gap: 14,
          }}
        >
          {items.map((item, i) => {
            const itemSp = sp(item.delay);
            return (
              <div
                key={i}
                style={{
                  opacity: itemSp,
                  transform: `translateX(${interpolate(itemSp, [0, 1], [-60, 0])}px)`,
                  display: 'flex',
                  alignItems: 'center',
                  gap: 20,
                  background: '#000000',
                  padding: '14px 26px',
                  borderRadius: 12,
                  borderLeft: '5px solid #F62440',
                  minWidth: 480,
                }}
              >
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
                  <rect x="3" y="11" width="18" height="11" rx="2" stroke="#F62440" strokeWidth="2" />
                  <path d="M7 11V7C7 5.67392 7.52678 4.40215 8.46447 3.46447C9.40215 2.52678 10.6739 2 12 2C13.3261 2 14.5979 2.52678 15.5355 3.46447C16.4732 4.40215 17 5.67392 17 7V11" stroke="#F62440" strokeWidth="2" />
                </svg>
                <span
                  style={{
                    fontFamily: 'Anton, sans-serif',
                    fontSize: 38,
                    color: '#DDDDDD',
                    letterSpacing: 2,
                  }}
                >
                  {item.label}
                </span>
                <span
                  style={{
                    marginLeft: 'auto',
                    fontFamily: 'Space Mono, monospace',
                    fontSize: 22,
                    color: '#F62440',
                    fontWeight: 700,
                    letterSpacing: 3,
                  }}
                >
                  BLOQUÉ
                </span>
              </div>
            );
          })}
        </div>

        {/* Thermometer */}
        <div
          style={{
            position: 'absolute',
            right: 180,
            bottom: 200,
            width: 120,
            opacity: tempOp,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 28,
              color: '#F62440',
              fontWeight: 700,
              marginBottom: 12,
            }}
          >
            CLIMAT RH
          </div>
          <div
            style={{
              position: 'relative',
              width: 60,
              height: 240,
              background: '#000000',
              borderRadius: 30,
              border: '3px solid #000000',
            }}
          >
            <div
              style={{
                position: 'absolute',
                bottom: 0,
                left: 0,
                right: 0,
                height: tempFill,
                background: 'linear-gradient(180deg, #F62440 0%, #000000 100%)',
                borderRadius: 30,
                transition: 'height 0.3s',
              }}
            />
          </div>
          <div
            style={{
              width: 90,
              height: 90,
              borderRadius: '50%',
              background: '#F62440',
              border: '4px solid #000000',
              marginTop: -10,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Anton, sans-serif',
              fontSize: 32,
              color: '#000000',
            }}
          >
            FROID
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 45%, rgba(0,0,0,0.3) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene54GelEmbauches;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene55SecteursTop',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene55SecteursTop: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header
  const headerOp = sp(10);

  // Bar chart sectors
  const sectors = [
    { name: 'FINANCE / ASSURANCE', value: 95, delay: 40, icon: 'finance' },
    { name: 'INFORMATIQUE / IA', value: 88, delay: 70, icon: 'tech' },
    { name: 'COMMUNICATION', value: 75, delay: 100, icon: 'comm' },
    { name: 'INDUSTRIE SPÉCIALISÉE', value: 70, delay: 130, icon: 'industry' },
  ];

  const camScale = interpolate(frame, [0, durationInFrames], [1.01, 1.04]);

  const renderIcon = (icon: string) => {
    switch (icon) {
      case 'finance':
        return (
          <svg width="36" height="36" viewBox="0 0 24 24" fill="none">
            <path d="M12 2V22M17 5H9.5C8.57174 5 7.6815 5.36875 7.02513 6.02513C6.36875 6.6815 6 7.57174 6 8.5C6 9.42826 6.36875 10.3185 7.02513 10.9749C7.6815 11.6313 8.57174 12 9.5 12H14.5C15.4283 12 16.3185 12.3687 16.9749 13.0251C17.6313 13.6815 18 14.5717 18 15.5C18 16.4283 17.6313 17.3185 16.9749 17.9749C16.3185 18.6313 15.4283 19 14.5 19H6" stroke="#DDDDDD" strokeWidth="2" strokeLinecap="round" />
          </svg>
        );
      case 'tech':
        return (
          <svg width="36" height="36" viewBox="0 0 24 24" fill="none">
            <rect x="3" y="4" width="18" height="12" rx="2" stroke="#DDDDDD" strokeWidth="2" />
            <path d="M8 20H16M12 16V20" stroke="#DDDDDD" strokeWidth="2" strokeLinecap="round" />
            <path d="M8 10L10 8L8 6M16 10L14 8L16 6" stroke="#F62440" strokeWidth="2" strokeLinecap="round" />
          </svg>
        );
      case 'comm':
        return (
          <svg width="36" height="36" viewBox="0 0 24 24" fill="none">
            <path d="M21 11.5C21.0034 12.8199 20.6951 14.1219 20.1 15.3C19.3944 16.7118 18.3098 17.8992 16.9674 18.7293C15.6251 19.5594 14.0782 19.9994 12.5 20C11.1801 20.0035 9.87812 19.6951 8.7 19.1L3 21L4.9 15.3C4.30493 14.1219 3.99656 12.8199 4 11.5C4.00061 9.92179 4.44061 8.37488 5.27072 7.03258C6.10083 5.69028 7.28825 4.6056 8.7 3.90003C9.87812 3.30496 11.1801 2.99659 12.5 3.00003H13C15.0843 3.11502 17.053 3.99479 18.5291 5.47089C20.0052 6.94699 20.885 8.91568 21 11V11.5Z" stroke="#DDDDDD" strokeWidth="2" />
          </svg>
        );
      case 'industry':
        return (
          <svg width="36" height="36" viewBox="0 0 24 24" fill="none">
            <path d="M2 22H22V11L17 14V11L12 14V11L7 14V7H2V22Z" stroke="#DDDDDD" strokeWidth="2" strokeLinejoin="round" />
            <path d="M2 7L5 4H8L11 7" stroke="#DDDDDD" strokeWidth="2" strokeLinejoin="round" />
          </svg>
        );
      default:
        return null;
    }
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(0,0,0,0.06) 0%, transparent 30%, rgba(246,36,64,0.06) 100%)',
        }}
      />

      {/* Grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="grid55" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid55)" />
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 550,
          height: 550,
          borderRadius: '50%',
          border: '3px solid #000000',
          opacity: 0.04,
          bottom: -200,
          right: -100,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 350,
          height: 350,
          background: '#F62440',
          opacity: 0.05,
          top: 80,
          left: -80,
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const seed = seededRandom(i + 55);
        const x = seed * 1920;
        const y = (seededRandom(i + 88) * 1080 + frame * 0.4) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: i % 3 === 0 ? '#F62440' : '#000000',
              opacity: 0.28,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 100,
            opacity: headerOp,
            transform: `translateX(${interpolate(headerOp, [0, 1], [-40, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 24,
              color: '#F62440',
              letterSpacing: 6,
              fontWeight: 700,
              marginBottom: 10,
            }}
          >
            CLASSEMENT 2026
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 110,
              color: '#000000',
              fontWeight: 800,
              lineHeight: 1,
              letterSpacing: -2,
            }}
          >
            Secteurs qui
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 110,
              color: '#F62440',
              fontWeight: 800,
              lineHeight: 1,
              letterSpacing: -2,
              fontStyle: 'italic',
            }}
          >
            payent encore.
          </div>
        </div>

        {/* Bar chart */}
        <div
          style={{
            position: 'absolute',
            top: 500,
            left: 100,
            right: 100,
            display: 'flex',
            flexDirection: 'column',
            gap: 24,
          }}
        >
          {sectors.map((s, i) => {
            const sectorSp = sp(s.delay);
            const barWidth = interpolate(sectorSp, [0, 1], [0, s.value]);
            return (
              <div
                key={i}
                style={{
                  opacity: sectorSp,
                  transform: `translateX(${interpolate(sectorSp, [0, 1], [-50, 0])}px)`,
                  display: 'flex',
                  alignItems: 'center',
                  gap: 24,
                }}
              >
                {/* Rank */}
                <div
                  style={{
                    width: 70,
                    height: 70,
                    borderRadius: '50%',
                    background: i === 0 ? '#F62440' : '#000000',
                    color: i === 0 ? '#000000' : '#DDDDDD',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    fontFamily: 'Anton, sans-serif',
                    fontSize: 44,
                    border: i === 0 ? '4px solid #000000' : 'none',
                  }}
                >
                  {i + 1}
                </div>

                {/* Bar with content */}
                <div
                  style={{
                    flex: 1,
                    height: 80,
                    background: i === 0 ? '#F62440' : '#000000',
                    width: `${barWidth}%`,
                    borderRadius: 12,
                    display: 'flex',
                    alignItems: 'center',
                    paddingLeft: 22,
                    gap: 20,
                    transition: 'width 0.1s',
                  }}
                >
                  {renderIcon(s.icon)}
                  <span
                    style={{
                      fontFamily: 'Anton, sans-serif',
                      fontSize: 38,
                      color: i === 0 ? '#000000' : '#DDDDDD',
                      letterSpacing: 2,
                      whiteSpace: 'nowrap',
                    }}
                  >
                    {s.name}
                  </span>
                </div>
              </div>
            );
          })}
        </div>

        {/* Side caption */}
        <div
          style={{
            position: 'absolute',
            top: 240,
            right: 100,
            background: '#000000',
            padding: '18px 28px',
            borderRadius: 14,
            borderLeft: '5px solid #F62440',
            opacity: sp(20),
            transform: `translateX(${interpolate(sp(20), [0, 1], [40, 0])}px)`,
            maxWidth: 380,
          }}
        >
          <div
            style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 26,
              color: '#9E9E9E',
              marginBottom: 6,
            }}
          >
            Critère clé
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 46,
              color: '#DDDDDD',
              lineHeight: 1.05,
              letterSpacing: 1,
            }}
          >
            BONUS &<br />PÉNURIE DE TALENTS
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene55SecteursTop;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene56SecteurVsDiplome',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene56SecteurVsDiplome: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Lesson title
  const lessonOp = sp(10);

  // Balance scale animation
  const scaleTilt = interpolate(sp(50), [0, 1], [0, -12]);

  // Left side (diploma) - lighter
  const leftSide = sp(80);
  const leftWeight = interpolate(sp(80), [0, 1], [60, 0]);

  // Right side (sector) - heavier
  const rightSide = sp(110);
  const rightWeight = interpolate(sp(110), [0, 1], [60, 0]);

  // Verdict
  const verdictOp = sp(180);
  const verdictPulse = 1 + Math.sin(frame * 0.12) * 0.04;

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.03]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 30%, rgba(246,36,64,0.1) 0%, transparent 60%)',
        }}
      />

      {/* Wavy lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <path
            key={i}
            d={`M 0 ${100 + i * 90} Q 480 ${50 + i * 90 + Math.sin(frame * 0.02 + i) * 20} 960 ${100 + i * 90} T 1920 ${100 + i * 90}`}
            stroke="#000000"
            strokeWidth="1.5"
            fill="none"
          />
        ))}
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: '#F62440',
          opacity: 0.05,
          top: -180,
          left: -150,
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 400,
          height: 400,
          border: '4px solid #000000',
          opacity: 0.05,
          bottom: -120,
          right: -100,
          transform: `rotate(${frame * 0.15}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = seededRandom(i + 56);
        const x = (seed * 1920 + frame * 0.5) % 1920;
        const y = seededRandom(i + 99) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: i % 3 === 0 ? '#F62440' : '#000000',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top lesson tag */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: '50%',
            transform: `translateX(-50%)`,
            opacity: lessonOp,
            display: 'flex',
            alignItems: 'center',
            gap: 14,
            background: '#000000',
            padding: '12px 28px',
            borderRadius: 50,
          }}
        >
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
            <path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z" fill="#F62440" />
          </svg>
          <span
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 22,
              color: '#DDDDDD',
              letterSpacing: 4,
              fontWeight: 700,
            }}
          >
            LA LEÇON DU MARCHÉ
          </span>
        </div>

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 150,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: lessonOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 90,
              color: '#000000',
              fontWeight: 800,
              lineHeight: 1,
              letterSpacing: -2,
            }}
          >
            Votre secteur pèse plus lourd que
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 90,
              color: '#F62440',
              fontWeight: 800,
              lineHeight: 1,
              letterSpacing: -2,
              marginTop: 4,
              fontStyle: 'italic',
            }}
          >
            votre diplôme.
          </div>
        </div>

        {/* Balance scale */}
        <div
          style={{
            position: 'absolute',
            top: 400,
            left: '50%',
            transform: `translateX(-50%) rotate(${scaleTilt}deg)`,
            transformOrigin: 'center 60px',
            width: 900,
            height: 80,
          }}
        >
          {/* Beam */}
          <div
            style={{
              position: 'absolute',
              top: 50,
              left: 0,
              right: 0,
              height: 14,
              background: '#000000',
              borderRadius: 7,
            }}
          />
          {/* Pivot */}
          <div
            style={{
              position: 'absolute',
              top: 0,
              left: '50%',
              transform: 'translateX(-50%)',
              width: 30,
              height: 80,
              background: '#F62440',
              clipPath: 'polygon(50% 0%, 0% 100%, 100% 100%)',
            }}
          />
          {/* Left chain */}
          <div
            style={{
              position: 'absolute',
              top: 60,
              left: 80,
              width: 3,
              height: 100,
              background: '#000000',
            }}
          />
          {/* Right chain */}
          <div
            style={{
              position: 'absolute',
              top: 60,
              right: 80,
              width: 3,
              height: 100,
              background: '#000000',
            }}
          />
        </div>

        {/* Left pan - DIPLÔME (lighter) */}
        <div
          style={{
            position: 'absolute',
            top: 560,
            left: '17%',
            opacity: leftSide,
            transform: `translateY(${leftWeight}px)`,
            background: '#000000',
            padding: '30px 40px',
            borderRadius: 20,
            width: 360,
            textAlign: 'center',
            border: '3px solid #9E9E9E',
          }}
        >
          <svg width="50" height="50" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 10 }}>
            <path d="M22 10V6L12 2L2 6L12 10L22 6" stroke="#9E9E9E" strokeWidth="2" strokeLinejoin="round" />
            <path d="M6 12V17C6 17 9 19 12 19C15 19 18 17 18 17V12" stroke="#9E9E9E" strokeWidth="2" />
          </svg>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 56,
              color: '#9E9E9E',
              letterSpacing: 2,
              lineHeight: 1,
            }}
          >
            DIPLÔME
          </div>
          <div
            style={{
              marginTop: 14,
              fontFamily: 'Space Mono, monospace',
              fontSize: 30,
              color: '#9E9E9E',
              fontWeight: 700,
            }}
          >
            poids : 30%
          </div>
        </div>

        {/* Right pan - SECTEUR (heavier) */}
        <div
          style={{
            position: 'absolute',
            top: 620,
            right: '17%',
            opacity: rightSide,
            transform: `translateY(${rightWeight}px)`,
            background: '#F62440',
            padding: '40px 50px',
            borderRadius: 20,
            width: 420,
            textAlign: 'center',
            border: '4px solid #000000',
          }}
        >
          <svg width="64" height="64" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 12 }}>
            <path d="M2 22H22V11L17 14V11L12 14V11L7 14V7H2V22Z" stroke="#000000" strokeWidth="2.5" strokeLinejoin="round" />
            <path d="M2 7L5 4H8L11 7" stroke="#000000" strokeWidth="2.5" strokeLinejoin="round" />
          </svg>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 70,
              color: '#000000',
              letterSpacing: 2,
              lineHeight: 1,
            }}
          >
            SECTEUR
          </div>
          <div
            style={{
              marginTop: 14,
              fontFamily: 'Space Mono, monospace',
              fontSize: 36,
              color: '#000000',
              fontWeight: 700,
            }}
          >
            poids : 70%
          </div>
        </div>

        {/* Verdict */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: '50%',
            transform: `translateX(-50%) scale(${verdictPulse})`,
            opacity: verdictOp,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            background: '#000000',
            padding: '18px 36px',
            borderRadius: 12,
            border: '3px solid #F62440',
          }}
        >
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <path d="M9 18L15 12L9 6" stroke="#F62440" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <span
            style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 34,
              color: '#DDDDDD',
              fontWeight: 600,
            }}
          >
            Choisir la bonne branche &gt; choisir la bonne école
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene56SecteurVsDiplome;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene57BuffettCitation',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene57BuffettCitation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Quote mark
  const quoteScale = sp(10);

  // Quote lines stagger
  const line1 = sp(35);
  const line2 = sp(60);
  const line3 = sp(85);

  // Attribution
  const attribOp = sp(115);

  // Risk box
  const riskBox = sp(150);

  // Knowledge box
  const knowBox = sp(180);

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.05]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0.06) 0%, transparent 60%), linear-gradient(135deg, rgba(246,36,64,0.05) 0%, transparent 50%)',
        }}
      />

      {/* Stripe texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="stripe57" width="20" height="20" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="20" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#stripe57)" />
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '3px solid #F62440',
          opacity: 0.05,
          top: -250,
          right: -250,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 450,
          height: 450,
          background: '#000000',
          opacity: 0.04,
          bottom: -150,
          left: -150,
          borderRadius: 30,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const seed = seededRandom(i + 57);
        const x = seed * 1920;
        const y = (seededRandom(i + 145) * 1080 + frame * 0.4) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: i % 3 === 0 ? '#F62440' : '#000000',
              opacity: 0.28,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Big opening quote mark */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 120,
            transform: `scale(${quoteScale})`,
            fontFamily: 'Clash Display, serif',
            fontSize: 320,
            color: '#F62440',
            fontWeight: 800,
            lineHeight: 0.8,
            opacity: 0.9,
          }}
        >
          "
        </div>

        {/* Quote text */}
        <div
          style={{
            position: 'absolute',
            top: 240,
            left: 220,
            right: 220,
          }}
        >
          <div
            style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 80,
              color: '#000000',
              fontWeight: 700,
              lineHeight: 1.1,
              letterSpacing: -1,
              opacity: line1,
              transform: `translateY(${interpolate(line1, [0, 1], [30, 0])}px)`,
            }}
          >
            Le risque vient
          </div>
          <div
            style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 80,
              color: '#000000',
              fontWeight: 700,
              lineHeight: 1.1,
              letterSpacing: -1,
              opacity: line2,
              transform: `translateY(${interpolate(line2, [0, 1], [30, 0])}px)`,
              marginTop: 8,
            }}
          >
            de <span style={{ background: '#F62440', color: '#000000', padding: '2px 18px', borderRadius: 8 }}>ne pas savoir</span>
          </div>
          <div
            style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 80,
              color: '#000000',
              fontWeight: 700,
              lineHeight: 1.1,
              letterSpacing: -1,
              opacity: line3,
              transform: `translateY(${interpolate(line3, [0, 1], [30, 0])}px)`,
              marginTop: 8,
            }}
          >
            ce que l'on fait.
          </div>
        </div>

        {/* Attribution */}
        <div
          style={{
            position: 'absolute',
            top: 580,
            left: 220,
            opacity: attribOp,
            transform: `translateX(${interpolate(attribOp, [0, 1], [-30, 0])}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
          }}
        >
          <div
            style={{
              width: 60,
              height: 4,
              background: '#F62440',
            }}
          />
          <div>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 44,
                color: '#000000',
                letterSpacing: 2,
                lineHeight: 1,
              }}
            >
              WARREN BUFFETT
            </div>
            <div
              style={{
                fontFamily: 'Space Mono, monospace',
                fontSize: 22,
                color: '#F62440',
                letterSpacing: 3,
                marginTop: 4,
                fontWeight: 700,
              }}
            >
              ORACLE D'OMAHA
            </div>
          </div>
        </div>

        {/* Two boxes - Risk vs Knowledge */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 30,
          }}
        >
          {/* Risk box */}
          <div
            style={{
              opacity: riskBox,
              transform: `translateY(${interpolate(riskBox, [0, 1], [40, 0])}px)`,
              background: '#000000',
              padding: '24px 38px',
              borderRadius: 16,
              border: '3px solid #F62440',
              display: 'flex',
              alignItems: 'center',
              gap: 18,
              minWidth: 440,
            }}
          >
            <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
              <path d="M10.29 3.86L1.82 18C1.64537 18.3024 1.55296 18.6453 1.55198 18.9945C1.551 19.3437 1.64148 19.6871 1.81441 19.9905C1.98734 20.2939 2.23672 20.5467 2.53771 20.7239C2.83869 20.901 3.18079 20.9962 3.53 21H20.47C20.8192 20.9962 21.1613 20.901 21.4623 20.7239C21.7633 20.5467 22.0127 20.2939 22.1856 19.9905C22.3585 19.6871 22.449 19.3437 22.448 18.9945C22.447 18.6453 22.3546 18.3024 22.18 18L13.71 3.86C13.5318 3.56611 13.2807 3.32312 12.9812 3.15448C12.6817 2.98585 12.3438 2.89725 12 2.89725C11.6562 2.89725 11.3183 2.98585 11.0188 3.15448C10.7193 3.32312 10.4682 3.56611 10.29 3.86Z" stroke="#F62440" strokeWidth="2.5" strokeLinejoin="round" />
              <path d="M12 9V13M12 17H12.01" stroke="#F62440" strokeWidth="2.5" strokeLinecap="round" />
            </svg>
            <div>
              <div
                style={{
                  fontFamily: 'Space Mono, monospace',
                  fontSize: 20,
                  color: '#9E9E9E',
                  letterSpacing: 3,
                  marginBottom: 4,
                }}
              >
                RISQUE
              </div>
              <div
                style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 40,
                  color: '#DDDDDD',
                  lineHeight: 1,
                  letterSpacing: 1,
                }}
              >
                IGNORANCE
              </div>
            </div>
          </div>

          {/* Knowledge box */}
          <div
            style={{
              opacity: knowBox,
              transform: `translateY(${interpolate(knowBox, [0, 1], [40, 0])}px)`,
              background: '#F62440',
              padding: '24px 38px',
              borderRadius: 16,
              border: '3px solid #000000',
              display: 'flex',
              alignItems: 'center',
              gap: 18,
              minWidth: 440,
            }}
          >
            <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
              <path d="M2 3H8C9.06087 3 10.0783 3.42143 10.8284 4.17157C11.5786 4.92172 12 5.93913 12 7V21C12 20.2044 11.6839 19.4413 11.1213 18.8787C10.5587 18.3161 9.79565 18 9 18H2V3Z" stroke="#000000" strokeWidth="2.5" strokeLinejoin="round" />
              <path d="M22 3H16C14.9391 3 13.9217 3.42143 13.1716 4.17157C12.4214 4.92172 12 5.93913 12 7V21C12 20.2044 12.3161 19.4413 12.8787 18.8787C13.4413 18.3161 14.2044 18 15 18H22V3Z" stroke="#000000" strokeWidth="2.5" strokeLinejoin="round" />
            </svg>
            <div>
              <div
                style={{
                  fontFamily: 'Space Mono, monospace',
                  fontSize: 20,
                  color: '#000000',
                  letterSpacing: 3,
                  marginBottom: 4,
                  fontWeight: 700,
                }}
              >
                ANTIDOTE
              </div>
              <div
                style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 40,
                  color: '#000000',
                  lineHeight: 1,
                  letterSpacing: 1,
                }}
              >
                CONNAISSANCE
              </div>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene57BuffettCitation;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene58Immobilisme',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene58Immobilisme: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Word "DANGER" reveals
  const dangerOp = sp(10);

  // IMMOBILISME huge text
  const immoScale = sp(30);
  const immoStrike = interpolate(frame, [120, 160], [0, 1], { extrapolateRight: 'clamp' });

  // Levers
  const levers = [
    { label: 'SÉCURISER', delay: 60 },
    { label: 'OPTIMISER', delay: 80 },
    { label: 'SE FORMER', delay: 100 },
    { label: 'INVESTIR', delay: 120 },
    { label: 'NÉGOCIER', delay: 140 },
    { label: 'ARBITRER', delay: 160 },
  ];

  // Final breakthrough
  const breakOp = sp(190);
  const breakPulse = 1 + Math.sin(frame * 0.15) * 0.04;

  // Screen shake on breakthrough
  const shakeY = frame > 190 && frame < 205 ? Math.sin(frame * 3) * 3 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 70%, rgba(246,36,64,0.12) 0%, transparent 60%), linear-gradient(180deg, rgba(0,0,0,0.05) 0%, transparent 30%)',
        }}
      />

      {/* Concentric circles */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <circle
            key={i}
            cx="960"
            cy="540"
            r={150 + i * 100}
            stroke="#000000"
            strokeWidth="1"
            fill="none"
          />
        ))}
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          background: '#F62440',
          opacity: 0.06,
          borderRadius: '50%',
          top: -200,
          left: '40%',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />

      {/* Particles - converging */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = seededRandom(i + 58);
        const angle = seed * Math.PI * 2;
        const radius = 600 - (frame * 1.5 + i * 20) % 600;
        const x = 960 + Math.cos(angle) * radius;
        const y = 540 + Math.sin(angle) * radius;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: i % 3 === 0 ? '#F62440' : '#000000',
              opacity: 0.4,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${shakeY}px)` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: '50%',
            transform: `translateX(-50%)`,
            opacity: dangerOp,
            display: 'flex',
            alignItems: 'center',
            gap: 14,
            background: '#F62440',
            padding: '12px 30px',
            borderRadius: 50,
            border: '3px solid #000000',
          }}
        >
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
            <path d="M10.29 3.86L1.82 18C1.64537 18.3024 1.55296 18.6453 1.55198 18.9945C1.551 19.3437 1.64148 19.6871 1.81441 19.9905C1.98734 20.2939 2.23672 20.5467 2.53771 20.7239C2.83869 20.901 3.18079 20.9962 3.53 21H20.47C20.8192 20.9962 21.1613 20.901 21.4623 20.7239C21.7633 20.5467 22.0127 20.2939 22.1856 19.9905C22.3585 19.6871 22.449 19.3437 22.448 18.9945C22.447 18.6453 22.3546 18.3024 22.18 18L13.71 3.86C13.5318 3.56611 13.2807 3.32312 12.9812 3.15448C12.6817 2.98585 12.3438 2.89725 12 2.89725C11.6562 2.89725 11.3183 2.98585 11.0188 3.15448C10.7193 3.32312 10.4682 3.56611 10.29 3.86Z" stroke="#000000" strokeWidth="2.5" strokeLinejoin="round" />
            <path d="M12 9V13M12 17H12.01" stroke="#000000" strokeWidth="2.5" strokeLinecap="round" />
          </svg>
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 30,
              color: '#000000',
              letterSpacing: 6,
            }}
          >
            LE VRAI DANGER
          </span>
        </div>

        {/* IMMOBILISME huge */}
        <div
          style={{
            position: 'absolute',
            top: 170,
            left: 0,
            right: 0,
            textAlign: 'center',
            transform: `scale(${immoScale})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 220,
              color: '#000000',
              fontWeight: 800,
              lineHeight: 1,
              letterSpacing: -6,
              position: 'relative',
              display: 'inline-block',
            }}
          >
            IMMOBILISME
            {/* Strike-through */}
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                width: `${immoStrike * 100}%`,
                height: 16,
                background: '#F62440',
                transform: 'translateY(-50%) rotate(-3deg)',
                borderRadius: 8,
              }}
            />
          </div>
        </div>

        {/* 6 levers in circle */}
        <div
          style={{
            position: 'absolute',
            top: 470,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            flexWrap: 'wrap',
            gap: 16,
            padding: '0 200px',
          }}
        >
          {levers.map((lever, i) => {
            const leverSp = sp(lever.delay);
            return (
              <div
                key={i}
                style={{
                  opacity: leverSp,
                  transform: `scale(${leverSp})`,
                  background: i % 2 === 0 ? '#000000' : '#F62440',
                  color: i % 2 === 0 ? '#DDDDDD' : '#000000',
                  padding: '18px 32px',
                  borderRadius: 12,
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 42,
                  letterSpacing: 2,
                  border: i % 2 === 0 ? '3px solid #F62440' : '3px solid #000000',
                }}
              >
                {lever.label}
              </div>
            );
          })}
        </div>

        {/* Final breakthrough */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: '50%',
            transform: `translateX(-50%) scale(${breakOp * breakPulse})`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 36,
              color: '#000000',
              marginBottom: 8,
              letterSpacing: 1,
            }}
          >
            Ce soir, votre décision change tout.
          </div>
          <div
            style={{
              background: '#000000',
              padding: '20px 60px',
              borderRadius: 60,
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 70,
              color: '#F62440',
              fontWeight: 800,
              fontStyle: 'italic',
              letterSpacing: -1,
              border: '4px solid #F62440',
            }}
          >
            VOUS POUVEZ.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene58Immobilisme;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene59Confiance',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene59Confiance: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Handshake icon
  const handScale = sp(10);
  const handBreath = 1 + Math.sin(frame * 0.06) * 0.03;

  // Title
  const titleOp = sp(35);

  // Disclaimer card
  const cardSlide = interpolate(sp(70), [0, 1], [100, 0]);
  const cardOp = sp(70);

  // Bullet points
  const bullets = [
    { text: 'Analyse pédagogique', delay: 110 },
    { text: 'Pas de conseil personnalisé', delay: 135 },
    { text: 'Validez avec un professionnel', delay: 160 },
  ];

  // Trust pulse rings
  const ringScale = (delay: number) => {
    const t = (frame - delay) % 60;
    return 1 + (t / 60) * 0.8;
  };
  const ringOp = (delay: number) => {
    const t = (frame - delay) % 60;
    return 1 - t / 60;
  };

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.03]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 50%, rgba(246,36,64,0.08) 0%, transparent 55%), radial-gradient(circle at 70% 50%, rgba(0,0,0,0.06) 0%, transparent 60%)',
        }}
      />

      {/* Soft wave texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <path
            key={i}
            d={`M 0 ${108 * i} Q 960 ${108 * i - 30 + Math.sin(frame * 0.02 + i) * 15} 1920 ${108 * i}`}
            stroke="#000000"
            strokeWidth="1.5"
            fill="none"
          />
        ))}
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          borderRadius: '50%',
          border: '3px solid #F62440',
          opacity: 0.06,
          top: 50,
          right: -150,
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 380,
          height: 380,
          background: '#000000',
          opacity: 0.04,
          bottom: -100,
          left: -100,
          borderRadius: 24,
          transform: `rotate(${frame * 0.12}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 59);
        const x = seed * 1920;
        const y = (seededRandom(i + 77) * 1080 + frame * 0.3) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: i % 3 === 0 ? '#F62440' : '#000000',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Handshake icon with pulse rings */}
        <div
          style={{
            position: 'absolute',
            top: 130,
            left: 220,
            width: 280,
            height: 280,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
          }}
        >
          {/* Pulse ring 1 */}
          <div
            style={{
              position: 'absolute',
              width: 200,
              height: 200,
              borderRadius: '50%',
              border: '3px solid #F62440',
              opacity: ringOp(0),
              transform: `scale(${ringScale(0)})`,
            }}
          />
          {/* Pulse ring 2 */}
          <div
            style={{
              position: 'absolute',
              width: 200,
              height: 200,
              borderRadius: '50%',
              border: '3px solid #F62440',
              opacity: ringOp(30),
              transform: `scale(${ringScale(30)})`,
            }}
          />
          {/* Center icon */}
          <div
            style={{
              width: 200,
              height: 200,
              borderRadius: '50%',
              background: '#000000',
              border: '4px solid #F62440',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              transform: `scale(${handScale * handBreath})`,
            }}
          >
            <svg width="110" height="110" viewBox="0 0 24 24" fill="none">
              <path
                d="M11 17L9 19L7 17M13 7L15 5L17 7M9 11L4 16C3.46957 16.5304 3.17157 17.2496 3.17157 18C3.17157 18.7504 3.46957 19.4696 4 20C4.53043 20.5304 5.24957 20.8284 6 20.8284C6.75043 20.8284 7.46957 20.5304 8 20L13 15M15 13L20 8C20.5304 7.46957 20.8284 6.75043 20.8284 6C20.8284 5.24957 20.5304 4.53043 20 4C19.4696 3.46957 18.7504 3.17157 18 3.17157C17.2496 3.17157 16.5304 3.46957 16 4L11 9"
                stroke="#F62440"
                strokeWidth="2.5"
                strokeLinecap="round"
                strokeLinejoin="round"
              />
              <path d="M9 11L13 15" stroke="#F62440" strokeWidth="2.5" strokeLinecap="round" />
            </svg>
          </div>
        </div>

        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 140,
            left: 560,
            right: 100,
            opacity: titleOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 24,
              color: '#F62440',
              letterSpacing: 6,
              fontWeight: 700,
              marginBottom: 14,
            }}
          >
            TRANSPARENCE TOTALE
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 110,
              color: '#000000',
              fontWeight: 800,
              lineHeight: 1,
              letterSpacing: -3,
            }}
          >
            La confiance
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 110,
              color: '#F62440',
              fontWeight: 800,
              lineHeight: 1,
              letterSpacing: -3,
              fontStyle: 'italic',
              marginTop: 4,
            }}
          >
            avant tout.
          </div>
        </div>

        {/* Disclaimer card */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: 100,
            right: 100,
            opacity: cardOp,
            transform: `translateY(${cardSlide}px)`,
            background: '#000000',
            padding: '36px 50px',
            borderRadius: 24,
            borderTop: '6px solid #F62440',
            display: 'flex',
            flexDirection: 'column',
            gap: 22,
          }}
        >
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 48,
              color: '#DDDDDD',
              letterSpacing: 2,
              lineHeight: 1,
            }}
          >
            CE QUE J'OFFRE, CE QUE JE N'OFFRE PAS
          </div>

          <div style={{ display: 'flex', gap: 30, justifyContent: 'space-between' }}>
            {bullets.map((b, i) => {
              const bSp = sp(b.delay);
              return (
                <div
                  key={i}
                  style={{
                    opacity: bSp,
                    transform: `translateY(${interpolate(bSp, [0, 1], [20, 0])}px)`,
                    flex: 1,
                    display: 'flex',
                    alignItems: 'center',
                    gap: 14,
                    background: i === 1 ? 'rgba(246,36,64,0.15)' : 'rgba(255,255,255,0.05)',
                    padding: '16px 22px',
                    borderRadius: 12,
                    borderLeft: `4px solid ${i === 1 ? '#F62440' : '#9E9E9E'}`,
                  }}
                >
                  {i === 1 ? (
                    <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
                      <circle cx="12" cy="12" r="10" stroke="#F62440" strokeWidth="2.5" />
                      <path d="M15 9L9 15M9 9L15 15" stroke="#F62440" strokeWidth="2.5" strokeLinecap="round" />
                    </svg>
                  ) : (
                    <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
                      <path d="M20 6L9 17L4 12" stroke="#F62440" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
                    </svg>
                  )}
                  <span
                    style={{
                      fontFamily: 'Bricolage Grotesque, sans-serif',
                      fontSize: 30,
                      color: i === 1 ? '#F62440' : '#DDDDDD',
                      fontWeight: 600,
                    }}
                  >
                    {b.text}
                  </span>
                </div>
              );
            })}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene59Confiance;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene60PremierPas',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene60PremierPas: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Top tag
  const tagOp = sp(10);

  // Big title
  const titleOp = sp(30);

  // 4 actions cards
  const actions = [
    { num: '01', text: 'OUVRIR LE PEA', delay: 60, icon: 'pea' },
    { num: '02', text: 'PROGRAMMER 50€/MOIS', delay: 85, icon: 'auto' },
    { num: '03', text: 'CONTACTER LES RH', delay: 110, icon: 'rh' },
    { num: '04', text: 'SE FORMER', delay: 135, icon: 'form' },
  ];

  // Footprint trail
  const trailDraw = interpolate(frame, [170, 220], [0, 1], { extrapolateRight: 'clamp' });
  const trailOp = sp(170);

  // Final CTA
  const ctaScale = sp(210);
  const ctaPulse = 1 + Math.sin(frame * 0.15) * 0.04;

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.06]);

  const renderActionIcon = (icon: string) => {
    switch (icon) {
      case 'pea':
        return (
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <rect x="3" y="6" width="18" height="14" rx="2" stroke="#F62440" strokeWidth="2" />
            <path d="M3 10H21M7 15H10" stroke="#F62440" strokeWidth="2" strokeLinecap="round" />
          </svg>
        );
      case 'auto':
        return (
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <path d="M12 8V12L15 15M21 12C21 16.9706 16.9706 21 12 21C7.02944 21 3 16.9706 3 12C3 7.02944 7.02944 3 12 3C16.9706 3 21 7.02944 21 12Z" stroke="#F62440" strokeWidth="2" strokeLinecap="round" />
          </svg>
        );
      case 'rh':
        return (
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <path d="M17 21V19C17 17.9391 16.5786 16.9217 15.8284 16.1716C15.0783 15.4214 14.0609 15 13 15H5C3.93913 15 2.92172 15.4214 2.17157 16.1716C1.42143 16.9217 1 17.9391 1 19V21M23 21V19C22.9993 18.1137 22.7044 17.2528 22.1614 16.5523C21.6184 15.8519 20.8581 15.3516 20 15.13M16 3.13C16.8604 3.35031 17.623 3.85071 18.1676 4.55232C18.7122 5.25392 19.0078 6.11683 19.0078 7.005C19.0078 7.89318 18.7122 8.75608 18.1676 9.45769C17.623 10.1593 16.8604 10.6597 16 10.88M13 7C13 9.20914 11.2091 11 9 11C6.79086 11 5 9.20914 5 7C5 4.79086 6.79086 3 9 3C11.2091 3 13 4.79086 13 7Z" stroke="#F62440" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        );
      case 'form':
        return (
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <path d="M22 10V6L12 2L2 6L12 10L22 6M6 12V17C9 19 15 19 18 17V12" stroke="#F62440" strokeWidth="2" strokeLinejoin="round" />
          </svg>
        );
      default:
        return null;
    }
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 100%, rgba(246,36,64,0.15) 0%, transparent 65%), linear-gradient(180deg, transparent 60%, rgba(0,0,0,0.08) 100%)',
        }}
      />

      {/* Bold grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="grid60" width="120" height="120" patternUnits="userSpaceOnUse">
            <path d="M 120 0 L 0 0 0 120" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid60)" />
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: '50%',
          background: '#F62440',
          opacity: 0.06,
          bottom: -300,
          right: -200,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '150px solid transparent',
          borderRight: '150px solid transparent',
          borderBottom: '260px solid #000000',
          opacity: 0.05,
          top: 100,
          left: 80,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Rising particles */}
      {Array.from({ length: 35 }).map((_, i) => {
        const seed = seededRandom(i + 60);
        const x = seed * 1920;
        const y = (seededRandom(i + 111) * 1080 - frame * 0.7 + 1080) % 1080;
        const size = 3 + seededRandom(i + 22) * 5;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: size,
              height: size,
              borderRadius: '50%',
              background: i % 3 === 0 ? '#F62440' : '#000000',
              opacity: 0.32,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top tag */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: '50%',
            transform: `translateX(-50%)`,
            opacity: tagOp,
            display: 'flex',
            alignItems: 'center',
            gap: 16,
            background: '#F62440',
            padding: '14px 36px',
            borderRadius: 50,
            border: '3px solid #000000',
          }}
        >
          <svg width="26" height="26" viewBox="0 0 24 24" fill="none">
            <path d="M13 2L3 14H12L11 22L21 10H12L13 2Z" fill="#000000" />
          </svg>
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 32,
              color: '#000000',
              letterSpacing: 6,
            }}
          >
            PLAN D'ACTION — CETTE SEMAINE
          </span>
        </div>

        {/* Big title */}
        <div
          style={{
            position: 'absolute',
            top: 140,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 130,
              color: '#000000',
              fontWeight: 800,
              lineHeight: 1,
              letterSpacing: -3,
            }}
          >
            UN SEUL PAS.
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 130,
              color: '#F62440',
              fontWeight: 800,
              lineHeight: 1,
              letterSpacing: -3,
              fontStyle: 'italic',
              marginTop: 4,
            }}
          >
            MAIS UN VRAI.
          </div>
        </div>

        {/* 4 actions grid */}
        <div
          style={{
            position: 'absolute',
            top: 470,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 20,
            padding: '0 100px',
          }}
        >
          {actions.map((a, i) => {
            const aSp = sp(a.delay);
            return (
              <div
                key={i}
                style={{
                  opacity: aSp,
                  transform: `translateY(${interpolate(aSp, [0, 1], [50, 0])}px)`,
                  background: '#000000',
                  padding: '24px 28px',
                  borderRadius: 18,
                  border: '3px solid #F62440',
                  flex: 1,
                  maxWidth: 380,
                  display: 'flex',
                  flexDirection: 'column',
                  gap: 14,
                }}
              >
                <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
                  <div
                    style={{
                      fontFamily: 'Space Mono, monospace',
                      fontSize: 42,
                      color: '#F62440',
                      fontWeight: 700,
                    }}
                  >
                    {a.num}
                  </div>
                  {renderActionIcon(a.icon)}
                </div>
                <div
                  style={{
                    fontFamily: 'Anton, sans-serif',
                    fontSize: 30,
                    color: '#DDDDDD',
                    letterSpacing: 1,
                    lineHeight: 1.05,
                  }}
                >
                  {a.text}
                </div>
              </div>
            );
          })}
        </div>

        {/* Footprint progress line */}
        <div
          style={{
            position: 'absolute',
            top: 760,
            left: 200,
            right: 200,
            height: 60,
            opacity: trailOp,
          }}
        >
          <svg width="100%" height="60" viewBox="0 0 1520 60" preserveAspectRatio="none">
            <line
              x1="0"
              y1="30"
              x2="1520"
              y2="30"
              stroke="#000000"
              strokeWidth="4"
              strokeDasharray="1520"
              strokeDashoffset={1520 - trailDraw * 1520}
              strokeLinecap="round"
            />
            {[100, 400, 760, 1100, 1420].map((cx, i) => (
              <circle
                key={i}
                cx={cx}
                cy={30}
                r={trailDraw * 1520 > cx ? 14 : 0}
                fill="#F62440"
                stroke="#000000"
                strokeWidth="3"
              />
            ))}
          </svg>
        </div>

        {/* Final CTA */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: '50%',
            transform: `translateX(-50%) scale(${ctaScale * ctaPulse})`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 12,
          }}
        >
          <div
            style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 32,
              color: '#000000',
              fontWeight: 600,
            }}
          >
            Le piège se referme uniquement sur les immobiles.
          </div>
          <div
            style={{
              background: '#000000',
              padding: '22px 60px',
              borderRadius: 18,
              border: '4px solid #F62440',
              display: 'flex',
              alignItems: 'center',
              gap: 18,
            }}
          >
            <svg width="38" height="38" viewBox="0 0 24 24" fill="none">
              <path d="M5 12L19 12M19 12L12 5M19 12L12 19" stroke="#F62440" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <span
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 56,
                color: '#DDDDDD',
                letterSpacing: 3,
              }}
            >
              AGISSEZ MAINTENANT
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene60PremierPas;
```

