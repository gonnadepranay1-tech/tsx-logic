```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene1Ch6',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1Ch6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Globe SVG rotation
  const globeRotate = interpolate(frame, [0, durationInFrames], [0, 30], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Line reveal
  const lineWidth = interpolate(frame, [20, 60], [0, 520], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Word stagger
  const word1Op = interpolate(frame, [10, 25], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const word2Op = interpolate(frame, [25, 40], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const word3Op = interpolate(frame, [40, 55], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Floating particles
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    y: seededRandom(i * 3 + 2) * 1080,
    size: seededRandom(i * 3 + 3) * 6 + 3,
    speed: seededRandom(i * 5 + 7) * 0.4 + 0.15,
    phase: seededRandom(i * 7 + 11) * Math.PI * 2,
  }));

  // Stick figure
  const figureY = sp(40) * 1;
  const headNod = Math.sin(frame * 0.04) * 3;
  const breathe = Math.sin(frame * 0.05) * 2;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FFFFFF',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, sans-serif',
      }}
    >
      {/* Subtle warm grid texture */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 22 }, (_, i) => (
            <line
              key={`h${i}`}
              x1="0"
              y1={i * 52}
              x2="1920"
              y2={i * 52}
              stroke="#1C1C1C"
              strokeWidth="1"
            />
          ))}
          {Array.from({ length: 38 }, (_, i) => (
            <line
              key={`v${i}`}
              x1={i * 52}
              y1="0"
              x2={i * 52}
              y2="1080"
              stroke="#1C1C1C"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Geometric background shapes */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {/* Large circle top-right */}
          <circle
            cx={1750 + Math.sin(frame * 0.01) * 8}
            cy={120 + Math.cos(frame * 0.008) * 6}
            r="280"
            fill="none"
            stroke="#e9d700"
            strokeWidth="1.5"
            opacity="0.12"
          />
          <circle
            cx={1750 + Math.sin(frame * 0.01) * 8}
            cy={120 + Math.cos(frame * 0.008) * 6}
            r="200"
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            opacity="0.08"
          />
          {/* Triangle bottom-left */}
          <polygon
            points={`80,${950 + Math.sin(frame * 0.012) * 4} 220,${700 + Math.sin(frame * 0.012) * 4} 350,${950 + Math.sin(frame * 0.012) * 4}`}
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="1"
            opacity="0.06"
          />
          {/* Small accent circle */}
          <circle
            cx={960}
            cy={540}
            r={380 + Math.sin(frame * 0.007) * 5}
            fill="none"
            stroke="#e9d700"
            strokeWidth="0.8"
            opacity="0.04"
          />
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 18}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 12}
              r={p.size}
              fill={i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#1C1C1C' : '#FFC107'}
              opacity={0.08 + Math.sin(frame * 0.04 + p.phase) * 0.03}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Rotating Globe SVG — center-left */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          left: 80,
          top: '50%',
          transform: `translateY(-50%) translateX(${sp(5) * 0 - (1 - sp(5)) * 120}px)`,
          width: 420,
          height: 420,
          opacity: sp(5),
        }}
      >
        <svg
          width="420"
          height="420"
          viewBox="0 0 420 420"
          style={{ transform: `rotate(${globeRotate}deg)`, transformOrigin: '210px 210px' }}
        >
          <circle cx="210" cy="210" r="180" fill="none" stroke="#1C1C1C" strokeWidth="2" opacity="0.15" />
          <ellipse cx="210" cy="210" rx="180" ry="60" fill="none" stroke="#e9d700" strokeWidth="2" opacity="0.25" />
          <ellipse cx="210" cy="210" rx="100" ry="180" fill="none" stroke="#1C1C1C" strokeWidth="1.5" opacity="0.12" />
          <ellipse cx="210" cy="210" rx="50" ry="180" fill="none" stroke="#1C1C1C" strokeWidth="1" opacity="0.08" />
          <line x1="30" y1="210" x2="390" y2="210" stroke="#e9d700" strokeWidth="2" opacity="0.3" />
          <line x1="210" y1="30" x2="210" y2="390" stroke="#1C1C1C" strokeWidth="1.5" opacity="0.12" />
          {/* Continents placeholder blobs */}
          <ellipse cx="170" cy="175" rx="38" ry="22" fill="#1C1C1C" opacity="0.12" />
          <ellipse cx="250" cy="200" rx="30" ry="18" fill="#e9d700" opacity="0.18" />
          <ellipse cx="190" cy="240" rx="22" ry="14" fill="#1C1C1C" opacity="0.10" />
          <ellipse cx="280" cy="155" rx="18" ry="10" fill="#FFC107" opacity="0.15" />
        </svg>
      </AbsoluteFill>

      {/* Main content — right side */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          left: 560,
          top: 0,
          width: 1260,
          height: 1080,
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          paddingRight: 120,
        }}
      >
        {/* Yellow accent bar */}
        <div
          style={{
            width: lineWidth,
            height: 6,
            backgroundColor: '#e9d700',
            borderRadius: 3,
            marginBottom: 36,
            boxShadow: '0 0 18px rgba(233,215,0,0.35)',
          }}
        />

        {/* Word 1 */}
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 52,
            fontWeight: 800,
            color: '#9E9E9E',
            letterSpacing: '-0.5px',
            marginBottom: 8,
            opacity: word1Op,
            transform: `translateY(${(1 - word1Op) * 22}px)`,
          }}
        >
          निवेश करना
        </div>

        {/* Hero text */}
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 110,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.05,
            letterSpacing: '-2px',
            marginBottom: 20,
            opacity: word2Op,
            transform: `translateY(${(1 - word2Op) * 28}px)`,
          }}
        >
          सिर्फ आर्थिक
          <br />
          <span style={{ color: '#e9d700' }}>क्रिया नहीं है</span>
        </div>

        {/* Subtext */}
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 54,
            fontWeight: 700,
            color: '#2D2D2D',
            lineHeight: 1.45,
            opacity: word3Op,
            transform: `translateY(${(1 - word3Op) * 18}px)`,
            maxWidth: 880,
          }}
        >
          ये एक{' '}
          <span
            style={{
              borderBottom: '4px solid #e9d700',
              paddingBottom: 2,
            }}
          >
            राजनीतिक वक्तव्य
          </span>{' '}
          है
        </div>

        {/* Tag */}
        <div
          style={{
            marginTop: 40,
            opacity: interpolate(frame, [65, 80], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
            transform: `translateY(${interpolate(frame, [65, 80], [14, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 16,
          }}
        >
          <div
            style={{
              backgroundColor: '#1C1C1C',
              color: '#FFFFFF',
              fontFamily: 'Inter, sans-serif',
              fontSize: 32,
              fontWeight: 700,
              padding: '10px 28px',
              borderRadius: 8,
              letterSpacing: '0.5px',
            }}
          >
            CHAPTER 6
          </div>
          <div
            style={{
              color: '#9E9E9E',
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 600,
            }}
          >
            The Ultimate Truth
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick figure — bottom right */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          bottom: 80,
          right: 160,
          width: 90,
          height: 140,
          opacity: sp(50),
          transform: `translateY(${(1 - sp(50)) * 40}px)`,
        }}
      >
        <svg
          width="90"
          height="140"
          viewBox="0 0 90 140"
          style={{
            transform: `translateY(${breathe}px)`,
          }}
        >
          {/* Glow */}
          <circle cx="45" cy="22" r="28" fill="#e9d700" opacity="0.12" />
          {/* Head */}
          <circle
            cx="45"
            cy="22"
            r="16"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="2.5"
            style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}
          />
          {/* Glasses */}
          <g style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}>
            <circle cx="39" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="51" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="44" y1="21" x2="46" y2="21" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="34" y1="21" x2="33" y2="19" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="56" y1="21" x2="57" y2="19" stroke="#e9d700" strokeWidth="1.5" />
          </g>
          {/* Body */}
          <line x1="45" y1="38" x2="45" y2="90" stroke="#1C1C1C" strokeWidth="2.5" />
          {/* Arms — raised slightly */}
          <line x1="45" y1="55" x2="18" y2="42" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="55" x2="72" y2="42" stroke="#1C1C1C" strokeWidth="2" />
          {/* Legs */}
          <line x1="45" y1="90" x2="28" y2="125" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="62" y2="125" stroke="#1C1C1C" strokeWidth="2" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.08) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: '0.5px',
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1Ch6;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2Ch6',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2Ch6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Coin pulse
  const coinPulse = 1 + Math.sin(frame * 0.08) * 0.04;

  // Arrow draw
  const arrowProgress = interpolate(frame, [30, 70], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Text reveals
  const t1 = interpolate(frame, [15, 32], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t2 = interpolate(frame, [35, 52], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t3 = interpolate(frame, [55, 72], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 4 + 2) * 1920,
    y: seededRandom(i * 4 + 3) * 1080,
    r: seededRandom(i * 4 + 4) * 5 + 2,
    phase: seededRandom(i * 6 + 9) * Math.PI * 2,
    speed: seededRandom(i * 3 + 1) * 0.3 + 0.1,
  }));

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FFFFFF',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, sans-serif',
      }}
    >
      {/* Dot matrix texture */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.035 }}>
          {Array.from({ length: 32 }, (_, row) =>
            Array.from({ length: 52 }, (_, col) => (
              <circle
                key={`${row}-${col}`}
                cx={col * 38 + 10}
                cy={row * 34 + 10}
                r="1.5"
                fill="#1C1C1C"
              />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed * 0.8 + p.phase) * 10}
              r={p.r}
              fill={i % 2 === 0 ? '#e9d700' : '#1C1C1C'}
              opacity={0.06 + Math.sin(frame * 0.05 + p.phase) * 0.025}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Big Coin icon — Left */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          left: 120,
          top: '50%',
          width: 380,
          height: 380,
          transform: `translateY(-50%) scale(${coinPulse}) translateX(${(1 - sp(5)) * -100}px)`,
          opacity: sp(5),
        }}
      >
        <svg width="380" height="380" viewBox="0 0 380 380">
          {/* Coin shadow */}
          <ellipse cx="195" cy="340" rx="100" ry="18" fill="#1C1C1C" opacity="0.07" />
          {/* Coin body */}
          <circle cx="190" cy="190" r="155" fill="#e9d700" opacity="0.12" />
          <circle cx="190" cy="190" r="155" fill="none" stroke="#e9d700" strokeWidth="3" />
          <circle cx="190" cy="190" r="125" fill="none" stroke="#e9d700" strokeWidth="1.5" opacity="0.5" />
          {/* ₹ symbol */}
          <text
            x="190"
            y="215"
            textAnchor="middle"
            fontFamily="Inter, sans-serif"
            fontWeight="900"
            fontSize="110"
            fill="#1C1C1C"
            opacity="0.85"
          >
            ₹
          </text>
          {/* Control ring */}
          <circle cx="190" cy="190" r="168" fill="none" stroke="#FFC107" strokeWidth="2" strokeDasharray="12 8" opacity="0.35" />
        </svg>
      </AbsoluteFill>

      {/* Arrow — Exchange → Control */}
      <AbsoluteFill
        style={{ position: 'absolute', left: 520, top: '50%', transform: 'translateY(-50%)', width: 200, height: 80 }}
      >
        <svg width="200" height="80" viewBox="0 0 200 80">
          <line
            x1="10"
            y1="40"
            x2={10 + arrowProgress * 155}
            y2="40"
            stroke="#1C1C1C"
            strokeWidth="3"
            strokeLinecap="round"
          />
          {arrowProgress > 0.85 && (
            <>
              <polygon points="175,30 200,40 175,50" fill="#1C1C1C" />
            </>
          )}
          <text x="100" y="28" textAnchor="middle" fontFamily="Inter" fontSize="20" fill="#9E9E9E" fontWeight="700">
            नहीं है
          </text>
          <text x="100" y="70" textAnchor="middle" fontFamily="Inter" fontSize="20" fill="#e9d700" fontWeight="700">
            है भी
          </text>
        </svg>
      </AbsoluteFill>

      {/* Right content */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          left: 760,
          top: 0,
          width: 1060,
          height: 1080,
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          paddingRight: 100,
        }}
      >
        {/* Label */}
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 26,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: '3px',
            textTransform: 'uppercase',
            marginBottom: 24,
            opacity: t1,
          }}
        >
          पैसे की असली परिभाषा
        </div>

        {/* Two boxes: Exchange vs Control */}
        <div
          style={{
            display: 'flex',
            gap: 24,
            marginBottom: 36,
            opacity: t1,
            transform: `translateY(${(1 - t1) * 20}px)`,
          }}
        >
          {/* Box 1 */}
          <div
            style={{
              flex: 1,
              border: '2px solid #E0E0E0',
              borderRadius: 12,
              padding: '20px 24px',
              position: 'relative',
            }}
          >
            <div
              style={{
                position: 'absolute',
                top: -14,
                left: 16,
                backgroundColor: '#FFFFFF',
                padding: '0 8px',
                fontFamily: 'Inter',
                fontSize: 18,
                color: '#9E9E9E',
                fontWeight: 600,
              }}
            >
              पहले सोचते थे
            </div>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 38,
                fontWeight: 800,
                color: '#9E9E9E',
                textDecoration: 'line-through',
              }}
            >
              Exchange Tool
            </div>
          </div>
          {/* Box 2 */}
          <div
            style={{
              flex: 1,
              border: '3px solid #e9d700',
              borderRadius: 12,
              padding: '20px 24px',
              position: 'relative',
              backgroundColor: 'rgba(233,215,0,0.04)',
            }}
          >
            <div
              style={{
                position: 'absolute',
                top: -14,
                left: 16,
                backgroundColor: '#FFFFFF',
                padding: '0 8px',
                fontFamily: 'Inter',
                fontSize: 18,
                color: '#e9d700',
                fontWeight: 700,
              }}
            >
              असलियत
            </div>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 38,
                fontWeight: 900,
                color: '#1C1C1C',
              }}
            >
              Control Tool
            </div>
          </div>
        </div>

        {/* Main statement */}
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 92,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.08,
            letterSpacing: '-1.5px',
            marginBottom: 24,
            opacity: t2,
            transform: `translateY(${(1 - t2) * 24}px)`,
          }}
        >
          जो समझता है
          <br />
          <span style={{ color: '#e9d700' }}>सावधानी से</span>
          <br />
          <span style={{ fontSize: 70 }}>इस्तेमाल करता है</span>
        </div>

        {/* Bottom line */}
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 44,
            fontWeight: 700,
            color: '#2D2D2D',
            borderLeft: '5px solid #1C1C1C',
            paddingLeft: 20,
            opacity: t3,
            transform: `translateY(${(1 - t3) * 14}px)`,
          }}
        >
          Random tips के पीछे नहीं भागते
        </div>
      </AbsoluteFill>

      {/* Stick figure */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          bottom: 70,
          right: 150,
          width: 90,
          height: 140,
          opacity: sp(45),
          transform: `translateY(${(1 - sp(45)) * 40}px)`,
        }}
      >
        <svg
          width="90"
          height="140"
          viewBox="0 0 90 140"
          style={{ transform: `translateY(${breathe}px)` }}
        >
          <circle cx="45" cy="22" r="26" fill="#e9d700" opacity="0.10" />
          <circle
            cx="45"
            cy="22"
            r="16"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="2.5"
            style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}
          />
          <g style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}>
            <circle cx="39" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="51" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="44" y1="21" x2="46" y2="21" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="34" y1="21" x2="33" y2="19" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="56" y1="21" x2="57" y2="19" stroke="#e9d700" strokeWidth="1.5" />
          </g>
          <line x1="45" y1="38" x2="45" y2="90" stroke="#1C1C1C" strokeWidth="2.5" />
          <line x1="45" y1="55" x2="18" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="55" x2="72" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="28" y2="125" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="62" y2="125" stroke="#1C1C1C" strokeWidth="2" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.07) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2Ch6;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene3Ch6',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3Ch6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Three pillar heights
  const pillar1H = sp(15) * 200;
  const pillar2H = sp(30) * 270;
  const pillar3H = sp(45) * 230;

  const t1 = interpolate(frame, [10, 28], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t2 = interpolate(frame, [30, 50], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t3 = interpolate(frame, [55, 72], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 3 + 7) * 1920,
    y: seededRandom(i * 3 + 8) * 1080,
    r: seededRandom(i * 3 + 9) * 5 + 2,
    phase: seededRandom(i * 5 + 3) * Math.PI * 2,
    speed: seededRandom(i * 4 + 2) * 0.25 + 0.12,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FFFFFF',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, sans-serif',
      }}
    >
      {/* Diagonal line texture */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 60 }, (_, i) => (
            <line
              key={i}
              x1={i * 36 - 200}
              y1="0"
              x2={i * 36 - 200 + 1080}
              y2="1080"
              stroke="#1C1C1C"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 16}
              cy={p.y + Math.cos(frame * p.speed + p.phase) * 10}
              r={p.r}
              fill={i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#1C1C1C' : '#FFC107'}
              opacity={0.07}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Top heading */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: t1,
          transform: `translateY(${(1 - t1) * 20}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 58,
            fontWeight: 900,
            color: '#1C1C1C',
            letterSpacing: '-0.5px',
          }}
        >
          सिस्टम की जड़ों को समझो
        </div>
        <div
          style={{
            width: 120,
            height: 5,
            backgroundColor: '#e9d700',
            borderRadius: 3,
            margin: '12px auto 0',
          }}
        />
      </div>

      {/* Three pillars — center */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          top: 200,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'flex-end',
          gap: 60,
          height: 340,
        }}
      >
        {/* Pillar 1 — तर्क */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12 }}>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 700,
              color: '#1C1C1C',
              opacity: sp(15),
            }}
          >
            तर्क
          </div>
          <div
            style={{
              width: 180,
              height: pillar1H,
              backgroundColor: '#1C1C1C',
              borderRadius: '8px 8px 0 0',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            {/* Logic icon */}
            <svg width="60" height="60" viewBox="0 0 60 60">
              <circle cx="30" cy="30" r="22" fill="none" stroke="#e9d700" strokeWidth="2.5" />
              <text x="30" y="38" textAnchor="middle" fontSize="24" fontWeight="900" fill="#e9d700" fontFamily="Inter">
                ?
              </text>
            </svg>
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 22,
              color: '#9E9E9E',
              fontWeight: 600,
            }}
          >
            Logic
          </div>
        </div>

        {/* Pillar 2 — रिस्क (tallest) */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12 }}>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 700,
              color: '#e9d700',
              opacity: sp(30),
            }}
          >
            रिस्क
          </div>
          <div
            style={{
              width: 180,
              height: pillar2H,
              backgroundColor: '#e9d700',
              borderRadius: '8px 8px 0 0',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <svg width="60" height="60" viewBox="0 0 60 60">
              <polygon points="30,8 52,48 8,48" fill="none" stroke="#1C1C1C" strokeWidth="2.5" />
              <line x1="30" y1="22" x2="30" y2="36" stroke="#1C1C1C" strokeWidth="2.5" />
              <circle cx="30" cy="41" r="2.5" fill="#1C1C1C" />
            </svg>
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 22,
              color: '#9E9E9E',
              fontWeight: 600,
            }}
          >
            Risk
          </div>
        </div>

        {/* Pillar 3 — रिवार्ड */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12 }}>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 700,
              color: '#FFC107',
              opacity: sp(45),
            }}
          >
            रिवार्ड
          </div>
          <div
            style={{
              width: 180,
              height: pillar3H,
              backgroundColor: '#FFC107',
              borderRadius: '8px 8px 0 0',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <svg width="60" height="60" viewBox="0 0 60 60">
              <circle cx="30" cy="22" r="14" fill="none" stroke="#1C1C1C" strokeWidth="2.5" />
              <line x1="22" y1="36" x2="30" y2="52" stroke="#1C1C1C" strokeWidth="2.5" />
              <line x1="38" y1="36" x2="30" y2="52" stroke="#1C1C1C" strokeWidth="2.5" />
              <line x1="18" y1="44" x2="42" y2="44" stroke="#1C1C1C" strokeWidth="2" />
            </svg>
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 22,
              color: '#9E9E9E',
              fontWeight: 600,
            }}
          >
            Reward
          </div>
        </div>
      </AbsoluteFill>

      {/* Bottom baseline */}
      <div
        style={{
          position: 'absolute',
          left: 340,
          right: 340,
          top: 540,
          height: 4,
          backgroundColor: '#1C1C1C',
          opacity: 0.15,
          borderRadius: 2,
        }}
      />

      {/* Bottom text */}
      <div
        style={{
          position: 'absolute',
          bottom: 120,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: t2,
          transform: `translateY(${(1 - t2) * 20}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 68,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.1,
          }}
        >
          अंधाधुंध निवेश{' '}
          <span
            style={{
              color: '#FFFFFF',
              backgroundColor: '#ff0000',
              padding: '0 16px',
              borderRadius: 6,
            }}
          >
            नहीं
          </span>{' '}
          करते
        </div>
      </div>

      {/* Sub note */}
      <div
        style={{
          position: 'absolute',
          bottom: 62,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: t3,
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 36,
          fontWeight: 700,
          color: '#9E9E9E',
        }}
      >
        हर निवेश की पूरी कहानी जानते हैं
      </div>

      {/* Stick figure */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          bottom: 70,
          right: 150,
          width: 90,
          height: 140,
          opacity: sp(50),
          transform: `translateY(${(1 - sp(50)) * 40}px)`,
        }}
      >
        <svg
          width="90"
          height="140"
          viewBox="0 0 90 140"
          style={{ transform: `translateY(${breathe}px)` }}
        >
          <circle cx="45" cy="22" r="26" fill="#e9d700" opacity="0.10" />
          <circle
            cx="45"
            cy="22"
            r="16"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="2.5"
            style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}
          />
          <g style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}>
            <circle cx="39" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="51" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="44" y1="21" x2="46" y2="21" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="34" y1="21" x2="33" y2="19" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="56" y1="21" x2="57" y2="19" stroke="#e9d700" strokeWidth="1.5" />
          </g>
          <line x1="45" y1="38" x2="45" y2="90" stroke="#1C1C1C" strokeWidth="2.5" />
          <line x1="45" y1="55" x2="18" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="55" x2="72" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="28" y2="125" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="62" y2="125" stroke="#1C1C1C" strokeWidth="2" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.07) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3Ch6;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene4Ch6',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4Ch6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Road/journey path
  const pathProgress = interpolate(frame, [0, durationInFrames - 20], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const t1 = interpolate(frame, [8, 26], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t2 = interpolate(frame, [28, 46], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t3 = interpolate(frame, [50, 68], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t4 = interpolate(frame, [70, 85], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 5 + 1) * 1920,
    y: seededRandom(i * 5 + 2) * 1080,
    r: seededRandom(i * 5 + 3) * 5 + 2,
    phase: seededRandom(i * 5 + 4) * Math.PI * 2,
    speed: seededRandom(i * 5 + 5) * 0.2 + 0.1,
  }));

  // Journey milestones
  const milestones = [
    { label: 'शुरुआत', sublabel: 'Financial Literacy', x: 150, active: true },
    { label: 'समझ', sublabel: 'Understanding', x: 480, active: pathProgress > 0.3 },
    { label: 'रणनीति', sublabel: 'Strategy', x: 810, active: pathProgress > 0.6 },
    { label: 'ताकत', sublabel: 'Power', x: 1140, active: pathProgress > 0.85 },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FFFFFF',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, sans-serif',
      }}
    >
      {/* Concentric circle texture */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 12 }, (_, i) => (
            <circle
              key={i}
              cx="960"
              cy="540"
              r={80 + i * 80}
              fill="none"
              stroke="#1C1C1C"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed + p.phase) * 10}
              r={p.r}
              fill={i % 2 === 0 ? '#e9d700' : '#1C1C1C'}
              opacity={0.06}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Main headline */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 100,
          right: 100,
          opacity: t1,
          transform: `translateY(${(1 - t1) * 20}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 42,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: '-0.3px',
            marginBottom: 6,
          }}
        >
          आपकी वित्तीय यात्रा
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 100,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.05,
            letterSpacing: '-2px',
          }}
        >
          साक्षरता से{' '}
          <span style={{ color: '#e9d700' }}>समझदारी</span>
        </div>
      </div>

      {/* Journey path SVG */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          top: 340,
          left: 80,
          right: 80,
          height: 240,
        }}
      >
        <svg width="1760" height="240" viewBox="0 0 1760 240">
          {/* Base path */}
          <path
            d="M100 120 Q400 80 700 120 Q1000 160 1300 120 Q1500 80 1660 120"
            fill="none"
            stroke="#E0E0E0"
            strokeWidth="4"
            strokeLinecap="round"
          />
          {/* Animated progress path */}
          <path
            d="M100 120 Q400 80 700 120 Q1000 160 1300 120 Q1500 80 1660 120"
            fill="none"
            stroke="#e9d700"
            strokeWidth="4"
            strokeLinecap="round"
            strokeDasharray="1760"
            strokeDashoffset={1760 * (1 - pathProgress)}
          />
          {/* Milestone dots */}
          {milestones.map((m, i) => (
            <g key={i}>
              <circle
                cx={m.x}
                cy="120"
                r="20"
                fill={m.active ? '#1C1C1C' : '#E0E0E0'}
              />
              <circle
                cx={m.x}
                cy="120"
                r="10"
                fill={m.active ? '#e9d700' : '#FFFFFF'}
              />
              <text
                x={m.x}
                y="168"
                textAnchor="middle"
                fontFamily="Noto Sans Devanagari, sans-serif"
                fontSize="26"
                fontWeight="700"
                fill={m.active ? '#1C1C1C' : '#9E9E9E'}
              >
                {m.label}
              </text>
              <text
                x={m.x}
                y="196"
                textAnchor="middle"
                fontFamily="Inter, sans-serif"
                fontSize="18"
                fill="#9E9E9E"
              >
                {m.sublabel}
              </text>
            </g>
          ))}
          {/* YOU ARE HERE marker */}
          <g
            style={{
              transform: `translateX(${interpolate(pathProgress, [0, 1], [80, 1620], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
            }}
          >
            <circle cx="0" cy="120" r="14" fill="#ff0000" />
            <text
              x="0"
              y="88"
              textAnchor="middle"
              fontFamily="Inter, sans-serif"
              fontSize="18"
              fontWeight="700"
              fill="#ff0000"
            >
              YOU
            </text>
          </g>
        </svg>
      </AbsoluteFill>

      {/* Bottom two cards */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: 100,
          right: 100,
          display: 'flex',
          gap: 40,
          opacity: t3,
          transform: `translateY(${(1 - t3) * 20}px)`,
        }}
      >
        {/* Card 1 */}
        <div
          style={{
            flex: 1,
            backgroundColor: '#F5F5F5',
            border: '2px solid #E0E0E0',
            borderRadius: 14,
            padding: '24px 30px',
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 32,
              fontWeight: 800,
              color: '#9E9E9E',
              marginBottom: 8,
            }}
          >
            पैसे के बारे में
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 44,
              fontWeight: 900,
              color: '#1C1C1C',
            }}
          >
            सब बात करते हैं
          </div>
        </div>
        {/* Card 2 */}
        <div
          style={{
            flex: 1,
            backgroundColor: '#1C1C1C',
            border: '2px solid #1C1C1C',
            borderRadius: 14,
            padding: '24px 30px',
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 32,
              fontWeight: 800,
              color: '#e9d700',
              marginBottom: 8,
            }}
          >
            पैसे की ताकत के बारे में
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 44,
              fontWeight: 900,
              color: '#FFFFFF',
            }}
          >
            कोई नहीं करता
          </div>
        </div>
      </div>

      {/* ProBuffet tag */}
      <div
        style={{
          position: 'absolute',
          top: 90,
          right: 100,
          opacity: t4,
          backgroundColor: '#e9d700',
          padding: '10px 24px',
          borderRadius: 8,
          fontFamily: 'Inter, sans-serif',
          fontSize: 26,
          fontWeight: 800,
          color: '#1C1C1C',
        }}
      >
        ProBuffet HQ
      </div>

      {/* Stick figure */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          bottom: 70,
          right: 150,
          width: 90,
          height: 140,
          opacity: sp(50),
          transform: `translateY(${(1 - sp(50)) * 40}px)`,
        }}
      >
        <svg
          width="90"
          height="140"
          viewBox="0 0 90 140"
          style={{ transform: `translateY(${breathe}px)` }}
        >
          <circle cx="45" cy="22" r="26" fill="#e9d700" opacity="0.10" />
          <circle
            cx="45"
            cy="22"
            r="16"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="2.5"
            style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}
          />
          <g style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}>
            <circle cx="39" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="51" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="44" y1="21" x2="46" y2="21" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="34" y1="21" x2="33" y2="19" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="56" y1="21" x2="57" y2="19" stroke="#e9d700" strokeWidth="1.5" />
          </g>
          <line x1="45" y1="38" x2="45" y2="90" stroke="#1C1C1C" strokeWidth="2.5" />
          <line x1="45" y1="55" x2="18" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="55" x2="72" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="28" y2="125" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="62" y2="125" stroke="#1C1C1C" strokeWidth="2" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.07) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4Ch6;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene5Ch6',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5Ch6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  const t1 = interpolate(frame, [8, 26], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t2 = interpolate(frame, [28, 46], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t3 = interpolate(frame, [50, 68], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Blueprint grid lines draw
  const gridOp = interpolate(frame, [5, 30], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Building blocks rise
  const block1 = sp(20) * 120;
  const block2 = sp(32) * 160;
  const block3 = sp(44) * 100;
  const block4 = sp(56) * 200;

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 4 + 1) * 1920,
    y: seededRandom(i * 4 + 2) * 1080,
    r: seededRandom(i * 4 + 3) * 5 + 2,
    phase: seededRandom(i * 4 + 4) * Math.PI * 2,
    speed: seededRandom(i * 3 + 2) * 0.22 + 0.1,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FFFFFF',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, sans-serif',
      }}
    >
      {/* Blueprint grid background */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: gridOp * 0.06 }}>
          {/* Horizontal lines */}
          {Array.from({ length: 28 }, (_, i) => (
            <line key={`h${i}`} x1="0" y1={i * 40} x2="1920" y2={i * 40} stroke="#1C1C1C" strokeWidth="0.8" />
          ))}
          {/* Vertical lines */}
          {Array.from({ length: 50 }, (_, i) => (
            <line key={`v${i}`} x1={i * 40} y1="0" x2={i * 40} y2="1080" stroke="#1C1C1C" strokeWidth="0.8" />
          ))}
          {/* Cross markers */}
          {Array.from({ length: 6 }, (_, row) =>
            Array.from({ length: 10 }, (_, col) => (
              <g key={`c${row}${col}`}>
                <line x1={col * 200 - 6} y1={row * 180} x2={col * 200 + 6} y2={row * 180} stroke="#e9d700" strokeWidth="1.2" opacity="0.6" />
                <line x1={col * 200} y1={row * 180 - 6} x2={col * 200} y2={row * 180 + 6} stroke="#e9d700" strokeWidth="1.2" opacity="0.6" />
              </g>
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed + p.phase) * 10}
              r={p.r}
              fill={i % 2 === 0 ? '#e9d700' : '#1C1C1C'}
              opacity={0.06}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Left — Building blocks (financial architecture) */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          left: 80,
          bottom: 120,
          width: 580,
          height: 500,
          display: 'flex',
          alignItems: 'flex-end',
          gap: 20,
        }}
      >
        {/* Block 1 */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
          <div
            style={{
              width: 110,
              height: block1,
              backgroundColor: '#e9d700',
              borderRadius: '6px 6px 0 0',
              border: '2px solid #1C1C1C',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <svg width="40" height="40" viewBox="0 0 40 40">
              <rect x="6" y="6" width="28" height="28" rx="4" fill="none" stroke="#1C1C1C" strokeWidth="2.5" />
              <line x1="6" y1="20" x2="34" y2="20" stroke="#1C1C1C" strokeWidth="2" />
              <line x1="20" y1="6" x2="20" y2="34" stroke="#1C1C1C" strokeWidth="2" />
            </svg>
          </div>
          <span style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 700, color: '#9E9E9E' }}>Equity</span>
        </div>

        {/* Block 2 */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
          <div
            style={{
              width: 110,
              height: block2,
              backgroundColor: '#1C1C1C',
              borderRadius: '6px 6px 0 0',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <svg width="40" height="40" viewBox="0 0 40 40">
              <circle cx="20" cy="20" r="14" fill="none" stroke="#e9d700" strokeWidth="2.5" />
              <text x="20" y="25" textAnchor="middle" fontSize="14" fontWeight="900" fill="#e9d700" fontFamily="Inter">
                ₹
              </text>
            </svg>
          </div>
          <span style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 700, color: '#9E9E9E' }}>Debt</span>
        </div>

        {/* Block 3 */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
          <div
            style={{
              width: 110,
              height: block3,
              backgroundColor: '#FFC107',
              borderRadius: '6px 6px 0 0',
              border: '2px solid #1C1C1C',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <svg width="40" height="40" viewBox="0 0 40 40">
              <polygon points="20,4 36,34 4,34" fill="none" stroke="#1C1C1C" strokeWidth="2.5" />
            </svg>
          </div>
          <span style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 700, color: '#9E9E9E' }}>Gold</span>
        </div>

        {/* Block 4 */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
          <div
            style={{
              width: 110,
              height: block4,
              backgroundColor: '#2D2D2D',
              borderRadius: '6px 6px 0 0',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <svg width="40" height="40" viewBox="0 0 40 40">
              <rect x="8" y="16" width="24" height="18" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
              <path d="M13 16 V12 a7 7 0 0 1 14 0 V16" fill="none" stroke="#e9d700" strokeWidth="2.5" />
              <circle cx="20" cy="25" r="3" fill="#e9d700" />
            </svg>
          </div>
          <span style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 700, color: '#9E9E9E' }}>Safety</span>
        </div>

        {/* Base */}
        <div
          style={{
            position: 'absolute',
            bottom: 36,
            left: 0,
            right: 0,
            height: 6,
            backgroundColor: '#1C1C1C',
            borderRadius: 3,
          }}
        />
      </AbsoluteFill>

      {/* Right content */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          left: 760,
          top: 0,
          width: 1060,
          height: 1080,
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          paddingRight: 100,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: '3px',
            textTransform: 'uppercase',
            marginBottom: 20,
            opacity: t1,
          }}
        >
          The Transformation
        </div>

        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 108,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.05,
            letterSpacing: '-2px',
            marginBottom: 28,
            opacity: t2,
            transform: `translateY(${(1 - t2) * 28}px)`,
          }}
        >
          अपना खुद का
          <br />
          <span style={{ color: '#e9d700' }}>Financial</span>
          <br />
          <span
            style={{
              fontSize: 90,
              borderBottom: '6px solid #1C1C1C',
              paddingBottom: 4,
            }}
          >
            Architect
          </span>{' '}
          बनो
        </div>

        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 46,
            fontWeight: 700,
            color: '#2D2D2D',
            lineHeight: 1.4,
            opacity: t3,
            transform: `translateY(${(1 - t3) * 16}px)`,
            maxWidth: 820,
          }}
        >
          यही बदलाव ProBuffet HQ{' '}
          <span style={{ color: '#e9d700', fontWeight: 900 }}>आप में</span>
          {' '}देखना चाहता है
        </div>
      </AbsoluteFill>

      {/* Stick figure */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          bottom: 70,
          right: 150,
          width: 90,
          height: 140,
          opacity: sp(50),
          transform: `translateY(${(1 - sp(50)) * 40}px)`,
        }}
      >
        <svg
          width="90"
          height="140"
          viewBox="0 0 90 140"
          style={{ transform: `translateY(${breathe}px)` }}
        >
          <circle cx="45" cy="22" r="26" fill="#e9d700" opacity="0.10" />
          <circle
            cx="45"
            cy="22"
            r="16"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="2.5"
            style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}
          />
          <g style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}>
            <circle cx="39" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="51" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="44" y1="21" x2="46" y2="21" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="34" y1="21" x2="33" y2="19" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="56" y1="21" x2="57" y2="19" stroke="#e9d700" strokeWidth="1.5" />
          </g>
          <line x1="45" y1="38" x2="45" y2="90" stroke="#1C1C1C" strokeWidth="2.5" />
          <line x1="45" y1="55" x2="20" y2="42" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="55" x2="70" y2="42" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="28" y2="125" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="62" y2="125" stroke="#1C1C1C" strokeWidth="2" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.07) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5Ch6;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene6Ch6',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6Ch6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  const t1 = interpolate(frame, [8, 26], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t2 = interpolate(frame, [28, 46], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t3 = interpolate(frame, [50, 68], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Compound growth curve
  const growthProgress = interpolate(frame, [15, durationInFrames - 20], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Seed pulse
  const seedPulse = 1 + Math.sin(frame * 0.1) * 0.06;

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 3 + 5) * 1920,
    y: seededRandom(i * 3 + 6) * 1080,
    r: seededRandom(i * 3 + 7) * 5 + 2,
    phase: seededRandom(i * 4 + 2) * Math.PI * 2,
    speed: seededRandom(i * 5 + 3) * 0.22 + 0.1,
  }));

  // Build chart points for compound growth
  const chartPoints = Array.from({ length: 100 }, (_, i) => {
    const t = (i / 99) * growthProgress;
    const x = 100 + t * 820;
    const y = 400 - Math.pow(t, 2.2) * 320;
    return `${x},${y}`;
  }).join(' ');

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FFFFFF',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, sans-serif',
      }}
    >
      {/* Subtle radial texture */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 16 }, (_, i) => (
            <circle
              key={i}
              cx="500"
              cy="700"
              r={50 + i * 55}
              fill="none"
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed + p.phase) * 10}
              r={p.r}
              fill={i % 2 === 0 ? '#e9d700' : '#FFC107'}
              opacity={0.07}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Left — Compound growth chart */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          left: 80,
          top: 140,
          width: 860,
          height: 480,
          opacity: sp(10),
          transform: `translateX(${(1 - sp(10)) * -60}px)`,
        }}
      >
        <svg width="860" height="480" viewBox="0 0 1020 480">
          {/* Grid */}
          {Array.from({ length: 5 }, (_, i) => (
            <line key={i} x1="80" y1={80 + i * 80} x2="940" y2={80 + i * 80} stroke="#E0E0E0" strokeWidth="1" />
          ))}
          {/* Curve */}
          <polyline
            points={chartPoints}
            fill="none"
            stroke="#e9d700"
            strokeWidth="4"
            strokeLinecap="round"
            strokeLinejoin="round"
          />
          {/* Fill under curve */}
          <polygon
            points={`100,400 ${chartPoints} ${100 + growthProgress * 820},400`}
            fill="#e9d700"
            opacity="0.08"
          />
          {/* Axes */}
          <line x1="100" y1="400" x2="940" y2="400" stroke="#1C1C1C" strokeWidth="2.5" />
          <line x1="100" y1="80" x2="100" y2="400" stroke="#1C1C1C" strokeWidth="2.5" />
          {/* Seed dot at start */}
          <circle cx="100" cy="400" r={12 * seedPulse} fill="#1C1C1C" />
          <text x="100" y="435" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="22" fill="#9E9E9E" fontWeight="700">
            छोटा निवेश
          </text>
          {/* End label */}
          {growthProgress > 0.85 && (
            <>
              <circle
                cx={100 + growthProgress * 820}
                cy={400 - Math.pow(growthProgress, 2.2) * 320}
                r="14"
                fill="#e9d700"
                stroke="#1C1C1C"
                strokeWidth="2"
              />
              <text
                x={100 + growthProgress * 820}
                y={400 - Math.pow(growthProgress, 2.2) * 320 - 22}
                textAnchor="middle"
                fontFamily="Noto Sans Devanagari"
                fontSize="22"
                fill="#1C1C1C"
                fontWeight="800"
              >
                बड़ी ताकत
              </text>
            </>
          )}
          {/* Axis labels */}
          <text x="94" y="460" textAnchor="middle" fontFamily="Inter" fontSize="18" fill="#9E9E9E">T=0</text>
          <text x="940" y="460" textAnchor="middle" fontFamily="Inter" fontSize="18" fill="#9E9E9E">Time →</text>
          <text
            x="40"
            y="80"
            textAnchor="middle"
            fontFamily="Noto Sans Devanagari"
            fontSize="18"
            fill="#9E9E9E"
            style={{ writingMode: 'vertical-rl' }}
          >
            Value ↑
          </text>
        </svg>
      </AbsoluteFill>

      {/* Right content */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          left: 1000,
          top: 0,
          width: 820,
          height: 1080,
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          paddingRight: 90,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 42,
            fontWeight: 700,
            color: '#9E9E9E',
            marginBottom: 16,
            opacity: t1,
          }}
        >
          Compound Power
        </div>

        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 100,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.05,
            letterSpacing: '-1.5px',
            marginBottom: 28,
            opacity: t2,
            transform: `translateY(${(1 - t2) * 24}px)`,
          }}
        >
          छोटा निवेश
          <br />
          <span style={{ color: '#e9d700' }}>+ सही Structure</span>
          <br />
          <span style={{ fontSize: 76 }}>= बड़ी ताकत</span>
        </div>

        {/* Three badges */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            gap: 16,
            opacity: t3,
            transform: `translateY(${(1 - t3) * 14}px)`,
          }}
        >
          {[
            { label: 'लंबी अवधि', icon: '⏳', note: 'Long Term Thinking' },
            { label: 'सही Structure', icon: '🏗', note: 'Right Framework' },
            { label: 'अटल संकल्प', icon: '🎯', note: 'Discipline' },
          ].map((item, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 18,
                padding: '14px 22px',
                border: `2px solid ${i === 1 ? '#e9d700' : '#E0E0E0'}`,
                borderRadius: 10,
                backgroundColor: i === 1 ? 'rgba(233,215,0,0.05)' : 'transparent',
              }}
            >
              <svg width="36" height="36" viewBox="0 0 36 36">
                {i === 0 && (
                  <>
                    <circle cx="18" cy="18" r="14" fill="none" stroke="#1C1C1C" strokeWidth="2" />
                    <line x1="18" y1="8" x2="18" y2="18" stroke="#e9d700" strokeWidth="2.5" />
                    <line x1="18" y1="18" x2="26" y2="22" stroke="#1C1C1C" strokeWidth="2" />
                  </>
                )}
                {i === 1 && (
                  <>
                    <rect x="4" y="20" width="28" height="12" rx="2" fill="none" stroke="#e9d700" strokeWidth="2" />
                    <rect x="8" y="12" width="20" height="8" rx="2" fill="none" stroke="#e9d700" strokeWidth="2" />
                    <rect x="12" y="6" width="12" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="2" />
                  </>
                )}
                {i === 2 && (
                  <>
                    <circle cx="18" cy="18" r="14" fill="none" stroke="#1C1C1C" strokeWidth="2" />
                    <circle cx="18" cy="18" r="5" fill="#e9d700" />
                  </>
                )}
              </svg>
              <div>
                <div
                  style={{
                    fontFamily: 'Noto Sans Devanagari, sans-serif',
                    fontSize: 34,
                    fontWeight: 800,
                    color: '#1C1C1C',
                    lineHeight: 1.1,
                  }}
                >
                  {item.label}
                </div>
                <div
                  style={{
                    fontFamily: 'Inter, sans-serif',
                    fontSize: 20,
                    color: '#9E9E9E',
                    fontWeight: 600,
                  }}
                >
                  {item.note}
                </div>
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Stick figure */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          bottom: 70,
          left: 420,
          width: 90,
          height: 140,
          opacity: sp(55),
          transform: `translateY(${(1 - sp(55)) * 40}px)`,
        }}
      >
        <svg
          width="90"
          height="140"
          viewBox="0 0 90 140"
          style={{ transform: `translateY(${breathe}px)` }}
        >
          <circle cx="45" cy="22" r="26" fill="#e9d700" opacity="0.10" />
          <circle
            cx="45"
            cy="22"
            r="16"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="2.5"
            style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}
          />
          <g style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}>
            <circle cx="39" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="51" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="44" y1="21" x2="46" y2="21" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="34" y1="21" x2="33" y2="19" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="56" y1="21" x2="57" y2="19" stroke="#e9d700" strokeWidth="1.5" />
          </g>
          <line x1="45" y1="38" x2="45" y2="90" stroke="#1C1C1C" strokeWidth="2.5" />
          <line x1="45" y1="55" x2="18" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="55" x2="72" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="28" y2="125" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="62" y2="125" stroke="#1C1C1C" strokeWidth="2" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.07) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6Ch6;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene7Ch6',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7Ch6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Chain link draw progress
  const chainProgress = interpolate(frame, [20, 70], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const t1 = interpolate(frame, [8, 26], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t2 = interpolate(frame, [28, 48], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 4 + 3) * 1920,
    y: seededRandom(i * 4 + 4) * 1080,
    r: seededRandom(i * 4 + 5) * 5 + 2,
    phase: seededRandom(i * 3 + 6) * Math.PI * 2,
    speed: seededRandom(i * 5 + 1) * 0.2 + 0.1,
  }));

  // India initiatives
  const initiatives = [
    { label: 'UPI', sublabel: 'Digital Payments', x: 240, y: 320, delay: 20 },
    { label: 'Rupee Trade', sublabel: 'Settlement', x: 620, y: 200, delay: 32 },
    { label: 'GIFT City', sublabel: 'Financial Hub', x: 1000, y: 320, delay: 44 },
    { label: 'Digital ₹', sublabel: 'CBDC', x: 1380, y: 200, delay: 56 },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FFFFFF',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, sans-serif',
      }}
    >
      {/* Map/network texture */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 20 }, (_, i) =>
            Array.from({ length: 34 }, (_, j) => (
              <circle
                key={`${i}-${j}`}
                cx={j * 58 + 10}
                cy={i * 58 + 10}
                r="1.8"
                fill="#1C1C1C"
              />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed + p.phase) * 10}
              r={p.r}
              fill={i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#FFC107' : '#1C1C1C'}
              opacity={0.07}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Top headline */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: t1,
          transform: `translateY(${(1 - t1) * 20}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 56,
            fontWeight: 900,
            color: '#1C1C1C',
          }}
        >
          भारत का{' '}
          <span style={{ color: '#e9d700' }}>Dollar Independence</span>
          {' '}Mission
        </div>
        <div
          style={{
            width: 200,
            height: 4,
            backgroundColor: '#e9d700',
            margin: '12px auto 0',
            borderRadius: 2,
          }}
        />
      </div>

      {/* Initiatives network */}
      <AbsoluteFill
        style={{ position: 'absolute', top: 200, left: 0, right: 0, height: 480 }}
      >
        <svg width="1920" height="480" viewBox="0 0 1920 480">
          {/* Connection lines */}
          {chainProgress > 0 && (
            <line
              x1="240"
              y1="320"
              x2={240 + chainProgress * 380}
              y2={320 - chainProgress * 120}
              stroke="#e9d700"
              strokeWidth="2.5"
              strokeDasharray="8 4"
            />
          )}
          {chainProgress > 0.33 && (
            <line
              x1="620"
              y1="200"
              x2={620 + chainProgress * 380}
              y2={200 + chainProgress * 120}
              stroke="#e9d700"
              strokeWidth="2.5"
              strokeDasharray="8 4"
            />
          )}
          {chainProgress > 0.66 && (
            <line
              x1="1000"
              y1="320"
              x2={1000 + chainProgress * 380}
              y2={320 - chainProgress * 120}
              stroke="#e9d700"
              strokeWidth="2.5"
              strokeDasharray="8 4"
            />
          )}

          {/* Initiative nodes */}
          {initiatives.map((init, i) => {
            const nodeOp = sp(init.delay);
            return (
              <g key={i} opacity={nodeOp}>
                {/* Glow */}
                <circle cx={init.x} cy={init.y} r="70" fill="#e9d700" opacity="0.06" />
                {/* Main circle */}
                <circle
                  cx={init.x}
                  cy={init.y}
                  r="55"
                  fill="#1C1C1C"
                  stroke="#e9d700"
                  strokeWidth="2.5"
                />
                {/* Label */}
                <text
                  x={init.x}
                  y={init.y + 7}
                  textAnchor="middle"
                  fontFamily="Inter, sans-serif"
                  fontSize="28"
                  fontWeight="900"
                  fill="#e9d700"
                >
                  {init.label}
                </text>
                {/* Sublabel */}
                <text
                  x={init.x}
                  y={init.y + 90}
                  textAnchor="middle"
                  fontFamily="Noto Sans Devanagari, sans-serif"
                  fontSize="22"
                  fontWeight="700"
                  fill="#9E9E9E"
                >
                  {init.sublabel}
                </text>
              </g>
            );
          })}

          {/* Dollar breaking symbol — center bottom */}
          <g
            opacity={interpolate(frame, [65, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          >
            <circle cx="960" cy="420" r="30" fill="#ff0000" opacity="0.12" />
            <circle cx="960" cy="420" r="30" fill="none" stroke="#ff0000" strokeWidth="2.5" />
            <line x1="938" y1="398" x2="982" y2="442" stroke="#ff0000" strokeWidth="3" />
            <text x="960" y="427" textAnchor="middle" fontFamily="Inter" fontSize="28" fontWeight="900" fill="#ff0000">
              $
            </text>
          </g>
        </svg>
      </AbsoluteFill>

      {/* Bottom statement */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: t2,
          transform: `translateY(${(1 - t2) * 16}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 68,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.1,
          }}
        >
          Dollar निर्भरता{' '}
          <span
            style={{
              color: '#FFFFFF',
              backgroundColor: '#1C1C1C',
              padding: '4px 18px',
              borderRadius: 8,
            }}
          >
            तोड़ने
          </span>{' '}
          की कोशिश
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 32,
            fontWeight: 600,
            color: '#9E9E9E',
            marginTop: 12,
          }}
        >
          रातों रात नहीं — लेकिन हो रहा है
        </div>
      </div>

      {/* Stick figure */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          bottom: 70,
          right: 150,
          width: 90,
          height: 140,
          opacity: sp(50),
          transform: `translateY(${(1 - sp(50)) * 40}px)`,
        }}
      >
        <svg
          width="90"
          height="140"
          viewBox="0 0 90 140"
          style={{ transform: `translateY(${breathe}px)` }}
        >
          <circle cx="45" cy="22" r="26" fill="#e9d700" opacity="0.10" />
          <circle
            cx="45"
            cy="22"
            r="16"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="2.5"
            style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}
          />
          <g style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}>
            <circle cx="39" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="51" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="44" y1="21" x2="46" y2="21" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="34" y1="21" x2="33" y2="19" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="56" y1="21" x2="57" y2="19" stroke="#e9d700" strokeWidth="1.5" />
          </g>
          <line x1="45" y1="38" x2="45" y2="90" stroke="#1C1C1C" strokeWidth="2.5" />
          <line x1="45" y1="55" x2="18" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="55" x2="72" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="28" y2="125" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="62" y2="125" stroke="#1C1C1C" strokeWidth="2" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.07) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7Ch6;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene8Ch6',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8Ch6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  const t1 = interpolate(frame, [8, 26], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t2 = interpolate(frame, [28, 46], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t3 = interpolate(frame, [50, 68], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 3 + 8) * 1920,
    y: seededRandom(i * 3 + 9) * 1080,
    r: seededRandom(i * 3 + 10) * 5 + 2,
    phase: seededRandom(i * 5 + 2) * Math.PI * 2,
    speed: seededRandom(i * 4 + 3) * 0.2 + 0.1,
  }));

  // Three investment lenses
  const lenses = [
    {
      icon: 'profit',
      labelHi: 'Profit',
      note: 'जरूरी है',
      note2: 'लेकिन काफी नहीं',
      color: '#9E9E9E',
      border: '#E0E0E0',
      delay: 15,
    },
    {
      icon: 'protection',
      labelHi: 'Protection',
      note: 'आपकी रक्षा',
      note2: 'downside से',
      color: '#1C1C1C',
      border: '#e9d700',
      delay: 30,
    },
    {
      icon: 'independence',
      labelHi: 'Structural Independence',
      note: 'असली लक्ष्य',
      note2: 'long term wealth',
      color: '#e9d700',
      border: '#1C1C1C',
      delay: 45,
    },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FFFFFF',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, sans-serif',
      }}
    >
      {/* Wave texture */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 16 }, (_, i) => (
            <path
              key={i}
              d={`M0 ${i * 72} Q480 ${i * 72 - 30} 960 ${i * 72} Q1440 ${i * 72 + 30} 1920 ${i * 72}`}
              fill="none"
              stroke="#1C1C1C"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed + p.phase) * 10}
              r={p.r}
              fill={i % 2 === 0 ? '#e9d700' : '#1C1C1C'}
              opacity={0.06}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Top: Main headline */}
      <div
        style={{
          position: 'absolute',
          top: 72,
          left: 100,
          right: 100,
          opacity: t1,
          transform: `translateY(${(1 - t1) * 20}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 96,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.05,
            letterSpacing: '-1.5px',
          }}
        >
          सिर्फ{' '}
          <span
            style={{
              color: '#9E9E9E',
              textDecoration: 'line-through',
              textDecorationColor: '#ff0000',
              textDecorationThickness: '4px',
            }}
          >
            Profit
          </span>{' '}
          नहीं —
        </div>
      </div>

      {/* Three lens cards */}
      <div
        style={{
          position: 'absolute',
          top: 240,
          left: 80,
          right: 80,
          display: 'flex',
          gap: 36,
          opacity: 1,
        }}
      >
        {lenses.map((lens, i) => (
          <div
            key={i}
            style={{
              flex: 1,
              border: `3px solid ${lens.border}`,
              borderRadius: 16,
              padding: '30px 28px',
              backgroundColor: i === 2 ? 'rgba(233,215,0,0.04)' : 'transparent',
              opacity: sp(lens.delay),
              transform: `translateY(${(1 - sp(lens.delay)) * 30}px)`,
            }}
          >
            {/* Icon */}
            <div style={{ marginBottom: 16 }}>
              <svg width="52" height="52" viewBox="0 0 52 52">
                {i === 0 && (
                  <>
                    <circle cx="26" cy="26" r="20" fill="none" stroke="#9E9E9E" strokeWidth="2.5" />
                    <text x="26" y="32" textAnchor="middle" fontSize="20" fontWeight="900" fill="#9E9E9E" fontFamily="Inter">
                      ₹+
                    </text>
                  </>
                )}
                {i === 1 && (
                  <>
                    <path
                      d="M26 4 L8 14 L8 28 C8 38 17 46 26 48 C35 46 44 38 44 28 L44 14 Z"
                      fill="none"
                      stroke="#1C1C1C"
                      strokeWidth="2.5"
                    />
                    <path d="M18 26 L23 31 L34 20" stroke="#e9d700" strokeWidth="2.5" fill="none" strokeLinecap="round" />
                  </>
                )}
                {i === 2 && (
                  <>
                    <circle cx="26" cy="26" r="20" fill="none" stroke="#e9d700" strokeWidth="2.5" />
                    <circle cx="26" cy="26" r="10" fill="none" stroke="#1C1C1C" strokeWidth="2" />
                    <circle cx="26" cy="26" r="3" fill="#e9d700" />
                  </>
                )}
              </svg>
            </div>

            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: i === 2 ? 38 : 44,
                fontWeight: 900,
                color: lens.color,
                marginBottom: 10,
                lineHeight: 1.1,
              }}
            >
              {lens.labelHi}
            </div>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 30,
                fontWeight: 700,
                color: '#1C1C1C',
                lineHeight: 1.3,
              }}
            >
              {lens.note}
            </div>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 24,
                fontWeight: 600,
                color: '#9E9E9E',
                marginTop: 4,
              }}
            >
              {lens.note2}
            </div>
          </div>
        ))}
      </div>

      {/* Bottom: Manufacturing sectors */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: 80,
          right: 80,
          opacity: t2,
          transform: `translateY(${(1 - t2) * 16}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 40,
            fontWeight: 800,
            color: '#9E9E9E',
            marginBottom: 16,
          }}
        >
          भारत के भविष्य के sectors → Manufacturing Hub 2035
        </div>
        <div style={{ display: 'flex', gap: 20 }}>
          {['Semiconductors', 'Defence', 'EV/Green Energy', 'Pharma', 'Railways'].map((s, i) => (
            <div
              key={i}
              style={{
                backgroundColor: i === 0 || i === 1 ? '#1C1C1C' : '#F5F5F5',
                border: `2px solid ${i === 0 || i === 1 ? '#e9d700' : '#E0E0E0'}`,
                borderRadius: 8,
                padding: '10px 22px',
                fontFamily: 'Inter, sans-serif',
                fontSize: 24,
                fontWeight: 700,
                color: i === 0 || i === 1 ? '#e9d700' : '#1C1C1C',
                opacity: sp(60 + i * 8),
                transform: `translateY(${(1 - sp(60 + i * 8)) * 14}px)`,
              }}
            >
              {s}
            </div>
          ))}
        </div>
      </div>

      {/* Stick figure */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          bottom: 70,
          right: 150,
          width: 90,
          height: 140,
          opacity: sp(50),
          transform: `translateY(${(1 - sp(50)) * 40}px)`,
        }}
      >
        <svg
          width="90"
          height="140"
          viewBox="0 0 90 140"
          style={{ transform: `translateY(${breathe}px)` }}
        >
          <circle cx="45" cy="22" r="26" fill="#e9d700" opacity="0.10" />
          <circle
            cx="45"
            cy="22"
            r="16"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="2.5"
            style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}
          />
          <g style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '45px 22px' }}>
            <circle cx="39" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="51" cy="21" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="44" y1="21" x2="46" y2="21" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="34" y1="21" x2="33" y2="19" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="56" y1="21" x2="57" y2="19" stroke="#e9d700" strokeWidth="1.5" />
          </g>
          <line x1="45" y1="38" x2="45" y2="90" stroke="#1C1C1C" strokeWidth="2.5" />
          <line x1="45" y1="55" x2="18" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="55" x2="72" y2="68" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="28" y2="125" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="45" y1="90" x2="62" y2="125" stroke="#1C1C1C" strokeWidth="2" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.07) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8Ch6;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene9Ch6',
  durationInSeconds: 25,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9Ch6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  const t1 = interpolate(frame, [8, 26], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t2 = interpolate(frame, [28, 48], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t3 = interpolate(frame, [52, 72], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t4 = interpolate(frame, [76, 96], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Eye open animation
  const eyeOpen = sp(20);

  // Market closed vs learning open
  const closedOp = interpolate(frame, [15, 35], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const openOp = interpolate(frame, [40, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 4 + 7) * 1920,
    y: seededRandom(i * 4 + 8) * 1080,
    r: seededRandom(i * 4 + 9) * 5 + 2,
    phase: seededRandom(i * 3 + 4) * Math.PI * 2,
    speed: seededRandom(i * 5 + 1) * 0.2 + 0.1,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FFFFFF',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, sans-serif',
      }}
    >
      {/* Hexagon texture */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 8 }, (_, row) =>
            Array.from({ length: 14 }, (_, col) => {
              const cx = col * 140 + (row % 2 === 0 ? 0 : 70);
              const cy = row * 110;
              const pts = Array.from({ length: 6 }, (_, k) => {
                const a = (k * 60 - 30) * (Math.PI / 180);
                return `${cx + 50 * Math.cos(a)},${cy + 50 * Math.sin(a)}`;
              }).join(' ');
              return <polygon key={`${row}-${col}`} points={pts} fill="none" stroke="#1C1C1C" strokeWidth="0.8" />;
            })
          )}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed + p.phase) * 10}
              r={p.r}
              fill={i % 2 === 0 ? '#e9d700' : '#1C1C1C'}
              opacity={0.06}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Left: Eye — seeing what others don't */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          left: 80,
          top: '50%',
          transform: `translateY(-50%) scale(${eyeOpen}) translateX(${(1 - eyeOpen) * -80}px)`,
          opacity: eyeOpen,
          width: 420,
          height: 420,
        }}
      >
        <svg width="420" height="420" viewBox="0 0 420 420">
          {/* Outer eye shape */}
          <path
            d="M30 210 Q210 40 390 210 Q210 380 30 210 Z"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="3"
          />
          {/* Iris */}
          <circle cx="210" cy="210" r="70" fill="none" stroke="#e9d700" strokeWidth="4" />
          {/* Pupil */}
          <circle cx="210" cy="210" r="32" fill="#1C1C1C" />
          {/* Shine */}
          <circle cx="224" cy="196" r="10" fill="#FFFFFF" opacity="0.9" />
          {/* Rays */}
          {Array.from({ length: 8 }, (_, i) => {
            const angle = (i * 45) * (Math.PI / 180);
            return (
              <line
                key={i}
                x1={210 + 80 * Math.cos(angle)}
                y1={210 + 80 * Math.sin(angle)}
                x2={210 + 105 * Math.cos(angle)}
                y2={210 + 105 * Math.sin(angle)}
                stroke="#e9d700"
                strokeWidth="2"
                opacity={0.5 + Math.sin(frame * 0.05 + i) * 0.2}
              />
            );
          })}
          {/* Label */}
          <text x="210" y="360" textAnchor="middle" fontFamily="Noto Sans Devanagari, sans-serif" fontSize="26" fontWeight="800" fill="#9E9E9E">
            वो नज़रिया जो
          </text>
          <text x="210" y="390" textAnchor="middle" fontFamily="Noto Sans Devanagari, sans-serif" fontSize="26" fontWeight="800" fill="#e9d700">
            बाकी नहीं देख पाते
          </text>
        </svg>
      </AbsoluteFill>

      {/* Right content */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          left: 580,
          top: 0,
          width: 1240,
          height: 1080,
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          paddingRight: 100,
        }}
      >
        {/* Closed market */}
        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            marginBottom: 20,
            opacity: closedOp,
            transform: `translateX(${(1 - closedOp) * -20}px)`,
          }}
        >
          <div
            style={{
              backgroundColor: '#F5F5F5',
              border: '2px solid #E0E0E0',
              borderRadius: 10,
              padding: '12px 24px',
              display: 'flex',
              alignItems: 'center',
              gap: 14,
            }}
          >
            <svg width="32" height="32" viewBox="0 0 32 32">
              <rect x="4" y="14" width="24" height="16" rx="3" fill="none" stroke="#9E9E9E" strokeWidth="2" />
              <path d="M10 14 V10 a6 6 0 0 1 12 0 V14" fill="none" stroke="#9E9E9E" strokeWidth="2" />
              <line x1="4" y1="14" x2="28" y2="28" stroke="#ff0000" strokeWidth="2" />
            </svg>
            <span
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 34,
                fontWeight: 800,
                color: '#9E9E9E',
                textDecoration: 'line-through',
                textDecorationColor: '#ff0000',
              }}
            >
              बाजार बंद हो सकता है
            </span>
          </div>
        </div>

        {/* But learning never stops */}
        <div
          style={{
            opacity: openOp,
            transform: `translateY(${(1 - openOp) * 20}px)`,
            marginBottom: 32,
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 100,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.05,
              letterSpacing: '-1.5px',
            }}
          >
            सीखना कभी
            <br />
            <span style={{ color: '#e9d700' }}>बंद नहीं</span>
            <br />
            <span style={{ fontSize: 76 }}>होना चाहिए</span>
          </div>
        </div>

        {/* The question */}
        <div
          style={{
            backgroundColor: '#1C1C1C',
            borderRadius: 14,
            padding: '24px 32px',
            opacity: t3,
            transform: `translateY(${(1 - t3) * 18}px)`,
            marginBottom: 24,
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 44,
              fontWeight: 900,
              color: '#e9d700',
              lineHeight: 1.3,
            }}
          >
            क्या आप तैयार हैं?
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 34,
              fontWeight: 700,
              color: '#F5F5F5',
              lineHeight: 1.4,
              marginTop: 8,
            }}
          >
            अपने portfolio को उस नजरिये से देखने के लिए
            <br />
            जो बाकी लोग नहीं देख पा रहे?
          </div>
        </div>

        {/* Answer prompt */}
        <div
          style={{
            opacity: t4,
            transform: `translateY(${(1 - t4) * 12}px)`,
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 38,
            fontWeight: 700,
            color: '#2D2D2D',
          }}
        >
          इस यात्रा को जारी रखते हैं —{' '}
          <span style={{ color: '#e9d700', fontWeight: 900 }}>साथ में मिल कर</span>
        </div>
      </AbsoluteFill>

      {/* Stick figure — larger, prominent */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          bottom: 70,
          right: 140,
          width: 100,
          height: 155,
          opacity: sp(55),
          transform: `translateY(${(1 - sp(55)) * 40}px)`,
        }}
      >
        <svg
          width="100"
          height="155"
          viewBox="0 0 100 155"
          style={{ transform: `translateY(${breathe}px)` }}
        >
          <circle cx="50" cy="24" r="30" fill="#e9d700" opacity="0.12" />
          <circle
            cx="50"
            cy="24"
            r="18"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="2.8"
            style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '50px 24px' }}
          />
          <g style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '50px 24px' }}>
            <circle cx="43" cy="23" r="5.5" fill="none" stroke="#e9d700" strokeWidth="2" />
            <circle cx="57" cy="23" r="5.5" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="48.5" y1="23" x2="51.5" y2="23" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="37.5" y1="23" x2="36" y2="21" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="62.5" y1="23" x2="64" y2="21" stroke="#e9d700" strokeWidth="1.8" />
          </g>
          {/* Wave/hand raised */}
          <line x1="50" y1="42" x2="50" y2="100" stroke="#1C1C1C" strokeWidth="2.8" />
          <line x1="50" y1="62" x2="22" y2="44" stroke="#1C1C1C" strokeWidth="2.2" />
          <line x1="50" y1="62" x2="78" y2="60" stroke="#1C1C1C" strokeWidth="2.2" />
          <line x1="50" y1="100" x2="32" y2="138" stroke="#1C1C1C" strokeWidth="2.2" />
          <line x1="50" y1="100" x2="68" y2="138" stroke="#1C1C1C" strokeWidth="2.2" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.07) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9Ch6;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene10Ch6',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10Ch6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Bell shake
  const bellShake = Math.sin(frame * 0.3) * (frame > 60 ? 6 : 0);

  // Subscribe button pulse
  const subPulse = 1 + Math.sin(frame * 0.12) * 0.04;

  const t1 = interpolate(frame, [5, 22], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t2 = interpolate(frame, [22, 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t3 = interpolate(frame, [42, 62], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t4 = interpolate(frame, [65, 82], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t5 = interpolate(frame, [84, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  // Celebration particles shooting up
  const celebParticles = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    startY: 1100,
    speed: seededRandom(i * 3 + 2) * 3 + 1.5,
    color: [
      '#e9d700',
      '#FFC107',
      '#1C1C1C',
      '#ff0000',
    ][Math.floor(seededRandom(i * 3 + 3) * 4)],
    r: seededRandom(i * 4 + 5) * 6 + 3,
    phase: seededRandom(i * 5 + 2) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FFFFFF',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, sans-serif',
      }}
    >
      {/* Clean radial gradient from center */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 50%, rgba(233,215,0,0.07) 0%, rgba(255,255,255,0) 70%)',
        }}
      />

      {/* Subtle grid */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 22 }, (_, i) => (
            <line key={`h${i}`} x1="0" y1={i * 52} x2="1920" y2={i * 52} stroke="#1C1C1C" strokeWidth="1" />
          ))}
          {Array.from({ length: 38 }, (_, i) => (
            <line key={`v${i}`} x1={i * 52} y1="0" x2={i * 52} y2="1080" stroke="#1C1C1C" strokeWidth="1" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Celebration particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          {celebParticles.map((p, i) => {
            const yPos = p.startY - frame * p.speed * 1.5;
            const xWobble = Math.sin(frame * 0.04 + p.phase) * 20;
            return (
              <circle
                key={i}
                cx={p.x + xWobble}
                cy={yPos}
                r={p.r}
                fill={p.color}
                opacity={Math.max(0, Math.min(0.5, (1080 - yPos) / 500))}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Geometric accent shapes */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          <circle
            cx={1750 + Math.sin(frame * 0.01) * 6}
            cy={150 + Math.cos(frame * 0.008) * 5}
            r="200"
            fill="none"
            stroke="#e9d700"
            strokeWidth="1.5"
            opacity="0.1"
          />
          <circle
            cx={170 + Math.sin(frame * 0.012) * 5}
            cy={900 + Math.cos(frame * 0.01) * 4}
            r="150"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="1"
            opacity="0.06"
          />
        </svg>
      </AbsoluteFill>

      {/* Center layout */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          paddingLeft: 100,
          paddingRight: 100,
        }}
      >
        {/* Tag line */}
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: '4px',
            textTransform: 'uppercase',
            marginBottom: 20,
            opacity: t1,
          }}
        >
          The Journey Continues
        </div>

        {/* Main logo / channel name */}
        <div
          style={{
            opacity: t2,
            transform: `scale(${0.8 + sp(22) * 0.2}) translateY(${(1 - t2) * 24}px)`,
            textAlign: 'center',
            marginBottom: 36,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 160,
              fontWeight: 900,
              color: '#1C1C1C',
              letterSpacing: '-4px',
              lineHeight: 1,
            }}
          >
            Pro<span style={{ color: '#e9d700' }}>Buffet</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 72,
              fontWeight: 700,
              color: '#2D2D2D',
              letterSpacing: '8px',
              marginTop: -8,
            }}
          >
            H Q
          </div>
        </div>

        {/* Tagline */}
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 54,
            fontWeight: 800,
            color: '#1C1C1C',
            textAlign: 'center',
            lineHeight: 1.3,
            marginBottom: 48,
            opacity: t3,
            transform: `translateY(${(1 - t3) * 18}px)`,
          }}
        >
          असली समझ जो कोई आसानी से नहीं बताता
        </div>

        {/* Subscribe + Bell row */}
        <div
          style={{
            display: 'flex',
            gap: 28,
            alignItems: 'center',
            opacity: t4,
            transform: `translateY(${(1 - t4) * 16}px) scale(${0.9 + sp(65) * 0.1})`,
            marginBottom: 36,
          }}
        >
          {/* Subscribe button */}
          <div
            style={{
              backgroundColor: '#ff0000',
              borderRadius: 12,
              padding: '20px 52px',
              fontFamily: 'Inter, sans-serif',
              fontSize: 44,
              fontWeight: 900,
              color: '#FFFFFF',
              letterSpacing: '0.5px',
              transform: `scale(${subPulse})`,
              boxShadow: '0 8px 40px rgba(255,0,0,0.25)',
              display: 'flex',
              alignItems: 'center',
              gap: 16,
            }}
          >
            {/* Subscribe icon */}
            <svg width="42" height="32" viewBox="0 0 42 32">
              <rect x="0" y="0" width="42" height="32" rx="8" fill="none" />
              <polygon points="16,6 16,26 34,16" fill="#FFFFFF" />
            </svg>
            SUBSCRIBE
          </div>

          {/* Bell */}
          <div
            style={{
              width: 90,
              height: 90,
              backgroundColor: '#e9d700',
              borderRadius: 18,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              transform: `rotate(${bellShake}deg)`,
              boxShadow: '0 6px 24px rgba(233,215,0,0.35)',
            }}
          >
            <svg width="50" height="50" viewBox="0 0 50 50">
              <path
                d="M25 5 C16 5 12 12 12 20 L12 32 L8 36 L42 36 L38 32 L38 20 C38 12 34 5 25 5Z"
                fill="#1C1C1C"
              />
              <circle cx="25" cy="43" r="4" fill="#1C1C1C" />
              <circle cx="30" cy="9" r="3" fill="#FFFFFF" opacity="0.6" />
            </svg>
          </div>
        </div>

        {/* साथ में मिल कर */}
        <div
          style={{
            opacity: t5,
            transform: `translateY(${(1 - t5) * 12}px)`,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 52,
              fontWeight: 900,
              color: '#1C1C1C',
            }}
          >
            साथ में मिल कर —{' '}
            <span style={{ color: '#e9d700' }}>इस यात्रा को जारी रखते हैं</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick figure — center bottom celebrating */}
      <AbsoluteFill
        style={{
          position: 'absolute',
          bottom: 40,
          right: 120,
          width: 110,
          height: 165,
          opacity: sp(60),
          transform: `translateY(${(1 - sp(60)) * 40}px)`,
        }}
      >
        <svg
          width="110"
          height="165"
          viewBox="0 0 110 165"
          style={{ transform: `translateY(${breathe}px)` }}
        >
          {/* Golden glow */}
          <circle cx="55" cy="26" r="34" fill="#e9d700" opacity="0.15" />
          {/* Head */}
          <circle
            cx="55"
            cy="26"
            r="20"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="3"
            style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '55px 26px' }}
          />
          {/* Glasses */}
          <g style={{ transform: `rotate(${headNod}deg)`, transformOrigin: '55px 26px' }}>
            <circle cx="47" cy="25" r="6" fill="none" stroke="#e9d700" strokeWidth="2" />
            <circle cx="63" cy="25" r="6" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="53" y1="25" x2="57" y2="25" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="41" y1="25" x2="40" y2="23" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="69" y1="25" x2="70" y2="23" stroke="#e9d700" strokeWidth="1.8" />
          </g>
          {/* Body */}
          <line x1="55" y1="46" x2="55" y2="108" stroke="#1C1C1C" strokeWidth="3" />
          {/* Arms raised in celebration */}
          <line
            x1="55"
            y1="68"
            x2={20 + Math.sin(frame * 0.08) * 4}
            y2={38 + Math.sin(frame * 0.08) * 3}
            stroke="#1C1C1C"
            strokeWidth="2.5"
          />
          <line
            x1="55"
            y1="68"
            x2={90 + Math.sin(frame * 0.08) * 4}
            y2={38 + Math.sin(frame * 0.08) * 3}
            stroke="#1C1C1C"
            strokeWidth="2.5"
          />
          {/* Legs */}
          <line x1="55" y1="108" x2="36" y2="148" stroke="#1C1C1C" strokeWidth="2.5" />
          <line x1="55" y1="108" x2="74" y2="148" stroke="#1C1C1C" strokeWidth="2.5" />
          {/* Star sparkles near hands */}
          <circle cx={18 + Math.sin(frame * 0.08) * 4} cy={35 + Math.sin(frame * 0.08) * 3} r="4" fill="#e9d700" opacity={0.6 + Math.sin(frame * 0.15) * 0.3} />
          <circle cx={92 + Math.sin(frame * 0.08) * 4} cy={35 + Math.sin(frame * 0.08) * 3} r="4" fill="#e9d700" opacity={0.6 + Math.sin(frame * 0.15 + 1) * 0.3} />
        </svg>
      </AbsoluteFill>

      {/* Bottom divider line */}
      <div
        style={{
          position: 'absolute',
          bottom: 75,
          left: '50%',
          transform: 'translateX(-50%)',
          width: interpolate(frame, [100, 130], [0, 400], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          height: 2,
          backgroundColor: '#e9d700',
          borderRadius: 1,
        }}
      />

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(28,28,28,0.08) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10Ch6;
```

