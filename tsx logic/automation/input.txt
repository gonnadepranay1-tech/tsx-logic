```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch1Scene01Hook',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch1Scene01Hook: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera — slow push-in with lateral drift
  const camScale = interpolate(frame, [0, durationInFrames], [1.1, 1.0], {
    extrapolateRight: 'clamp', easing: Easing.out(Easing.quad),
  });
  const camX = Math.sin(frame * 0.006) * 22;
  const camY = Math.cos(frame * 0.005) * 12;

  // Beats
  const markIn = sp(8, { damping: 10, stiffness: 90 });
  const b1 = sp(40);
  const b2 = sp(190);
  const b3 = sp(400);
  const b4 = sp(600, { damping: 9, stiffness: 80 });

  // Impact shake on the payoff stamp
  const shake = frame > 600 && frame < 626 ? Math.sin(frame * 2.2) * (626 - frame) * 0.45 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A77F60', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at ${48 + Math.sin(frame * 0.012) * 8}% ${42 + Math.cos(frame * 0.01) * 6}%, #C69A76 0%, #A77F60 48%, #7E5E45 100%)`,
        }}
      />

      {/* Layer 3 — Motion texture (halftone dot field) */}
      <AbsoluteFill
        style={{
          backgroundImage: 'radial-gradient(#000000 1.6px, transparent 1.6px)',
          backgroundSize: '26px 26px',
          backgroundPosition: `${frame * 0.35}px ${frame * 0.18}px`,
          opacity: 0.09,
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${camX}px, ${camY}px)` }}>
        {/* Layer 2 — Animated shape layers (slow drifting geometry) */}
        <div
          style={{
            position: 'absolute', left: -180, top: 120, width: 760, height: 760, borderRadius: '50%',
            border: '3px solid #000000', opacity: 0.06,
            transform: `rotate(${frame * 0.12}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute', right: -120, top: -100, width: 620, height: 620,
            background: '#66BB6A', opacity: 0.07,
            transform: `rotate(${18 + Math.sin(frame * 0.01) * 6}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute', left: 700, bottom: -260, width: 0, height: 0,
            borderLeft: '300px solid transparent', borderRight: '300px solid transparent',
            borderBottom: '480px solid #000000', opacity: 0.05,
            transform: `rotate(${-10 + frame * 0.05}deg)`,
          }}
        />

        {/* Punk collage — torn paper strips */}
        <div
          style={{
            position: 'absolute', left: 90, top: 640, width: 520, height: 26, background: '#000000',
            opacity: 0.5 * b1, transform: `rotate(-2.5deg) scaleX(${b1})`, transformOrigin: 'left center',
          }}
        />
        <div
          style={{
            position: 'absolute', right: 110, top: 250, width: 300, height: 18, background: '#66BB6A',
            opacity: 0.85 * b2, transform: `rotate(6deg) scaleX(${b2})`, transformOrigin: 'right center',
          }}
        />

        {/* Layer 4 — Floating particles */}
        {new Array(32).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 3);
          const r2 = seededRandom(i + 77);
          const size = 4 + r1 * 11;
          const x = r1 * 1920;
          const y = (r2 * 1080 + frame * (0.28 + r1 * 0.55)) % 1140 - 60;
          return (
            <div
              key={`particle-${i}`}
              style={{
                position: 'absolute', left: x, top: y, width: size, height: size,
                borderRadius: i % 4 === 0 ? '0%' : '50%',
                background: i % 3 === 0 ? '#66BB6A' : '#000000',
                opacity: 0.1 + r2 * 0.16,
                transform: `rotate(${frame * (0.4 + r1)}deg)`,
              }}
            />
          );
        })}

        {/* Layer 5 — Main content */}
        {/* Icon card — question mark cut-out */}
        <div
          style={{
            position: 'absolute', left: 150, top: 240,
            width: 210, height: 210, background: '#000000',
            transform: `rotate(${-6 + Math.sin(frame * 0.03) * 1.5}deg) scale(${markIn})`,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            boxShadow: '18px 18px 0px rgba(0,0,0,0.18)',
          }}
        >
          <svg width="120" height="120" viewBox="0 0 24 24" fill="none">
            <path
              d="M9 9a3 3 0 1 1 4.2 2.75C12.4 12.1 12 12.9 12 13.8V15"
              stroke="#66BB6A" strokeWidth="2.2" strokeLinecap="round"
            />
            <circle cx="12" cy="18.4" r="1.4" fill="#66BB6A" />
          </svg>
        </div>

        {/* Headline block */}
        <div style={{ position: 'absolute', left: 420, top: 250, width: 1320 }}>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 200, lineHeight: 0.95, color: '#000000',
              transform: `translateY(${(1 - b1) * 70}px)`, opacity: b1,
            }}
          >
            एक सवाल
          </div>
          <div
            style={{
              marginTop: 18,
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 68, fontWeight: 700, color: '#000000',
              opacity: b2, transform: `translateX(${(1 - b2) * -60}px)`,
            }}
          >
            बिल्कुल सीधा — बिना किसी लाग लपेट के
          </div>

          {/* Beat 3 — counter strip */}
          <div
            style={{
              marginTop: 46, display: 'flex', alignItems: 'center', gap: 22,
              opacity: b3, transform: `scale(${0.9 + b3 * 0.1})`, transformOrigin: 'left center',
            }}
          >
            <div style={{ background: '#66BB6A', padding: '14px 30px', transform: 'rotate(-1.5deg)' }}>
              <span style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 62, fontWeight: 700, color: '#000000' }}>
                5 साल
              </span>
            </div>
            <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 50, color: '#000000', fontWeight: 600 }}>
              में कितनी बार सुना?
            </span>
          </div>
        </div>

        {/* Beat 4 — payoff stamp */}
        <div
          style={{
            position: 'absolute', left: 430, top: 790,
            padding: '18px 44px', border: '6px solid #000000', background: '#A77F60',
            opacity: b4,
            transform: `rotate(${-3 + shake * 0.2}deg) scale(${b4}) translateX(${shake}px)`,
          }}
        >
          <span style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 76, color: '#000000' }}>
            आज ईमानदारी की बात
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 7 — Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.40) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Layer 8 — Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E', opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch1Scene01Hook;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch1Scene02EchoChamber',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const SOURCES = [
  { label: 'FINANCE PAGE', icon: 'page' },
  { label: 'REEL', icon: 'reel' },
  { label: 'PODCAST', icon: 'mic' },
  { label: 'BANK RM', icon: 'bank' },
  { label: 'चाचा जी', icon: 'chat' },
  { label: 'MUTUAL FUND ADS', icon: 'chart' },
];

const Icon: React.FC<{ type: string; color: string }> = ({ type, color }) => {
  const common = { stroke: color, strokeWidth: 2, fill: 'none', strokeLinecap: 'round' as const, strokeLinejoin: 'round' as const };
  return (
    <svg width="74" height="74" viewBox="0 0 24 24">
      {type === 'page' && (
        <>
          <rect x="4" y="3" width="16" height="18" rx="1.5" {...common} />
          <path d="M8 8h8M8 12h8M8 16h5" {...common} />
        </>
      )}
      {type === 'reel' && (
        <>
          <rect x="3" y="4" width="18" height="16" rx="2" {...common} />
          <path d="M10 9l5 3-5 3z" {...common} />
        </>
      )}
      {type === 'mic' && (
        <>
          <rect x="9" y="3" width="6" height="11" rx="3" {...common} />
          <path d="M5 11a7 7 0 0 0 14 0M12 18v3" {...common} />
        </>
      )}
      {type === 'bank' && (
        <>
          <path d="M3 9l9-5 9 5" {...common} />
          <path d="M5 9v9M10 9v9M14 9v9M19 9v9M3 20h18" {...common} />
        </>
      )}
      {type === 'chat' && (
        <>
          <path d="M4 5h16v11H9l-5 4z" {...common} />
          <path d="M8 10h8" {...common} />
        </>
      )}
      {type === 'chart' && (
        <>
          <path d="M4 20V6M4 20h16" {...common} />
          <path d="M8 16v-4M12 16V8M16 16v-6" {...common} />
        </>
      )}
    </svg>
  );
};

const Ch1Scene02EchoChamber: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera — tilt then settle
  const camRot = interpolate(frame, [0, 260], [-1.6, 0], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.09], { extrapolateRight: 'clamp' });

  const title = sp(15);
  const sub = sp(560);

  return (
    <AbsoluteFill style={{ backgroundColor: '#A77F60', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${125 + Math.sin(frame * 0.008) * 12}deg, #B98D6B 0%, #A77F60 40%, #86654B 100%)`,
        }}
      />

      {/* Layer 3 — Motion texture (grid) */}
      <AbsoluteFill
        style={{
          backgroundImage:
            'linear-gradient(#000000 1px, transparent 1px), linear-gradient(90deg, #000000 1px, transparent 1px)',
          backgroundSize: '90px 90px',
          backgroundPosition: `${-frame * 0.5}px ${frame * 0.3}px`,
          opacity: 0.07,
        }}
      />

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg) scale(${camScale})` }}>
        {/* Layer 2 — Animated shape layers */}
        <div
          style={{
            position: 'absolute', left: 1250, top: 520, width: 900, height: 900, borderRadius: '50%',
            background: '#000000', opacity: 0.05,
            transform: `scale(${1 + Math.sin(frame * 0.018) * 0.06})`,
          }}
        />
        <div
          style={{
            position: 'absolute', left: -220, top: 420, width: 700, height: 380,
            border: '4px solid #66BB6A', opacity: 0.12,
            transform: `rotate(${-12 + frame * 0.04}deg)`,
          }}
        />

        {/* Layer 4 — Floating particles */}
        {new Array(26).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 11);
          const r2 = seededRandom(i + 91);
          const x = (r1 * 1920 + frame * 0.4) % 1920;
          const y = r2 * 1080;
          return (
            <div
              key={`particle-${i}`}
              style={{
                position: 'absolute', left: x, top: y, width: 6 + r2 * 8, height: 2,
                background: i % 2 === 0 ? '#000000' : '#66BB6A',
                opacity: 0.18, transform: `rotate(${r1 * 60}deg)`,
              }}
            />
          );
        })}

        {/* Layer 5 — Main content: quote slab */}
        <div
          style={{
            position: 'absolute', left: 120, top: 110, width: 1180,
            background: '#000000', padding: '40px 52px',
            transform: `translateY(${(1 - title) * -60}px) rotate(-1deg)`, opacity: title,
            boxShadow: '20px 20px 0px rgba(0,0,0,0.15)',
          }}
        >
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 32, color: '#66BB6A', letterSpacing: 4 }}>
            THE ADVICE EVERYONE REPEATS
          </div>
          <div
            style={{
              marginTop: 14,
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 112, lineHeight: 1.05, color: '#F2E8DC',
            }}
          >
            "6 महीने का फंड बना लो,
            <br />
            <span style={{ color: '#66BB6A' }}>ज़िंदगी सेट है"</span>
          </div>
        </div>

        {/* Animation cards — echo sources popping in sequence */}
        {SOURCES.map((s, i) => {
          const delay = 140 + i * 55;
          const p = sp(delay, { damping: 11, stiffness: 110 });
          const col = i % 3;
          const row = Math.floor(i / 3);
          const bob = Math.sin((frame + i * 30) * 0.045) * 6;
          const tilt = (seededRandom(i + 5) - 0.5) * 6;
          return (
            <div
              key={`card-${i}`}
              style={{
                position: 'absolute',
                left: 190 + col * 520,
                top: 560 + row * 230,
                width: 440, height: 190,
                background: i % 2 === 0 ? '#F2E8DC' : '#66BB6A',
                border: '5px solid #000000',
                display: 'flex', alignItems: 'center', gap: 26, padding: '0 30px',
                opacity: p,
                transform: `scale(${p}) rotate(${tilt}deg) translateY(${bob}px)`,
                boxShadow: '12px 12px 0px #000000',
              }}
            >
              <Icon type={s.icon} color="#000000" />
              <span
                style={{
                  fontFamily: "'Noto Sans Devanagari', Inter, sans-serif",
                  fontSize: 46, fontWeight: 800, color: '#000000',
                }}
              >
                {s.label}
              </span>
            </div>
          );
        })}

        {/* Payoff line */}
        <div
          style={{
            position: 'absolute', left: 1380, top: 200, width: 460,
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 84, color: '#000000', textAlign: 'right',
            opacity: sub, transform: `translateX(${(1 - sub) * 60}px)`,
          }}
        >
          सब यही रट
          <br />
          लगाए बैठे हैं
        </div>
      </AbsoluteFill>

      {/* Layer 7 — Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.42) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Layer 8 — Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E', opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch1Scene02EchoChamber;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch1Scene03FormulaBreak',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch1Scene03FormulaBreak: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BREAK = 300;

  // Camera — orbit-ish sway plus impact zoom on break
  const camScale =
    interpolate(frame, [0, BREAK], [1.14, 1.0], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }) +
    (frame >= BREAK && frame < BREAK + 20 ? (1 - (frame - BREAK) / 20) * 0.05 : 0);
  const shake = frame >= BREAK && frame < BREAK + 26 ? Math.sin(frame * 2.6) * (BREAK + 26 - frame) * 0.9 : 0;

  const formulaIn = sp(20, { damping: 14, stiffness: 90 });
  const stamp2026 = sp(180, { damping: 9, stiffness: 80 });
  const afterLine = sp(430);
  const pitLine = sp(640);

  const shatter = interpolate(frame, [BREAK, BREAK + 90], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.quad),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A77F60', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(ellipse at 50% ${30 + shatter * 20}%, #CBA07C 0%, #A77F60 50%, #74563E 100%)`,
        }}
      />

      {/* Layer 3 — Motion texture (diagonal stripes) */}
      <AbsoluteFill
        style={{
          backgroundImage: 'repeating-linear-gradient(45deg, #000000 0 3px, transparent 3px 34px)',
          backgroundPosition: `${frame * 0.9}px 0px`,
          opacity: 0.07,
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${shake}px, ${shake * 0.4}px)` }}>
        {/* Layer 2 — Animated shape layers */}
        <div
          style={{
            position: 'absolute', left: 120, top: -160, width: 620, height: 620,
            border: '5px solid #000000', borderRadius: '50%', opacity: 0.06,
            transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute', right: 60, bottom: 40, width: 560, height: 560,
            background: '#66BB6A', opacity: 0.08,
            transform: `rotate(${25 + frame * 0.06}deg)`,
          }}
        />

        {/* Layer 4 — Floating particles */}
        {new Array(30).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 21);
          const r2 = seededRandom(i + 61);
          const y = (r2 * 1080 - frame * (0.4 + r1 * 0.7) + 1080) % 1080;
          return (
            <div
              key={`particle-${i}`}
              style={{
                position: 'absolute', left: r1 * 1920, top: y,
                width: 5 + r1 * 7, height: 5 + r1 * 7,
                background: i % 3 === 0 ? '#66BB6A' : '#000000',
                opacity: 0.14, transform: `rotate(${frame * 0.8 + i * 20}deg)`,
              }}
            />
          );
        })}

        {/* Layer 5 — Main content: the formula that breaks */}
        <div style={{ position: 'absolute', left: 0, top: 300, width: 1920, textAlign: 'center' }}>
          {new Array(6).fill(0).map((_, i) => {
            const chars = ['6', ' ', 'म', 'ह', 'ी', 'ने'];
            const dir = i % 2 === 0 ? -1 : 1;
            const fly = shatter * (140 + i * 60);
            return (
              <span
                key={`chunk-${i}`}
                style={{
                  display: 'inline-block',
                  fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                  fontSize: 210, color: '#000000', lineHeight: 1,
                  opacity: formulaIn * (1 - shatter * 0.85),
                  transform: `translate(${dir * fly}px, ${shatter * (i % 3) * 90 - shatter * 40}px) rotate(${dir * shatter * 32}deg) scale(${formulaIn})`,
                }}
              >
                {chars[i]}
              </span>
            );
          })}
          <div
            style={{
              marginTop: -20,
              fontFamily: "'JetBrains Mono', monospace", fontSize: 54, letterSpacing: 8,
              color: '#000000', opacity: formulaIn * (1 - shatter),
            }}
          >
            THE OLD FORMULA
          </div>
        </div>

        {/* 2026 stamp slamming in */}
        <div
          style={{
            position: 'absolute', left: 640, top: 130,
            background: '#66BB6A', border: '8px solid #000000', padding: '10px 46px',
            transform: `rotate(-7deg) scale(${1.6 - stamp2026 * 0.6})`, opacity: stamp2026,
          }}
        >
          <span style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 96, fontWeight: 800, color: '#000000' }}>
            2026
          </span>
        </div>

        {/* Broken-formula payoff */}
        <div
          style={{
            position: 'absolute', left: 200, top: 620, width: 1520,
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 118, color: '#000000', textAlign: 'center',
            opacity: afterLine, transform: `translateY(${(1 - afterLine) * 50}px)`,
          }}
        >
          फॉर्मूले की धज्जियां उड़ चुकी हैं
        </div>

        {/* Pit warning card with icon */}
        <div
          style={{
            position: 'absolute', left: 560, top: 820,
            display: 'flex', alignItems: 'center', gap: 24,
            background: '#000000', padding: '18px 38px',
            opacity: pitLine, transform: `scale(${pitLine}) rotate(1.5deg)`,
          }}
        >
          <svg width="62" height="62" viewBox="0 0 24 24" fill="none">
            <path d="M12 3l9 16H3z" stroke="#66BB6A" strokeWidth="2.2" strokeLinejoin="round" />
            <path d="M12 9v5" stroke="#66BB6A" strokeWidth="2.4" strokeLinecap="round" />
            <circle cx="12" cy="16.6" r="1.2" fill="#66BB6A" />
          </svg>
          <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 52, fontWeight: 700, color: '#F2E8DC' }}>
            पता तब चलेगा जब आप गड्ढे में गिरेंगे
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 7 — Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 40%, rgba(0,0,0,0.45) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Layer 8 — Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E', opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch1Scene03FormulaBreak;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch1Scene04Identity',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch1Scene04Identity: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera — slow pan left to right
  const camX = interpolate(frame, [0, durationInFrames], [40, -40], { extrapolateRight: 'clamp' });
  const camScale = 1.04 + Math.sin(frame * 0.01) * 0.02;

  const cardIn = sp(20, { damping: 13, stiffness: 100 });
  const nameIn = sp(70);
  const chip1 = sp(230);
  const chip2 = sp(290);
  const chip3 = sp(350);
  const truth = sp(560, { damping: 10, stiffness: 90 });

  const tickerX = -(frame * 2.6) % 1200;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A77F60', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${200 + Math.sin(frame * 0.007) * 15}deg, #8E6C51 0%, #A77F60 45%, #C49B77 100%)`,
        }}
      />

      {/* Layer 3 — Motion texture (concentric rings) */}
      <AbsoluteFill
        style={{
          backgroundImage: 'repeating-radial-gradient(circle at 22% 60%, transparent 0 46px, #000000 46px 48px)',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.015) * 0.04})`,
        }}
      />

      <AbsoluteFill style={{ transform: `translateX(${camX}px) scale(${camScale})` }}>
        {/* Layer 2 — Animated shape layers */}
        <div
          style={{
            position: 'absolute', right: -200, top: 150, width: 800, height: 800,
            border: '6px solid #66BB6A', opacity: 0.1, borderRadius: '50%',
            transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
          }}
        />
        <div
          style={{
            position: 'absolute', left: 60, bottom: -180, width: 520, height: 520,
            background: '#000000', opacity: 0.05,
            transform: `rotate(${-frame * 0.05}deg)`,
          }}
        />

        {/* Layer 4 — Floating particles */}
        {new Array(24).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 33);
          const r2 = seededRandom(i + 133);
          const y = (r2 * 1080 + frame * (0.3 + r1 * 0.4)) % 1080;
          return (
            <div
              key={`particle-${i}`}
              style={{
                position: 'absolute', left: r1 * 1920, top: y,
                width: 9, height: 9, borderRadius: '50%',
                border: `2px solid ${i % 2 === 0 ? '#000000' : '#66BB6A'}`,
                opacity: 0.2,
              }}
            />
          );
        })}

        {/* Layer 5 — Main content: identity card (collage style) */}
        <div
          style={{
            position: 'absolute', left: 140, top: 210, width: 900,
            background: '#F2E8DC', border: '6px solid #000000', padding: '46px 50px',
            transform: `rotate(-2deg) scale(${cardIn})`, opacity: cardIn,
            boxShadow: '22px 22px 0px rgba(0,0,0,0.22)',
          }}
        >
          {/* Tape strip on top of card */}
          <div
            style={{
              position: 'absolute', left: 330, top: -26, width: 240, height: 46,
              background: '#66BB6A', opacity: 0.9, transform: 'rotate(-4deg)',
            }}
          />
          <div style={{ display: 'flex', alignItems: 'center', gap: 24 }}>
            <svg width="86" height="86" viewBox="0 0 24 24" fill="none">
              <rect x="3" y="5" width="18" height="14" rx="2" stroke="#000000" strokeWidth="2" />
              <path d="M7 10h4M7 14h7" stroke="#66BB6A" strokeWidth="2.2" strokeLinecap="round" />
              <path d="M15 9.5l2.5 2.5L15 14.5" stroke="#000000" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 30, letterSpacing: 5, color: '#000000' }}>
              WHO IS TALKING
            </div>
          </div>

          <div
            style={{
              marginTop: 22,
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 128, color: '#000000', lineHeight: 1,
              opacity: nameIn, transform: `translateX(${(1 - nameIn) * -40}px)`,
            }}
          >
            मैं कुनाल
          </div>
          <div
            style={{
              marginTop: 10,
              fontFamily: 'Inter, Poppins, sans-serif', fontSize: 62, fontWeight: 800,
              color: '#3E7D42', opacity: nameIn,
            }}
          >
            ProBuffet HQ
          </div>
        </div>

        {/* Animation cards — experience chips */}
        {[
          { t: 'लोगों की गलतियां', p: chip1, top: 250 },
          { t: 'उनके आँसू', p: chip2, top: 400 },
          { t: '"काश पहले पता होता"', p: chip3, top: 550 },
        ].map((c, i) => (
          <div
            key={`chip-${i}`}
            style={{
              position: 'absolute', right: 150, top: c.top,
              background: i === 2 ? '#000000' : '#66BB6A',
              border: '5px solid #000000', padding: '20px 34px',
              opacity: c.p,
              transform: `translateX(${(1 - c.p) * 120}px) rotate(${i % 2 === 0 ? 2 : -2}deg) translateY(${Math.sin((frame + i * 40) * 0.05) * 5}px)`,
              boxShadow: '10px 10px 0px rgba(0,0,0,0.25)',
            }}
          >
            <span
              style={{
                fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 54, fontWeight: 800,
                color: i === 2 ? '#66BB6A' : '#000000',
              }}
            >
              {c.t}
            </span>
          </div>
        ))}

        {/* Payoff — three-beat truth line */}
        <div
          style={{
            position: 'absolute', left: 150, top: 760,
            display: 'flex', alignItems: 'center', gap: 26,
            opacity: truth, transform: `scale(${0.92 + truth * 0.08})`, transformOrigin: 'left center',
          }}
        >
          <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 62, fontWeight: 700, color: '#000000', textDecoration: 'line-through' }}>
            सेफ नहीं
          </span>
          <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 62, fontWeight: 700, color: '#000000', textDecoration: 'line-through' }}>
            वायरल नहीं
          </span>
          <span
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 96,
              color: '#000000', background: '#66BB6A', padding: '6px 30px',
              transform: `rotate(-1.5deg) scale(${1 + Math.sin(frame * 0.08) * 0.015})`, display: 'inline-block',
            }}
          >
            लेकिन सच
          </span>
        </div>
      </AbsoluteFill>

      {/* Ticker strip */}
      <div style={{ position: 'absolute', bottom: 0, left: 0, width: 1920, height: 74, background: '#000000', overflow: 'hidden' }}>
        <div
          style={{
            position: 'absolute', left: tickerX, top: 14, whiteSpace: 'nowrap',
            fontFamily: "'JetBrains Mono', monospace", fontSize: 34, color: '#66BB6A', letterSpacing: 6,
          }}
        >
          EMERGENCY FUND ILLUSION 2026 • EMERGENCY FUND ILLUSION 2026 • EMERGENCY FUND ILLUSION 2026 • EMERGENCY FUND ILLUSION 2026
        </div>
      </div>

      {/* Layer 7 — Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.40) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Layer 8 — Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 92, right: 45,
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E', opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch1Scene04Identity;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch1Scene05TheMath',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch1Scene05TheMath: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera — tilt down slightly across the scene
  const camY = interpolate(frame, [0, durationInFrames], [-26, 26], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08], { extrapolateRight: 'clamp' });

  const labelIn = sp(15);
  const monthly = Math.round(
    interpolate(frame, [50, 150], [0, 60000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })
  );
  const multIn = sp(210, { damping: 10, stiffness: 90 });
  const total = Math.round(
    interpolate(frame, [430, 540], [0, 360000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })
  );
  const totalIn = sp(420, { damping: 11, stiffness: 100 });
  const safeIn = sp(660);
  const slamShake = frame > 420 && frame < 444 ? Math.sin(frame * 2.4) * (444 - frame) * 0.5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A77F60', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(160deg, #B98D6B 0%, #A77F60 55%, #7C5C43 100%)`,
        }}
      />

      {/* Layer 3 — Motion texture (vertical bars) */}
      <AbsoluteFill
        style={{
          backgroundImage: 'repeating-linear-gradient(90deg, #000000 0 2px, transparent 2px 60px)',
          backgroundPosition: `${frame * 0.6}px 0`,
          opacity: 0.06,
        }}
      />

      <AbsoluteFill style={{ transform: `translateY(${camY}px) scale(${camScale})` }}>
        {/* Layer 2 — Animated shape layers */}
        <div
          style={{
            position: 'absolute', left: -140, top: 300, width: 640, height: 640, borderRadius: '50%',
            background: '#66BB6A', opacity: 0.07,
            transform: `scale(${1 + Math.sin(frame * 0.017) * 0.07})`,
          }}
        />
        <div
          style={{
            position: 'absolute', right: 80, top: 80, width: 460, height: 460,
            border: '5px solid #000000', opacity: 0.07,
            transform: `rotate(${frame * 0.07}deg)`,
          }}
        />

        {/* Layer 4 — Floating particles (coin dots) */}
        {new Array(28).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 41);
          const r2 = seededRandom(i + 141);
          const y = (r2 * 1080 - frame * (0.3 + r1 * 0.5) + 1080) % 1080;
          return (
            <div
              key={`particle-${i}`}
              style={{
                position: 'absolute', left: r1 * 1920, top: y,
                width: 10, height: 10, borderRadius: '50%',
                background: i % 4 === 0 ? '#66BB6A' : '#000000', opacity: 0.14,
              }}
            />
          );
        })}

        {/* Layer 5 — Main content: math strip */}
        <div
          style={{
            position: 'absolute', left: 130, top: 130,
            fontFamily: "'JetBrains Mono', monospace", fontSize: 34, letterSpacing: 6, color: '#000000',
            opacity: labelIn, transform: `translateY(${(1 - labelIn) * -30}px)`,
          }}
        >
          MONTHLY EXPENSE
        </div>

        {/* Monthly number card */}
        <div
          style={{
            position: 'absolute', left: 130, top: 190,
            background: '#F2E8DC', border: '6px solid #000000', padding: '20px 44px',
            transform: `rotate(-1deg) scale(${labelIn})`, opacity: labelIn,
            boxShadow: '16px 16px 0px rgba(0,0,0,0.22)',
          }}
        >
          <span style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 150, fontWeight: 800, color: '#000000' }}>
            ₹{monthly.toLocaleString('en-IN')}
          </span>
        </div>

        {/* Multiplier — six month blocks */}
        <div style={{ position: 'absolute', left: 130, top: 430, display: 'flex', gap: 18 }}>
          {new Array(6).fill(0).map((_, i) => {
            const p = sp(210 + i * 26, { damping: 11, stiffness: 120 });
            return (
              <div
                key={`month-${i}`}
                style={{
                  width: 150, height: 150, background: '#66BB6A', border: '5px solid #000000',
                  display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
                  opacity: p, transform: `scale(${p}) translateY(${Math.sin((frame + i * 25) * 0.06) * 5}px)`,
                }}
              >
                <svg width="46" height="46" viewBox="0 0 24 24" fill="none">
                  <rect x="3" y="5" width="18" height="16" rx="2" stroke="#000000" strokeWidth="2" />
                  <path d="M3 10h18M8 3v4M16 3v4" stroke="#000000" strokeWidth="2" strokeLinecap="round" />
                </svg>
                <span style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 40, fontWeight: 800, color: '#000000', marginTop: 6 }}>
                  M{i + 1}
                </span>
              </div>
            );
          })}
        </div>

        {/* Equals sign */}
        <div
          style={{
            position: 'absolute', left: 1160, top: 470,
            fontFamily: "'JetBrains Mono', monospace", fontSize: 130, color: '#000000',
            opacity: multIn, transform: `scale(${multIn})`,
          }}
        >
          =
        </div>

        {/* Total slab */}
        <div
          style={{
            position: 'absolute', left: 1290, top: 400,
            background: '#000000', padding: '26px 40px',
            opacity: totalIn, transform: `rotate(1.5deg) scale(${totalIn}) translateX(${slamShake}px)`,
          }}
        >
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 30, letterSpacing: 5, color: '#66BB6A' }}>
            EMERGENCY FUND
          </div>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 116, fontWeight: 800, color: '#F2E8DC' }}>
            ₹{total.toLocaleString('en-IN')}
          </div>
        </div>

        {/* Storage icons + false comfort line */}
        <div
          style={{
            position: 'absolute', left: 130, top: 700, display: 'flex', alignItems: 'center', gap: 30,
            opacity: safeIn, transform: `translateY(${(1 - safeIn) * 40}px)`,
          }}
        >
          <div style={{ background: '#F2E8DC', border: '5px solid #000000', padding: '16px 26px', display: 'flex', alignItems: 'center', gap: 16 }}>
            <svg width="52" height="52" viewBox="0 0 24 24" fill="none">
              <path d="M3 9l9-5 9 5" stroke="#000000" strokeWidth="2" strokeLinejoin="round" />
              <path d="M5 9v9M10 9v9M14 9v9M19 9v9M3 20h18" stroke="#000000" strokeWidth="2" strokeLinecap="round" />
            </svg>
            <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 46, fontWeight: 800, color: '#000000' }}>SAVINGS A/C</span>
          </div>
          <div style={{ background: '#66BB6A', border: '5px solid #000000', padding: '16px 26px', display: 'flex', alignItems: 'center', gap: 16 }}>
            <svg width="52" height="52" viewBox="0 0 24 24" fill="none">
              <rect x="3" y="4" width="18" height="16" rx="2" stroke="#000000" strokeWidth="2" />
              <path d="M7 12h10M7 16h6M7 8h4" stroke="#000000" strokeWidth="2" strokeLinecap="round" />
            </svg>
            <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 46, fontWeight: 800, color: '#000000' }}>FIXED DEPOSIT</span>
          </div>
        </div>

        <div
          style={{
            position: 'absolute', left: 130, top: 850,
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 104, color: '#000000',
            opacity: safeIn, transform: `translateX(${(1 - safeIn) * -50}px)`,
          }}
        >
          "अब मैं सुरक्षित हूँ"
        </div>
      </AbsoluteFill>

      {/* Layer 7 — Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 43%, rgba(0,0,0,0.42) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Layer 8 — Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E', opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch1Scene05TheMath;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch1Scene06ThreeHits',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch1Scene06ThreeHits: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const HITS = [80, 200, 320];

  // Camera — micro shake on each impact
  const impactShake = HITS.reduce((acc, h) => {
    if (frame >= h && frame < h + 18) {
      return acc + Math.sin(frame * 2.8) * (h + 18 - frame) * 0.7;
    }
    return acc;
  }, 0);
  const camScale = 1.03 + Math.sin(frame * 0.012) * 0.02;

  const barP = interpolate(frame, [460, 700], [1, 0.36], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad),
  });
  const answer = sp(720, { damping: 10, stiffness: 90 });

  const CARDS = [
    { title: 'नौकरी गई', icon: 'job' },
    { title: 'मेडिकल इमरजेंसी', icon: 'med' },
    { title: 'अचानक बड़ा खर्च', icon: 'bolt' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#A77F60', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 50% 110%, #C69A76 0%, #A77F60 45%, #6F523C 100%)`,
        }}
      />

      {/* Layer 3 — Motion texture (crosshatch) */}
      <AbsoluteFill
        style={{
          backgroundImage:
            'repeating-linear-gradient(45deg, #000000 0 2px, transparent 2px 40px), repeating-linear-gradient(-45deg, #000000 0 2px, transparent 2px 40px)',
          backgroundPosition: `${frame * 0.4}px ${-frame * 0.4}px`,
          opacity: 0.055,
        }}
      />

      <AbsoluteFill style={{ transform: `translate(${impactShake}px, ${impactShake * 0.35}px) scale(${camScale})` }}>
        {/* Layer 2 — Animated shape layers */}
        <div
          style={{
            position: 'absolute', left: 640, top: -280, width: 700, height: 700,
            border: '6px solid #000000', opacity: 0.06,
            transform: `rotate(${45 + frame * 0.05}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute', right: -180, bottom: -180, width: 620, height: 620, borderRadius: '50%',
            background: '#66BB6A', opacity: 0.08,
            transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})`,
          }}
        />

        {/* Layer 4 — Floating particles */}
        {new Array(26).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 51);
          const r2 = seededRandom(i + 151);
          const y = (r2 * 1080 + frame * (0.35 + r1 * 0.5)) % 1080;
          return (
            <div
              key={`particle-${i}`}
              style={{
                position: 'absolute', left: r1 * 1920, top: y,
                width: 3, height: 16, background: i % 3 === 0 ? '#66BB6A' : '#000000',
                opacity: 0.16, transform: `rotate(${r1 * 40 - 20}deg)`,
              }}
            />
          );
        })}

        {/* Layer 5 — Main content: three impact cards */}
        {CARDS.map((c, i) => {
          const p = sp(HITS[i], { damping: 9, stiffness: 130 });
          const hover = Math.sin((frame + i * 40) * 0.05) * 7;
          return (
            <div
              key={`hit-${i}`}
              style={{
                position: 'absolute', left: 150 + i * 560, top: 170,
                width: 480, height: 380,
                background: i === 1 ? '#66BB6A' : '#F2E8DC',
                border: '6px solid #000000',
                display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: 22,
                opacity: p,
                transform: `scale(${p}) rotate(${(i - 1) * 2.5}deg) translateY(${hover}px)`,
                boxShadow: '16px 16px 0px rgba(0,0,0,0.24)',
              }}
            >
              <svg width="112" height="112" viewBox="0 0 24 24" fill="none">
                {c.icon === 'job' && (
                  <>
                    <rect x="3" y="7" width="18" height="13" rx="2" stroke="#000000" strokeWidth="2" />
                    <path d="M9 7V5h6v2" stroke="#000000" strokeWidth="2" strokeLinecap="round" />
                    <path d="M8 12l8 5M16 12l-8 5" stroke="#000000" strokeWidth="2.4" strokeLinecap="round" />
                  </>
                )}
                {c.icon === 'med' && (
                  <>
                    <rect x="3" y="6" width="18" height="14" rx="2" stroke="#000000" strokeWidth="2" />
                    <path d="M12 10v6M9 13h6" stroke="#000000" strokeWidth="2.6" strokeLinecap="round" />
                    <path d="M9 6V4h6v2" stroke="#000000" strokeWidth="2" strokeLinecap="round" />
                  </>
                )}
                {c.icon === 'bolt' && (
                  <>
                    <path d="M13 2L5 14h6l-1 8 8-12h-6z" stroke="#000000" strokeWidth="2" strokeLinejoin="round" />
                  </>
                )}
              </svg>
              <span
                style={{
                  fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 52, fontWeight: 800, color: '#000000',
                  textAlign: 'center', padding: '0 16px',
                }}
              >
                {c.title}
              </span>
            </div>
          );
        })}

        {/* Draining fund bar */}
        <div style={{ position: 'absolute', left: 150, top: 640, width: 1600 }}>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 32, letterSpacing: 5, color: '#000000', marginBottom: 12 }}>
            ₹3,60,000 — REAL SURVIVAL RUNWAY
          </div>
          <div style={{ width: '100%', height: 96, border: '6px solid #000000', background: '#F2E8DC', position: 'relative', overflow: 'hidden' }}>
            <div
              style={{
                position: 'absolute', left: 0, top: 0, bottom: 0, width: `${barP * 100}%`,
                background: '#66BB6A',
              }}
            />
            {new Array(6).fill(0).map((_, i) => (
              <div
                key={`tick-${i}`}
                style={{ position: 'absolute', left: `${((i + 1) / 6) * 100}%`, top: 0, bottom: 0, width: 4, background: '#000000', opacity: 0.5 }}
              />
            ))}
          </div>
        </div>

        {/* Payoff answer */}
        <div
          style={{
            position: 'absolute', left: 150, top: 810,
            display: 'flex', alignItems: 'center', gap: 26,
            opacity: answer, transform: `translateY(${(1 - answer) * 40}px)`,
          }}
        >
          <div style={{ background: '#000000', padding: '16px 34px', transform: 'rotate(-1.5deg)' }}>
            <span style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 84, color: '#66BB6A' }}>
              जवाब: बहुत कम cases में
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 7 — Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.44) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Layer 8 — Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E', opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch1Scene06ThreeHits;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch1Scene07RealInflation',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ITEMS = [
  { label: 'थाली', icon: 'plate' },
  { label: 'स्कूल फीस', icon: 'school' },
  { label: 'बिजली बिल', icon: 'bulb' },
  { label: 'पेट्रोल', icon: 'fuel' },
];

const Ch1Scene07RealInflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera — slow orbit sway
  const camRot = Math.sin(frame * 0.008) * 1.2;
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1.0], { extrapolateRight: 'clamp' });

  const head = sp(15);
  const govLine = sp(600);

  // Rising line path progress
  const lineP = interpolate(frame, [280, 620], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A77F60', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${300 + Math.sin(frame * 0.006) * 20}deg, #A77F60 0%, #BE9270 50%, #7A5B42 100%)`,
        }}
      />

      {/* Layer 3 — Motion texture (dotted grid) */}
      <AbsoluteFill
        style={{
          backgroundImage: 'radial-gradient(#66BB6A 2px, transparent 2px)',
          backgroundSize: '48px 48px',
          backgroundPosition: `${-frame * 0.3}px ${-frame * 0.25}px`,
          opacity: 0.12,
        }}
      />

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg) scale(${camScale})` }}>
        {/* Layer 2 — Animated shape layers */}
        <div
          style={{
            position: 'absolute', left: -160, bottom: -220, width: 720, height: 720,
            background: '#000000', opacity: 0.05, borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})`,
          }}
        />
        <div
          style={{
            position: 'absolute', right: 120, top: 60, width: 460, height: 460,
            border: '5px solid #66BB6A', opacity: 0.14,
            transform: `rotate(${frame * 0.09}deg)`,
          }}
        />

        {/* Layer 4 — Floating particles (rising) */}
        {new Array(30).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 61);
          const r2 = seededRandom(i + 161);
          const y = (r2 * 1080 - frame * (0.5 + r1 * 0.8) + 1080) % 1080;
          return (
            <div
              key={`particle-${i}`}
              style={{
                position: 'absolute', left: r1 * 1920, top: y,
                width: 7, height: 7, background: i % 3 === 0 ? '#66BB6A' : '#000000',
                opacity: 0.15, transform: `rotate(45deg)`,
              }}
            />
          );
        })}

        {/* Layer 5 — Main content */}
        <div
          style={{
            position: 'absolute', left: 130, top: 90,
            opacity: head, transform: `translateY(${(1 - head) * -40}px)`,
          }}
        >
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 34, letterSpacing: 6, color: '#000000' }}>
            HIT NUMBER ONE
          </div>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 150, color: '#000000', lineHeight: 1 }}>
            असली महँगाई
          </div>
        </div>

        {/* Animation cards — where inflation actually sits */}
        {ITEMS.map((it, i) => {
          const p = sp(120 + i * 70, { damping: 11, stiffness: 110 });
          const rise = interpolate(frame, [180 + i * 70, 520], [0, -26], {
            extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad),
          });
          return (
            <div
              key={`item-${i}`}
              style={{
                position: 'absolute', left: 130 + i * 420, top: 380,
                width: 350, height: 330,
                background: i % 2 === 0 ? '#F2E8DC' : '#66BB6A',
                border: '6px solid #000000',
                display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: 18,
                opacity: p,
                transform: `scale(${p}) translateY(${rise + Math.sin((frame + i * 35) * 0.05) * 5}px) rotate(${(i % 2 === 0 ? -1 : 1) * 2}deg)`,
                boxShadow: '14px 14px 0px rgba(0,0,0,0.22)',
              }}
            >
              <svg width="98" height="98" viewBox="0 0 24 24" fill="none">
                {it.icon === 'plate' && (
                  <>
                    <circle cx="12" cy="12" r="8.5" stroke="#000000" strokeWidth="2" />
                    <circle cx="12" cy="12" r="4" stroke="#000000" strokeWidth="2" />
                  </>
                )}
                {it.icon === 'school' && (
                  <>
                    <path d="M12 4l9 4-9 4-9-4z" stroke="#000000" strokeWidth="2" strokeLinejoin="round" />
                    <path d="M7 10v5c0 1.5 2.2 3 5 3s5-1.5 5-3v-5" stroke="#000000" strokeWidth="2" strokeLinecap="round" />
                  </>
                )}
                {it.icon === 'bulb' && (
                  <>
                    <path d="M9 18h6M10 21h4" stroke="#000000" strokeWidth="2" strokeLinecap="round" />
                    <path d="M12 3a6 6 0 0 0-3.5 10.9V16h7v-2.1A6 6 0 0 0 12 3z" stroke="#000000" strokeWidth="2" strokeLinejoin="round" />
                  </>
                )}
                {it.icon === 'fuel' && (
                  <>
                    <rect x="4" y="4" width="10" height="16" rx="1.5" stroke="#000000" strokeWidth="2" />
                    <path d="M6 8h6" stroke="#000000" strokeWidth="2" strokeLinecap="round" />
                    <path d="M14 9h3a2 2 0 0 1 2 2v5a1.5 1.5 0 0 0 1.5 1.5" stroke="#000000" strokeWidth="2" strokeLinecap="round" />
                  </>
                )}
              </svg>
              <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 50, fontWeight: 800, color: '#000000' }}>
                {it.label}
              </span>
              {/* Micro rising arrow */}
              <svg width="40" height="40" viewBox="0 0 24 24" fill="none" style={{ transform: `translateY(${Math.sin((frame + i * 20) * 0.12) * 4}px)` }}>
                <path d="M12 19V5M12 5l-6 6M12 5l6 6" stroke="#000000" strokeWidth="2.6" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </div>
          );
        })}

        {/* Rising trend line across the cards */}
        <svg width="1920" height="1080" style={{ position: 'absolute', left: 0, top: 0, pointerEvents: 'none' }}>
          <path
            d="M180 760 L560 720 L960 660 L1380 580 L1720 470"
            stroke="#000000" strokeWidth="7" fill="none" strokeLinecap="round"
            strokeDasharray={2000}
            strokeDashoffset={2000 - lineP * 2000}
            opacity={0.75}
          />
        </svg>

        {/* Payoff line */}
        <div
          style={{
            position: 'absolute', left: 130, top: 830,
            background: '#000000', padding: '18px 38px',
            opacity: govLine, transform: `scale(${govLine}) rotate(-1deg)`, transformOrigin: 'left center',
          }}
        >
          <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 58, fontWeight: 700, color: '#F2E8DC' }}>
            सरकारी रिपोर्ट वाली नहीं — हर महीने चुपचाप बैठने वाली
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 7 — Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.42) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Layer 8 — Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E', opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch1Scene07RealInflation;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch1Scene08DataBars',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BARS = [
  { label: 'CPI HEADLINE', value: 3.93, delay: 90 },
  { label: 'FOOD (MAY 26)', value: 4.78, delay: 190 },
  { label: 'RURAL INDIA', value: 4.85, delay: 290 },
  { label: 'FOOD (FEB 26)', value: 7.2, delay: 400 },
];

const Ch1Scene08DataBars: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera — push in on the final bar
  const camScale = interpolate(frame, [0, 400, durationInFrames], [1.0, 1.02, 1.09], { extrapolateRight: 'clamp' });
  const camX = interpolate(frame, [400, durationInFrames], [0, -60], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const head = sp(10);
  const kicker = sp(560);
  const maxVal = 8;
  const chartH = 460;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A77F60', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(180deg, #C1946F 0%, #A77F60 55%, #7C5C43 100%)`,
        }}
      />

      {/* Layer 3 — Motion texture (horizontal rules) */}
      <AbsoluteFill
        style={{
          backgroundImage: 'repeating-linear-gradient(0deg, #000000 0 1.5px, transparent 1.5px 56px)',
          backgroundPosition: `0 ${frame * 0.4}px`,
          opacity: 0.07,
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX}px)` }}>
        {/* Layer 2 — Animated shape layers */}
        <div
          style={{
            position: 'absolute', right: -260, top: 140, width: 820, height: 820, borderRadius: '50%',
            border: '7px solid #000000', opacity: 0.05,
            transform: `scale(${1 + Math.sin(frame * 0.016) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute', left: 40, top: 620, width: 420, height: 420,
            background: '#66BB6A', opacity: 0.07,
            transform: `rotate(${-20 + frame * 0.05}deg)`,
          }}
        />

        {/* Layer 4 — Floating particles */}
        {new Array(24).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 71);
          const r2 = seededRandom(i + 171);
          const x = (r1 * 1920 + frame * 0.5) % 1920;
          return (
            <div
              key={`particle-${i}`}
              style={{
                position: 'absolute', left: x, top: r2 * 1080,
                width: 8, height: 8, borderRadius: '50%',
                border: '2px solid #000000', opacity: 0.16,
              }}
            />
          );
        })}

        {/* Layer 5 — Main content: headline */}
        <div style={{ position: 'absolute', left: 130, top: 80, opacity: head, transform: `translateY(${(1 - head) * -30}px)` }}>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 32, letterSpacing: 6, color: '#000000' }}>
            HEADLINE NUMBER vs AAPKI ZINDAGI KA NUMBER
          </div>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 120, color: '#000000', lineHeight: 1.05 }}>
            असली खेल यहीं है
          </div>
        </div>

        {/* Bar chart */}
        <div style={{ position: 'absolute', left: 180, top: 320, width: 1560, height: chartH }}>
          {/* Baseline */}
          <div style={{ position: 'absolute', left: 0, bottom: 0, width: '100%', height: 6, background: '#000000' }} />

          {BARS.map((b, i) => {
            const p = sp(b.delay, { damping: 13, stiffness: 90 });
            const h = (b.value / maxVal) * chartH * p;
            const isPeak = i === 3;
            const shown = (b.value * p).toFixed(2);
            return (
              <div key={`bar-${i}`} style={{ position: 'absolute', left: i * 380, bottom: 6, width: 300 }}>
                <div
                  style={{
                    height: h,
                    background: isPeak ? '#66BB6A' : '#F2E8DC',
                    border: '6px solid #000000',
                    boxShadow: '12px 0px 0px rgba(0,0,0,0.18)',
                    display: 'flex', alignItems: 'flex-start', justifyContent: 'center',
                  }}
                >
                  <span
                    style={{
                      marginTop: -96, fontFamily: "'JetBrains Mono', monospace",
                      fontSize: isPeak ? 92 : 68, fontWeight: 800, color: '#000000', opacity: p,
                      transform: `translateY(${isPeak ? Math.sin(frame * 0.1) * 4 : 0}px)`,
                    }}
                  >
                    {shown}%
                  </span>
                </div>
                <div
                  style={{
                    marginTop: 14, fontFamily: "'JetBrains Mono', monospace", fontSize: 30,
                    letterSpacing: 3, color: '#000000', opacity: p, textAlign: 'center',
                  }}
                >
                  {b.label}
                </div>
              </div>
            );
          })}
        </div>

        {/* Peak callout card */}
        <div
          style={{
            position: 'absolute', right: 130, top: 250,
            background: '#000000', padding: '16px 30px',
            opacity: sp(430), transform: `rotate(3deg) scale(${sp(430)})`,
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 14 }}>
            <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
              <path d="M12 19V5M12 5l-6 6M12 5l6 6" stroke="#66BB6A" strokeWidth="2.6" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 44, fontWeight: 800, color: '#66BB6A' }}>
              थाली की महँगाई
            </span>
          </div>
        </div>

        {/* Kicker */}
        <div
          style={{
            position: 'absolute', left: 180, top: 900,
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 62, fontWeight: 800, color: '#000000',
            opacity: kicker, transform: `translateX(${(1 - kicker) * -60}px)`,
          }}
        >
          हेडलाइन नंबर आरामदायक है — आपका नंबर नहीं
        </div>
      </AbsoluteFill>

      {/* Layer 7 — Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.40) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Layer 8 — Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E', opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch1Scene08DataBars;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch1Scene09SilentLeak',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch1Scene09SilentLeak: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera — drift down toward the leak
  const camY = interpolate(frame, [0, durationInFrames], [0, -50], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.07], { extrapolateRight: 'clamp' });

  const leftIn = sp(20);
  const rightIn = sp(110);
  const gapIn = sp(260, { damping: 10, stiffness: 90 });
  const stampIn = sp(560, { damping: 9, stiffness: 80 });
  const stampShake = frame > 560 && frame < 586 ? Math.sin(frame * 2.6) * (586 - frame) * 0.55 : 0;

  // Bucket water level falling
  const level = interpolate(frame, [300, 780], [0.88, 0.42], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A77F60', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 70% 30%, #C69A76 0%, #A77F60 50%, #6F523C 100%)`,
        }}
      />

      {/* Layer 3 — Motion texture (radial rays) */}
      <AbsoluteFill
        style={{
          backgroundImage:
            'repeating-conic-gradient(from 0deg at 72% 46%, #000000 0deg 1.2deg, transparent 1.2deg 9deg)',
          opacity: 0.05,
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      <AbsoluteFill style={{ transform: `translateY(${camY}px) scale(${camScale})` }}>
        {/* Layer 2 — Animated shape layers */}
        <div
          style={{
            position: 'absolute', left: -200, top: 200, width: 700, height: 700,
            border: '6px solid #66BB6A', opacity: 0.1,
            transform: `rotate(${frame * 0.06}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute', right: 200, bottom: -240, width: 560, height: 560, borderRadius: '50%',
            background: '#000000', opacity: 0.05,
            transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />

        {/* Layer 4 — Floating particles (drip trail) */}
        {new Array(22).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 81);
          const y = (frame * (2.4 + r1 * 1.6) + i * 55) % 620;
          return (
            <div
              key={`drip-${i}`}
              style={{
                position: 'absolute', left: 500 + r1 * 60, top: 620 + y,
                width: 8, height: 18, borderRadius: '50%',
                background: '#66BB6A', opacity: interpolate(y, [0, 620], [0.55, 0]),
              }}
            />
          );
        })}

        {/* Layer 5 — Main content: comparison */}
        <div
          style={{
            position: 'absolute', left: 120, top: 110,
            background: '#000000', padding: '22px 40px',
            opacity: leftIn, transform: `translateX(${(1 - leftIn) * -80}px) rotate(-1deg)`,
          }}
        >
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 30, letterSpacing: 5, color: '#66BB6A' }}>
            INFLATION
          </div>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 150, fontWeight: 800, color: '#F2E8DC', lineHeight: 1 }}>
            7.2%
          </div>
        </div>

        <div
          style={{
            position: 'absolute', right: 120, top: 110,
            background: '#F2E8DC', border: '6px solid #000000', padding: '22px 40px',
            opacity: rightIn, transform: `translateX(${(1 - rightIn) * 80}px) rotate(1.5deg)`,
            boxShadow: '14px 14px 0px rgba(0,0,0,0.22)',
          }}
        >
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 30, letterSpacing: 5, color: '#000000' }}>
            YOUR BANK A/C
          </div>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 150, fontWeight: 800, color: '#000000', lineHeight: 1 }}>
            3.5%
          </div>
        </div>

        {/* Gap bar between the two */}
        <div
          style={{
            position: 'absolute', left: 620, top: 250, width: 700, height: 60,
            border: '5px solid #000000', overflow: 'hidden', opacity: gapIn,
          }}
        >
          <div style={{ width: `${gapIn * 100}%`, height: '100%', background: '#66BB6A' }} />
        </div>
        <div
          style={{
            position: 'absolute', left: 620, top: 320, width: 700, textAlign: 'center',
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 46, fontWeight: 800, color: '#000000',
            opacity: gapIn,
          }}
        >
          हर साल का असली नुकसान
        </div>

        {/* Leaking bucket icon (SVG, animated level) */}
        <div style={{ position: 'absolute', left: 380, top: 430, opacity: sp(240), transform: `scale(${sp(240)})` }}>
          <svg width="380" height="360" viewBox="0 0 200 190" fill="none">
            <path d="M30 40h140l-18 130H48z" stroke="#000000" strokeWidth="7" strokeLinejoin="round" fill="#F2E8DC" />
            <rect
              x="42" y={170 - 130 * level} width="116" height={130 * level - 4}
              fill="#66BB6A" opacity="0.9"
            />
            <path d="M22 40h156" stroke="#000000" strokeWidth="9" strokeLinecap="round" />
            <circle cx="100" cy="150" r="9" fill="#A77F60" stroke="#000000" strokeWidth="5" />
          </svg>
        </div>

        {/* Explanation card */}
        <div
          style={{
            position: 'absolute', left: 840, top: 470, width: 900,
            background: '#66BB6A', border: '6px solid #000000', padding: '30px 38px',
            opacity: sp(380), transform: `translateY(${(1 - sp(380)) * 50}px) rotate(-1deg)`,
            boxShadow: '16px 16px 0px rgba(0,0,0,0.2)',
          }}
        >
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 54, fontWeight: 800, color: '#000000', lineHeight: 1.3 }}>
            आप अमीर नहीं — हर साल थोड़े-थोड़े ग़रीब हो रहे हैं
          </div>
          <div style={{ marginTop: 12, fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 42, color: '#000000' }}>
            बिना आपको एहसास हुए
          </div>
        </div>

        {/* Payoff stamp */}
        <div
          style={{
            position: 'absolute', left: 840, top: 730,
            border: '8px solid #000000', background: '#A77F60', padding: '14px 44px',
            opacity: stampIn,
            transform: `rotate(-3deg) scale(${stampIn}) translateX(${stampShake}px)`,
          }}
        >
          <span style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 110, color: '#000000' }}>
            SILENT LEAK
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 7 — Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.44) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Layer 8 — Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E', opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch1Scene09SilentLeak;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch1Scene10RajeshProfile',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CHIPS = [
  { k: 'CITY', v: 'पुणे', icon: 'pin' },
  { k: 'ROLE', v: 'IT — Mid Level Manager', icon: 'case' },
  { k: 'AGE', v: '31 साल', icon: 'clock' },
  { k: 'FAMILY', v: 'पत्नी वर्किंग + 3.5 साल का बच्चा', icon: 'home' },
];

const OUTGO = [
  { label: 'EMI', icon: 'card' },
  { label: 'INSURANCE', icon: 'shield' },
  { label: 'माँ-बाप का सपोर्ट', icon: 'hand' },
];

const Ch1Scene10RajeshProfile: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera — dossier drop, then slow pan right
  const camX = interpolate(frame, [120, durationInFrames], [0, -70], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, 120], [1.12, 1.03], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const fileIn = sp(10, { damping: 13, stiffness: 95 });
  const nameIn = sp(60);
  const outgoIn = sp(520, { damping: 10, stiffness: 95 });
  const amount = Math.round(
    interpolate(frame, [560, 660], [0, 80000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })
  );

  return (
    <AbsoluteFill style={{ backgroundColor: '#A77F60', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${140 + Math.sin(frame * 0.006) * 14}deg, #B98D6B 0%, #A77F60 50%, #79593F 100%)`,
        }}
      />

      {/* Layer 3 — Motion texture (paper noise blocks) */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {new Array(40).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 200);
          const r2 = seededRandom(i + 300);
          return (
            <div
              key={`noise-${i}`}
              style={{
                position: 'absolute',
                left: (r1 * 1920 + frame * 0.2) % 1920,
                top: r2 * 1080,
                width: 30 + r1 * 90, height: 6,
                background: '#000000',
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `translateX(${camX}px) scale(${camScale})` }}>
        {/* Layer 2 — Animated shape layers */}
        <div
          style={{
            position: 'absolute', right: -180, top: 100, width: 760, height: 760,
            background: '#66BB6A', opacity: 0.07, borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame * 0.018) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute', left: 60, bottom: -200, width: 560, height: 560,
            border: '5px solid #000000', opacity: 0.06,
            transform: `rotate(${frame * 0.05}deg)`,
          }}
        />

        {/* Layer 4 — Floating particles */}
        {new Array(24).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 91);
          const r2 = seededRandom(i + 191);
          const y = (r2 * 1080 + frame * (0.3 + r1 * 0.45)) % 1080;
          return (
            <div
              key={`particle-${i}`}
              style={{
                position: 'absolute', left: r1 * 1920, top: y,
                width: 6, height: 6, borderRadius: '50%',
                background: i % 3 === 0 ? '#66BB6A' : '#000000', opacity: 0.15,
              }}
            />
          );
        })}

        {/* Layer 5 — Main content: case file card */}
        <div
          style={{
            position: 'absolute', left: 110, top: 120, width: 1000,
            background: '#F2E8DC', border: '7px solid #000000', padding: '38px 46px',
            opacity: fileIn, transform: `rotate(-1.5deg) scale(${fileIn}) translateY(${(1 - fileIn) * -60}px)`,
            boxShadow: '24px 24px 0px rgba(0,0,0,0.22)',
          }}
        >
          {/* Tape strip */}
          <div style={{ position: 'absolute', right: -30, top: 40, width: 200, height: 44, background: '#66BB6A', transform: 'rotate(8deg)' }} />

          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 30, letterSpacing: 6, color: '#000000' }}>
            CASE FILE / 2026
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 168, color: '#000000', lineHeight: 1,
              opacity: nameIn, transform: `translateX(${(1 - nameIn) * -40}px)`,
            }}
          >
            राजेश
          </div>

          {/* Detail chips */}
          <div style={{ marginTop: 26, display: 'flex', flexDirection: 'column', gap: 16 }}>
            {CHIPS.map((c, i) => {
              const p = sp(150 + i * 60);
              return (
                <div
                  key={`chip-${i}`}
                  style={{
                    display: 'flex', alignItems: 'center', gap: 18,
                    opacity: p, transform: `translateX(${(1 - p) * -50}px)`,
                  }}
                >
                  <div style={{ background: '#000000', padding: 10, display: 'flex' }}>
                    <svg width="38" height="38" viewBox="0 0 24 24" fill="none">
                      {c.icon === 'pin' && (
                        <>
                          <path d="M12 21s7-6.2 7-11a7 7 0 1 0-14 0c0 4.8 7 11 7 11z" stroke="#66BB6A" strokeWidth="2" strokeLinejoin="round" />
                          <circle cx="12" cy="10" r="2.4" stroke="#66BB6A" strokeWidth="2" />
                        </>
                      )}
                      {c.icon === 'case' && (
                        <>
                          <rect x="3" y="7" width="18" height="13" rx="2" stroke="#66BB6A" strokeWidth="2" />
                          <path d="M9 7V5h6v2" stroke="#66BB6A" strokeWidth="2" strokeLinecap="round" />
                        </>
                      )}
                      {c.icon === 'clock' && (
                        <>
                          <circle cx="12" cy="12" r="8.5" stroke="#66BB6A" strokeWidth="2" />
                          <path d="M12 7v5l3.5 2" stroke="#66BB6A" strokeWidth="2" strokeLinecap="round" />
                        </>
                      )}
                      {c.icon === 'home' && (
                        <>
                          <path d="M4 11l8-6 8 6v9H4z" stroke="#66BB6A" strokeWidth="2" strokeLinejoin="round" />
                          <path d="M10 20v-5h4v5" stroke="#66BB6A" strokeWidth="2" strokeLinejoin="round" />
                        </>
                      )}
                    </svg>
                  </div>
                  <span style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 28, letterSpacing: 3, color: '#3E7D42' }}>
                    {c.k}
                  </span>
                  <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 46, fontWeight: 700, color: '#000000' }}>
                    {c.v}
                  </span>
                </div>
              );
            })}
          </div>
        </div>

        {/* Outgo stack on the right */}
        <div style={{ position: 'absolute', right: 130, top: 190, display: 'flex', flexDirection: 'column', gap: 20 }}>
          {OUTGO.map((o, i) => {
            const p = sp(330 + i * 60, { damping: 11, stiffness: 110 });
            return (
              <div
                key={`outgo-${i}`}
                style={{
                  width: 560, background: i === 1 ? '#66BB6A' : '#000000',
                  border: '5px solid #000000', padding: '18px 26px',
                  display: 'flex', alignItems: 'center', gap: 20,
                  opacity: p,
                  transform: `translateX(${(1 - p) * 100}px) rotate(${i % 2 === 0 ? 1.5 : -1.5}deg) translateY(${Math.sin((frame + i * 30) * 0.05) * 5}px)`,
                }}
              >
                <svg width="52" height="52" viewBox="0 0 24 24" fill="none">
                  {o.icon === 'card' && (
                    <>
                      <rect x="3" y="6" width="18" height="12" rx="2" stroke={i === 1 ? '#000000' : '#66BB6A'} strokeWidth="2" />
                      <path d="M3 10h18" stroke={i === 1 ? '#000000' : '#66BB6A'} strokeWidth="2" />
                    </>
                  )}
                  {o.icon === 'shield' && (
                    <path d="M12 3l7 3v6c0 4.2-3 7.6-7 9-4-1.4-7-4.8-7-9V6z" stroke="#000000" strokeWidth="2" strokeLinejoin="round" />
                  )}
                  {o.icon === 'hand' && (
                    <>
                      <path d="M6 12V7a1.6 1.6 0 0 1 3.2 0v4" stroke="#66BB6A" strokeWidth="2" strokeLinecap="round" />
                      <path d="M9.2 11V5.6a1.6 1.6 0 0 1 3.2 0V11" stroke="#66BB6A" strokeWidth="2" strokeLinecap="round" />
                      <path d="M12.4 11V7a1.6 1.6 0 0 1 3.2 0v6.5c0 3.6-2.4 6.5-5.6 6.5S6 17.6 6 14v-2" stroke="#66BB6A" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
                    </>
                  )}
                </svg>
                <span
                  style={{
                    fontFamily: "'Noto Sans Devanagari', Inter, sans-serif", fontSize: 44, fontWeight: 800,
                    color: i === 1 ? '#000000' : '#F2E8DC',
                  }}
                >
                  {o.label}
                </span>
              </div>
            );
          })}
        </div>

        {/* Monthly outgo payoff */}
        <div
          style={{
            position: 'absolute', right: 130, top: 560,
            background: '#F2E8DC', border: '7px solid #000000', padding: '24px 40px',
            opacity: outgoIn, transform: `scale(${outgoIn}) rotate(-2deg)`,
            boxShadow: '18px 18px 0px rgba(0,0,0,0.24)',
          }}
        >
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 30, letterSpacing: 5, color: '#000000' }}>
            MONTHLY OUTGO
          </div>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 132, fontWeight: 800, color: '#000000', lineHeight: 1.05 }}>
            ₹{amount.toLocaleString('en-IN')}
          </div>
        </div>

        {/* Bottom kicker strip */}
        <div
          style={{
            position: 'absolute', left: 110, bottom: 90, background: '#66BB6A', padding: '14px 34px',
            opacity: sp(700), transform: `rotate(-1deg) scale(${sp(700)})`, transformOrigin: 'left center',
          }}
        >
          <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 52, fontWeight: 800, color: '#000000' }}>
            अब देखिए इस फंड के साथ हुआ क्या
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 7 — Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.42) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Layer 8 — Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E', opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch1Scene10RajeshProfile;
```

