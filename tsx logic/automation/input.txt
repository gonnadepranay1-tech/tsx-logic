```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene1',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Knife slash reveal — blade cuts across screen
  const slashProgress = interpolate(frame, [0, 18], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Three segment reveals staggered
  const seg1 = sp(20);
  const seg2 = sp(35);
  const seg3 = sp(50);

  // Pulsing coin stack
  const coinPulse = Math.sin(frame * 0.12) * 4;

  // Background grid drift
  const gridOffset = frame * 0.6;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BACKGROUND === */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 20% 50%, #2D1A00 0%, #1C1C1C 55%, #0A0A1A 100%)',
        }} />
      </AbsoluteFill>

      {/* === MOVING GRID TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          <defs>
            <pattern id="grid6s1" x={gridOffset % 60} y={0} width="60" height="60" patternUnits="userSpaceOnUse">
              <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#e9d700" strokeWidth="0.8" />
            </pattern>
          </defs>
          <rect width="1920" height="1080" fill="url(#grid6s1)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYER — Large transparent circle === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          <circle
            cx="1700"
            cy="200"
            r="320"
            fill="none"
            stroke="#e9d700"
            strokeWidth="60"
            style={{ transform: `rotate(${frame * 0.08}deg)`, transformOrigin: '1700px 200px' }}
          />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYER — Triangle drift === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          <polygon
            points="150,900 350,650 550,900"
            fill="#FFC107"
            style={{ transform: `translateY(${Math.sin(frame * 0.03) * 15}px)` }}
          />
        </svg>
      </AbsoluteFill>

      {/* === SLASH REVEAL LINE === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <line
            x1={-100}
            y1={540}
            x2={slashProgress * 2100 - 100}
            y2={540}
            stroke="#e9d700"
            strokeWidth="3"
            opacity="0.6"
          />
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES === */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i * 7) * 1920;
        const py = seededRandom(i * 13) * 1080;
        const speed = seededRandom(i * 3) * 0.8 + 0.2;
        const size = seededRandom(i * 17) * 3 + 1;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: (py + frame * speed * 0.4) % 1080,
            width: size,
            height: size,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#e9d700' : '#FFC107',
            opacity: 0.25,
          }} />
        );
      })}

      {/* === MAIN HEADLINE === */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 0,
        right: 0,
        textAlign: 'center',
        transform: `translateY(${(1 - sp(5)) * -80}px)`,
        opacity: sp(5),
      }}>
        <div style={{
          fontFamily: 'Yantra One, sans-serif',
          fontSize: 72,
          fontWeight: 900,
          color: '#F5F5F5',
          letterSpacing: 2,
        }}>
          दूसरा कदम
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 28,
          color: '#e9d700',
          fontWeight: 700,
          letterSpacing: 6,
          marginTop: 4,
          textTransform: 'uppercase',
        }}>
          STEP 2 — DIVIDE YOUR MONEY
        </div>
      </div>

      {/* === CENTRAL COIN STACK SVG === */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: '50%',
        transform: `translateY(calc(-50% + ${coinPulse}px)) scale(${sp(10)})`,
        opacity: sp(10),
      }}>
        <svg width="200" height="260" viewBox="0 0 200 260">
          {/* Coin stack layers */}
          {[5, 4, 3, 2, 1, 0].map((i) => (
            <ellipse key={i}
              cx="100" cy={200 - i * 28} rx="80" ry="22"
              fill={i === 0 ? '#e9d700' : '#FFC107'}
              opacity={0.9 - i * 0.1}
              stroke="#1C1C1C"
              strokeWidth="1.5"
            />
          ))}
          {/* Glow */}
          <ellipse cx="100" cy="200" rx="80" ry="22" fill="#e9d700" opacity="0.3" />
        </svg>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 22,
          color: '#9E9E9E',
          textAlign: 'center',
          marginTop: 8,
          fontWeight: 600,
        }}>
          आपका पैसा
        </div>
      </div>

      {/* === THREE SEGMENTS === */}
      {[
        { label: 'हिस्सा १', sub: 'अगले 3 साल की जरूरत', color: '#e9d700', delay: seg1, icon: 'M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5' },
        { label: 'हिस्सा २', sub: '3 साल के बाद', color: '#FFC107', delay: seg2, icon: 'M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z' },
        { label: 'हिस्सा ३', sub: 'दीर्घकालिक भविष्य', color: '#ff0000', delay: seg3, icon: 'M13 7h8m0 0v8m0-8l-8 8-4-4-6 6' },
      ].map((seg, idx) => (
        <div key={idx} style={{
          position: 'absolute',
          left: 380 + idx * 460,
          top: '50%',
          transform: `translateY(-50%) translateX(${(1 - seg.delay) * 100}px)`,
          opacity: seg.delay,
          width: 380,
        }}>
          {/* Card */}
          <div style={{
            background: `linear-gradient(135deg, #2D2D2D 0%, #1C1C1C 100%)`,
            border: `3px solid ${seg.color}`,
            borderRadius: 20,
            padding: '36px 32px',
            boxShadow: `0 0 40px ${seg.color}22`,
          }}>
            {/* Number badge */}
            <div style={{
              width: 56,
              height: 56,
              borderRadius: '50%',
              backgroundColor: seg.color,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              marginBottom: 20,
            }}>
              <span style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 28,
                fontWeight: 900,
                color: '#1C1C1C',
              }}>{idx + 1}</span>
            </div>

            {/* SVG Icon */}
            <svg width="36" height="36" viewBox="0 0 24 24" fill="none"
              stroke={seg.color} strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"
              style={{ marginBottom: 16 }}>
              <path d={seg.icon} />
            </svg>

            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 40,
              fontWeight: 900,
              color: '#F5F5F5',
              marginBottom: 10,
            }}>{seg.label}</div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 26,
              color: '#9E9E9E',
              lineHeight: 1.5,
            }}>{seg.sub}</div>
          </div>

          {/* Arrow connector (not last) */}
          {idx < 2 && (
            <div style={{
              position: 'absolute',
              right: -52,
              top: '50%',
              transform: 'translateY(-50%)',
            }}>
              <svg width="40" height="40" viewBox="0 0 40 40">
                <path d="M5 20 H35 M22 8 L35 20 L22 32" stroke="#9E9E9E" strokeWidth="3"
                  fill="none" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </div>
          )}
        </div>
      ))}

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        right: 60,
        bottom: 100,
        opacity: sp(45),
        transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(45)})`,
      }}>
        <svg width="90" height="140" viewBox="0 0 90 140">
          {/* Glow */}
          <circle cx="45" cy="22" r="28" fill="#e9d700" opacity="0.12" />
          {/* Head */}
          <circle cx="45" cy="22" r="18" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          {/* Glasses */}
          <circle cx="38" cy="22" r="6" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <circle cx="52" cy="22" r="6" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="44" y1="22" x2="46" y2="22" stroke="#e9d700" strokeWidth="2" />
          <line x1="26" y1="22" x2="32" y2="22" stroke="#e9d700" strokeWidth="2" />
          <line x1="58" y1="22" x2="64" y2="22" stroke="#e9d700" strokeWidth="2" />
          {/* Body */}
          <line x1="45" y1="40" x2="45" y2="90" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms — spread wide like presenting */}
          <line x1="45" y1="55" x2="10" y2="70" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
            style={{ transform: `rotate(${Math.sin(frame * 0.04) * 4}deg)`, transformOrigin: '45px 55px' }} />
          <line x1="45" y1="55" x2="80" y2="70" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
            style={{ transform: `rotate(${-Math.sin(frame * 0.04) * 4}deg)`, transformOrigin: '45px 55px' }} />
          {/* Legs */}
          <line x1="45" y1="90" x2="28" y2="130" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="90" x2="62" y2="130" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.75) 100%)',
        }} />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Ch6Scene1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene2',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene2: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Calendar flip animation
  const calFlip = interpolate(frame, [10, 30], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.back(1.5)),
  });

  // Urgency pulse
  const urgPulse = 1 + Math.sin(frame * 0.15) * 0.04;

  // Items reveal
  const items = [
    { text: 'बेटे की शादी', icon: 'M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z', delay: 35 },
    { text: 'घर की मरम्मत', icon: 'M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6', delay: 50 },
    { text: 'चिकित्सा खर्च', icon: 'M4.5 12.75l6 6 9-13.5', delay: 65 },
    { text: 'दैनिक जीवन', icon: 'M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z', delay: 80 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === BACKGROUND — warm amber gradient === */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 75% 30%, #2A1800 0%, #1C1C1C 60%, #0D0D0D 100%)',
        }} />
      </AbsoluteFill>

      {/* === HORIZONTAL SCAN LINES TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 54 }).map((_, i) => (
            <line key={i} x1="0" y1={i * 20} x2="1920" y2={i * 20}
              stroke="#F5F5F5" strokeWidth="0.5" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080">
          {/* Large rectangle drifting */}
          <rect
            x="1400" y="50"
            width="400" height="300"
            rx="20"
            fill="none"
            stroke="#FFC107"
            strokeWidth="40"
            style={{ transform: `rotate(${frame * 0.05}deg)`, transformOrigin: '1600px 200px' }}
          />
          {/* Small circles */}
          <circle cx="200" cy="900" r="120" fill="#e9d700" opacity="0.5"
            style={{ transform: `scale(${0.95 + Math.sin(frame * 0.04) * 0.05})`, transformOrigin: '200px 900px' }} />
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES === */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i * 5) * 1920;
        const py = seededRandom(i * 11) * 1080;
        const spd = seededRandom(i * 7) * 0.5 + 0.1;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: (py - frame * spd * 0.3 + 1080) % 1080,
            width: seededRandom(i * 3) * 4 + 1,
            height: seededRandom(i * 3) * 4 + 1,
            borderRadius: '50%',
            backgroundColor: '#FFC107',
            opacity: 0.2,
          }} />
        );
      })}

      {/* === LEFT — CALENDAR VISUAL === */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: '50%',
        transform: `translateY(-50%) scale(${calFlip})`,
        opacity: calFlip,
      }}>
        <svg width="320" height="360" viewBox="0 0 320 360">
          {/* Calendar body */}
          <rect x="10" y="50" width="300" height="300" rx="16" fill="#2D2D2D" stroke="#e9d700" strokeWidth="3" />
          {/* Header */}
          <rect x="10" y="50" width="300" height="70" rx="16" fill="#e9d700" />
          <rect x="10" y="100" width="300" height="20" fill="#e9d700" />
          {/* Title */}
          <text x="160" y="95" textAnchor="middle" fill="#1C1C1C"
            fontFamily="Inter" fontWeight="900" fontSize="28">3 साल</text>
          {/* Rings */}
          <circle cx="90" cy="50" r="14" fill="#1C1C1C" />
          <circle cx="230" cy="50" r="14" fill="#1C1C1C" />
          {/* Grid dots */}
          {[0, 1, 2, 3, 4, 5, 6].map((col) =>
            [0, 1, 2, 3].map((row) => (
              <rect key={`${col}-${row}`}
                x={30 + col * 40} y={148 + row * 52}
                width="26" height="26" rx="4"
                fill={row === 0 && col < 3 ? '#e9d700' : '#3D3D3D'}
                opacity={row === 0 && col < 3 ? 0.9 : 0.6}
              />
            ))
          )}
          {/* Urgency X on far dates */}
          <text x="230" y="200" textAnchor="middle" fill="#ff0000" fontSize="20" fontWeight="900">✗</text>
          <text x="270" y="200" textAnchor="middle" fill="#ff0000" fontSize="20" fontWeight="900">✗</text>
        </svg>

        {/* Label */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 22,
          color: '#9E9E9E',
          textAlign: 'center',
          marginTop: 12,
          fontWeight: 600,
        }}>
          निकट भविष्य
        </div>
      </div>

      {/* === CENTER DIVIDER === */}
      <div style={{
        position: 'absolute',
        left: 460,
        top: 100,
        bottom: 100,
        width: 3,
        background: 'linear-gradient(to bottom, transparent, #e9d700, transparent)',
        opacity: sp(15) * 0.5,
      }} />

      {/* === RIGHT — HEADLINE + ITEMS === */}
      <div style={{
        position: 'absolute',
        left: 500,
        top: 120,
        right: 100,
      }}>
        {/* Headline */}
        <div style={{
          transform: `translateY(${(1 - sp(8)) * -50}px)`,
          opacity: sp(8),
          marginBottom: 40,
        }}>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 56,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.2,
          }}>
            पहला हिस्सा
          </div>
          <div style={{
            display: 'inline-block',
            background: '#e9d700',
            color: '#1C1C1C',
            fontFamily: 'Inter, sans-serif',
            fontSize: 24,
            fontWeight: 800,
            padding: '6px 20px',
            borderRadius: 8,
            marginTop: 10,
            letterSpacing: 2,
          }}>
            NEXT 3 YEARS
          </div>
        </div>

        {/* Urgency bar */}
        <div style={{
          display: 'flex',
          alignItems: 'center',
          gap: 16,
          marginBottom: 32,
          opacity: sp(20),
          transform: `translateX(${(1 - sp(20)) * -40}px)`,
        }}>
          <div style={{
            width: 8,
            height: 60,
            backgroundColor: '#ff0000',
            borderRadius: 4,
            transform: `scaleY(${urgPulse})`,
          }} />
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 34,
            color: '#F5F5F5',
            fontWeight: 700,
            lineHeight: 1.4,
          }}>
            ये पैसा सबसे पहले<br />
            <span style={{ color: '#e9d700' }}>सुरक्षित</span> रखो
          </div>
        </div>

        {/* Items list */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 18 }}>
          {items.map((item, idx) => (
            <div key={idx} style={{
              display: 'flex',
              alignItems: 'center',
              gap: 20,
              opacity: sp(item.delay),
              transform: `translateX(${(1 - sp(item.delay)) * 60}px)`,
            }}>
              <div style={{
                width: 48,
                height: 48,
                borderRadius: 12,
                backgroundColor: '#2D2D2D',
                border: '2px solid #e9d700',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                flexShrink: 0,
              }}>
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none"
                  stroke="#e9d700" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <path d={item.icon} />
                </svg>
              </div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 32,
                color: '#F5F5F5',
                fontWeight: 600,
              }}>
                {item.text}
              </div>
              {/* Check mark */}
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none"
                stroke="#e9d700" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round"
                style={{ marginLeft: 'auto', opacity: sp(item.delay + 15) }}>
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </div>
          ))}
        </div>
      </div>

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        right: 50,
        bottom: 80,
        opacity: sp(55),
        transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(55)})`,
      }}>
        <svg width="80" height="120" viewBox="0 0 80 120">
          <circle cx="40" cy="20" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="33" cy="20" r="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          <circle cx="47" cy="20" r="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="38" y1="20" x2="42" y2="20" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="23" y1="20" x2="28" y2="20" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="52" y1="20" x2="57" y2="20" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="40" y1="38" x2="40" y2="80" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="52" x2="15" y2="65" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round"
            style={{ transform: `rotate(${Math.sin(frame * 0.06) * 8}deg)`, transformOrigin: '40px 52px' }} />
          <line x1="40" y1="52" x2="65" y2="65" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round"
            style={{ transform: `rotate(${-Math.sin(frame * 0.06) * 8}deg)`, transformOrigin: '40px 52px' }} />
          <line x1="40" y1="80" x2="25" y2="115" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="80" x2="55" y2="115" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.8) 100%)',
        }} />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Ch6Scene2;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene3',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene3: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Lock unlock animation
  const lockOpen = interpolate(frame, [20, 45], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Shimmer sweep
  const shimmer = interpolate(frame, [0, durationInFrames], [-400, 2400], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const options = [
    {
      title: 'सरकारी बचत प्रमाणपत्र',
      subtitle: 'NSC / Post Office',
      tag: 'सबसे सुरक्षित',
      tagColor: '#e9d700',
      delay: 25,
      highlight: true,
    },
    {
      title: 'शॉर्ट-टर्म FD',
      subtitle: '1-3 साल की अवधि',
      tag: 'स्थिर ब्याज',
      tagColor: '#FFC107',
      delay: 45,
      highlight: false,
    },
    {
      title: 'कैश / सेविंग्स',
      subtitle: 'तुरंत उपलब्ध',
      tag: 'अधिकतम तरलता',
      tagColor: '#F5F5F5',
      delay: 65,
      highlight: false,
    },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === BACKGROUND — deep green-tinted dark === */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 30% 70%, #001A0A 0%, #1C1C1C 50%, #0A0A0A 100%)',
        }} />
      </AbsoluteFill>

      {/* === DIAMOND PATTERN TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          <defs>
            <pattern id="diamonds" x="0" y="0" width="80" height="80" patternUnits="userSpaceOnUse">
              <polygon points="40,0 80,40 40,80 0,40" fill="none" stroke="#e9d700" strokeWidth="0.8" />
            </pattern>
          </defs>
          <rect width="1920" height="1080" fill="url(#diamonds)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          <circle cx="960" cy="-100" r="400" fill="none" stroke="#e9d700" strokeWidth="80"
            style={{ transform: `scale(${1 + Math.sin(frame * 0.02) * 0.03})`, transformOrigin: '960px -100px' }} />
        </svg>
      </AbsoluteFill>

      {/* === SHIMMER OVERLAY === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute',
          top: 0, bottom: 0,
          left: shimmer,
          width: 200,
          background: 'linear-gradient(90deg, transparent, rgba(233,215,0,0.04), transparent)',
        }} />
      </AbsoluteFill>

      {/* === FLOATING PARTICLES === */}
      {Array.from({ length: 22 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 6) * 1920,
          top: (seededRandom(i * 9) * 1080 + frame * seededRandom(i * 4) * 0.3) % 1080,
          width: seededRandom(i * 2) * 3 + 1,
          height: seededRandom(i * 2) * 3 + 1,
          borderRadius: '50%',
          backgroundColor: i % 2 === 0 ? '#e9d700' : '#FFFFFF',
          opacity: 0.15,
        }} />
      ))}

      {/* === TOP HEADLINE === */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: sp(5),
        transform: `translateY(${(1 - sp(5)) * -40}px)`,
      }}>
        <div style={{
          fontFamily: 'Yantra One, sans-serif',
          fontSize: 64,
          fontWeight: 900,
          color: '#F5F5F5',
        }}>
          पहले हिस्से के लिए
        </div>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 22,
          color: '#e9d700',
          letterSpacing: 4,
          marginTop: 6,
          opacity: 0.8,
        }}>
          BUCKET 1 — SAFE & LIQUID OPTIONS
        </div>
      </div>

      {/* === LOCK ICON — unlocking === */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 180,
        transform: `translateX(-50%) scale(${sp(10)})`,
        opacity: sp(10),
      }}>
        <svg width="70" height="70" viewBox="0 0 70 70">
          {/* Shackle */}
          <path
            d={lockOpen > 0.5
              ? 'M20 28 Q10 15 35 15 Q55 15 50 28'
              : 'M20 28 Q20 10 35 10 Q50 10 50 28'}
            fill="none" stroke="#e9d700" strokeWidth="5" strokeLinecap="round"
            style={{ transition: 'd 0.3s' }}
          />
          {/* Body */}
          <rect x="10" y="28" width="50" height="36" rx="8" fill="#e9d700" />
          {/* Keyhole */}
          <circle cx="35" cy="44" r="6" fill="#1C1C1C" />
          <rect x="32" y="44" width="6" height="10" rx="2" fill="#1C1C1C" />
        </svg>
      </div>

      {/* === THREE OPTION CARDS === */}
      <div style={{
        position: 'absolute',
        left: 80,
        right: 80,
        top: 300,
        display: 'flex',
        gap: 40,
        justifyContent: 'center',
      }}>
        {options.map((opt, idx) => (
          <div key={idx} style={{
            flex: 1,
            opacity: sp(opt.delay),
            transform: `translateY(${(1 - sp(opt.delay)) * 80}px)`,
          }}>
            <div style={{
              background: opt.highlight
                ? 'linear-gradient(135deg, #2A2200 0%, #1C1C1C 100%)'
                : 'linear-gradient(135deg, #2D2D2D 0%, #1C1C1C 100%)',
              border: `3px solid ${opt.tagColor}`,
              borderRadius: 24,
              padding: '40px 36px',
              height: 320,
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'space-between',
              boxShadow: opt.highlight ? `0 0 60px ${opt.tagColor}33` : 'none',
              position: 'relative',
              overflow: 'hidden',
            }}>
              {/* Highlight glow sweep */}
              {opt.highlight && (
                <div style={{
                  position: 'absolute',
                  top: 0, left: 0, right: 0, height: 3,
                  background: `linear-gradient(90deg, transparent, ${opt.tagColor}, transparent)`,
                }} />
              )}

              {/* Tag badge */}
              <div style={{
                display: 'inline-block',
                backgroundColor: opt.tagColor,
                color: '#1C1C1C',
                fontFamily: 'Inter, sans-serif',
                fontSize: 16,
                fontWeight: 800,
                padding: '4px 14px',
                borderRadius: 20,
                alignSelf: 'flex-start',
                letterSpacing: 1,
              }}>
                {opt.tag}
              </div>

              {/* Shield icon */}
              <div style={{ textAlign: 'center', margin: '10px 0' }}>
                <svg width="56" height="56" viewBox="0 0 24 24" fill="none"
                  stroke={opt.tagColor} strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
                  <polyline points="9 12 11 14 15 10" />
                </svg>
              </div>

              {/* Title */}
              <div>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: 34,
                  fontWeight: 800,
                  color: '#F5F5F5',
                  lineHeight: 1.2,
                  marginBottom: 10,
                }}>
                  {opt.title}
                </div>
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 22,
                  color: '#9E9E9E',
                }}>
                  {opt.subtitle}
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* === BOTTOM TAGLINE === */}
      <div style={{
        position: 'absolute',
        bottom: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: sp(80),
        transform: `translateY(${(1 - sp(80)) * 30}px)`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 36,
          color: '#9E9E9E',
          fontWeight: 600,
        }}>
          ये वो चीज़ें हैं जो{' '}
          <span style={{ color: '#e9d700', fontWeight: 800 }}>बिना चिंता के</span>
          {' '}उपलब्ध हो सकें
        </div>
      </div>

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        right: 55,
        bottom: 85,
        opacity: sp(50),
        transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(50)})`,
      }}>
        <svg width="75" height="115" viewBox="0 0 75 115">
          <circle cx="37" cy="20" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="30" cy="20" r="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          <circle cx="44" cy="20" r="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="35" y1="20" x2="39" y2="20" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="20" y1="20" x2="25" y2="20" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="49" y1="20" x2="54" y2="20" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="37" y1="38" x2="37" y2="80" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          {/* Thumbs up arm */}
          <line x1="37" y1="52" x2="8" y2="62" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <circle cx="5" cy="60" r="4" fill="#e9d700" />
          <line x1="37" y1="52" x2="66" y2="42" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round"
            style={{ transform: `rotate(${Math.sin(frame * 0.06) * 5}deg)`, transformOrigin: '37px 52px' }} />
          <line x1="37" y1="80" x2="22" y2="112" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="80" x2="52" y2="112" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.85) 100%)',
        }} />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Ch6Scene3;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene4',
  durationInSeconds: 24,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene4: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Scatter animation — pieces fly in from random positions
  const scatterPieces = [
    { label: '20 लाख', sub: 'अनजान कंपनी के शेयर', x: 900, y: 200, delay: 20, angle: -25, color: '#ff0000' },
    { label: '15 लाख', sub: 'रिटायरमेंट प्लान', sub2: '10 साल बंद', x: 1350, y: 350, delay: 35, angle: 15, color: '#FFC107' },
    { label: '15 लाख', sub: 'बाकी इधर-उधर', x: 600, y: 620, delay: 50, angle: -10, color: '#9E9E9E' },
  ];

  // Red X pulse
  const xPulse = 1 + Math.sin(frame * 0.18) * 0.06;

  // Vijay nameplate reveal
  const nameReveal = interpolate(frame, [5, 25], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === BACKGROUND — dark crimson === */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 60% 40%, #1A0505 0%, #1C1C1C 55%, #0A0A0A 100%)',
        }} />
      </AbsoluteFill>

      {/* === NOISE TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.025 }}>
        <svg width="1920" height="1080">
          <defs>
            <filter id="noise6s4">
              <feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch" />
              <feColorMatrix type="saturate" values="0" />
            </filter>
          </defs>
          <rect width="1920" height="1080" filter="url(#noise6s4)" opacity="1" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          <circle cx="200" cy="300" r="250" fill="#ff0000"
            style={{ transform: `scale(${1 + Math.sin(frame * 0.025) * 0.04})`, transformOrigin: '200px 300px' }} />
          <rect x="1600" y="700" width="280" height="280" rx="10" fill="none"
            stroke="#9E9E9E" strokeWidth="40"
            style={{ transform: `rotate(${frame * 0.06}deg)`, transformOrigin: '1740px 840px' }} />
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES === */}
      {Array.from({ length: 20 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 8) * 1920,
          top: (seededRandom(i * 12) * 1080 + frame * 0.25) % 1080,
          width: seededRandom(i * 4) * 3 + 1,
          height: seededRandom(i * 4) * 3 + 1,
          borderRadius: '50%',
          backgroundColor: '#ff0000',
          opacity: 0.12,
        }} />
      ))}

      {/* === LEFT — VIJAY PROFILE === */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: '50%',
        transform: `translateY(-50%)`,
        opacity: nameReveal,
      }}>
        {/* Profile card */}
        <div style={{
          background: 'linear-gradient(135deg, #2D2D2D 0%, #1C1C1C 100%)',
          border: '2px solid #9E9E9E',
          borderRadius: 20,
          padding: '36px 40px',
          width: 340,
        }}>
          {/* Stick figure - confused pose */}
          <div style={{ textAlign: 'center', marginBottom: 20 }}>
            <svg width="100" height="150" viewBox="0 0 100 150">
              <circle cx="50" cy="28" r="28" fill="none" stroke="#F5F5F5" strokeWidth="3" />
              {/* Worried face */}
              <circle cx="42" cy="26" r="4" fill="#F5F5F5" />
              <circle cx="58" cy="26" r="4" fill="#F5F5F5" />
              {/* Frown */}
              <path d="M38 38 Q50 34 62 38" fill="none" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
              {/* Question marks around head */}
              <text x="14" y="18" fill="#ff0000" fontSize="20" fontWeight="900"
                style={{ transform: `rotate(${Math.sin(frame * 0.07) * 5}deg)`, transformOrigin: '14px 18px' }}>?</text>
              <text x="76" y="20" fill="#ff0000" fontSize="16" fontWeight="900"
                style={{ transform: `rotate(${-Math.sin(frame * 0.07) * 5}deg)`, transformOrigin: '76px 20px' }}>?</text>
              {/* Body */}
              <line x1="50" y1="56" x2="50" y2="108" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
              {/* Arms — confused shrug */}
              <line x1="50" y1="70" x2="20" y2="88" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
                style={{ transform: `rotate(${Math.sin(frame * 0.05) * 8}deg)`, transformOrigin: '50px 70px' }} />
              <line x1="50" y1="70" x2="80" y2="88" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
                style={{ transform: `rotate(${-Math.sin(frame * 0.05) * 8}deg)`, transformOrigin: '50px 70px' }} />
              <line x1="50" y1="108" x2="32" y2="145" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
              <line x1="50" y1="108" x2="68" y2="145" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
            </svg>
          </div>

          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 52,
            fontWeight: 900,
            color: '#F5F5F5',
            textAlign: 'center',
          }}>
            विजय
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            color: '#9E9E9E',
            textAlign: 'center',
            marginTop: 6,
          }}>
            72 साल
          </div>

          <div style={{
            marginTop: 24,
            padding: '14px 20px',
            background: '#1C1C1C',
            borderRadius: 12,
            border: '2px solid #e9d700',
            textAlign: 'center',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 36,
              fontWeight: 900,
              color: '#e9d700',
            }}>
              ₹50 लाख
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 20,
              color: '#9E9E9E',
              marginTop: 4,
            }}>
              कुल बचत
            </div>
          </div>
        </div>
      </div>

      {/* === CENTER — CHAOS LABEL === */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 100,
        transform: `translateX(-50%)`,
        opacity: sp(10),
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Yantra One, sans-serif',
          fontSize: 56,
          fontWeight: 900,
          color: '#ff0000',
          letterSpacing: 2,
          transform: `scale(${xPulse})`,
        }}>
          बिखरा हुआ पैसा
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 24,
          color: '#9E9E9E',
          letterSpacing: 4,
          marginTop: 6,
        }}>
          NO PLAN — SCATTERED INVESTMENTS
        </div>
      </div>

      {/* === SCATTER PIECES === */}
      {scatterPieces.map((piece, idx) => (
        <div key={idx} style={{
          position: 'absolute',
          left: piece.x,
          top: piece.y,
          transform: `rotate(${piece.angle}deg) scale(${sp(piece.delay)}) translateX(${(1 - sp(piece.delay)) * -100}px)`,
          opacity: sp(piece.delay),
        }}>
          <div style={{
            background: '#2D2D2D',
            border: `2px solid ${piece.color}`,
            borderRadius: 16,
            padding: '20px 28px',
            minWidth: 240,
            position: 'relative',
            boxShadow: `0 0 30px ${piece.color}33`,
          }}>
            {/* Red X overlay */}
            <div style={{
              position: 'absolute',
              top: -12, right: -12,
              width: 36, height: 36,
              borderRadius: '50%',
              backgroundColor: '#ff0000',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none"
                stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round">
                <line x1="18" y1="6" x2="6" y2="18" />
                <line x1="6" y1="6" x2="18" y2="18" />
              </svg>
            </div>

            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 32,
              fontWeight: 900,
              color: piece.color,
              marginBottom: 6,
            }}>
              {piece.label}
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 22,
              color: '#9E9E9E',
              lineHeight: 1.4,
            }}>
              {piece.sub}
              {piece.sub2 && <><br /><span style={{ color: '#ff0000', fontWeight: 700 }}>{piece.sub2}</span></>}
            </div>
          </div>
        </div>
      ))}

      {/* === BOTTOM QUOTE === */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 80,
        right: 80,
        opacity: sp(85),
        transform: `translateY(${(1 - sp(85)) * 30}px)`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 34,
          color: '#9E9E9E',
          fontStyle: 'italic',
        }}>
          "पैसा है लेकिन{' '}
          <span style={{ color: '#ff0000', fontWeight: 800 }}>योजना नहीं है</span>
          {' '}— निवेश है लेकिन{' '}
          <span style={{ color: '#ff0000', fontWeight: 800 }}>समझ नहीं है"</span>
        </div>
      </div>

      {/* === STICK FIGURE WATERMARK CORNER === */}
      <div style={{
        position: 'absolute',
        right: 60,
        bottom: 90,
        opacity: sp(55) * 0.5,
        transform: `translateY(${Math.sin(frame * 0.05) * 3}px)`,
      }}>
        <svg width="70" height="105" viewBox="0 0 70 105">
          <circle cx="35" cy="18" r="16" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="28" cy="17" r="4" fill="none" stroke="#e9d700" strokeWidth="1.8" />
          <circle cx="42" cy="17" r="4" fill="none" stroke="#e9d700" strokeWidth="1.8" />
          <line x1="32" y1="17" x2="38" y2="17" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="35" y1="34" x2="35" y2="72" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="35" y1="48" x2="12" y2="60" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="35" y1="48" x2="58" y2="60" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="35" y1="72" x2="20" y2="102" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="35" y1="72" x2="50" y2="102" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.8) 100%)',
        }} />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Ch6Scene4;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene5',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene5: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Income vs expense bars
  const incomeBar = interpolate(frame, [30, 65], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const expenseBar = interpolate(frame, [50, 85], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Counter animation — income
  const incomeCount = Math.floor(interpolate(frame, [30, 70], [0, 80000], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));
  const expenseCount = Math.floor(interpolate(frame, [50, 90], [0, 60000], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));
  const surplusCount = Math.max(0, incomeCount - expenseCount);

  const incomeSources = [
    { label: 'पेंशन', amount: '40,000', icon: 'M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z' },
    { label: 'PPF / किराया', amount: '25,000', icon: 'M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6' },
    { label: 'अन्य आय', amount: '15,000', icon: 'M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === BACKGROUND — deep blue-black === */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 50% 20%, #050A2A 0%, #1C1C1C 55%, #0A0A0A 100%)',
        }} />
      </AbsoluteFill>

      {/* === CIRCUIT PATTERN TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 12 }).map((_, i) => (
            <g key={i}>
              <line x1={i * 160} y1="0" x2={i * 160} y2="1080" stroke="#e9d700" strokeWidth="0.5" />
              <line x1="0" y1={i * 90} x2="1920" y2={i * 90} stroke="#e9d700" strokeWidth="0.5" />
            </g>
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          <circle cx="1800" cy="540" r="300" fill="none" stroke="#e9d700" strokeWidth="60"
            style={{ transform: `scale(${1 + Math.sin(frame * 0.03) * 0.04})`, transformOrigin: '1800px 540px' }} />
          <polygon points="100,900 250,700 400,900"
            fill="#2D2D2D" opacity="0.8"
            style={{ transform: `translateY(${Math.sin(frame * 0.035) * 12}px)` }} />
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES === */}
      {Array.from({ length: 16 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 7) * 1920,
          top: (seededRandom(i * 11) * 1080 + frame * seededRandom(i * 3) * 0.4) % 1080,
          width: 3, height: 3,
          borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: 0.2,
        }} />
      ))}

      {/* === STEP BADGE === */}
      <div style={{
        position: 'absolute',
        top: 50,
        left: 80,
        opacity: sp(5),
        transform: `translateX(${(1 - sp(5)) * -60}px)`,
      }}>
        <div style={{
          display: 'flex',
          alignItems: 'center',
          gap: 20,
        }}>
          <div style={{
            width: 70,
            height: 70,
            borderRadius: '50%',
            backgroundColor: '#e9d700',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
          }}>
            <span style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 36,
              fontWeight: 900,
              color: '#1C1C1C',
            }}>3</span>
          </div>
          <div>
            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 52,
              fontWeight: 900,
              color: '#F5F5F5',
            }}>
              तीसरा कदम
            </div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 22,
              color: '#e9d700',
              letterSpacing: 3,
            }}>
              INCOME vs EXPENSE AUDIT
            </div>
          </div>
        </div>
      </div>

      {/* === LEFT — INCOME SOURCES === */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: 200,
        width: 420,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 30,
          color: '#9E9E9E',
          fontWeight: 700,
          marginBottom: 24,
          opacity: sp(15),
        }}>
          आय के श्रोत
        </div>

        {incomeSources.map((src, idx) => (
          <div key={idx} style={{
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            marginBottom: 18,
            opacity: sp(20 + idx * 15),
            transform: `translateX(${(1 - sp(20 + idx * 15)) * -50}px)`,
          }}>
            <div style={{
              width: 44,
              height: 44,
              borderRadius: 10,
              border: '2px solid #e9d700',
              backgroundColor: '#2D2D2D',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              flexShrink: 0,
            }}>
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none"
                stroke="#e9d700" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                <path d={src.icon} />
              </svg>
            </div>
            <div style={{ flex: 1 }}>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 24,
                color: '#F5F5F5',
                fontWeight: 600,
              }}>
                {src.label}
              </div>
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 24,
              color: '#e9d700',
              fontWeight: 700,
            }}>
              ₹{src.amount}
            </div>
          </div>
        ))}

        {/* Total income */}
        <div style={{
          borderTop: '2px solid #e9d700',
          paddingTop: 16,
          display: 'flex',
          justifyContent: 'space-between',
          opacity: sp(65),
          marginTop: 8,
        }}>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 30,
            color: '#F5F5F5',
            fontWeight: 900,
          }}>
            कुल आय
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 36,
            color: '#e9d700',
            fontWeight: 900,
          }}>
            ₹{incomeCount.toLocaleString('en-IN')}
          </div>
        </div>
      </div>

      {/* === CENTER VISUAL — Bar comparison === */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 180,
        transform: 'translateX(-50%)',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: 20,
      }}>
        {/* Bar chart */}
        <div style={{
          display: 'flex',
          alignItems: 'flex-end',
          gap: 30,
          height: 300,
          opacity: sp(25),
        }}>
          {/* Income bar */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 10 }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 20,
              color: '#e9d700',
              fontWeight: 700,
            }}>
              ₹80k
            </div>
            <div style={{
              width: 80,
              height: 300 * incomeBar,
              backgroundColor: '#e9d700',
              borderRadius: '8px 8px 0 0',
              transition: 'height 0.1s',
              boxShadow: '0 0 20px #e9d70044',
            }} />
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 20,
              color: '#9E9E9E',
            }}>
              आय
            </div>
          </div>

          {/* vs */}
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            color: '#9E9E9E',
            fontWeight: 700,
            marginBottom: 40,
          }}>vs</div>

          {/* Expense bar */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 10 }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 20,
              color: '#FFC107',
              fontWeight: 700,
            }}>
              ₹60k
            </div>
            <div style={{
              width: 80,
              height: 300 * 0.75 * expenseBar,
              backgroundColor: '#FFC107',
              borderRadius: '8px 8px 0 0',
              boxShadow: '0 0 20px #FFC10744',
            }} />
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 20,
              color: '#9E9E9E',
            }}>
              खर्च
            </div>
          </div>
        </div>

        {/* Surplus display */}
        <div style={{
          opacity: sp(90),
          transform: `scale(${sp(90)})`,
          background: 'linear-gradient(135deg, #2A2200, #1C1C1C)',
          border: '3px solid #e9d700',
          borderRadius: 16,
          padding: '16px 32px',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 26,
            color: '#9E9E9E',
          }}>
            बची हुई बचत
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 44,
            fontWeight: 900,
            color: '#e9d700',
          }}>
            +₹{surplusCount.toLocaleString('en-IN')}
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 18,
            color: '#9E9E9E',
            letterSpacing: 2,
          }}>
            प्रति माह
          </div>
        </div>
      </div>

      {/* === RIGHT — 3 MONTH TRACKER === */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: 200,
        width: 360,
        opacity: sp(70),
        transform: `translateX(${(1 - sp(70)) * 80}px)`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 28,
          color: '#9E9E9E',
          fontWeight: 700,
          marginBottom: 20,
        }}>
          3 महीने की जांच
        </div>

        {['महीना 1', 'महीना 2', 'महीना 3'].map((m, idx) => (
          <div key={idx} style={{
            background: '#2D2D2D',
            borderRadius: 12,
            padding: '18px 24px',
            marginBottom: 14,
            border: '1px solid #3D3D3D',
            display: 'flex',
            alignItems: 'center',
            gap: 16,
            opacity: sp(75 + idx * 12),
          }}>
            <div style={{
              width: 36,
              height: 36,
              borderRadius: '50%',
              backgroundColor: '#e9d700',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              flexShrink: 0,
            }}>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none"
                stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round">
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 26,
              color: '#F5F5F5',
              fontWeight: 600,
            }}>
              {m}
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 20,
              color: '#9E9E9E',
              marginLeft: 'auto',
            }}>
              लिखो
            </div>
          </div>
        ))}

        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 24,
          color: '#e9d700',
          fontStyle: 'italic',
          marginTop: 16,
          lineHeight: 1.5,
          opacity: sp(105),
        }}>
          "हर पैसा कहां गया — लिखो, फिर गिनो"
        </div>
      </div>

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        right: 55,
        bottom: 80,
        opacity: sp(40),
        transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(40)})`,
      }}>
        <svg width="80" height="120" viewBox="0 0 80 120">
          <circle cx="40" cy="20" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="33" cy="20" r="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          <circle cx="47" cy="20" r="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="38" y1="20" x2="42" y2="20" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="23" y1="20" x2="28" y2="20" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="52" y1="20" x2="57" y2="20" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="40" y1="38" x2="40" y2="80" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          {/* Writing arm */}
          <line x1="40" y1="52" x2="18" y2="72" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round"
            style={{ transform: `rotate(${Math.sin(frame * 0.1) * 10}deg)`, transformOrigin: '40px 52px' }} />
          <line x1="40" y1="52" x2="68" y2="46" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="80" x2="25" y2="115" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="80" x2="55" y2="115" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.82) 100%)',
        }} />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Ch6Scene5;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene6',
  durationInSeconds: 22,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake when deficit revealed
  const shakeX = frame > 60 && frame < 75
    ? Math.sin(frame * 1.8) * 8 * interpolate(frame, [60, 75], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })
    : 0;
  const shakeY = frame > 60 && frame < 75
    ? Math.cos(frame * 2.2) * 5 * interpolate(frame, [60, 75], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })
    : 0;

  // Drain animation — meter going down
  const drainLevel = interpolate(frame, [70, 130], [1, 0.6], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Deficit counter
  const deficit = -5000;
  const deficitDisplay = Math.floor(interpolate(frame, [65, 95], [0, 5000], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));

  // Warning flash
  const warnFlash = frame > 60 ? (Math.floor(frame / 12) % 2 === 0 ? 1 : 0.3) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === BACKGROUND — warning red === */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 50% 50%, #1A0000 0%, #1C1C1C 50%, #0A0000 100%)',
        }} />
        {/* Red warning flash */}
        <div style={{
          position: 'absolute', inset: 0,
          backgroundColor: '#ff0000',
          opacity: frame > 58 && frame < 75
            ? interpolate(frame, [58, 62, 68, 75], [0, 0.12, 0.12, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })
            : 0,
        }} />
      </AbsoluteFill>

      {/* === DIAGONAL STRIPES TEXTURE — danger === */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080">
          <defs>
            <pattern id="stripes6s6" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
              <rect x="0" y="0" width="20" height="40" fill="#ff0000" />
            </pattern>
          </defs>
          <rect width="1920" height="1080" fill="url(#stripes6s6)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080" style={{ transform: `translate(${shakeX}px, ${shakeY}px)` }}>
          <circle cx="100" cy="100" r="200" fill="none" stroke="#ff0000" strokeWidth="50"
            style={{ transform: `scale(${1 + Math.sin(frame * 0.03) * 0.04})`, transformOrigin: '100px 100px' }} />
          <circle cx="1800" cy="900" r="150" fill="#ff0000" opacity="0.4"
            style={{ transform: `scale(${1 + Math.sin(frame * 0.04 + 1) * 0.05})`, transformOrigin: '1800px 900px' }} />
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES — red === */}
      {Array.from({ length: 18 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 9) * 1920,
          top: (seededRandom(i * 6) * 1080 + frame * 0.3) % 1080,
          width: 3, height: 3,
          borderRadius: '50%',
          backgroundColor: '#ff0000',
          opacity: 0.2,
        }} />
      ))}

      {/* === MAIN CONTENT (with shake) === */}
      <div style={{ transform: `translate(${shakeX}px, ${shakeY}px)` }}>

        {/* === HEADLINE === */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: sp(5),
        }}>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 60,
            fontWeight: 900,
            color: '#F5F5F5',
          }}>
            खतरे का संकेत
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            color: '#ff0000',
            letterSpacing: 4,
            marginTop: 6,
          }}>
            WARNING — DEFICIT SITUATION
          </div>
        </div>

        {/* === INCOME vs EXPENSE COMPARISON === */}
        <div style={{
          position: 'absolute',
          left: 100,
          top: 200,
          display: 'flex',
          gap: 80,
          alignItems: 'center',
        }}>
          {/* Income box */}
          <div style={{
            opacity: sp(15),
            transform: `translateY(${(1 - sp(15)) * -50}px)`,
          }}>
            <div style={{
              background: 'linear-gradient(135deg, #2A2200, #1C1C1C)',
              border: '3px solid #e9d700',
              borderRadius: 20,
              padding: '30px 40px',
              textAlign: 'center',
              minWidth: 280,
            }}>
              <svg width="40" height="40" viewBox="0 0 24 24" fill="none"
                stroke="#e9d700" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"
                style={{ marginBottom: 12 }}>
                <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
                <polyline points="17 6 23 6 23 12" />
              </svg>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 28,
                color: '#9E9E9E',
                marginBottom: 8,
              }}>
                कुल आय
              </div>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 52,
                fontWeight: 900,
                color: '#e9d700',
              }}>
                ₹80,000
              </div>
            </div>
          </div>

          {/* MINUS */}
          <div style={{
            opacity: sp(35),
            fontSize: 80,
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            color: '#ff0000',
          }}>
            −
          </div>

          {/* Expense box */}
          <div style={{
            opacity: sp(40),
            transform: `translateY(${(1 - sp(40)) * -50}px)`,
          }}>
            <div style={{
              background: 'linear-gradient(135deg, #1A0000, #1C1C1C)',
              border: '3px solid #ff0000',
              borderRadius: 20,
              padding: '30px 40px',
              textAlign: 'center',
              minWidth: 280,
              boxShadow: '0 0 40px #ff000033',
            }}>
              <svg width="40" height="40" viewBox="0 0 24 24" fill="none"
                stroke="#ff0000" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"
                style={{ marginBottom: 12 }}>
                <polyline points="23 18 13.5 8.5 8.5 13.5 1 6" />
                <polyline points="17 18 23 18 23 12" />
              </svg>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 28,
                color: '#9E9E9E',
                marginBottom: 8,
              }}>
                कुल खर्च
              </div>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 52,
                fontWeight: 900,
                color: '#ff0000',
              }}>
                ₹85,000
              </div>
            </div>
          </div>

          {/* EQUALS */}
          <div style={{
            opacity: sp(55),
            fontSize: 80,
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            color: '#9E9E9E',
          }}>
            =
          </div>

          {/* Result — DEFICIT */}
          <div style={{
            opacity: sp(60),
            transform: `scale(${sp(60, { stiffness: 200, damping: 8 })})`,
          }}>
            <div style={{
              background: 'linear-gradient(135deg, #1A0000, #0A0000)',
              border: `3px solid #ff0000`,
              borderRadius: 20,
              padding: '30px 40px',
              textAlign: 'center',
              minWidth: 300,
              boxShadow: `0 0 60px #ff000055`,
              opacity: warnFlash,
            }}>
              <svg width="40" height="40" viewBox="0 0 24 24" fill="none"
                stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"
                style={{ marginBottom: 12 }}>
                <path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z" />
                <line x1="12" y1="9" x2="12" y2="13" />
                <line x1="12" y1="17" x2="12.01" y2="17" />
              </svg>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 28,
                color: '#ff0000',
                marginBottom: 8,
                fontWeight: 700,
              }}>
                घाटा
              </div>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 52,
                fontWeight: 900,
                color: '#ff0000',
              }}>
                −₹{deficitDisplay.toLocaleString('en-IN')}
              </div>
            </div>
          </div>
        </div>

        {/* === DRAIN TANK VISUAL === */}
        <div style={{
          position: 'absolute',
          right: 120,
          top: 200,
          opacity: sp(70),
        }}>
          <svg width="160" height="340" viewBox="0 0 160 340">
            {/* Tank outline */}
            <rect x="20" y="20" width="120" height="280" rx="12"
              fill="none" stroke="#9E9E9E" strokeWidth="3" />
            {/* Water level — draining */}
            <rect x="23" y={20 + 277 * (1 - drainLevel)} width="114" height={277 * drainLevel}
              rx="10" fill="#ff0000" opacity="0.7" />
            {/* Level markers */}
            {[0.25, 0.5, 0.75].map((level, i) => (
              <line key={i} x1="20" y1={20 + 277 * (1 - level)} x2="140" y2={20 + 277 * (1 - level)}
                stroke="#F5F5F5" strokeWidth="1" opacity="0.3" strokeDasharray="4 4" />
            ))}
            {/* Drain arrow */}
            <path d="M80 300 L80 330 M68 318 L80 330 L92 318"
              stroke="#ff0000" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 22,
            color: '#ff0000',
            textAlign: 'center',
            fontWeight: 700,
          }}>
            निवेश सिमट रहा है
          </div>
        </div>

        {/* === BOTTOM MESSAGE === */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 80, right: 80,
          opacity: sp(105),
          transform: `translateY(${(1 - sp(105)) * 30}px)`,
        }}>
          <div style={{
            background: '#2D2D2D',
            border: '2px solid #ff0000',
            borderRadius: 16,
            padding: '24px 36px',
            display: 'flex',
            gap: 24,
            alignItems: 'center',
          }}>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none"
              stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round">
              <circle cx="12" cy="12" r="10" />
              <line x1="12" y1="8" x2="12" y2="12" />
              <line x1="12" y1="16" x2="12.01" y2="16" />
            </svg>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 30,
              color: '#F5F5F5',
              fontWeight: 700,
              lineHeight: 1.4,
            }}>
              इसलिए या तो{' '}
              <span style={{ color: '#e9d700' }}>आय बढ़ानी होगी</span>
              {' '}या{' '}
              <span style={{ color: '#e9d700' }}>खर्चे कम करने होंगे</span>
            </div>
          </div>
        </div>

      </div>

      {/* === STICK FIGURE — worried === */}
      <div style={{
        position: 'absolute',
        left: 60,
        bottom: 80,
        opacity: sp(45),
        transform: `translateY(${Math.sin(frame * 0.05) * 3}px) translate(${shakeX * 0.5}px, ${shakeY * 0.5}px) scale(${sp(45)})`,
      }}>
        <svg width="80" height="120" viewBox="0 0 80 120">
          <circle cx="40" cy="20" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="33" cy="18" r="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          <circle cx="47" cy="18" r="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="38" y1="18" x2="42" y2="18" stroke="#e9d700" strokeWidth="1.5" />
          {/* Worried mouth */}
          <path d="M33 30 Q40 26 47 30" fill="none" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
          <line x1="40" y1="38" x2="40" y2="80" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          {/* Head in hands arms */}
          <line x1="40" y1="52" x2="16" y2="42" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="52" x2="64" y2="42" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="80" x2="25" y2="115" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="80" x2="55" y2="115" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.88) 100%)',
        }} />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Ch6Scene6;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene7',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene7: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Fade out bad investments
  const badFade1 = interpolate(frame, [40, 65], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const badFade2 = interpolate(frame, [60, 80], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const badFade3 = interpolate(frame, [80, 100], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Scale down bad items
  const badScale1 = interpolate(frame, [40, 65], [1, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const badScale2 = interpolate(frame, [60, 80], [1, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const badScale3 = interpolate(frame, [80, 100], [1, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const badItems = [
    { label: 'ULIP पॉलिसी', sub: 'समझ नहीं आता', fade: badFade1, scale: badScale1 },
    { label: 'जटिल बॉंड', sub: 'बहुत अधिक कमीशन', fade: badFade2, scale: badScale2 },
    { label: 'अज्ञात कंपनी', sub: 'पैसा बंद है', fade: badFade3, scale: badScale3 },
  ];

  // Broom sweep progress
  const sweepProgress = interpolate(frame, [35, 105], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  const broomX = 200 + sweepProgress * 900;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === BACKGROUND — authoritative dark === */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 40% 60%, #0D1A0D 0%, #1C1C1C 55%, #0A0A0A 100%)',
        }} />
      </AbsoluteFill>

      {/* === HEXAGON TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080">
          <defs>
            <pattern id="hex6s7" x="0" y="0" width="100" height="86.6" patternUnits="userSpaceOnUse">
              <polygon points="50,0 100,25 100,75 50,100 0,75 0,25"
                fill="none" stroke="#e9d700" strokeWidth="0.6" />
            </pattern>
          </defs>
          <rect width="1920" height="1080" fill="url(#hex6s7)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          <circle cx="1700" cy="200" r="300" fill="none" stroke="#FFC107" strokeWidth="60"
            style={{ transform: `rotate(${frame * 0.04}deg)`, transformOrigin: '1700px 200px' }} />
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES — green tint === */}
      {Array.from({ length: 16 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 8) * 1920,
          top: (seededRandom(i * 11) * 1080 - frame * 0.3 + 1080) % 1080,
          width: 2, height: 2,
          borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: 0.2,
        }} />
      ))}

      {/* === HEADLINE === */}
      <div style={{
        position: 'absolute',
        top: 55,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(5),
        transform: `translateY(${(1 - sp(5)) * -40}px)`,
      }}>
        <div style={{
          fontFamily: 'Yantra One, sans-serif',
          fontSize: 60,
          fontWeight: 900,
          color: '#F5F5F5',
        }}>
          सफाई का समय
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          color: '#e9d700',
          letterSpacing: 4,
          marginTop: 6,
        }}>
          PORTFOLIO CLEAN-UP — STEP BY STEP
        </div>
      </div>

      {/* === BAD INVESTMENTS BEING SWEPT === */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: 200,
        right: 80,
      }}>
        {/* Label */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 32,
          color: '#ff0000',
          fontWeight: 700,
          marginBottom: 28,
          opacity: sp(10),
        }}>
          बाहर निकालो ↓
        </div>

        <div style={{ display: 'flex', gap: 40 }}>
          {badItems.map((item, idx) => (
            <div key={idx} style={{
              opacity: item.fade,
              transform: `scale(${item.scale}) translateY(${(1 - item.scale) * 40}px)`,
              flex: 1,
            }}>
              <div style={{
                background: '#2D2D2D',
                border: '2px solid #ff0000',
                borderRadius: 16,
                padding: '28px 28px',
                position: 'relative',
              }}>
                {/* Slash overlay */}
                <svg style={{ position: 'absolute', top: 0, left: 0, right: 0, bottom: 0 }}
                  width="100%" height="100%">
                  <line x1="0" y1="0" x2="100%" y2="100%"
                    stroke="#ff0000" strokeWidth="4" opacity={1 - item.fade} />
                  <line x1="100%" y1="0" x2="0" y2="100%"
                    stroke="#ff0000" strokeWidth="4" opacity={1 - item.fade} />
                </svg>

                <div style={{
                  fontFamily: 'Yantra One, sans-serif',
                  fontSize: 36,
                  fontWeight: 900,
                  color: '#F5F5F5',
                  marginBottom: 10,
                }}>
                  {item.label}
                </div>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: 26,
                  color: '#ff0000',
                }}>
                  {item.sub}
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* === BROOM SWEEP === */}
      <div style={{
        position: 'absolute',
        left: broomX,
        top: 380,
        opacity: interpolate(frame, [30, 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <svg width="80" height="120" viewBox="0 0 80 120">
          {/* Broom handle */}
          <line x1="40" y1="0" x2="15" y2="100" stroke="#FFC107" strokeWidth="6" strokeLinecap="round" />
          {/* Bristles */}
          {[-20, -10, 0, 10, 20].map((offset, i) => (
            <line key={i}
              x1={15 + offset} y1="100"
              x2={15 + offset * 1.5} y2="120"
              stroke="#e9d700" strokeWidth="3" strokeLinecap="round"
              style={{ transform: `rotate(${Math.sin(frame * 0.2 + i) * 5}deg)`, transformOrigin: `${15 + offset}px 100px` }}
            />
          ))}
        </svg>
      </div>

      {/* === RULES TO FOLLOW === */}
      <div style={{
        position: 'absolute',
        left: 80,
        bottom: 80,
        right: 80,
        display: 'flex',
        gap: 30,
      }}>
        {[
          { rule: '30% मुनाफा है?', action: 'अभी बेचो — लाभ लॉक करो', color: '#e9d700', delay: 110 },
          { rule: 'नुकसान में है?', action: 'क्या कभी ठीक होगा?', color: '#FFC107', delay: 125 },
          { rule: '10 साल नहीं है?', action: 'बाहर निकल जाओ', color: '#ff0000', delay: 140 },
        ].map((card, idx) => (
          <div key={idx} style={{
            flex: 1,
            opacity: sp(card.delay),
            transform: `translateY(${(1 - sp(card.delay)) * 50}px)`,
          }}>
            <div style={{
              background: '#2D2D2D',
              border: `2px solid ${card.color}`,
              borderRadius: 14,
              padding: '20px 24px',
              display: 'flex',
              alignItems: 'flex-start',
              gap: 16,
            }}>
              <div style={{
                width: 10,
                height: '100%',
                minHeight: 40,
                backgroundColor: card.color,
                borderRadius: 4,
                flexShrink: 0,
                alignSelf: 'stretch',
              }} />
              <div>
                <div style={{
                  fontFamily: 'Yantra One, sans-serif',
                  fontSize: 28,
                  fontWeight: 900,
                  color: card.color,
                  marginBottom: 8,
                }}>
                  {card.rule}
                </div>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: 22,
                  color: '#9E9E9E',
                }}>
                  {card.action}
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* === STICK FIGURE — confident === */}
      <div style={{
        position: 'absolute',
        right: 60,
        top: 200,
        opacity: sp(15),
        transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(15)})`,
      }}>
        <svg width="90" height="140" viewBox="0 0 90 140">
          <circle cx="45" cy="22" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <circle cx="38" cy="21" r="5.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <circle cx="52" cy="21" r="5.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="43" y1="21" x2="47" y2="21" stroke="#e9d700" strokeWidth="2" />
          {/* Smile */}
          <path d="M37 30 Q45 36 53 30" fill="none" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="45" y1="42" x2="45" y2="92" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Confident arms */}
          <line x1="45" y1="58" x2="10" y2="48" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="58" x2="80" y2="48" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="92" x2="28" y2="132" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="92" x2="62" y2="132" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.85) 100%)',
        }} />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Ch6Scene7;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene8',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene8: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Typewriter effect for "SIMPLE"
  const typewriterChars = 'SIMPLE'.split('');
  const typeProgress = Math.floor(interpolate(frame, [20, 50], [0, 6], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));

  // 3 lines writing animation
  const line1 = interpolate(frame, [60, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const line2 = interpolate(frame, [80, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const line3 = interpolate(frame, [100, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Breathing glow
  const glow = 0.5 + Math.sin(frame * 0.06) * 0.3;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === BACKGROUND — clean premium === */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 50% 50%, #1A1A00 0%, #1C1C1C 50%, #0A0A0A 100%)',
        }} />
      </AbsoluteFill>

      {/* === CONCENTRIC CIRCLES TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {[100, 200, 300, 400, 500, 600, 700].map((r) => (
            <circle key={r} cx="960" cy="540" r={r}
              fill="none" stroke="#e9d700" strokeWidth="0.8"
              style={{ transform: `scale(${1 + Math.sin(frame * 0.02 + r * 0.01) * 0.02})`, transformOrigin: '960px 540px' }}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          <rect x="50" y="50" width="200" height="200" rx="12"
            fill="none" stroke="#e9d700" strokeWidth="30"
            style={{ transform: `rotate(${frame * 0.04}deg)`, transformOrigin: '150px 150px' }} />
          <circle cx="1750" cy="850" r="180" fill="none" stroke="#FFC107" strokeWidth="40"
            style={{ transform: `scale(${1 + Math.sin(frame * 0.03) * 0.04})`, transformOrigin: '1750px 850px' }} />
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES === */}
      {Array.from({ length: 20 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 5) * 1920,
          top: (seededRandom(i * 9) * 1080 - frame * seededRandom(i * 4) * 0.4 + 1080) % 1080,
          width: seededRandom(i * 3) * 4 + 1,
          height: seededRandom(i * 3) * 4 + 1,
          borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: 0.15,
        }} />
      ))}

      {/* === HERO WORD "SIMPLE" typewriter === */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: 0, right: 0,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 180,
          fontWeight: 900,
          color: '#e9d700',
          letterSpacing: 20,
          lineHeight: 1,
          textShadow: `0 0 ${60 * glow}px #e9d70044`,
        }}>
          {typewriterChars.slice(0, typeProgress).join('')}
          {typeProgress < 6 && (
            <span style={{
              opacity: Math.floor(frame / 8) % 2 === 0 ? 1 : 0,
              color: '#F5F5F5',
            }}>|</span>
          )}
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 44,
          fontWeight: 700,
          color: '#F5F5F5',
          marginTop: -10,
          opacity: sp(55),
        }}>
          सरल निवेश योजना
        </div>
      </div>

      {/* === THREE LINES OF SIMPLICITY === */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 360,
        transform: 'translateX(-50%)',
        width: 900,
      }}>
        {[
          { text: 'खुद समझ सको', icon: 'M15 12a3 3 0 11-6 0 3 3 0 016 0z M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z', progress: line1, delay: 55 },
          { text: 'तीन लाइन में समझा सको', icon: 'M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z', progress: line2, delay: 75 },
          { text: 'विशेषज्ञ की जरूरत न हो', icon: 'M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z', progress: line3, delay: 95 },
        ].map((item, idx) => (
          <div key={idx} style={{
            display: 'flex',
            alignItems: 'center',
            gap: 28,
            marginBottom: 32,
            opacity: sp(item.delay),
          }}>
            {/* Number */}
            <div style={{
              width: 60,
              height: 60,
              borderRadius: '50%',
              backgroundColor: '#e9d700',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              flexShrink: 0,
            }}>
              <span style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 28,
                fontWeight: 900,
                color: '#1C1C1C',
              }}>{idx + 1}</span>
            </div>

            {/* Progress bar underline */}
            <div style={{ flex: 1, position: 'relative' }}>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 44,
                fontWeight: 700,
                color: '#F5F5F5',
                lineHeight: 1.3,
              }}>
                {item.text}
              </div>
              {/* Underline progress */}
              <div style={{
                position: 'absolute',
                bottom: -6,
                left: 0,
                height: 3,
                width: `${item.progress * 100}%`,
                background: 'linear-gradient(90deg, #e9d700, #FFC107)',
                borderRadius: 2,
                transition: 'width 0.1s',
              }} />
            </div>

            {/* Icon */}
            <svg width="44" height="44" viewBox="0 0 24 24" fill="none"
              stroke="#e9d700" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"
              style={{ flexShrink: 0, opacity: item.progress }}>
              <path d={item.icon} />
            </svg>
          </div>
        ))}
      </div>

      {/* === BOTTOM KEY PRINCIPLE === */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 80, right: 80,
        textAlign: 'center',
        opacity: sp(140),
        transform: `translateY(${(1 - sp(140)) * 30}px)`,
      }}>
        <div style={{
          fontFamily: 'Yantra One, sans-serif',
          fontSize: 38,
          fontWeight: 900,
          color: '#9E9E9E',
        }}>
          जितना{' '}
          <span style={{ color: '#e9d700', textShadow: `0 0 ${40 * glow}px #e9d70044` }}>सरल</span>
          {' '}— उतना{' '}
          <span style={{ color: '#e9d700', textShadow: `0 0 ${40 * glow}px #e9d70044` }}>शक्तिशाली</span>
        </div>
      </div>

      {/* === STICK FIGURE — teaching pose === */}
      <div style={{
        position: 'absolute',
        right: 80,
        bottom: 80,
        opacity: sp(15),
        transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(15)})`,
      }}>
        <svg width="90" height="140" viewBox="0 0 90 140">
          {/* Golden glow */}
          <circle cx="45" cy="22" r="32" fill="#e9d700" opacity={0.1 + glow * 0.05} />
          <circle cx="45" cy="22" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <circle cx="38" cy="21" r="5.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <circle cx="52" cy="21" r="5.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="43" y1="21" x2="47" y2="21" stroke="#e9d700" strokeWidth="2" />
          <line x1="24" y1="21" x2="32" y2="21" stroke="#e9d700" strokeWidth="2" />
          <line x1="58" y1="21" x2="66" y2="21" stroke="#e9d700" strokeWidth="2" />
          {/* Smile */}
          <path d="M37 30 Q45 36 53 30" fill="none" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="45" y1="42" x2="45" y2="92" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Pointing arm */}
          <line x1="45" y1="58" x2="5" y2="42" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
            style={{ transform: `rotate(${Math.sin(frame * 0.05) * 6}deg)`, transformOrigin: '45px 58px' }} />
          <line x1="45" y1="58" x2="80" y2="72" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="92" x2="28" y2="132" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="92" x2="62" y2="132" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.82) 100%)',
        }} />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Ch6Scene8;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene9',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene9: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pie chart animation
  const pieProgress = interpolate(frame, [25, 70], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const allocations = [
    { label: 'सेविंग्स', amount: '₹3 लाख', percent: 10, color: '#9E9E9E', delay: 40 },
    { label: 'NSC / FD', amount: '₹15 लाख', percent: 50, color: '#e9d700', delay: 55 },
    { label: 'Index Fund', amount: '₹9 लाख', percent: 30, color: '#FFC107', delay: 70 },
    { label: 'Blue Chip', amount: '₹3 लाख', percent: 10, color: '#F5F5F5', delay: 85 },
  ];

  // SVG pie chart
  const createPieSlice = (startAngle: number, endAngle: number, r: number, cx: number, cy: number) => {
    const start = (startAngle - 90) * Math.PI / 180;
    const end = (endAngle - 90) * Math.PI / 180;
    const x1 = cx + r * Math.cos(start);
    const y1 = cy + r * Math.sin(start);
    const x2 = cx + r * Math.cos(end);
    const y2 = cy + r * Math.sin(end);
    const largeArc = endAngle - startAngle > 180 ? 1 : 0;
    return `M ${cx} ${cy} L ${x1} ${y1} A ${r} ${r} 0 ${largeArc} 1 ${x2} ${y2} Z`;
  };

  let cumAngle = 0;
  const slices = allocations.map((alloc) => {
    const startAngle = cumAngle;
    const endAngle = cumAngle + 360 * alloc.percent / 100 * pieProgress;
    cumAngle += 360 * alloc.percent / 100;
    return { ...alloc, startAngle, endAngle: Math.min(endAngle, startAngle + 360 * alloc.percent / 100) };
  });

  // Peaceful shimmer
  const shimmer = (0.8 + Math.sin(frame * 0.04) * 0.2);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === BACKGROUND — warm golden === */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 35% 50%, #1A1400 0%, #1C1C1C 55%, #0A0A0A 100%)',
        }} />
      </AbsoluteFill>

      {/* === DOT PATTERN TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          <defs>
            <pattern id="dots6s9" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
              <circle cx="20" cy="20" r="2" fill="#e9d700" />
            </pattern>
          </defs>
          <rect width="1920" height="1080" fill="url(#dots6s9)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          <polygon points="1800,100 1920,400 1680,400"
            fill="#e9d700" opacity="0.6"
            style={{ transform: `translateY(${Math.sin(frame * 0.03) * 15}px)` }} />
          <circle cx="100" cy="800" r="200" fill="none" stroke="#FFC107" strokeWidth="40"
            style={{ transform: `scale(${1 + Math.sin(frame * 0.025) * 0.04})`, transformOrigin: '100px 800px' }} />
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES === */}
      {Array.from({ length: 18 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 7) * 1920,
          top: (seededRandom(i * 4) * 1080 - frame * 0.35 + 1080) % 1080,
          width: seededRandom(i * 2) * 3 + 1,
          height: seededRandom(i * 2) * 3 + 1,
          borderRadius: '50%',
          backgroundColor: '#FFC107',
          opacity: 0.18,
        }} />
      ))}

      {/* === LEFT — GEETA PROFILE === */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: '50%',
        transform: `translateY(-50%)`,
        opacity: sp(5),
        transform: `translateY(calc(-50% + ${(1 - sp(5)) * 60}px))`,
      }}>
        <div style={{
          background: 'linear-gradient(135deg, #2A2200, #1C1C1C)',
          border: '3px solid #e9d700',
          borderRadius: 24,
          padding: '40px',
          width: 320,
          textAlign: 'center',
          boxShadow: `0 0 60px #e9d70022`,
        }}>
          {/* Geeta stick figure — happy */}
          <svg width="100" height="150" viewBox="0 0 100 150" style={{ marginBottom: 16 }}>
            <circle cx="50" cy="30" r="24" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="42" cy="28" r="5.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <circle cx="58" cy="28" r="5.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="47" y1="28" x2="53" y2="28" stroke="#e9d700" strokeWidth="2" />
            {/* Big smile */}
            <path d="M40 40 Q50 50 60 40" fill="none" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
            {/* Body */}
            <line x1="50" y1="54" x2="50" y2="100" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
            {/* Raised arms — happy */}
            <line x1="50" y1="65" x2="18" y2="48" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
              style={{ transform: `rotate(${Math.sin(frame * 0.06) * 5}deg)`, transformOrigin: '50px 65px' }} />
            <line x1="50" y1="65" x2="82" y2="48" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
              style={{ transform: `rotate(${-Math.sin(frame * 0.06) * 5}deg)`, transformOrigin: '50px 65px' }} />
            {/* Legs */}
            <line x1="50" y1="100" x2="32" y2="142" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
            <line x1="50" y1="100" x2="68" y2="142" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
            {/* Saree dots */}
            <circle cx="28" cy="78" r="3" fill="#e9d700" />
            <circle cx="72" cy="78" r="3" fill="#e9d700" />
          </svg>

          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 52,
            fontWeight: 900,
            color: '#F5F5F5',
          }}>
            गीता आंटी
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 26,
            color: '#9E9E9E',
            marginTop: 4,
          }}>
            68 साल
          </div>
          <div style={{
            marginTop: 20,
            background: '#1C1C1C',
            borderRadius: 12,
            padding: '14px 20px',
            border: '2px solid #e9d700',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 36,
              fontWeight: 900,
              color: '#e9d700',
              opacity: shimmer,
            }}>
              ₹30 लाख
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 18,
              color: '#9E9E9E',
              marginTop: 4,
            }}>
              सादगी से निवेश
            </div>
          </div>
        </div>
      </div>

      {/* === CENTER — PIE CHART === */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: 'translate(-50%, -50%)',
      }}>
        <svg width="380" height="380" viewBox="0 0 380 380">
          {/* Glow behind */}
          <circle cx="190" cy="190" r="180" fill="#e9d700" opacity={0.04 * shimmer} />

          {/* Pie slices */}
          {slices.map((slice, idx) => (
            <path key={idx}
              d={createPieSlice(slice.startAngle, slice.endAngle, 160, 190, 190)}
              fill={slice.color}
              stroke="#1C1C1C"
              strokeWidth="3"
              opacity="0.9"
            />
          ))}

          {/* Center donut hole */}
          <circle cx="190" cy="190" r="70" fill="#1C1C1C" />
          <text x="190" y="184" textAnchor="middle" fill="#e9d700"
            fontFamily="JetBrains Mono" fontWeight="900" fontSize="26">
            ₹30
          </text>
          <text x="190" y="208" textAnchor="middle" fill="#9E9E9E"
            fontFamily="Noto Sans Devanagari" fontWeight="600" fontSize="18">
            लाख
          </text>
        </svg>
      </div>

      {/* === RIGHT — ALLOCATION LEGEND === */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: '50%',
        transform: 'translateY(-50%)',
        width: 380,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 28,
          color: '#9E9E9E',
          fontWeight: 700,
          marginBottom: 28,
          opacity: sp(30),
        }}>
          बंटवारा
        </div>

        {allocations.map((alloc, idx) => (
          <div key={idx} style={{
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            marginBottom: 22,
            opacity: sp(alloc.delay),
            transform: `translateX(${(1 - sp(alloc.delay)) * 60}px)`,
          }}>
            {/* Color swatch */}
            <div style={{
              width: 20,
              height: 50,
              backgroundColor: alloc.color,
              borderRadius: 4,
              flexShrink: 0,
            }} />

            <div style={{ flex: 1 }}>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 28,
                color: '#F5F5F5',
                fontWeight: 700,
              }}>
                {alloc.label}
              </div>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22,
                color: alloc.color,
              }}>
                {alloc.amount}
              </div>
            </div>

            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 32,
              fontWeight: 900,
              color: alloc.color,
            }}>
              {alloc.percent}%
            </div>
          </div>
        ))}

        {/* Result */}
        <div style={{
          marginTop: 20,
          padding: '18px 24px',
          background: 'linear-gradient(135deg, #2A2200, #1C1C1C)',
          border: '2px solid #e9d700',
          borderRadius: 14,
          opacity: sp(100),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 26,
            color: '#e9d700',
            fontWeight: 700,
          }}>
            ✓ बाजार गिरा — घबराई नहीं
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 22,
            color: '#9E9E9E',
            marginTop: 6,
          }}>
            क्योंकि योजना पहले से थी
          </div>
        </div>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.85) 100%)',
        }} />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Ch6Scene9;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch6Scene10',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch6Scene10: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Road path progress
  const roadProgress = interpolate(frame, [10, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Walking figure progress along road
  const figureX = 150 + roadProgress * 1550;
  const figureY = 600 - roadProgress * 120; // slight upward path

  // Golden hour glow
  const goldenGlow = 0.6 + Math.sin(frame * 0.04) * 0.2;

  // Stars/dots along path
  const pathSteps = [
    { x: 300, y: 590, label: 'योजना', delay: 20 },
    { x: 600, y: 560, label: 'सेविंग', delay: 35 },
    { x: 900, y: 530, label: 'समीक्षा', delay: 50 },
    { x: 1200, y: 510, label: 'धैर्य', delay: 65 },
    { x: 1500, y: 490, label: 'शांति', delay: 80 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === BACKGROUND — golden sunrise gradient === */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: `radial-gradient(ellipse at 80% 30%, rgba(233,215,0,${0.08 * goldenGlow}) 0%, transparent 50%), 
                       radial-gradient(ellipse at 50% 100%, #1A0A00 0%, #1C1C1C 40%, #080808 100%)`,
        }} />
      </AbsoluteFill>

      {/* === HORIZON GLOW === */}
      <div style={{
        position: 'absolute',
        left: 0, right: 0,
        top: 280,
        height: 120,
        background: `linear-gradient(to bottom, transparent, rgba(233,215,0,${0.06 * goldenGlow}), transparent)`,
      }} />

      {/* === STARS/SKY TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        <svg width="1920" height="400">
          {Array.from({ length: 40 }).map((_, i) => (
            <circle key={i}
              cx={seededRandom(i * 7) * 1920}
              cy={seededRandom(i * 11) * 300}
              r={seededRandom(i * 3) * 2 + 0.5}
              fill="#F5F5F5"
              opacity={0.5 + Math.sin(frame * 0.05 + i) * 0.3}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080">
          {/* Sun rising */}
          <circle cx="1700" cy="200" r="220"
            fill="#e9d700"
            opacity={0.4 * goldenGlow}
            style={{ transform: `scale(${1 + Math.sin(frame * 0.03) * 0.03})`, transformOrigin: '1700px 200px' }}
          />
          {/* Sun rays */}
          {Array.from({ length: 8 }).map((_, i) => (
            <line key={i}
              x1="1700" y1="200"
              x2={1700 + Math.cos((i * 45 + frame * 0.5) * Math.PI / 180) * 350}
              y2={200 + Math.sin((i * 45 + frame * 0.5) * Math.PI / 180) * 350}
              stroke="#e9d700"
              strokeWidth="2"
              opacity="0.2"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES — golden dust === */}
      {Array.from({ length: 25 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 6) * 1920,
          top: (seededRandom(i * 9) * 600 - frame * seededRandom(i * 4) * 0.5 + 600) % 600,
          width: seededRandom(i * 2) * 4 + 1,
          height: seededRandom(i * 2) * 4 + 1,
          borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: 0.2 * goldenGlow,
        }} />
      ))}

      {/* === CHAPTER COMPLETE BADGE === */}
      <div style={{
        position: 'absolute',
        top: 55,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(5),
        transform: `translateY(${(1 - sp(5)) * -50}px)`,
      }}>
        <div style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 16,
          background: 'linear-gradient(135deg, #2A2200, #1C1C1C)',
          border: '2px solid #e9d700',
          borderRadius: 40,
          padding: '12px 36px',
        }}>
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none"
            stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round">
            <polyline points="20 6 9 17 4 12" />
          </svg>
          <span style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            color: '#e9d700',
            letterSpacing: 3,
          }}>
            CHAPTER 6 — PLAN READY
          </span>
        </div>
      </div>

      {/* === ROAD / PATH SVG === */}
      <svg style={{ position: 'absolute', top: 0, left: 0 }} width="1920" height="1080">
        {/* Road path — dashed white line */}
        <path
          d={`M 100 620 Q 500 580 900 540 Q 1300 500 1750 470`}
          fill="none"
          stroke="#9E9E9E"
          strokeWidth="4"
          strokeDasharray="20 15"
          opacity="0.3"
          strokeDashoffset={-frame * 2}
        />

        {/* Animated path reveal */}
        <path
          d={`M 100 620 Q 500 580 900 540 Q 1300 500 1750 470`}
          fill="none"
          stroke="#e9d700"
          strokeWidth="6"
          strokeDasharray="2000"
          strokeDashoffset={2000 - 2000 * roadProgress}
          opacity="0.8"
        />
      </svg>

      {/* === PATH MILESTONE DOTS === */}
      {pathSteps.map((step, idx) => (
        <div key={idx} style={{
          position: 'absolute',
          left: step.x - 30,
          top: step.y - 30,
          opacity: sp(step.delay),
          transform: `scale(${sp(step.delay)})`,
        }}>
          <div style={{ position: 'relative', textAlign: 'center' }}>
            <div style={{
              width: 60,
              height: 60,
              borderRadius: '50%',
              backgroundColor: '#e9d700',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              margin: '0 auto',
              boxShadow: `0 0 20px #e9d70066`,
            }}>
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none"
                stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round">
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 22,
              color: '#F5F5F5',
              fontWeight: 700,
              marginTop: 8,
              whiteSpace: 'nowrap',
            }}>
              {step.label}
            </div>
          </div>
        </div>
      ))}

      {/* === WALKING STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        left: figureX - 45,
        top: figureY - 140,
        opacity: sp(5),
      }}>
        <svg width="90" height="145" viewBox="0 0 90 145">
          {/* Golden glow behind */}
          <circle cx="45" cy="24" r="35" fill="#e9d700" opacity={0.12 * goldenGlow} />

          {/* Head */}
          <circle cx="45" cy="24" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3" />

          {/* Glasses */}
          <circle cx="38" cy="23" r="6" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <circle cx="52" cy="23" r="6" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="44" y1="23" x2="46" y2="23" stroke="#e9d700" strokeWidth="2" />
          <line x1="26" y1="23" x2="32" y2="23" stroke="#e9d700" strokeWidth="2" />
          <line x1="58" y1="23" x2="64" y2="23" stroke="#e9d700" strokeWidth="2" />

          {/* Serene smile */}
          <path d="M37 33 Q45 39 53 33" fill="none" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />

          {/* Body */}
          <line x1="45" y1="44" x2="45" y2="96" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />

          {/* Walking arms — swinging */}
          <line x1="45" y1="60" x2="15" y2={72 + Math.sin(frame * 0.18) * 10} stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="60" x2="75" y2={72 - Math.sin(frame * 0.18) * 10} stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />

          {/* Walking legs */}
          <line x1="45" y1="96" x2={30 + Math.sin(frame * 0.18) * 12} y2="136" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="96" x2={60 - Math.sin(frame * 0.18) * 12} y2="136" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* === HERO TEXT === */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: 160,
        opacity: sp(8),
        transform: `translateY(${(1 - sp(8)) * -40}px)`,
        maxWidth: 700,
      }}>
        <div style={{
          fontFamily: 'Yantra One, sans-serif',
          fontSize: 80,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.15,
        }}>
          स्थिर गति से
        </div>
        <div style={{
          fontFamily: 'Yantra One, sans-serif',
          fontSize: 80,
          fontWeight: 900,
          color: '#e9d700',
          lineHeight: 1.15,
          textShadow: `0 0 ${50 * goldenGlow}px #e9d70033`,
        }}>
          बिना घबराहट
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 36,
          color: '#9E9E9E',
          marginTop: 20,
          opacity: sp(20),
          lineHeight: 1.5,
        }}>
          एक योजना — और उसके साथ चलते रहो
        </div>
      </div>

      {/* === BOTTOM CHAPTER END CARD === */}
      <div style={{
        position: 'absolute',
        bottom: 70,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(85),
        transform: `scale(${sp(85)})`,
      }}>
        <div style={{
          display: 'inline-block',
          fontFamily: 'Inter, sans-serif',
          fontSize: 20,
          fontWeight: 700,
          color: '#9E9E9E',
          letterSpacing: 4,
          borderTop: '1px solid #3D3D3D',
          paddingTop: 16,
        }}>
          CHAPTER 6 COMPLETE ✓ &nbsp;|&nbsp; अगला: गीता आंटी का सरल फॉर्मूला
        </div>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.82) 100%)',
        }} />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Ch6Scene10;
```

