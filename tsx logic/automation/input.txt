```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene51_PermanentRaise',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene51_PermanentRaise: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom in
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateRight: 'clamp' });

  // Title reveal
  const titleY = interpolate(sp(8), [0, 1], [60, 0]);
  const titleOp = interpolate(sp(8), [0, 1], [0, 1]);

  // Heart pulse
  const heartScale = 1 + Math.sin(frame * 0.12) * 0.08;
  const heartGlow = 0.4 + Math.sin(frame * 0.1) * 0.2;

  // Bars rising
  const bar1H = interpolate(sp(40), [0, 1], [0, 180]);
  const bar2H = interpolate(sp(55), [0, 1], [0, 320]);
  const bar3H = interpolate(sp(70), [0, 1], [0, 480]);

  const bar1Op = interpolate(sp(40), [0, 1], [0, 1]);
  const bar2Op = interpolate(sp(55), [0, 1], [0, 1]);
  const bar3Op = interpolate(sp(70), [0, 1], [0, 1]);

  // Connecting arrow
  const arrowOp = interpolate(sp(95), [0, 1], [0, 1]);
  const arrowX = interpolate(sp(95), [0, 1], [-100, 0]);

  // Subtitle reveal
  const subOp = interpolate(sp(120), [0, 1], [0, 1]);
  const subY = interpolate(sp(120), [0, 1], [40, 0]);

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => {
    const seed = i + 1;
    const x = seededRandom(seed) * 1920;
    const baseY = seededRandom(seed * 2) * 1080;
    const y = (baseY + frame * 0.6) % 1080;
    const size = seededRandom(seed * 3) * 4 + 2;
    const op = seededRandom(seed * 4) * 0.5 + 0.2;
    return { x, y, size, op };
  });

  // Shape drift
  const shape1Rot = frame * 0.15;
  const shape2Rot = -frame * 0.1;
  const shape3Y = Math.sin(frame * 0.03) * 25;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 40%, rgba(250,185,91,0.12) 0%, rgba(44,57,71,0) 60%)',
        }}
      />

      {/* Shape layers — large transparent geometry */}
      <div
        style={{
          position: 'absolute',
          top: -200,
          left: -150,
          width: 700,
          height: 700,
          border: '2px solid #E8EDF2',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `rotate(${shape1Rot}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -250,
          right: -200,
          width: 800,
          height: 800,
          border: '2px solid #FAB95B',
          opacity: 0.06,
          transform: `rotate(${shape2Rot}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '40%',
          left: '70%',
          width: 300,
          height: 300,
          backgroundColor: '#FAB95B',
          opacity: 0.04,
          clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          transform: `translateY(${shape3Y}px) rotate(${shape1Rot * 0.5}deg)`,
        }}
      />

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        <defs>
          <pattern id="grid51" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid51)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
            boxShadow: '0 0 10px #FAB95B',
          }}
        />
      ))}

      {/* Main content wrapper with camera scale */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `translateY(${titleY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 8,
              textTransform: 'uppercase',
              marginBottom: 18,
            }}
          >
            ── A Gift That Outlives You ──
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 110,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1.0,
              letterSpacing: -2,
            }}
          >
            A PERMANENT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 145,
              fontWeight: 900,
              color: '#FAB95B',
              lineHeight: 1.0,
              letterSpacing: -3,
              textShadow: '0 0 40px rgba(250,185,91,0.4)',
            }}
          >
            PAY RAISE
          </div>
        </div>

        {/* Comparison bars — Small check vs Big check vs Lifetime gift */}
        <div
          style={{
            position: 'absolute',
            bottom: 240,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'flex-end',
            gap: 80,
            height: 500,
          }}
        >
          {/* Bar 1 — Just You */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', opacity: bar1Op }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 38,
                fontWeight: 700,
                color: '#E8EDF2',
                marginBottom: 14,
              }}
            >
              $2,400
            </div>
            <div
              style={{
                width: 180,
                height: bar1H,
                background: 'linear-gradient(180deg, #4A5A6B 0%, #2C3947 100%)',
                border: '2px solid #9E9E9E',
                borderRadius: '6px 6px 0 0',
              }}
            />
            <div
              style={{
                marginTop: 18,
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 600,
                color: '#9E9E9E',
                textTransform: 'uppercase',
                letterSpacing: 2,
              }}
            >
              Just You
            </div>
          </div>

          {/* Arrow */}
          <div
            style={{
              opacity: arrowOp,
              transform: `translateX(${arrowX}px)`,
              marginBottom: 200,
              fontFamily: 'Inter, Poppins',
              fontSize: 80,
              color: '#FAB95B',
              fontWeight: 900,
            }}
          >
            →
          </div>

          {/* Bar 2 — Bigger Check */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', opacity: bar2Op }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 42,
                fontWeight: 700,
                color: '#E8EDF2',
                marginBottom: 14,
              }}
            >
              $3,600
            </div>
            <div
              style={{
                width: 180,
                height: bar2H,
                background: 'linear-gradient(180deg, #E8EDF2 0%, #9E9E9E 100%)',
                border: '2px solid #E8EDF2',
                borderRadius: '6px 6px 0 0',
              }}
            />
            <div
              style={{
                marginTop: 18,
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 600,
                color: '#E8EDF2',
                textTransform: 'uppercase',
                letterSpacing: 2,
              }}
            >
              Bigger Check
            </div>
          </div>

          {/* Arrow 2 */}
          <div
            style={{
              opacity: arrowOp,
              transform: `translateX(${arrowX}px) scale(${heartScale})`,
              marginBottom: 200,
              fontFamily: 'Inter, Poppins',
              fontSize: 80,
              color: '#FAB95B',
              fontWeight: 900,
            }}
          >
            →
          </div>

          {/* Bar 3 — Lifetime Love */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', opacity: bar3Op }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 48,
                fontWeight: 900,
                color: '#FAB95B',
                marginBottom: 14,
                textShadow: '0 0 20px rgba(250,185,91,0.5)',
              }}
            >
              LIFETIME
            </div>
            <div
              style={{
                width: 180,
                height: bar3H,
                background: 'linear-gradient(180deg, #FAB95B 0%, #c98e3a 100%)',
                border: '3px solid #FAB95B',
                borderRadius: '6px 6px 0 0',
                boxShadow: `0 0 ${30 + heartGlow * 40}px rgba(250,185,91,${heartGlow})`,
                position: 'relative',
              }}
            >
              {/* Heart icon inside */}
              <svg
                viewBox="0 0 24 24"
                width="80"
                height="80"
                style={{
                  position: 'absolute',
                  top: '50%',
                  left: '50%',
                  transform: `translate(-50%, -50%) scale(${heartScale})`,
                }}
              >
                <path
                  d="M12 21s-7-4.5-9.5-9.5C1 7.5 4 4 7.5 4c2 0 3.5 1 4.5 2.5C13 5 14.5 4 16.5 4 20 4 23 7.5 21.5 11.5 19 16.5 12 21 12 21z"
                  fill="#2C3947"
                />
              </svg>
            </div>
            <div
              style={{
                marginTop: 18,
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 700,
                color: '#FAB95B',
                textTransform: 'uppercase',
                letterSpacing: 2,
              }}
            >
              Love Lasts
            </div>
          </div>
        </div>

        {/* Bottom subtitle */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: subOp,
            transform: `translateY(${subY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 54,
              fontWeight: 700,
              color: '#E8EDF2',
              letterSpacing: -0.5,
            }}
          >
            For the person you love —{' '}
            <span style={{ color: '#FAB95B', fontWeight: 900 }}>long after you're gone.</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene51_PermanentRaise;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene52_ShieldOfLove',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene52_ShieldOfLove: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera orbit
  const camRot = interpolate(frame, [0, durationInFrames], [-1, 1]);

  // Shield reveal
  const shieldScale = sp(10, { damping: 10, stiffness: 100 });
  const shieldGlow = 0.5 + Math.sin(frame * 0.08) * 0.25;

  // Headline
  const headOp = interpolate(sp(50), [0, 1], [0, 1]);
  const headY = interpolate(sp(50), [0, 1], [40, 0]);

  // Three pillars
  const p1 = sp(80);
  const p2 = sp(100);
  const p3 = sp(120);

  // Bottom CTA
  const ctaOp = interpolate(sp(160), [0, 1], [0, 1]);

  // Particles — gentle rising
  const particles = Array.from({ length: 22 }, (_, i) => {
    const seed = i + 5;
    const x = seededRandom(seed) * 1920;
    const baseY = seededRandom(seed * 2) * 1080;
    const y = (baseY - frame * 0.5 + 1080) % 1080;
    const size = seededRandom(seed * 3) * 5 + 2;
    const op = seededRandom(seed * 4) * 0.4 + 0.15;
    return { x, y, size, op };
  });

  // Concentric ring breathe
  const ringScale1 = 1 + Math.sin(frame * 0.05) * 0.04;
  const ringScale2 = 1 + Math.sin(frame * 0.05 + 1) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Radial gradient backdrop */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 50%, rgba(250,185,91,0.15) 0%, rgba(44,57,71,0) 55%)',
        }}
      />

      {/* Diagonal line texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.04 }} width="1920" height="1080">
        <defs>
          <pattern id="diag52" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M0,60 L60,0" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag52)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '30%',
          left: '20%',
          width: 600,
          height: 600,
          border: '2px solid #FAB95B',
          borderRadius: '50%',
          opacity: 0.06,
          transform: `translate(-50%, -50%) scale(${ringScale1})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '30%',
          left: '20%',
          width: 850,
          height: 850,
          border: '1px solid #E8EDF2',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `translate(-50%, -50%) scale(${ringScale2})`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
            boxShadow: '0 0 8px #FAB95B',
          }}
        />
      ))}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)`, transformOrigin: 'center center' }}>
        {/* LEFT — Shield visual */}
        <div
          style={{
            position: 'absolute',
            top: '50%',
            left: '22%',
            transform: `translate(-50%, -50%) scale(${shieldScale})`,
          }}
        >
          {/* Shield SVG */}
          <svg width="500" height="580" viewBox="0 0 100 116">
            <defs>
              <linearGradient id="shieldGrad" x1="0%" y1="0%" x2="0%" y2="100%">
                <stop offset="0%" stopColor="#FAB95B" />
                <stop offset="100%" stopColor="#c98e3a" />
              </linearGradient>
            </defs>
            <path
              d="M50 4 L92 18 L92 58 Q92 92 50 112 Q8 92 8 58 L8 18 Z"
              fill="url(#shieldGrad)"
              stroke="#E8EDF2"
              strokeWidth="2"
              style={{ filter: `drop-shadow(0 0 ${20 + shieldGlow * 30}px rgba(250,185,91,${shieldGlow}))` }}
            />
            {/* Heart in center */}
            <path
              d="M50 78 C50 78 32 65 32 50 C32 42 38 36 45 36 C47 36 49 37 50 40 C51 37 53 36 55 36 C62 36 68 42 68 50 C68 65 50 78 50 78 Z"
              fill="#2C3947"
            />
          </svg>
        </div>

        {/* RIGHT — Text content */}
        <div
          style={{
            position: 'absolute',
            top: 130,
            right: 110,
            width: 920,
            opacity: headOp,
            transform: `translateY(${headY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 6,
              textTransform: 'uppercase',
              marginBottom: 24,
            }}
          >
            The True Power
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 140,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 0.95,
              letterSpacing: -3,
              marginBottom: 12,
            }}
          >
            AN ACT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 140,
              fontWeight: 900,
              color: '#FAB95B',
              lineHeight: 0.95,
              letterSpacing: -3,
              textShadow: '0 0 30px rgba(250,185,91,0.4)',
            }}
          >
            OF LOVE.
          </div>

          {/* Three pillar tags */}
          <div style={{ display: 'flex', gap: 18, marginTop: 50, flexWrap: 'wrap' }}>
            {[
              { txt: 'DELAY', d: p1 },
              { txt: 'PROTECT', d: p2 },
              { txt: 'PROVIDE', d: p3 },
            ].map((tag, i) => (
              <div
                key={i}
                style={{
                  opacity: tag.d,
                  transform: `translateY(${interpolate(tag.d, [0, 1], [30, 0])}px)`,
                  padding: '18px 36px',
                  backgroundColor: 'rgba(232,237,242,0.08)',
                  border: '2px solid #FAB95B',
                  borderRadius: 8,
                  fontFamily: 'Inter, Poppins',
                  fontSize: 38,
                  fontWeight: 700,
                  color: '#E8EDF2',
                  letterSpacing: 4,
                }}
              >
                {tag.txt}
              </div>
            ))}
          </div>
        </div>

        {/* Bottom subtitle */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: ctaOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 48,
              fontWeight: 600,
              color: '#E8EDF2',
            }}
          >
            The ultimate financial shield for{' '}
            <span style={{ color: '#FAB95B', fontWeight: 900 }}>your family.</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene52_ShieldOfLove;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene53_RecapBonus1',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene53_RecapBonus1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [-15, 15]);

  // Five number reveals
  const num1 = sp(15);
  const num2 = sp(25);
  const num3 = sp(35);
  const num4 = sp(45);
  const num5 = sp(55);

  // Bonus 1 highlight
  const highlightOp = interpolate(sp(80), [0, 1], [0, 1]);
  const highlightScale = sp(80, { damping: 10 });

  // Card content
  const cardOp = interpolate(sp(110), [0, 1], [0, 1]);
  const cardY = interpolate(sp(110), [0, 1], [50, 0]);

  // Refund icon pulse
  const refundPulse = 1 + Math.sin(frame * 0.15) * 0.06;

  // Header
  const headOp = interpolate(sp(5), [0, 1], [0, 1]);

  // Particles
  const particles = Array.from({ length: 15 }, (_, i) => {
    const seed = i + 9;
    const x = (seededRandom(seed) * 1920 + frame * 0.4) % 1920;
    const y = seededRandom(seed * 2) * 1080;
    const size = seededRandom(seed * 3) * 4 + 2;
    const op = seededRandom(seed * 4) * 0.4 + 0.2;
    return { x, y, size, op };
  });

  // Shape drift
  const sRot = frame * 0.12;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(250,185,91,0.08) 0%, rgba(44,57,71,0) 50%, rgba(232,237,242,0.04) 100%)',
        }}
      />

      {/* Dot grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="dots53" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#E8EDF2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots53)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '-15%',
          right: '-10%',
          width: 600,
          height: 600,
          backgroundColor: '#FAB95B',
          opacity: 0.05,
          borderRadius: 60,
          transform: `rotate(${sRot}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-20%',
          left: '-10%',
          width: 700,
          height: 700,
          border: '2px solid #E8EDF2',
          opacity: 0.04,
          borderRadius: '50%',
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            backgroundColor: '#FAB95B',
            opacity: p.op,
            borderRadius: '50%',
            boxShadow: '0 0 8px #FAB95B',
          }}
        />
      ))}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 30,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 6,
              textTransform: 'uppercase',
              marginBottom: 12,
            }}
          >
            ── Quick Recap ──
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 95,
              fontWeight: 900,
              color: '#E8EDF2',
              letterSpacing: -2,
            }}
          >
            5 BONUSES AT 65
          </div>
        </div>

        {/* Five number row */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 50,
          }}
        >
          {[
            { n: '1', d: num1, active: true },
            { n: '2', d: num2, active: false },
            { n: '3', d: num3, active: false },
            { n: '4', d: num4, active: false },
            { n: '5', d: num5, active: false },
          ].map((b, i) => (
            <div
              key={i}
              style={{
                opacity: b.d,
                transform: `scale(${b.d})`,
                width: 130,
                height: 130,
                borderRadius: '50%',
                backgroundColor: b.active ? '#FAB95B' : 'rgba(232,237,242,0.08)',
                border: b.active ? '4px solid #FAB95B' : '2px solid #9E9E9E',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontFamily: 'Inter, Poppins',
                fontSize: 70,
                fontWeight: 900,
                color: b.active ? '#2C3947' : '#9E9E9E',
                boxShadow: b.active ? `0 0 ${20 + Math.sin(frame * 0.1) * 15}px rgba(250,185,91,0.6)` : 'none',
              }}
            >
              {b.n}
            </div>
          ))}
        </div>

        {/* Highlight Card — Bonus 1 */}
        <div
          style={{
            position: 'absolute',
            top: 520,
            left: '50%',
            transform: `translateX(-50%) scale(${highlightScale})`,
            opacity: highlightOp,
            width: 1500,
            padding: '50px 70px',
            background: 'linear-gradient(135deg, rgba(250,185,91,0.15) 0%, rgba(250,185,91,0.05) 100%)',
            border: '3px solid #FAB95B',
            borderRadius: 24,
            display: 'flex',
            alignItems: 'center',
            gap: 60,
          }}
        >
          {/* Refund icon */}
          <div
            style={{
              flexShrink: 0,
              width: 180,
              height: 180,
              borderRadius: '50%',
              backgroundColor: '#FAB95B',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              transform: `scale(${refundPulse})`,
              boxShadow: '0 0 40px rgba(250,185,91,0.5)',
            }}
          >
            <svg width="100" height="100" viewBox="0 0 24 24" fill="none">
              <path d="M3 12a9 9 0 1 0 3-6.7" stroke="#2C3947" strokeWidth="2.5" strokeLinecap="round" />
              <path d="M3 4v6h6" stroke="#2C3947" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
              <path d="M12 7v5l3 2" stroke="#2C3947" strokeWidth="2.5" strokeLinecap="round" />
            </svg>
          </div>

          <div style={{ flex: 1, opacity: cardOp, transform: `translateY(${cardY}px)` }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 26,
                fontWeight: 700,
                color: '#FAB95B',
                letterSpacing: 4,
                textTransform: 'uppercase',
                marginBottom: 10,
              }}
            >
              BONUS #1
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 88,
                fontWeight: 900,
                color: '#E8EDF2',
                lineHeight: 1.0,
                letterSpacing: -2,
                marginBottom: 16,
              }}
            >
              The Working Bonus
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 38,
                fontWeight: 500,
                color: '#E8EDF2',
                opacity: 0.85,
                lineHeight: 1.3,
              }}
            >
              SSA <span style={{ color: '#FAB95B', fontWeight: 800 }}>recalculates</span> & refunds your withheld months.
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene53_RecapBonus1;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene54_RecapBonus2And3',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene54_RecapBonus2And3: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Card 2 slide in from left
  const c2X = interpolate(sp(10), [0, 1], [-300, 0]);
  const c2Op = interpolate(sp(10), [0, 1], [0, 1]);

  // Card 3 slide in from right
  const c3X = interpolate(sp(80), [0, 1], [300, 0]);
  const c3Op = interpolate(sp(80), [0, 1], [0, 1]);

  // Percentage counter
  const pctCount = Math.floor(interpolate(sp(110), [0, 1], [0, 50]));
  const pct8Count = Math.floor(interpolate(sp(180), [0, 1], [0, 8]));

  // Header
  const headOp = interpolate(sp(0), [0, 1], [0, 1]);

  // Background subtle camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => {
    const seed = i + 12;
    const x = seededRandom(seed) * 1920;
    const y = (seededRandom(seed * 2) * 1080 + frame * 0.45) % 1080;
    const size = seededRandom(seed * 3) * 5 + 2;
    const op = seededRandom(seed * 4) * 0.4 + 0.15;
    return { x, y, size, op };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 60%, rgba(250,185,91,0.1) 0%, rgba(44,57,71,0) 60%)',
        }}
      />

      {/* Cross-hatch texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        <defs>
          <pattern id="hatch54" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M0,40 L40,0 M-10,10 L10,-10 M30,50 L50,30" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hatch54)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '20%',
          left: '50%',
          width: 900,
          height: 900,
          border: '1px solid #FAB95B',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `translate(-50%, -50%) rotate(${frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 200,
          height: 200,
          backgroundColor: '#FAB95B',
          opacity: 0.04,
          clipPath: 'polygon(50% 0%, 100% 38%, 82% 100%, 18% 100%, 0% 38%)',
          transform: `translate(-50%, -50%) rotate(${frame * 0.2}deg)`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#E8EDF2',
            opacity: p.op,
          }}
        />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 8,
              textTransform: 'uppercase',
              marginBottom: 8,
            }}
          >
            RECAP CONTINUED
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 70,
              fontWeight: 900,
              color: '#E8EDF2',
              letterSpacing: -1,
            }}
          >
            BONUS 2 + BONUS 3
          </div>
        </div>

        {/* Bonus 2 Card */}
        <div
          style={{
            position: 'absolute',
            top: 260,
            left: 90,
            width: 820,
            height: 700,
            padding: '50px 50px',
            background: 'linear-gradient(160deg, rgba(232,237,242,0.1) 0%, rgba(232,237,242,0.02) 100%)',
            border: '3px solid #E8EDF2',
            borderRadius: 24,
            opacity: c2Op,
            transform: `translateX(${c2X}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 14,
            }}
          >
            BONUS #2
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 78,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1.0,
              letterSpacing: -2,
              marginBottom: 30,
            }}
          >
            Spousal Kicker
          </div>

          {/* Big percentage */}
          <div style={{ display: 'flex', alignItems: 'baseline', marginBottom: 28 }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 220,
                fontWeight: 900,
                color: '#FAB95B',
                lineHeight: 0.9,
                letterSpacing: -6,
                textShadow: '0 0 40px rgba(250,185,91,0.4)',
              }}
            >
              {pctCount}
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 120,
                fontWeight: 900,
                color: '#FAB95B',
                marginLeft: 8,
              }}
            >
              %
            </div>
          </div>

          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 34,
              fontWeight: 500,
              color: '#E8EDF2',
              opacity: 0.9,
              lineHeight: 1.3,
            }}
          >
            Lower earner claims half of higher earner's full benefit.
          </div>
        </div>

        {/* Bonus 3 Card */}
        <div
          style={{
            position: 'absolute',
            top: 260,
            right: 90,
            width: 820,
            height: 700,
            padding: '50px 50px',
            background: 'linear-gradient(160deg, rgba(250,185,91,0.18) 0%, rgba(250,185,91,0.04) 100%)',
            border: '3px solid #FAB95B',
            borderRadius: 24,
            opacity: c3Op,
            transform: `translateX(${c3X}px)`,
            boxShadow: '0 0 40px rgba(250,185,91,0.15)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 14,
            }}
          >
            BONUS #3
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 78,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1.0,
              letterSpacing: -2,
              marginBottom: 30,
            }}
          >
            Delayed Multiplier
          </div>

          {/* Big percentage with arrow up */}
          <div style={{ display: 'flex', alignItems: 'center', marginBottom: 28, gap: 20 }}>
            <svg width="100" height="120" viewBox="0 0 24 24" fill="none">
              <path d="M12 4 L12 20 M5 11 L12 4 L19 11" stroke="#FAB95B" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div style={{ display: 'flex', alignItems: 'baseline' }}>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 220,
                  fontWeight: 900,
                  color: '#FAB95B',
                  lineHeight: 0.9,
                  letterSpacing: -6,
                  textShadow: '0 0 40px rgba(250,185,91,0.5)',
                }}
              >
                {pct8Count}
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 120,
                  fontWeight: 900,
                  color: '#FAB95B',
                  marginLeft: 8,
                }}
              >
                %
              </div>
            </div>
          </div>

          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 34,
              fontWeight: 500,
              color: '#E8EDF2',
              opacity: 0.9,
              lineHeight: 1.3,
            }}
          >
            Guaranteed raise — every year you wait past 67.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene54_RecapBonus2And3;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene55_RecapBonus4',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene55_RecapBonus4: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Premium amount strikethrough animation
  const strikeProgress = interpolate(sp(70), [0, 1], [0, 100]);
  const strikeOp = interpolate(sp(40), [0, 1], [0, 1]);

  // Number reveal
  const numScale = sp(20, { damping: 10 });
  const numOp = interpolate(sp(20), [0, 1], [0, 1]);

  // Result $0
  const zeroScale = sp(110, { damping: 9, stiffness: 110 });
  const zeroOp = interpolate(sp(110), [0, 1], [0, 1]);

  // Arrow
  const arrowOp = interpolate(sp(90), [0, 1], [0, 1]);

  // Header
  const headOp = interpolate(sp(0), [0, 1], [0, 1]);
  const headY = interpolate(sp(0), [0, 1], [30, 0]);

  // Bottom label
  const labelOp = interpolate(sp(150), [0, 1], [0, 1]);

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-0.5, 0.5]);

  // Particles
  const particles = Array.from({ length: 20 }, (_, i) => {
    const seed = i + 17;
    const x = (seededRandom(seed) * 1920 - frame * 0.35 + 1920) % 1920;
    const y = seededRandom(seed * 2) * 1080;
    const size = seededRandom(seed * 3) * 5 + 2;
    const op = seededRandom(seed * 4) * 0.4 + 0.15;
    return { x, y, size, op };
  });

  // Glow pulse
  const glowPulse = 0.4 + Math.sin(frame * 0.1) * 0.2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 60% 50%, rgba(250,185,91,0.12) 0%, rgba(44,57,71,0) 55%)',
        }}
      />

      {/* Vertical bars texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.04 }} width="1920" height="1080">
        <defs>
          <pattern id="vbar55" width="80" height="80" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="80" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vbar55)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '10%',
          left: '5%',
          width: 400,
          height: 400,
          border: '2px solid #FAB95B',
          borderRadius: 40,
          opacity: 0.06,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '5%',
          right: '8%',
          width: 500,
          height: 500,
          backgroundColor: '#E8EDF2',
          opacity: 0.04,
          borderRadius: '50%',
          transform: `translateY(${Math.sin(frame * 0.04) * 20}px)`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
            boxShadow: '0 0 8px #FAB95B',
          }}
        />
      ))}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)`, transformOrigin: 'center center' }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headOp,
            transform: `translateY(${headY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 6,
              textTransform: 'uppercase',
              marginBottom: 14,
            }}
          >
            BONUS #4
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 130,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1.0,
              letterSpacing: -3,
            }}
          >
            MEDICARE <span style={{ color: '#FAB95B' }}>GIVE-BACK</span>
          </div>
        </div>

        {/* Equation: $202.90 → $0 */}
        <div
          style={{
            position: 'absolute',
            top: 460,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 70,
          }}
        >
          {/* Premium amount with strike */}
          <div
            style={{
              opacity: numOp,
              transform: `scale(${numScale})`,
              position: 'relative',
              padding: '40px 60px',
              background: 'rgba(232,237,242,0.06)',
              border: '3px solid #9E9E9E',
              borderRadius: 18,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 26,
                fontWeight: 600,
                color: '#9E9E9E',
                letterSpacing: 3,
                textTransform: 'uppercase',
                marginBottom: 10,
              }}
            >
              Part B / month
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 130,
                fontWeight: 900,
                color: '#E8EDF2',
                lineHeight: 1.0,
                letterSpacing: -3,
                position: 'relative',
              }}
            >
              $202.90
              {/* Strike line */}
              <div
                style={{
                  position: 'absolute',
                  top: '50%',
                  left: 0,
                  height: 10,
                  width: `${strikeProgress}%`,
                  backgroundColor: '#ff5252',
                  opacity: strikeOp,
                  borderRadius: 5,
                  transform: 'translateY(-50%)',
                }}
              />
            </div>
          </div>

          {/* Arrow */}
          <div
            style={{
              opacity: arrowOp,
              fontFamily: 'Inter, Poppins',
              fontSize: 100,
              color: '#FAB95B',
              fontWeight: 900,
              transform: `scale(${1 + Math.sin(frame * 0.15) * 0.08})`,
            }}
          >
            →
          </div>

          {/* $0 result */}
          <div
            style={{
              opacity: zeroOp,
              transform: `scale(${zeroScale})`,
              padding: '40px 80px',
              background: 'linear-gradient(135deg, #FAB95B 0%, #c98e3a 100%)',
              borderRadius: 18,
              boxShadow: `0 0 ${30 + glowPulse * 40}px rgba(250,185,91,${glowPulse})`,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 26,
                fontWeight: 700,
                color: '#2C3947',
                letterSpacing: 3,
                textTransform: 'uppercase',
                marginBottom: 10,
              }}
            >
              You Pay
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 200,
                fontWeight: 900,
                color: '#2C3947',
                lineHeight: 1.0,
                letterSpacing: -5,
              }}
            >
              $0
            </div>
          </div>
        </div>

        {/* Bottom subtitle */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: labelOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 50,
              fontWeight: 700,
              color: '#E8EDF2',
            }}
          >
            Part B premium —{' '}
            <span style={{ color: '#FAB95B', fontWeight: 900 }}>wiped out entirely.</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene55_RecapBonus4;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene56_RecapBonus5Shield',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene56_RecapBonus5Shield: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Shield grand entrance
  const shieldScale = sp(15, { damping: 9, stiffness: 110 });
  const shieldRot = interpolate(sp(15), [0, 1], [-15, 0]);

  // Glow pulse
  const glowPulse = 0.45 + Math.sin(frame * 0.08) * 0.25;

  // Title
  const titleOp = interpolate(sp(50), [0, 1], [0, 1]);
  const titleX = interpolate(sp(50), [0, 1], [80, 0]);

  // Three bullet reveals
  const bul1 = sp(90);
  const bul2 = sp(110);
  const bul3 = sp(130);

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  // Particles — gold ascending
  const particles = Array.from({ length: 24 }, (_, i) => {
    const seed = i + 23;
    const x = seededRandom(seed) * 1920;
    const y = (seededRandom(seed * 2) * 1080 - frame * 0.6 + 1080) % 1080;
    const size = seededRandom(seed * 3) * 5 + 2;
    const op = seededRandom(seed * 4) * 0.5 + 0.2;
    return { x, y, size, op };
  });

  // Rings around shield
  const ring1S = 1 + Math.sin(frame * 0.06) * 0.05;
  const ring2S = 1 + Math.sin(frame * 0.06 + 1) * 0.06;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Radial gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 28% 50%, rgba(250,185,91,0.18) 0%, rgba(44,57,71,0) 55%)',
        }}
      />

      {/* Hexagon texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.04 }} width="1920" height="1080">
        <defs>
          <pattern id="hex56" width="90" height="80" patternUnits="userSpaceOnUse">
            <polygon points="45,5 80,25 80,55 45,75 10,55 10,25" fill="none" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hex56)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '28%',
          width: 700,
          height: 700,
          border: '2px solid #FAB95B',
          borderRadius: '50%',
          opacity: 0.06,
          transform: `translate(-50%, -50%) scale(${ring1S})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '28%',
          width: 900,
          height: 900,
          border: '1px solid #E8EDF2',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `translate(-50%, -50%) scale(${ring2S})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: 100,
          right: 100,
          width: 250,
          height: 250,
          backgroundColor: '#FAB95B',
          opacity: 0.04,
          clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          transform: `rotate(${frame * 0.15}deg)`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
            boxShadow: '0 0 10px #FAB95B',
          }}
        />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* LEFT — Shield */}
        <div
          style={{
            position: 'absolute',
            top: '50%',
            left: '28%',
            transform: `translate(-50%, -50%) scale(${shieldScale}) rotate(${shieldRot}deg)`,
          }}
        >
          <svg width="520" height="600" viewBox="0 0 100 116">
            <defs>
              <linearGradient id="shg56" x1="0%" y1="0%" x2="0%" y2="100%">
                <stop offset="0%" stopColor="#FAB95B" />
                <stop offset="100%" stopColor="#c98e3a" />
              </linearGradient>
            </defs>
            <path
              d="M50 4 L92 18 L92 58 Q92 92 50 112 Q8 92 8 58 L8 18 Z"
              fill="url(#shg56)"
              stroke="#E8EDF2"
              strokeWidth="2"
              style={{ filter: `drop-shadow(0 0 ${25 + glowPulse * 35}px rgba(250,185,91,${glowPulse}))` }}
            />
            {/* "5" inside */}
            <text
              x="50"
              y="72"
              textAnchor="middle"
              fontFamily="Inter, Poppins"
              fontSize="48"
              fontWeight="900"
              fill="#2C3947"
            >
              5
            </text>
          </svg>
        </div>

        {/* RIGHT — Content */}
        <div
          style={{
            position: 'absolute',
            top: 140,
            right: 100,
            width: 920,
            opacity: titleOp,
            transform: `translateX(${titleX}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 5,
              textTransform: 'uppercase',
              marginBottom: 16,
            }}
          >
            BONUS #5
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 105,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 0.95,
              letterSpacing: -2,
              marginBottom: 6,
            }}
          >
            SURVIVOR
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 105,
              fontWeight: 900,
              color: '#FAB95B',
              lineHeight: 0.95,
              letterSpacing: -2,
              marginBottom: 50,
              textShadow: '0 0 30px rgba(250,185,91,0.4)',
            }}
          >
            BENEFIT SHIELD
          </div>

          {/* Three bullets */}
          {[
            { txt: 'Higher earner delays', d: bul1 },
            { txt: 'Maximizes lifetime check', d: bul2 },
            { txt: 'Protects surviving spouse', d: bul3 },
          ].map((b, i) => (
            <div
              key={i}
              style={{
                opacity: b.d,
                transform: `translateX(${interpolate(b.d, [0, 1], [-40, 0])}px)`,
                display: 'flex',
                alignItems: 'center',
                gap: 22,
                marginBottom: 22,
              }}
            >
              <div
                style={{
                  width: 18,
                  height: 18,
                  borderRadius: '50%',
                  backgroundColor: '#FAB95B',
                  boxShadow: '0 0 12px #FAB95B',
                  flexShrink: 0,
                }}
              />
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 44,
                  fontWeight: 600,
                  color: '#E8EDF2',
                }}
              >
                {b.txt}
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene56_RecapBonus5Shield;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene57_NotAutomatic',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene57_NotAutomatic: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big "NOT" reveal
  const notScale = sp(8, { damping: 8, stiffness: 100 });
  const notOp = interpolate(sp(8), [0, 1], [0, 1]);

  // Automatic word strike
  const autoOp = interpolate(sp(35), [0, 1], [0, 1]);
  const strikeW = interpolate(sp(55), [0, 1], [0, 100]);

  // Two action items
  const act1 = sp(85);
  const act2 = sp(115);

  // Bottom emphasis
  const emphOp = interpolate(sp(150), [0, 1], [0, 1]);

  // Camera shake at frame 8
  const shakeIntensity = interpolate(frame, [8, 25], [15, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shakeX = Math.sin(frame * 1.5) * shakeIntensity;
  const shakeY = Math.cos(frame * 1.7) * shakeIntensity;

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => {
    const seed = i + 30;
    const x = (seededRandom(seed) * 1920 + frame * 0.4) % 1920;
    const y = seededRandom(seed * 2) * 1080;
    const size = seededRandom(seed * 3) * 4 + 2;
    const op = seededRandom(seed * 4) * 0.4 + 0.15;
    return { x, y, size, op };
  });

  // Warning ring pulse
  const warnPulse = 0.5 + Math.sin(frame * 0.12) * 0.3;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 35%, rgba(255,82,82,0.1) 0%, rgba(44,57,71,0) 55%)',
        }}
      />

      {/* Warning line texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        <defs>
          <pattern id="warn57" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M0,100 L100,0" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#warn57)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 1100,
          height: 1100,
          border: '2px solid #FAB95B',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `translate(-50%, -50%) scale(${1 + warnPulse * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 80,
          width: 200,
          height: 200,
          border: '2px solid #E8EDF2',
          opacity: 0.06,
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          right: 80,
          width: 250,
          height: 250,
          backgroundColor: '#FAB95B',
          opacity: 0.04,
          borderRadius: 30,
          transform: `rotate(${-frame * 0.15}deg)`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
          }}
        />
      ))}

      <AbsoluteFill style={{ transform: `translate(${shakeX}px, ${shakeY}px)` }}>
        {/* Hero text block */}
        <div
          style={{
            position: 'absolute',
            top: 130,
            left: 0,
            right: 0,
            textAlign: 'center',
          }}
        >
          {/* NONE OF THIS IS */}
          <div
            style={{
              opacity: notOp,
              transform: `scale(${notScale})`,
              fontFamily: 'Inter, Poppins',
              fontSize: 70,
              fontWeight: 700,
              color: '#E8EDF2',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 10,
            }}
          >
            None of this is
          </div>

          {/* AUTOMATIC with strike */}
          <div
            style={{
              opacity: autoOp,
              fontFamily: 'Inter, Poppins',
              fontSize: 240,
              fontWeight: 900,
              color: '#ff5252',
              lineHeight: 1.0,
              letterSpacing: -5,
              position: 'relative',
              display: 'inline-block',
              textShadow: '0 0 30px rgba(255,82,82,0.4)',
            }}
          >
            AUTOMATIC
            {/* Strike */}
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: '50%',
                transform: 'translate(-50%, -50%)',
                height: 14,
                width: `${strikeW}%`,
                backgroundColor: '#ff5252',
                borderRadius: 7,
              }}
            />
          </div>
        </div>

        {/* Two action cards */}
        <div
          style={{
            position: 'absolute',
            top: 620,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 60,
          }}
        >
          {/* Card 1 — KNOW */}
          <div
            style={{
              opacity: act1,
              transform: `translateY(${interpolate(act1, [0, 1], [40, 0])}px)`,
              width: 560,
              padding: '40px 50px',
              background: 'linear-gradient(135deg, rgba(232,237,242,0.08) 0%, rgba(232,237,242,0.02) 100%)',
              border: '3px solid #E8EDF2',
              borderRadius: 20,
              textAlign: 'center',
            }}
          >
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 18 }}>
              <circle cx="12" cy="12" r="10" stroke="#FAB95B" strokeWidth="2.5" />
              <path d="M9.5 9a2.5 2.5 0 0 1 5 0c0 1.5-2.5 2-2.5 3.5" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round" />
              <circle cx="12" cy="17" r="1.2" fill="#FAB95B" />
            </svg>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 700,
                color: '#FAB95B',
                letterSpacing: 5,
                textTransform: 'uppercase',
                marginBottom: 10,
              }}
            >
              Step 1
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 78,
                fontWeight: 900,
                color: '#E8EDF2',
                letterSpacing: -2,
              }}
            >
              KNOW
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 500,
                color: '#E8EDF2',
                opacity: 0.75,
                marginTop: 10,
              }}
            >
              they exist
            </div>
          </div>

          {/* Card 2 — CHOOSE */}
          <div
            style={{
              opacity: act2,
              transform: `translateY(${interpolate(act2, [0, 1], [40, 0])}px)`,
              width: 560,
              padding: '40px 50px',
              background: 'linear-gradient(135deg, rgba(250,185,91,0.18) 0%, rgba(250,185,91,0.04) 100%)',
              border: '3px solid #FAB95B',
              borderRadius: 20,
              textAlign: 'center',
              boxShadow: '0 0 30px rgba(250,185,91,0.15)',
            }}
          >
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 18 }}>
              <path d="M5 12 L10 17 L19 7" stroke="#FAB95B" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 700,
                color: '#FAB95B',
                letterSpacing: 5,
                textTransform: 'uppercase',
                marginBottom: 10,
              }}
            >
              Step 2
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 78,
                fontWeight: 900,
                color: '#FAB95B',
                letterSpacing: -2,
              }}
            >
              CHOOSE
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 500,
                color: '#E8EDF2',
                opacity: 0.75,
                marginTop: 10,
              }}
            >
              to claim them
            </div>
          </div>
        </div>

        {/* Bottom emphasis */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: emphOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 600,
              color: '#9E9E9E',
              letterSpacing: 2,
            }}
          >
            The money is yours — but only if you ask.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene57_NotAutomatic;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene58_YouVsMoney',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene58_YouVsMoney: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // YOU box
  const youOp = interpolate(sp(8), [0, 1], [0, 1]);
  const youX = interpolate(sp(8), [0, 1], [-200, 0]);

  // MONEY box
  const moneyOp = interpolate(sp(8), [0, 1], [0, 1]);
  const moneyX = interpolate(sp(8), [0, 1], [200, 0]);

  // Center barrier "CHOICE"
  const choiceScale = sp(60, { damping: 9, stiffness: 110 });
  const choiceOp = interpolate(sp(60), [0, 1], [0, 1]);

  // Bridge connector
  const bridgeW = interpolate(sp(100), [0, 1], [0, 100]);

  // CTA subscribe
  const ctaOp = interpolate(sp(140), [0, 1], [0, 1]);
  const ctaY = interpolate(sp(140), [0, 1], [40, 0]);

  // Camera subtle
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.03]);

  // Particles
  const particles = Array.from({ length: 20 }, (_, i) => {
    const seed = i + 40;
    const x = seededRandom(seed) * 1920;
    const y = (seededRandom(seed * 2) * 1080 + frame * 0.4) % 1080;
    const size = seededRandom(seed * 3) * 4 + 2;
    const op = seededRandom(seed * 4) * 0.4 + 0.2;
    return { x, y, size, op };
  });

  // Choice glow
  const choiceGlow = 0.4 + Math.sin(frame * 0.1) * 0.25;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(90deg, rgba(232,237,242,0.06) 0%, rgba(250,185,91,0.15) 50%, rgba(232,237,242,0.06) 100%)',
        }}
      />

      {/* Crosshair grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        <defs>
          <pattern id="cross58" width="120" height="120" patternUnits="userSpaceOnUse">
            <path d="M60 0 L60 120 M0 60 L120 60" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross58)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 1300,
          height: 1300,
          border: '2px solid #FAB95B',
          borderRadius: '50%',
          opacity: 0.04,
          transform: `translate(-50%, -50%) rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 600,
          height: 600,
          border: '1px solid #E8EDF2',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `translate(-50%, -50%)`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
            boxShadow: '0 0 8px #FAB95B',
          }}
        />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top header */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            ── Only One Thing Stands Between ──
          </div>
        </div>

        {/* YOU box LEFT */}
        <div
          style={{
            position: 'absolute',
            top: 340,
            left: 110,
            width: 580,
            height: 380,
            padding: 50,
            background: 'linear-gradient(135deg, rgba(232,237,242,0.12) 0%, rgba(232,237,242,0.03) 100%)',
            border: '4px solid #E8EDF2',
            borderRadius: 24,
            opacity: youOp,
            transform: `translateX(${youX}px)`,
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
          }}
        >
          {/* Person icon */}
          <svg width="120" height="120" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 20 }}>
            <circle cx="12" cy="7" r="4" stroke="#E8EDF2" strokeWidth="2.5" />
            <path d="M4 21 C 4 16 8 14 12 14 C 16 14 20 16 20 21" stroke="#E8EDF2" strokeWidth="2.5" strokeLinecap="round" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 160,
              fontWeight: 900,
              color: '#E8EDF2',
              letterSpacing: -3,
              lineHeight: 0.9,
            }}
          >
            YOU
          </div>
        </div>

        {/* CHOICE in center */}
        <div
          style={{
            position: 'absolute',
            top: 410,
            left: '50%',
            transform: `translateX(-50%) scale(${choiceScale})`,
            opacity: choiceOp,
            textAlign: 'center',
          }}
        >
          {/* Bridge line */}
          <div
            style={{
              position: 'absolute',
              top: 120,
              left: '50%',
              transform: 'translateX(-50%)',
              width: `${bridgeW * 3.2}px`,
              maxWidth: 320,
              height: 6,
              background: 'linear-gradient(90deg, #E8EDF2 0%, #FAB95B 50%, #E8EDF2 100%)',
              borderRadius: 3,
              boxShadow: '0 0 15px rgba(250,185,91,0.5)',
            }}
          />
          <div
            style={{
              width: 240,
              height: 240,
              borderRadius: '50%',
              backgroundColor: '#2C3947',
              border: '5px solid #FAB95B',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: `0 0 ${30 + choiceGlow * 45}px rgba(250,185,91,${choiceGlow})`,
              flexDirection: 'column',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 24,
                fontWeight: 700,
                color: '#FAB95B',
                letterSpacing: 4,
                textTransform: 'uppercase',
                marginBottom: 6,
              }}
            >
              The
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 68,
                fontWeight: 900,
                color: '#FAB95B',
                letterSpacing: -1,
              }}
            >
              CHOICE
            </div>
          </div>
        </div>

        {/* MONEY box RIGHT */}
        <div
          style={{
            position: 'absolute',
            top: 340,
            right: 110,
            width: 580,
            height: 380,
            padding: 50,
            background: 'linear-gradient(135deg, rgba(250,185,91,0.2) 0%, rgba(250,185,91,0.05) 100%)',
            border: '4px solid #FAB95B',
            borderRadius: 24,
            opacity: moneyOp,
            transform: `translateX(${moneyX}px)`,
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            boxShadow: '0 0 40px rgba(250,185,91,0.2)',
          }}
        >
          {/* Stack of bills icon */}
          <svg width="120" height="120" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 20 }}>
            <rect x="3" y="6" width="18" height="12" rx="2" stroke="#FAB95B" strokeWidth="2.5" />
            <circle cx="12" cy="12" r="3" stroke="#FAB95B" strokeWidth="2.5" />
            <path d="M3 9 L21 9 M3 15 L21 15" stroke="#FAB95B" strokeWidth="1.5" opacity="0.5" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 160,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: -3,
              lineHeight: 0.9,
              textShadow: '0 0 25px rgba(250,185,91,0.4)',
            }}
          >
            MONEY
          </div>
        </div>

        {/* CTA bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: ctaOp,
            transform: `translateY(${ctaY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 46,
              fontWeight: 700,
              color: '#E8EDF2',
            }}
          >
            If this helped — please{' '}
            <span
              style={{
                color: '#FAB95B',
                fontWeight: 900,
                padding: '6px 22px',
                border: '3px solid #FAB95B',
                borderRadius: 10,
                display: 'inline-block',
                marginLeft: 8,
              }}
            >
              SUBSCRIBE
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene58_YouVsMoney;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene59_PassItAlong',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene59_PassItAlong: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header
  const headOp = interpolate(sp(5), [0, 1], [0, 1]);
  const headY = interpolate(sp(5), [0, 1], [40, 0]);

  // Four cards staggered reveal
  const c1 = sp(40);
  const c2 = sp(60);
  const c3 = sp(80);
  const c4 = sp(100);

  // Connecting line draw
  const lineW = interpolate(sp(130), [0, 1], [0, 100]);

  // Bottom CTA
  const ctaOp = interpolate(sp(155), [0, 1], [0, 1]);

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => {
    const seed = i + 50;
    const x = (seededRandom(seed) * 1920 + frame * 0.3) % 1920;
    const y = seededRandom(seed * 2) * 1080;
    const size = seededRandom(seed * 3) * 4 + 2;
    const op = seededRandom(seed * 4) * 0.4 + 0.2;
    return { x, y, size, op };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(250,185,91,0.1) 0%, rgba(44,57,71,0) 60%)',
        }}
      />

      {/* Soft dot grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="dot59" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#E8EDF2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dot59)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: -250,
          left: '50%',
          width: 900,
          height: 900,
          border: '2px solid #FAB95B',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `translateX(-50%) rotate(${frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -300,
          left: 100,
          width: 500,
          height: 500,
          backgroundColor: '#E8EDF2',
          opacity: 0.04,
          borderRadius: '50%',
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -250,
          right: 80,
          width: 400,
          height: 400,
          backgroundColor: '#FAB95B',
          opacity: 0.05,
          borderRadius: 40,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
            boxShadow: '0 0 8px #FAB95B',
          }}
        />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headOp,
            transform: `translateY(${headY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 6,
              textTransform: 'uppercase',
              marginBottom: 14,
            }}
          >
            Share with someone turning 65
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 120,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1.0,
              letterSpacing: -3,
            }}
          >
            PASS IT <span style={{ color: '#FAB95B' }}>ALONG</span>
          </div>
        </div>

        {/* Connecting horizontal line behind cards */}
        <div
          style={{
            position: 'absolute',
            top: 620,
            left: '50%',
            transform: 'translateX(-50%)',
            width: `${lineW * 14.5}px`,
            maxWidth: 1450,
            height: 4,
            background: 'linear-gradient(90deg, transparent 0%, #FAB95B 20%, #FAB95B 80%, transparent 100%)',
            opacity: 0.5,
          }}
        />

        {/* Four cards row */}
        <div
          style={{
            position: 'absolute',
            top: 460,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 50,
          }}
        >
          {[
            {
              label: 'HUSBAND',
              d: c1,
              accent: false,
              icon: (
                <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
                  <circle cx="12" cy="7" r="4" stroke="#E8EDF2" strokeWidth="2.5" />
                  <path d="M4 21 C 4 16 8 14 12 14 C 16 14 20 16 20 21" stroke="#E8EDF2" strokeWidth="2.5" strokeLinecap="round" />
                </svg>
              ),
            },
            {
              label: 'WIFE',
              d: c2,
              accent: true,
              icon: (
                <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
                  <circle cx="12" cy="7" r="4" stroke="#FAB95B" strokeWidth="2.5" />
                  <path d="M4 21 C 4 16 8 14 12 14 C 16 14 20 16 20 21" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round" />
                </svg>
              ),
            },
            {
              label: 'FRIEND',
              d: c3,
              accent: false,
              icon: (
                <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
                  <circle cx="9" cy="8" r="3.5" stroke="#E8EDF2" strokeWidth="2.5" />
                  <circle cx="17" cy="9" r="3" stroke="#E8EDF2" strokeWidth="2.5" />
                  <path d="M3 20 C 3 16 6 14 9 14 C 12 14 15 16 15 20" stroke="#E8EDF2" strokeWidth="2.5" strokeLinecap="round" />
                  <path d="M15 20 C 15 17 18 16 17 16 C 20 16 22 17 22 20" stroke="#E8EDF2" strokeWidth="2.5" strokeLinecap="round" />
                </svg>
              ),
            },
            {
              label: 'NEIGHBOR',
              d: c4,
              accent: true,
              icon: (
                <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
                  <path d="M3 11 L12 4 L21 11 L21 20 L14 20 L14 14 L10 14 L10 20 L3 20 Z" stroke="#FAB95B" strokeWidth="2.5" strokeLinejoin="round" />
                </svg>
              ),
            },
          ].map((card, i) => (
            <div
              key={i}
              style={{
                opacity: card.d,
                transform: `translateY(${interpolate(card.d, [0, 1], [60, 0])}px) scale(${card.d})`,
                width: 290,
                height: 320,
                padding: 30,
                background: card.accent
                  ? 'linear-gradient(160deg, rgba(250,185,91,0.18) 0%, rgba(250,185,91,0.04) 100%)'
                  : 'linear-gradient(160deg, rgba(232,237,242,0.1) 0%, rgba(232,237,242,0.02) 100%)',
                border: `3px solid ${card.accent ? '#FAB95B' : '#E8EDF2'}`,
                borderRadius: 18,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                gap: 22,
                boxShadow: card.accent ? '0 0 25px rgba(250,185,91,0.15)' : 'none',
              }}
            >
              {card.icon}
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 56,
                  fontWeight: 900,
                  color: card.accent ? '#FAB95B' : '#E8EDF2',
                  letterSpacing: -1,
                }}
              >
                {card.label}
              </div>
            </div>
          ))}
        </div>

        {/* Bottom CTA */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: ctaOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 600,
              color: '#E8EDF2',
            }}
          >
            One share can change someone's{' '}
            <span style={{ color: '#FAB95B', fontWeight: 900 }}>entire retirement.</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene59_PassItAlong;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene60_FinalSignOff',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene60_FinalSignOff: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // "ONE CONVERSATION" reveal
  const oneScale = sp(8, { damping: 9, stiffness: 110 });
  const oneOp = interpolate(sp(8), [0, 1], [0, 1]);

  // Timeline reveal — decades
  const timelineOp = interpolate(sp(45), [0, 1], [0, 1]);
  const timelineW = interpolate(sp(60), [0, 1], [0, 100]);

  // Year markers
  const yr1 = sp(80);
  const yr2 = sp(95);
  const yr3 = sp(110);
  const yr4 = sp(125);

  // Sign off
  const signScale = sp(155, { damping: 10 });
  const signOp = interpolate(sp(155), [0, 1], [0, 1]);

  // Brand seal
  const sealOp = interpolate(sp(195), [0, 1], [0, 1]);

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // Particles — celebratory
  const particles = Array.from({ length: 30 }, (_, i) => {
    const seed = i + 60;
    const x = seededRandom(seed) * 1920;
    const y = (seededRandom(seed * 2) * 1080 - frame * 0.7 + 1080) % 1080;
    const size = seededRandom(seed * 3) * 6 + 2;
    const op = seededRandom(seed * 4) * 0.5 + 0.2;
    return { x, y, size, op };
  });

  // Shape rotation
  const sRot = frame * 0.1;

  // Pulse glow
  const pulse = 0.4 + Math.sin(frame * 0.1) * 0.25;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 40%, rgba(250,185,91,0.18) 0%, rgba(44,57,71,0) 60%)',
        }}
      />

      {/* Soft grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        <defs>
          <pattern id="grid60" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid60)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 1200,
          height: 1200,
          border: '2px solid #FAB95B',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `translate(-50%, -50%) rotate(${sRot}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 800,
          height: 800,
          border: '1px solid #E8EDF2',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `translate(-50%, -50%) rotate(${-sRot}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 80,
          width: 180,
          height: 180,
          backgroundColor: '#FAB95B',
          opacity: 0.04,
          borderRadius: 30,
          transform: `rotate(${sRot}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          right: 80,
          width: 220,
          height: 220,
          border: '2px solid #E8EDF2',
          opacity: 0.05,
          borderRadius: '50%',
          transform: `translateY(${Math.sin(frame * 0.05) * 15}px)`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
            boxShadow: '0 0 10px #FAB95B',
          }}
        />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Hero text */}
        <div
          style={{
            position: 'absolute',
            top: 130,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: oneOp,
            transform: `scale(${oneScale})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 8,
              textTransform: 'uppercase',
              marginBottom: 18,
            }}
          >
            ── One Conversation ──
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 110,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1.0,
              letterSpacing: -2.5,
            }}
          >
            CHANGES A LIFE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 140,
              fontWeight: 900,
              color: '#FAB95B',
              lineHeight: 1.0,
              letterSpacing: -3,
              textShadow: `0 0 ${20 + pulse * 30}px rgba(250,185,91,${pulse})`,
              marginTop: 4,
            }}
          >
            FOR DECADES.
          </div>
        </div>

        {/* Timeline */}
        <div
          style={{
            position: 'absolute',
            top: 600,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            opacity: timelineOp,
          }}
        >
          <div style={{ position: 'relative', width: 1300, height: 100 }}>
            {/* Line */}
            <div
              style={{
                position: 'absolute',
                top: 50,
                left: 0,
                width: `${timelineW}%`,
                height: 4,
                background: 'linear-gradient(90deg, #E8EDF2 0%, #FAB95B 50%, #E8EDF2 100%)',
                borderRadius: 2,
              }}
            />
            {/* Year nodes */}
            {[
              { label: 'TODAY', x: 0, d: yr1, accent: true },
              { label: '+10 YRS', x: 33, d: yr2, accent: false },
              { label: '+20 YRS', x: 66, d: yr3, accent: false },
              { label: '+30 YRS', x: 100, d: yr4, accent: true },
            ].map((n, i) => (
              <div
                key={i}
                style={{
                  position: 'absolute',
                  top: 0,
                  left: `${n.x}%`,
                  transform: 'translateX(-50%)',
                  opacity: n.d,
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                }}
              >
                <div
                  style={{
                    width: n.accent ? 32 : 22,
                    height: n.accent ? 32 : 22,
                    borderRadius: '50%',
                    backgroundColor: n.accent ? '#FAB95B' : '#E8EDF2',
                    border: '3px solid #2C3947',
                    boxShadow: n.accent ? '0 0 15px rgba(250,185,91,0.6)' : 'none',
                    marginTop: n.accent ? 34 : 39,
                  }}
                />
                <div
                  style={{
                    marginTop: 18,
                    fontFamily: 'Inter, Poppins',
                    fontSize: 26,
                    fontWeight: 700,
                    color: n.accent ? '#FAB95B' : '#E8EDF2',
                    letterSpacing: 2,
                  }}
                >
                  {n.label}
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Sign off */}
        <div
          style={{
            position: 'absolute',
            bottom: 200,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: signOp,
            transform: `scale(${signScale})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 64,
              fontWeight: 700,
              color: '#E8EDF2',
              letterSpacing: -0.5,
            }}
          >
            See you in the <span style={{ color: '#FAB95B', fontWeight: 900 }}>next one.</span>
          </div>
        </div>

        {/* Brand seal */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sealOp,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '12px 36px',
              border: '2px solid #FAB95B',
              borderRadius: 30,
              fontFamily: 'Inter, Poppins',
              fontSize: 24,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 6,
              textTransform: 'uppercase',
            }}
          >
            Senior Fund USA
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene60_FinalSignOff;
```

