```tsx
import React from 'react';
Audio: "Les propriétaires qui souffrent aujourd'hui sont ceux qui n'avaient jamais intégré la performance énergétique dans leur réflexion d'achat ou de gestion."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene51',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene51: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleY = interpolate(sp(10), [0, 1], [-80, 0]);
  const titleOp = interpolate(sp(10), [0, 1], [0, 1]);

  const lineGrow = interpolate(frame, [40, 90], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  
  const sufferOp = interpolate(sp(60), [0, 1], [0, 1]);
  const sufferScale = sp(60);
  
  const arrowProgress = interpolate(frame, [110, 170], [0, 1], { extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  
  const causeOp = interpolate(sp(180), [0, 1], [0, 1]);
  const causeScale = sp(180);

  const highlightProgress = interpolate(frame, [240, 320], [0, 1], { extrapolateRight: 'clamp' });

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(251,245,167,0.4) 0%, transparent 60%)',
      }} />

      {/* Shape Layers */}
      {[0, 1, 2, 3, 4].map((i) => {
        const seed = seededRandom(i + 51);
        const size = 300 + seed * 400;
        const x = seededRandom(i * 3) * 1920;
        const y = seededRandom(i * 7) * 1080;
        const rot = frame * 0.1 + seed * 360;
        const breathe = 1 + Math.sin(frame * 0.02 + i) * 0.05;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid #1E2022`,
            borderRadius: i % 2 === 0 ? '50%' : '0%',
            opacity: 0.06,
            transform: `rotate(${rot}deg) scale(${breathe})`,
          }} />
        );
      })}

      {/* Grid texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.08 }}>
        <defs>
          <pattern id="grid51" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#1E2022" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid51)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = seededRandom(i + 100);
        const x = (seededRandom(i * 5) * 1920 + frame * 0.5) % 1920;
        const y = seededRandom(i * 11) * 1080 + Math.sin(frame * 0.03 + i) * 20;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 6,
            height: 6,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.3 + seed * 0.3,
          }} />
        );
      })}

      {/* Main content wrapper with camera zoom */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>
        
        {/* Top Title */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 100,
            color: '#1E2022',
            letterSpacing: 4,
            textTransform: 'uppercase',
            lineHeight: 1,
          }}>
            POURQUOI ILS SOUFFRENT
          </div>
          <div style={{
            width: `${lineGrow * 600}px`,
            height: 6,
            backgroundColor: '#1E2022',
            margin: '20px auto 0',
          }} />
        </div>

        {/* Central diagram: SUFFERING => CAUSE */}
        <div style={{
          position: 'absolute',
          top: 320,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 80,
        }}>
          {/* Left block: SOUFFRENT */}
          <div style={{
            opacity: sufferOp,
            transform: `scale(${sufferScale})`,
            width: 520,
            padding: '50px 40px',
            backgroundColor: '#1E2022',
            borderRadius: 20,
            border: '4px solid #1E2022',
            position: 'relative',
          }}>
            {/* Icon: broken house */}
            <svg width="100" height="100" viewBox="0 0 100 100" style={{ display: 'block', margin: '0 auto 20px' }}>
              <path d="M 20 50 L 50 20 L 80 50 L 80 85 L 20 85 Z" fill="none" stroke="#FBF5A7" strokeWidth="4" />
              <path d="M 35 85 L 35 60 L 50 60 L 50 85" fill="none" stroke="#FBF5A7" strokeWidth="4" />
              <path d="M 60 50 L 70 75" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
              <path d="M 70 50 L 60 75" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
            </svg>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 72,
              color: '#FBF5A7',
              textAlign: 'center',
              letterSpacing: 2,
              textTransform: 'uppercase',
            }}>
              Souffrent
            </div>
            <div style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 38,
              color: '#FBF5A7',
              textAlign: 'center',
              marginTop: 15,
              fontWeight: 500,
              opacity: 0.8,
            }}>
              aujourd'hui
            </div>
          </div>

          {/* Arrow */}
          <svg width="180" height="80" viewBox="0 0 180 80">
            <defs>
              <marker id="ah51" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
                <path d="M 0 0 L 10 5 L 0 10 z" fill="#1E2022" />
              </marker>
            </defs>
            <line
              x1="10"
              y1="40"
              x2={10 + arrowProgress * 160}
              y2="40"
              stroke="#1E2022"
              strokeWidth="8"
              strokeLinecap="round"
              markerEnd={arrowProgress > 0.95 ? 'url(#ah51)' : undefined}
            />
            <text x="90" y="25" textAnchor="middle" fontFamily="Bricolage Grotesque, sans-serif" fontSize="30" fill="#1E2022" fontWeight="700" opacity={arrowProgress}>
              CAUSE
            </text>
          </svg>

          {/* Right block: IGNORED ENERGY */}
          <div style={{
            opacity: causeOp,
            transform: `scale(${causeScale})`,
            width: 520,
            padding: '50px 40px',
            backgroundColor: '#FBF5A7',
            borderRadius: 20,
            border: '4px solid #1E2022',
          }}>
            <svg width="100" height="100" viewBox="0 0 100 100" style={{ display: 'block', margin: '0 auto 20px' }}>
              <path d="M 50 15 L 30 55 L 45 55 L 35 85 L 70 45 L 55 45 Z" fill="#1E2022" stroke="#1E2022" strokeWidth="3" />
              <line x1="15" y1="15" x2="85" y2="85" stroke="#ff0000" strokeWidth="6" strokeLinecap="round" />
            </svg>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 60,
              color: '#1E2022',
              textAlign: 'center',
              letterSpacing: 1,
              textTransform: 'uppercase',
              lineHeight: 1.1,
            }}>
              ÉNERGIE
            </div>
            <div style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 32,
              color: '#1E2022',
              textAlign: 'center',
              marginTop: 15,
              fontWeight: 700,
            }}>
              jamais considérée
            </div>
          </div>
        </div>

        {/* Bottom highlight strip */}
        <div style={{
          position: 'absolute',
          bottom: 130,
          left: '50%',
          transform: `translateX(-50%) scaleX(${highlightProgress})`,
          transformOrigin: 'center',
          backgroundColor: '#1E2022',
          padding: '25px 60px',
          borderRadius: 12,
        }}>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 42,
            color: '#F4CE14',
            fontWeight: 700,
            letterSpacing: 1,
          }}>
            Ni à l'achat, ni dans la gestion
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.35) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#1E2022',
          opacity: 0.5,
        }}>
          Matures Finance
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene51;
```

```tsx
import React from 'react';
Audio: "Cette discipline de l'anticipation, vous pouvez l'appliquer partout dans votre patrimoine. C'est la même logique qui doit guider votre épargne longue."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene52',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene52: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headlineOp = interpolate(sp(10), [0, 1], [0, 1]);
  const headlineY = interpolate(sp(10), [0, 1], [40, 0]);

  const radarRotate = frame * 1.2;
  const radarScale = sp(40);

  const dotDelays = [80, 110, 140, 170, 200];
  
  const subtextOp = interpolate(sp(280), [0, 1], [0, 1]);
  const subtextScale = sp(280);

  const camPan = interpolate(frame, [0, durationInFrames], [0, -15]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(251,245,167,0.3) 0%, transparent 50%, rgba(30,32,34,0.15) 100%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const seed = seededRandom(i + 200);
        const size = 400 + seed * 350;
        const x = seededRandom(i * 4) * 1920;
        const y = seededRandom(i * 9) * 1080;
        const rot = frame * 0.08 + seed * 180;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `3px solid #1E2022`,
            transform: `rotate(${rot}deg)`,
            opacity: 0.05,
          }} />
        );
      })}

      {/* Circle texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.07 }}>
        <defs>
          <pattern id="dots52" width="60" height="60" patternUnits="userSpaceOnUse">
            <circle cx="30" cy="30" r="2" fill="#1E2022" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots52)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = seededRandom(i + 300);
        const x = (seededRandom(i * 7) * 1920 - frame * 0.4 + 1920) % 1920;
        const y = seededRandom(i * 13) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y + Math.sin(frame * 0.04 + i) * 15,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.25 + seed * 0.25,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>
        
        {/* Headline */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: headlineOp,
          transform: `translateY(${headlineY}px)`,
        }}>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 38,
            color: '#1E2022',
            letterSpacing: 8,
            fontWeight: 600,
            marginBottom: 10,
            opacity: 0.7,
          }}>
            DISCIPLINE UNIVERSELLE
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 130,
            color: '#1E2022',
            letterSpacing: 2,
            lineHeight: 0.95,
            textTransform: 'uppercase',
          }}>
            ANTICIPER
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 130,
            color: '#FBF5A7',
            letterSpacing: 2,
            lineHeight: 0.95,
            textTransform: 'uppercase',
            WebkitTextStroke: '3px #1E2022',
          }}>
            PARTOUT
          </div>
        </div>

        {/* Central radar with patrimoine domains */}
        <div style={{
          position: 'absolute',
          top: 500,
          left: '50%',
          transform: `translateX(-50%) scale(${radarScale})`,
          width: 520,
          height: 520,
        }}>
          {/* Radar circles */}
          <svg width="520" height="520" viewBox="0 0 520 520">
            <circle cx="260" cy="260" r="240" fill="none" stroke="#1E2022" strokeWidth="2" opacity="0.3" />
            <circle cx="260" cy="260" r="170" fill="none" stroke="#1E2022" strokeWidth="2" opacity="0.4" />
            <circle cx="260" cy="260" r="100" fill="none" stroke="#1E2022" strokeWidth="2" opacity="0.5" />
            <circle cx="260" cy="260" r="40" fill="#1E2022" />
            
            {/* Rotating radar sweep */}
            <g transform={`rotate(${radarRotate} 260 260)`}>
              <path d="M 260 260 L 500 260 A 240 240 0 0 1 260 500 Z" fill="#1E2022" opacity="0.12" />
            </g>

            {/* Center text */}
            <text x="260" y="270" textAnchor="middle" fontFamily="Anton, sans-serif" fontSize="28" fill="#F4CE14" letterSpacing="2">
              VOUS
            </text>
          </svg>

          {/* Domain dots */}
          {[
            { label: 'IMMOBILIER', angle: -90, delay: dotDelays[0] },
            { label: 'ACTIONS', angle: -18, delay: dotDelays[1] },
            { label: 'RETRAITE', angle: 54, delay: dotDelays[2] },
            { label: 'FISCALITÉ', angle: 126, delay: dotDelays[3] },
            { label: 'ÉPARGNE', angle: 198, delay: dotDelays[4] },
          ].map((d, i) => {
            const dotOp = interpolate(sp(d.delay), [0, 1], [0, 1]);
            const dotScale = sp(d.delay);
            const rad = (d.angle * Math.PI) / 180;
            const x = 260 + Math.cos(rad) * 200;
            const y = 260 + Math.sin(rad) * 200;
            const pulse = 1 + Math.sin(frame * 0.1 + i) * 0.1;
            return (
              <div key={i} style={{
                position: 'absolute',
                left: x - 70,
                top: y - 25,
                width: 140,
                opacity: dotOp,
                transform: `scale(${dotScale * pulse})`,
                textAlign: 'center',
              }}>
                <div style={{
                  width: 18,
                  height: 18,
                  borderRadius: '50%',
                  backgroundColor: '#ff0000',
                  margin: '0 auto 6px',
                  boxShadow: '0 0 15px #ff0000',
                }} />
                <div style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 24,
                  color: '#1E2022',
                  letterSpacing: 1,
                  backgroundColor: '#FBF5A7',
                  padding: '4px 10px',
                  borderRadius: 6,
                  border: '2px solid #1E2022',
                }}>
                  {d.label}
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom subtext */}
        <div style={{
          position: 'absolute',
          bottom: 90,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: subtextOp,
          transform: `scale(${subtextScale})`,
        }}>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 44,
            color: '#1E2022',
            fontWeight: 700,
            fontStyle: 'italic',
          }}>
            Même logique → toute votre épargne longue
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#1E2022',
          opacity: 0.5,
        }}>
          Matures Finance
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene52;
```

```tsx
import React from 'react';
Audio: "Un patrimoine équilibré combine l'immobilier avec une épargne financière. Le PEA permet d'investir en actions européennes avec une fiscalité allégée après cinq ans."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene53',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene53: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = interpolate(sp(8), [0, 1], [0, 1]);
  const titleY = interpolate(sp(8), [0, 1], [-50, 0]);

  // Balance scale animation
  const tiltAmplitude = Math.sin(frame * 0.04) * 4;
  const balanceScale = sp(40);

  // PEA card
  const peaOp = interpolate(sp(150), [0, 1], [0, 1]);
  const peaY = interpolate(sp(150), [0, 1], [60, 0]);

  // Counter for years
  const yearCount = Math.min(5, Math.floor(interpolate(frame, [220, 290], [0, 5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })));

  const camZoom = interpolate(frame, [0, durationInFrames], [1.02, 1.05]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 70% 30%, rgba(251,245,167,0.4) 0%, transparent 55%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const seed = seededRandom(i + 400);
        const size = 350 + seed * 400;
        const x = seededRandom(i * 6) * 1920;
        const y = seededRandom(i * 11) * 1080;
        const breathe = 1 + Math.sin(frame * 0.025 + i) * 0.06;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid #1E2022`,
            borderRadius: i % 3 === 0 ? '50%' : '8px',
            opacity: 0.06,
            transform: `scale(${breathe}) rotate(${frame * 0.05 + i * 30}deg)`,
          }} />
        );
      })}

      {/* Diagonal lines texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.06 }}>
        <defs>
          <pattern id="diag53" width="40" height="40" patternUnits="userSpaceOnUse">
            <line x1="0" y1="40" x2="40" y2="0" stroke="#1E2022" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag53)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 500);
        const x = seededRandom(i * 8) * 1920;
        const y = (seededRandom(i * 14) * 1080 + frame * 0.6) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x + Math.sin(frame * 0.03 + i) * 12,
            top: y,
            width: 4,
            height: 4,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.3 + seed * 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>
        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 110,
            color: '#1E2022',
            letterSpacing: 3,
            textTransform: 'uppercase',
            lineHeight: 1,
          }}>
            PATRIMOINE
          </div>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 50,
            color: '#1E2022',
            fontStyle: 'italic',
            marginTop: 5,
            opacity: 0.85,
          }}>
            équilibré = combiner
          </div>
        </div>

        {/* Balance scale visualization */}
        <div style={{
          position: 'absolute',
          top: 280,
          left: '50%',
          transform: `translateX(-50%) scale(${balanceScale})`,
          width: 1100,
        }}>
          <svg width="1100" height="320" viewBox="0 0 1100 320">
            {/* Fulcrum base */}
            <rect x="540" y="280" width="20" height="40" fill="#1E2022" />
            <polygon points="510,280 590,280 550,200" fill="#1E2022" />
            
            {/* Beam */}
            <g transform={`rotate(${tiltAmplitude} 550 200)`}>
              <rect x="120" y="195" width="860" height="14" fill="#1E2022" rx="7" />
              {/* Left pan strings */}
              <line x1="220" y1="200" x2="220" y2="240" stroke="#1E2022" strokeWidth="3" />
              {/* Right pan strings */}
              <line x1="880" y1="200" x2="880" y2="240" stroke="#1E2022" strokeWidth="3" />
              
              {/* LEFT PAN - IMMOBILIER */}
              <rect x="80" y="240" width="280" height="70" fill="#FBF5A7" stroke="#1E2022" strokeWidth="4" rx="10" />
              <text x="220" y="275" textAnchor="middle" fontFamily="Anton, sans-serif" fontSize="32" fill="#1E2022" letterSpacing="1">
                IMMOBILIER
              </text>
              <g transform="translate(110, 245)">
                <path d="M 0 30 L 20 10 L 40 30 L 40 50 L 0 50 Z" fill="#1E2022" />
              </g>
              
              {/* RIGHT PAN - FINANCIER */}
              <rect x="740" y="240" width="280" height="70" fill="#FBF5A7" stroke="#1E2022" strokeWidth="4" rx="10" />
              <text x="880" y="275" textAnchor="middle" fontFamily="Anton, sans-serif" fontSize="32" fill="#1E2022" letterSpacing="1">
                FINANCIER
              </text>
              <g transform="translate(960, 250)">
                <circle cx="15" cy="20" r="15" fill="none" stroke="#1E2022" strokeWidth="3" />
                <text x="15" y="27" textAnchor="middle" fontFamily="Anton, sans-serif" fontSize="20" fill="#1E2022">€</text>
              </g>
            </g>
          </svg>
        </div>

        {/* PEA highlight card */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: '50%',
          transform: `translateX(-50%) translateY(${peaY}px)`,
          opacity: peaOp,
          backgroundColor: '#1E2022',
          padding: '30px 50px',
          borderRadius: 16,
          display: 'flex',
          alignItems: 'center',
          gap: 35,
          border: '4px solid #FBF5A7',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 95,
            color: '#F4CE14',
            letterSpacing: 3,
            lineHeight: 1,
          }}>
            PEA
          </div>
          <div style={{ width: 4, height: 90, backgroundColor: '#FBF5A7' }} />
          <div>
            <div style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 30,
              color: '#FBF5A7',
              fontWeight: 600,
              marginBottom: 4,
            }}>
              Actions européennes
            </div>
            <div style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 38,
              color: '#F4CE14',
              fontWeight: 700,
            }}>
              Fiscalité allégée → {yearCount} ans
            </div>
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.32) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#1E2022',
          opacity: 0.5,
        }}>
          Matures Finance
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene53;
```

```tsx
import React from 'react';
Audio: "L'assurance-vie reste un outil souple pour fructifier et transmettre. Le PER offre un avantage fiscal à l'entrée pour ceux qui sont fortement imposés."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene54',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene54: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headOp = interpolate(sp(8), [0, 1], [0, 1]);
  const headScale = sp(8);

  // Card 1 - Assurance Vie
  const card1Op = interpolate(sp(50), [0, 1], [0, 1]);
  const card1X = interpolate(sp(50), [0, 1], [-200, 0]);

  // Card 2 - PER
  const card2Op = interpolate(sp(140), [0, 1], [0, 1]);
  const card2X = interpolate(sp(140), [0, 1], [200, 0]);

  // VS in middle
  const vsOp = interpolate(sp(220), [0, 1], [0, 1]);
  const vsRotate = Math.sin(frame * 0.08) * 5;

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.03]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, transparent 0%, rgba(251,245,167,0.25) 100%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3, 4].map((i) => {
        const seed = seededRandom(i + 600);
        const size = 280 + seed * 380;
        const x = seededRandom(i * 5) * 1920;
        const y = seededRandom(i * 9) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid #1E2022`,
            transform: `rotate(${frame * 0.06 + seed * 360}deg)`,
            opacity: 0.05,
            borderRadius: i % 2 === 0 ? '20px' : '50%',
          }} />
        );
      })}

      {/* Hexagonal texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.06 }}>
        <defs>
          <pattern id="cross54" width="50" height="50" patternUnits="userSpaceOnUse">
            <line x1="25" y1="0" x2="25" y2="50" stroke="#1E2022" strokeWidth="1" />
            <line x1="0" y1="25" x2="50" y2="25" stroke="#1E2022" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#cross54)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const seed = seededRandom(i + 700);
        const x = seededRandom(i * 6) * 1920;
        const y = (seededRandom(i * 12) * 1080 - frame * 0.5 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.3 + seed * 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>
        {/* Headline */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: headOp,
          transform: `scale(${headScale})`,
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 110,
            color: '#1E2022',
            letterSpacing: 2,
            textTransform: 'uppercase',
            lineHeight: 1,
          }}>
            DEUX OUTILS
          </div>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 42,
            color: '#1E2022',
            marginTop: 8,
            fontWeight: 600,
            opacity: 0.8,
          }}>
            stratégies complémentaires
          </div>
        </div>

        {/* Two big cards side by side */}
        <div style={{
          position: 'absolute',
          top: 320,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 50,
        }}>
          {/* CARD 1 - ASSURANCE-VIE */}
          <div style={{
            opacity: card1Op,
            transform: `translateX(${card1X}px)`,
            width: 640,
            height: 540,
            backgroundColor: '#FBF5A7',
            border: '5px solid #1E2022',
            borderRadius: 24,
            padding: 40,
            position: 'relative',
            overflow: 'hidden',
          }}>
            {/* Corner ribbon */}
            <div style={{
              position: 'absolute',
              top: 0,
              right: 0,
              backgroundColor: '#1E2022',
              padding: '8px 20px',
              borderBottomLeftRadius: 16,
            }}>
              <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 24, color: '#F4CE14', letterSpacing: 1 }}>
                SOUPLE
              </span>
            </div>

            {/* Icon */}
            <svg width="90" height="90" viewBox="0 0 90 90" style={{ marginBottom: 15 }}>
              <path d="M 45 12 L 75 25 L 75 50 C 75 65 60 78 45 82 C 30 78 15 65 15 50 L 15 25 Z" fill="none" stroke="#1E2022" strokeWidth="5" />
              <path d="M 30 45 L 42 55 L 60 35" fill="none" stroke="#1E2022" strokeWidth="6" strokeLinecap="round" strokeLinejoin="round" />
            </svg>

            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 70,
              color: '#1E2022',
              letterSpacing: 1,
              lineHeight: 0.95,
            }}>
              ASSURANCE
            </div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 70,
              color: '#1E2022',
              letterSpacing: 1,
              lineHeight: 0.95,
              marginBottom: 25,
            }}>
              -VIE
            </div>

            <div style={{ height: 4, backgroundColor: '#1E2022', width: '60%', marginBottom: 20 }} />

            <div style={{ display: 'flex', alignItems: 'center', gap: 15, marginBottom: 16 }}>
              <div style={{ width: 14, height: 14, backgroundColor: '#1E2022', borderRadius: '50%' }} />
              <span style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 32, color: '#1E2022', fontWeight: 700 }}>
                Fructifier l'épargne
              </span>
            </div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 15 }}>
              <div style={{ width: 14, height: 14, backgroundColor: '#1E2022', borderRadius: '50%' }} />
              <span style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 32, color: '#1E2022', fontWeight: 700 }}>
                Préparer transmission
              </span>
            </div>
          </div>

          {/* VS BADGE */}
          <div style={{
            opacity: vsOp,
            transform: `rotate(${vsRotate}deg)`,
            width: 110,
            height: 110,
            borderRadius: '50%',
            backgroundColor: '#ff0000',
            border: '5px solid #1E2022',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            position: 'absolute',
            left: '50%',
            top: '50%',
            marginLeft: -55,
            marginTop: -55,
            zIndex: 10,
          }}>
            <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 48, color: '#FBF5A7', letterSpacing: 2 }}>
              +
            </span>
          </div>

          {/* CARD 2 - PER */}
          <div style={{
            opacity: card2Op,
            transform: `translateX(${card2X}px)`,
            width: 640,
            height: 540,
            backgroundColor: '#1E2022',
            border: '5px solid #1E2022',
            borderRadius: 24,
            padding: 40,
            position: 'relative',
            overflow: 'hidden',
          }}>
            {/* Corner ribbon */}
            <div style={{
              position: 'absolute',
              top: 0,
              right: 0,
              backgroundColor: '#FBF5A7',
              padding: '8px 20px',
              borderBottomLeftRadius: 16,
            }}>
              <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 24, color: '#1E2022', letterSpacing: 1 }}>
                FISCAL
              </span>
            </div>

            {/* Icon */}
            <svg width="90" height="90" viewBox="0 0 90 90" style={{ marginBottom: 15 }}>
              <rect x="15" y="25" width="60" height="50" rx="6" fill="none" stroke="#FBF5A7" strokeWidth="5" />
              <line x1="15" y1="40" x2="75" y2="40" stroke="#FBF5A7" strokeWidth="5" />
              <circle cx="65" cy="55" r="6" fill="#F4CE14" />
              <path d="M 30 15 L 30 35 M 60 15 L 60 35" stroke="#FBF5A7" strokeWidth="5" strokeLinecap="round" />
            </svg>

            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 130,
              color: '#F4CE14',
              letterSpacing: 6,
              lineHeight: 0.9,
              marginBottom: 8,
            }}>
              PER
            </div>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 28,
              color: '#FBF5A7',
              marginBottom: 25,
              fontStyle: 'italic',
            }}>
              Plan d'Épargne Retraite
            </div>

            <div style={{ height: 4, backgroundColor: '#FBF5A7', width: '60%', marginBottom: 20 }} />

            <div style={{ display: 'flex', alignItems: 'center', gap: 15, marginBottom: 16 }}>
              <div style={{ width: 14, height: 14, backgroundColor: '#F4CE14', borderRadius: '50%' }} />
              <span style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 30, color: '#FBF5A7', fontWeight: 700 }}>
                Déduction à l'entrée
              </span>
            </div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 15 }}>
              <div style={{ width: 14, height: 14, backgroundColor: '#F4CE14', borderRadius: '50%' }} />
              <span style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 30, color: '#FBF5A7', fontWeight: 700 }}>
                Idéal si fortement imposé
              </span>
            </div>
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#1E2022',
          opacity: 0.5,
        }}>
          Matures Finance
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene54;
```

```tsx
import React from 'react';
Audio: "L'investissement régulier en ETF, ces fonds indiciels à frais réduits, est souvent la colonne vertébrale d'une stratégie patrimoniale moderne sur le long terme."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene55',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene55: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = interpolate(sp(8), [0, 1], [0, 1]);
  const titleY = interpolate(sp(8), [0, 1], [-50, 0]);

  // Growth chart progressive draw
  const chartProgress = interpolate(frame, [50, 380], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  
  // Counter
  const valueCount = Math.floor(interpolate(frame, [50, 380], [10000, 187000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Bottom etf badge
  const badgeOp = interpolate(sp(200), [0, 1], [0, 1]);
  const badgeScale = sp(200);

  // ETF label dots on chart
  const dotMonths = ['2026', '2030', '2035', '2040', '2045'];

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  // Build growth curve points
  const buildCurve = () => {
    const points = [];
    const totalPoints = 80;
    for (let i = 0; i <= totalPoints; i++) {
      const t = i / totalPoints;
      const x = 100 + t * 1200;
      // Exponential growth curve
      const y = 500 - Math.pow(t, 1.8) * 400 - i * 0.2;
      points.push({ x, y });
    }
    return points;
  };
  const curvePoints = buildCurve();
  const visiblePoints = Math.floor(curvePoints.length * chartProgress);
  const pathD = curvePoints.slice(0, Math.max(1, visiblePoints)).map((p, i) => `${i === 0 ? 'M' : 'L'} ${p.x} ${p.y}`).join(' ');

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 80% 80%, rgba(251,245,167,0.35) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const seed = seededRandom(i + 800);
        const size = 350 + seed * 350;
        const x = seededRandom(i * 7) * 1920;
        const y = seededRandom(i * 13) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid #1E2022`,
            transform: `rotate(${frame * 0.08 + seed * 200}deg) scale(${1 + Math.sin(frame * 0.03 + i) * 0.05})`,
            opacity: 0.05,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Grid texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.07 }}>
        <defs>
          <pattern id="grid55" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#1E2022" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid55)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 900);
        const x = (seededRandom(i * 5) * 1920 + frame * 0.4) % 1920;
        const y = seededRandom(i * 11) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y + Math.cos(frame * 0.05 + i) * 18,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.3 + seed * 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>
        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 80,
          right: 80,
          display: 'flex',
          justifyContent: 'space-between',
          alignItems: 'flex-start',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 150,
              color: '#1E2022',
              letterSpacing: 8,
              lineHeight: 0.9,
            }}>
              ETF
            </div>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 38,
              color: '#1E2022',
              fontWeight: 600,
              marginTop: 5,
            }}>
              colonne vertébrale du patrimoine
            </div>
          </div>
          <div style={{
            backgroundColor: '#1E2022',
            padding: '20px 30px',
            borderRadius: 14,
            textAlign: 'right',
          }}>
            <div style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 22,
              color: '#FBF5A7',
              fontWeight: 600,
              letterSpacing: 2,
            }}>
              VALEUR PORTEFEUILLE
            </div>
            <div style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 56,
              color: '#F4CE14',
              fontWeight: 700,
            }}>
              {valueCount.toLocaleString('fr-FR')} €
            </div>
          </div>
        </div>

        {/* Main chart area */}
        <div style={{
          position: 'absolute',
          top: 340,
          left: 200,
          width: 1520,
          height: 580,
          backgroundColor: 'rgba(30,32,34,0.06)',
          borderRadius: 20,
          border: '3px solid #1E2022',
          padding: 30,
        }}>
          <svg width="100%" height="100%" viewBox="0 0 1400 540" preserveAspectRatio="none">
            {/* Y axis grid lines */}
            {[0, 1, 2, 3, 4].map((i) => (
              <line key={i} x1="80" y1={100 + i * 100} x2="1380" y2={100 + i * 100} stroke="#1E2022" strokeWidth="1" opacity="0.2" />
            ))}
            
            {/* X axis */}
            <line x1="80" y1="500" x2="1380" y2="500" stroke="#1E2022" strokeWidth="3" />
            <line x1="80" y1="80" x2="80" y2="500" stroke="#1E2022" strokeWidth="3" />

            {/* Area fill under curve */}
            {visiblePoints > 2 && (
              <path
                d={`${pathD} L ${curvePoints[visiblePoints - 1]?.x || 100} 500 L 100 500 Z`}
                fill="#1E2022"
                opacity="0.15"
              />
            )}

            {/* Growth curve */}
            <path
              d={pathD}
              fill="none"
              stroke="#1E2022"
              strokeWidth="8"
              strokeLinecap="round"
              strokeLinejoin="round"
            />

            {/* Active dot at end of curve */}
            {visiblePoints > 0 && (
              <circle
                cx={curvePoints[visiblePoints - 1]?.x || 100}
                cy={curvePoints[visiblePoints - 1]?.y || 500}
                r={12 + Math.sin(frame * 0.2) * 3}
                fill="#ff0000"
                stroke="#1E2022"
                strokeWidth="3"
              />
            )}

            {/* X labels */}
            {dotMonths.map((m, i) => (
              <text
                key={i}
                x={150 + i * 290}
                y="530"
                textAnchor="middle"
                fontFamily="Space Mono, monospace"
                fontSize="22"
                fill="#1E2022"
                fontWeight="700"
              >
                {m}
              </text>
            ))}

            {/* Investment arrows (regular contributions) */}
            {[0, 1, 2, 3, 4].map((i) => {
              const x = 200 + i * 250;
              const showArrow = frame > 100 + i * 50;
              if (!showArrow) return null;
              return (
                <g key={i} opacity="0.6">
                  <line x1={x} y1="480" x2={x} y2="420" stroke="#1E2022" strokeWidth="3" markerEnd="url(#arrh)" />
                  <text x={x} y="475" textAnchor="middle" fontFamily="Space Mono, monospace" fontSize="18" fill="#1E2022" fontWeight="700">
                    +€
                  </text>
                </g>
              );
            })}
            <defs>
              <marker id="arrh" viewBox="0 0 10 10" refX="5" refY="5" markerWidth="6" markerHeight="6" orient="auto">
                <path d="M 0 0 L 10 5 L 0 10 z" fill="#1E2022" />
              </marker>
            </defs>
          </svg>
        </div>

        {/* Badge bottom */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: '50%',
          transform: `translateX(-50%) scale(${badgeScale})`,
          opacity: badgeOp,
          backgroundColor: '#1E2022',
          padding: '18px 40px',
          borderRadius: 50,
          display: 'flex',
          alignItems: 'center',
          gap: 25,
          border: '3px solid #FBF5A7',
        }}>
          <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 36, color: '#F4CE14', letterSpacing: 2 }}>
            FRAIS RÉDUITS
          </span>
          <div style={{ width: 3, height: 30, backgroundColor: '#FBF5A7' }} />
          <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 36, color: '#FBF5A7', letterSpacing: 2 }}>
            LONG TERME
          </span>
          <div style={{ width: 3, height: 30, backgroundColor: '#FBF5A7' }} />
          <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 36, color: '#F4CE14', letterSpacing: 2 }}>
            RÉGULIER
          </span>
        </div>

        {/* Vignette */}
        <AbsoluteFill style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.32) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#1E2022',
          opacity: 0.5,
        }}>
          Matures Finance
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene55;
```

```tsx
import React from 'react';
Audio: "La pire erreur : le propriétaire obligé de casser son épargne, vider son assurance-vie ou stopper son PER pour financer une rénovation imprévue."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene56',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene56: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Warning header shake
  const warnOp = interpolate(sp(5), [0, 1], [0, 1]);
  const warnShake = frame < 60 ? Math.sin(frame * 0.8) * 4 : 0;

  // 3 broken vases
  const breakDelays = [80, 150, 220];
  
  // Bottom big text
  const bottomOp = interpolate(sp(290), [0, 1], [0, 1]);
  const bottomY = interpolate(sp(290), [0, 1], [50, 0]);

  const camShake = frame < 30 ? Math.sin(frame * 1.5) * 3 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient with red tint */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(255,0,0,0.08) 0%, transparent 50%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const seed = seededRandom(i + 1000);
        const size = 300 + seed * 400;
        const x = seededRandom(i * 8) * 1920;
        const y = seededRandom(i * 12) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `3px solid #1E2022`,
            transform: `rotate(${frame * 0.1 + seed * 360}deg)`,
            opacity: 0.05,
          }} />
        );
      })}

      {/* X pattern texture for warning */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.06 }}>
        <defs>
          <pattern id="x56" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="60" y2="60" stroke="#1E2022" strokeWidth="1" />
            <line x1="60" y1="0" x2="0" y2="60" stroke="#1E2022" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#x56)" />
      </svg>

      {/* Particles - falling like broken pieces */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = seededRandom(i + 1100);
        const x = seededRandom(i * 4) * 1920;
        const y = ((seededRandom(i * 10) * 1080) + frame * 1.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x + Math.sin(frame * 0.06 + i) * 10,
            top: y,
            width: 6,
            height: 6,
            backgroundColor: '#1E2022',
            opacity: 0.3 + seed * 0.3,
            transform: `rotate(${frame * 2 + i * 30}deg)`,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camShake}px) translateY(${camShake * 0.5}px)` }}>
        
        {/* WARNING HEADER */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: warnOp,
          transform: `translateX(${warnShake}px)`,
        }}>
          <div style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 20,
            backgroundColor: '#ff0000',
            padding: '18px 45px',
            borderRadius: 12,
            border: '4px solid #1E2022',
          }}>
            <svg width="60" height="60" viewBox="0 0 60 60">
              <polygon points="30,5 55,52 5,52" fill="#FBF5A7" stroke="#1E2022" strokeWidth="3" />
              <line x1="30" y1="22" x2="30" y2="38" stroke="#1E2022" strokeWidth="5" strokeLinecap="round" />
              <circle cx="30" cy="45" r="3" fill="#1E2022" />
            </svg>
            <span style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 56,
              color: '#FBF5A7',
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}>
              LA PIRE ERREUR
            </span>
          </div>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 38,
            color: '#1E2022',
            fontWeight: 700,
            marginTop: 20,
            fontStyle: 'italic',
          }}>
            casser tout pour financer dans l'urgence
          </div>
        </div>

        {/* Three "broken" containers */}
        <div style={{
          position: 'absolute',
          top: 360,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 60,
        }}>
          {[
            { label: 'ÉPARGNE', sub: 'précaution', delay: breakDelays[0] },
            { label: 'ASSURANCE-VIE', sub: 'fonds vidé', delay: breakDelays[1] },
            { label: 'PER', sub: 'versements stoppés', delay: breakDelays[2] },
          ].map((item, idx) => {
            const opItem = interpolate(sp(item.delay), [0, 1], [0, 1]);
            const breakProgress = interpolate(frame, [item.delay + 30, item.delay + 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            const tilt = breakProgress * (idx % 2 === 0 ? -10 : 10);
            return (
              <div key={idx} style={{
                width: 360,
                opacity: opItem,
                transform: `scale(${sp(item.delay)})`,
                position: 'relative',
              }}>
                {/* Container box */}
                <div style={{
                  backgroundColor: '#1E2022',
                  borderRadius: 16,
                  padding: 30,
                  textAlign: 'center',
                  position: 'relative',
                  transform: `rotate(${tilt}deg)`,
                  border: '4px solid #1E2022',
                }}>
                  {/* Crack line over the box */}
                  {breakProgress > 0.1 && (
                    <svg style={{
                      position: 'absolute',
                      top: 0,
                      left: 0,
                      width: '100%',
                      height: '100%',
                      pointerEvents: 'none',
                    }} viewBox="0 0 360 200" preserveAspectRatio="none">
                      <path
                        d="M 20 10 L 80 60 L 60 110 L 130 150 L 110 190 L 200 170 L 180 100 L 280 80 L 260 30 L 340 50"
                        fill="none"
                        stroke="#ff0000"
                        strokeWidth="4"
                        strokeDasharray="500"
                        strokeDashoffset={500 - breakProgress * 500}
                      />
                    </svg>
                  )}
                  
                  {/* Icon - jar/safe */}
                  <svg width="80" height="80" viewBox="0 0 80 80" style={{ display: 'block', margin: '0 auto 15px' }}>
                    <rect x="15" y="20" width="50" height="50" rx="6" fill="none" stroke="#FBF5A7" strokeWidth="4" />
                    <circle cx="40" cy="45" r="10" fill="none" stroke="#FBF5A7" strokeWidth="3" />
                    <line x1="40" y1="35" x2="40" y2="55" stroke="#FBF5A7" strokeWidth="3" />
                    <line x1="30" y1="45" x2="50" y2="45" stroke="#FBF5A7" strokeWidth="3" />
                    <line x1="25" y1="20" x2="25" y2="14" stroke="#FBF5A7" strokeWidth="4" strokeLinecap="round" />
                    <line x1="55" y1="20" x2="55" y2="14" stroke="#FBF5A7" strokeWidth="4" strokeLinecap="round" />
                  </svg>
                  
                  <div style={{
                    fontFamily: 'Anton, sans-serif',
                    fontSize: 42,
                    color: '#F4CE14',
                    letterSpacing: 1,
                    lineHeight: 1,
                  }}>
                    {item.label}
                  </div>
                  <div style={{
                    fontFamily: 'Space Mono, monospace',
                    fontSize: 26,
                    color: '#ff0000',
                    marginTop: 10,
                    fontWeight: 700,
                    textDecoration: 'line-through',
                  }}>
                    {item.sub}
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom punch line */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: bottomOp,
          transform: `translateY(${bottomY}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            padding: '20px 50px',
            borderRadius: 14,
          }}>
            <span style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 48,
              color: '#FBF5A7',
              letterSpacing: 2,
              textTransform: 'uppercase',
            }}>
              Rénovation imprévue = catastrophe
            </span>
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.4) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#1E2022',
          opacity: 0.5,
        }}>
          Matures Finance
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene56;
```

```tsx
import React from 'react';
Audio: "Avec de la liquidité de côté, vous abordez les travaux avec sérénité. La trésorerie, c'est la liberté de choisir son moment plutôt que de le subir."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene57',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene57: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = interpolate(sp(10), [0, 1], [0, 1]);
  const titleScale = sp(10);

  // Liquid wave animation - vault filling
  const fillProgress = interpolate(frame, [60, 220], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const waveOffset = Math.sin(frame * 0.15) * 8;

  // Key insights cards
  const card1Op = interpolate(sp(200), [0, 1], [0, 1]);
  const card2Op = interpolate(sp(260), [0, 1], [0, 1]);
  const card3Op = interpolate(sp(320), [0, 1], [0, 1]);

  // Quote at bottom
  const quoteOp = interpolate(sp(380), [0, 1], [0, 1]);

  const camZoom = interpolate(frame, [0, durationInFrames], [1.02, 1.05]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(251,245,167,0.4) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const seed = seededRandom(i + 1200);
        const size = 350 + seed * 350;
        const x = seededRandom(i * 7) * 1920;
        const y = seededRandom(i * 13) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid #1E2022`,
            borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame * 0.025 + i) * 0.05})`,
            opacity: 0.05,
          }} />
        );
      })}

      {/* Wave texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.06 }}>
        <defs>
          <pattern id="wave57" width="80" height="20" patternUnits="userSpaceOnUse">
            <path d={`M 0 10 Q 20 0, 40 10 T 80 10`} fill="none" stroke="#1E2022" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#wave57)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 1300);
        const x = (seededRandom(i * 6) * 1920) % 1920;
        const y = (seededRandom(i * 11) * 1080 - frame * 0.6 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x + Math.cos(frame * 0.04 + i) * 14,
            top: y,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.3 + seed * 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>
        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `scale(${titleScale})`,
        }}>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 36,
            color: '#1E2022',
            letterSpacing: 8,
            fontWeight: 600,
            opacity: 0.75,
          }}>
            LA SOLUTION
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 140,
            color: '#1E2022',
            letterSpacing: 4,
            lineHeight: 0.95,
            marginTop: 5,
          }}>
            TRÉSORERIE
          </div>
        </div>

        {/* Left side: vault filling animation */}
        <div style={{
          position: 'absolute',
          top: 340,
          left: 200,
          width: 480,
          height: 540,
        }}>
          {/* Vault container */}
          <svg width="480" height="540" viewBox="0 0 480 540">
            {/* Vault outer */}
            <rect x="40" y="40" width="400" height="460" rx="20" fill="none" stroke="#1E2022" strokeWidth="6" />
            {/* Vault inner */}
            <rect x="70" y="70" width="340" height="400" rx="12" fill="#1E2022" />
            
            {/* Liquid fill */}
            <defs>
              <clipPath id="vaultClip">
                <rect x="70" y="70" width="340" height="400" rx="12" />
              </clipPath>
            </defs>
            <g clipPath="url(#vaultClip)">
              <rect 
                x="70" 
                y={470 - fillProgress * 380} 
                width="340" 
                height="400" 
                fill="#FBF5A7" 
              />
              {/* Wave on top of fill */}
              <path
                d={`M 70 ${470 - fillProgress * 380 + waveOffset} 
                    Q 155 ${460 - fillProgress * 380} 240 ${470 - fillProgress * 380 + waveOffset}
                    T 410 ${470 - fillProgress * 380 + waveOffset}
                    L 410 470 L 70 470 Z`}
                fill="#FBF5A7"
              />
              <path
                d={`M 70 ${470 - fillProgress * 380 + waveOffset} 
                    Q 155 ${460 - fillProgress * 380} 240 ${470 - fillProgress * 380 + waveOffset}
                    T 410 ${470 - fillProgress * 380 + waveOffset}`}
                fill="none"
                stroke="#1E2022"
                strokeWidth="3"
              />
              
              {/* Euro symbols floating */}
              {fillProgress > 0.3 && [0, 1, 2, 3, 4].map((i) => {
                const ex = 100 + (i * 65);
                const ey = 460 - fillProgress * 200 + Math.sin(frame * 0.1 + i) * 10;
                return (
                  <text
                    key={i}
                    x={ex}
                    y={ey}
                    fontFamily="Space Mono, monospace"
                    fontSize="36"
                    fontWeight="700"
                    fill="#1E2022"
                    opacity={fillProgress}
                  >
                    €
                  </text>
                );
              })}
            </g>

            {/* Vault dial */}
            <circle cx="240" cy="510" r="20" fill="#1E2022" stroke="#1E2022" strokeWidth="3" />
            <line 
              x1="240" 
              y1="510" 
              x2={240 + Math.cos(frame * 0.05) * 15} 
              y2={510 + Math.sin(frame * 0.05) * 15} 
              stroke="#F4CE14" 
              strokeWidth="3" 
              strokeLinecap="round" 
            />
            
            {/* Label */}
            <text x="240" y="30" textAnchor="middle" fontFamily="Anton, sans-serif" fontSize="28" fill="#1E2022" letterSpacing="2">
              LIQUIDITÉ
            </text>
          </svg>
        </div>

        {/* Right side: benefits cards */}
        <div style={{
          position: 'absolute',
          top: 340,
          left: 760,
          width: 960,
          display: 'flex',
          flexDirection: 'column',
          gap: 20,
        }}>
          {[
            { icon: 'shield', label: 'SÉRÉNITÉ', desc: 'face aux travaux obligatoires', op: card1Op },
            { icon: 'clock', label: 'NÉGOCIATION', desc: 'meilleurs devis, pas de pression', op: card2Op },
            { icon: 'arrow', label: 'CHOISIR SON MOMENT', desc: 'au lieu de le subir', op: card3Op },
          ].map((c, i) => (
            <div key={i} style={{
              opacity: c.op,
              backgroundColor: '#1E2022',
              padding: '25px 35px',
              borderRadius: 16,
              display: 'flex',
              alignItems: 'center',
              gap: 28,
              border: '3px solid #FBF5A7',
            }}>
              <div style={{
                width: 70,
                height: 70,
                borderRadius: '50%',
                backgroundColor: '#F4CE14',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                flexShrink: 0,
              }}>
                <svg width="40" height="40" viewBox="0 0 40 40">
                  {c.icon === 'shield' && (
                    <path d="M 20 5 L 33 10 L 33 22 C 33 30 27 35 20 37 C 13 35 7 30 7 22 L 7 10 Z" fill="none" stroke="#1E2022" strokeWidth="3" />
                  )}
                  {c.icon === 'clock' && (
                    <>
                      <circle cx="20" cy="20" r="14" fill="none" stroke="#1E2022" strokeWidth="3" />
                      <line x1="20" y1="20" x2="20" y2="10" stroke="#1E2022" strokeWidth="3" strokeLinecap="round" />
                      <line x1="20" y1="20" x2="27" y2="22" stroke="#1E2022" strokeWidth="3" strokeLinecap="round" />
                    </>
                  )}
                  {c.icon === 'arrow' && (
                    <>
                      <path d="M 6 20 L 30 20 M 22 12 L 30 20 L 22 28" fill="none" stroke="#1E2022" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
                    </>
                  )}
                </svg>
              </div>
              <div style={{ flex: 1 }}>
                <div style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 44,
                  color: '#F4CE14',
                  letterSpacing: 1,
                  lineHeight: 1,
                }}>
                  {c.label}
                </div>
                <div style={{
                  fontFamily: 'Montserrat, sans-serif',
                  fontSize: 26,
                  color: '#FBF5A7',
                  marginTop: 5,
                  fontWeight: 500,
                }}>
                  {c.desc}
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Quote bottom */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: quoteOp,
        }}>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 38,
            color: '#1E2022',
            fontWeight: 700,
            fontStyle: 'italic',
          }}>
            « Ne soyez jamais un vendeur forcé »
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#1E2022',
          opacity: 0.5,
        }}>
          Matures Finance
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene57;
```

```tsx
import React from 'react';
Audio: "Sur le plan fiscal, les travaux peuvent être déductibles de vos revenus fonciers. Ne laissez jamais traîner une facture sans vérifier si elle allège votre note fiscale."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene58',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene58: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = interpolate(sp(8), [0, 1], [0, 1]);
  const titleY = interpolate(sp(8), [0, 1], [-50, 0]);

  // Receipt slide in
  const receiptOp = interpolate(sp(60), [0, 1], [0, 1]);
  const receiptX = interpolate(sp(60), [0, 1], [-400, 0]);
  const receiptTilt = Math.sin(frame * 0.04) * 2;

  // Equation animation
  const eqOp = interpolate(sp(160), [0, 1], [0, 1]);
  const minusReveal = interpolate(frame, [180, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const resultReveal = interpolate(frame, [240, 290], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Counter for savings
  const savedAmount = Math.floor(interpolate(frame, [250, 340], [0, 4200], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Warning footer
  const warnOp = interpolate(sp(360), [0, 1], [0, 1]);

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 20% 80%, rgba(251,245,167,0.4) 0%, transparent 55%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const seed = seededRandom(i + 1400);
        const size = 320 + seed * 380;
        const x = seededRandom(i * 6) * 1920;
        const y = seededRandom(i * 11) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid #1E2022`,
            transform: `rotate(${frame * 0.07 + seed * 360}deg)`,
            opacity: 0.05,
            borderRadius: i % 2 === 0 ? '20px' : '50%',
          }} />
        );
      })}

      {/* Texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.07 }}>
        <defs>
          <pattern id="lines58" width="50" height="50" patternUnits="userSpaceOnUse">
            <line x1="0" y1="25" x2="50" y2="25" stroke="#1E2022" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#lines58)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 1500);
        const x = (seededRandom(i * 7) * 1920 + frame * 0.4) % 1920;
        const y = seededRandom(i * 13) * 1080 + Math.sin(frame * 0.04 + i) * 16;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.3 + seed * 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>
        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 120,
            color: '#1E2022',
            letterSpacing: 3,
            lineHeight: 0.95,
            textTransform: 'uppercase',
          }}>
            DÉDUCTION
          </div>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 44,
            color: '#1E2022',
            fontWeight: 700,
            marginTop: 8,
          }}>
            les travaux allègent vos revenus fonciers
          </div>
        </div>

        {/* Left: Receipt visual */}
        <div style={{
          position: 'absolute',
          top: 330,
          left: 180,
          width: 480,
          opacity: receiptOp,
          transform: `translateX(${receiptX}px) rotate(${receiptTilt}deg)`,
        }}>
          <div style={{
            backgroundColor: '#FBF5A7',
            padding: '30px 35px',
            borderRadius: 8,
            border: '4px solid #1E2022',
            position: 'relative',
            boxShadow: '8px 8px 0 #1E2022',
          }}>
            {/* Zig zag top */}
            <svg width="100%" height="20" style={{ position: 'absolute', top: -16, left: 0 }} viewBox="0 0 400 20" preserveAspectRatio="none">
              <polygon points="0,20 20,0 40,20 60,0 80,20 100,0 120,20 140,0 160,20 180,0 200,20 220,0 240,20 260,0 280,20 300,0 320,20 340,0 360,20 380,0 400,20" fill="#FBF5A7" stroke="#1E2022" strokeWidth="3" />
            </svg>

            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 36,
              color: '#1E2022',
              textAlign: 'center',
              letterSpacing: 2,
              borderBottom: '3px dashed #1E2022',
              paddingBottom: 12,
              marginBottom: 16,
            }}>
              FACTURE TRAVAUX
            </div>

            {[
              { label: 'Isolation combles', val: '3 200 €' },
              { label: 'Pompe à chaleur', val: '8 500 €' },
              { label: 'Fenêtres double vitrage', val: '4 800 €' },
            ].map((item, i) => (
              <div key={i} style={{
                display: 'flex',
                justifyContent: 'space-between',
                fontFamily: 'Space Mono, monospace',
                fontSize: 22,
                color: '#1E2022',
                marginBottom: 10,
                fontWeight: 600,
              }}>
                <span>{item.label}</span>
                <span>{item.val}</span>
              </div>
            ))}

            <div style={{
              borderTop: '3px solid #1E2022',
              marginTop: 12,
              paddingTop: 12,
              display: 'flex',
              justifyContent: 'space-between',
              fontFamily: 'Anton, sans-serif',
              fontSize: 32,
              color: '#1E2022',
              letterSpacing: 1,
            }}>
              <span>TOTAL</span>
              <span>16 500 €</span>
            </div>

            {/* Zig zag bottom */}
            <svg width="100%" height="20" style={{ position: 'absolute', bottom: -16, left: 0 }} viewBox="0 0 400 20" preserveAspectRatio="none">
              <polygon points="0,0 20,20 40,0 60,20 80,0 100,20 120,0 140,20 160,0 180,20 200,0 220,20 240,0 260,20 280,0 300,20 320,0 340,20 360,0 380,20 400,0" fill="#FBF5A7" stroke="#1E2022" strokeWidth="3" />
            </svg>
          </div>
        </div>

        {/* Right: Equation */}
        <div style={{
          position: 'absolute',
          top: 360,
          right: 180,
          width: 720,
          opacity: eqOp,
        }}>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 30,
            color: '#1E2022',
            opacity: 0.7,
            marginBottom: 20,
            fontWeight: 600,
            letterSpacing: 3,
          }}>
            IMPACT FISCAL
          </div>

          {/* Revenus fonciers */}
          <div style={{
            backgroundColor: '#1E2022',
            padding: '20px 30px',
            borderRadius: 12,
            marginBottom: 15,
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
          }}>
            <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 38, color: '#FBF5A7', letterSpacing: 1 }}>
              Revenus fonciers
            </span>
            <span style={{ fontFamily: 'Space Mono, monospace', fontSize: 42, color: '#F4CE14', fontWeight: 700 }}>
              14 000 €
            </span>
          </div>

          {/* Minus travaux */}
          <div style={{
            opacity: minusReveal,
            padding: '20px 30px',
            borderRadius: 12,
            marginBottom: 15,
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
            backgroundColor: '#FBF5A7',
            border: '3px solid #1E2022',
          }}>
            <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 38, color: '#1E2022', letterSpacing: 1 }}>
              − Travaux déduits
            </span>
            <span style={{ fontFamily: 'Space Mono, monospace', fontSize: 42, color: '#ff0000', fontWeight: 700 }}>
              −16 500 €
            </span>
          </div>

          {/* Result */}
          <div style={{
            opacity: resultReveal,
            transform: `scale(${0.9 + resultReveal * 0.1})`,
            padding: '25px 30px',
            borderRadius: 12,
            backgroundColor: '#1E2022',
            border: '5px solid #FBF5A7',
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
          }}>
            <div>
              <div style={{ fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 24, color: '#FBF5A7', fontWeight: 600, marginBottom: 4 }}>
                ÉCONOMIE FISCALE
              </div>
              <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 42, color: '#F4CE14', letterSpacing: 1 }}>
                ESTIMÉE
              </div>
            </div>
            <span style={{ fontFamily: 'Space Mono, monospace', fontSize: 64, color: '#F4CE14', fontWeight: 700 }}>
              {savedAmount.toLocaleString('fr-FR')} €
            </span>
          </div>
        </div>

        {/* Warning footer */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: warnOp,
        }}>
          <div style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 18,
            backgroundColor: '#ff0000',
            padding: '14px 35px',
            borderRadius: 50,
            border: '3px solid #1E2022',
          }}>
            <svg width="32" height="32" viewBox="0 0 32 32">
              <circle cx="16" cy="16" r="13" fill="#FBF5A7" stroke="#1E2022" strokeWidth="2" />
              <line x1="16" y1="9" x2="16" y2="18" stroke="#1E2022" strokeWidth="3" strokeLinecap="round" />
              <circle cx="16" cy="23" r="2" fill="#1E2022" />
            </svg>
            <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 32, color: '#FBF5A7', letterSpacing: 1 }}>
              NE LAISSEZ JAMAIS TRAÎNER UNE FACTURE
            </span>
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#1E2022',
          opacity: 0.5,
        }}>
          Matures Finance
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene58;
```

```tsx
import React from 'react';
Audio: "Votre feuille de route : vérifiez votre DPE, identifiez votre catégorie, raisonnez en gestes prioritaires, montez un plan de financement complet avec les aides."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene59',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene59: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = interpolate(sp(8), [0, 1], [0, 1]);
  const titleY = interpolate(sp(8), [0, 1], [-50, 0]);

  // Roadmap line draw
  const lineProgress = interpolate(frame, [50, 280], [0, 1], { extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  // 4 steps delays
  const stepDelays = [70, 140, 210, 280];

  const camPan = interpolate(frame, [0, durationInFrames], [0, -10]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, transparent 0%, rgba(251,245,167,0.3) 50%, transparent 100%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3, 4].map((i) => {
        const seed = seededRandom(i + 1600);
        const size = 280 + seed * 400;
        const x = seededRandom(i * 5) * 1920;
        const y = seededRandom(i * 11) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid #1E2022`,
            transform: `rotate(${frame * 0.08 + seed * 360}deg) scale(${1 + Math.sin(frame * 0.03 + i) * 0.05})`,
            opacity: 0.05,
            borderRadius: i % 2 === 0 ? '20px' : '50%',
          }} />
        );
      })}

      {/* Texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.06 }}>
        <defs>
          <pattern id="dot59" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#1E2022" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dot59)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const seed = seededRandom(i + 1700);
        const x = (seededRandom(i * 7) * 1920 + frame * 0.5) % 1920;
        const y = seededRandom(i * 13) * 1080 + Math.sin(frame * 0.04 + i) * 15;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.3 + seed * 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>
        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 36,
            color: '#1E2022',
            letterSpacing: 8,
            fontWeight: 600,
            opacity: 0.75,
          }}>
            VOTRE FEUILLE DE ROUTE
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 130,
            color: '#1E2022',
            letterSpacing: 4,
            lineHeight: 0.95,
            marginTop: 5,
          }}>
            4 ÉTAPES
          </div>
        </div>

        {/* Roadmap line + steps */}
        <div style={{
          position: 'absolute',
          top: 460,
          left: 100,
          right: 100,
          height: 500,
        }}>
          {/* Horizontal path with progress */}
          <svg width="100%" height="500" viewBox="0 0 1720 500" preserveAspectRatio="none">
            {/* Background path */}
            <path
              d="M 100 250 Q 530 100, 860 250 T 1620 250"
              fill="none"
              stroke="#1E2022"
              strokeWidth="6"
              strokeDasharray="15 10"
              opacity="0.25"
            />
            {/* Animated progress path */}
            <path
              d="M 100 250 Q 530 100, 860 250 T 1620 250"
              fill="none"
              stroke="#1E2022"
              strokeWidth="8"
              strokeLinecap="round"
              pathLength={1}
              strokeDasharray={1}
              strokeDashoffset={1 - lineProgress}
            />
          </svg>

          {/* 4 step nodes */}
          {[
            { num: '01', label: 'VÉRIFIEZ', desc: 'votre DPE actuel', x: 100, y: 250, delay: stepDelays[0] },
            { num: '02', label: 'IDENTIFIEZ', desc: 'votre catégorie', x: 605, y: 180, delay: stepDelays[1] },
            { num: '03', label: 'PRIORISEZ', desc: 'les gestes utiles', x: 1115, y: 250, delay: stepDelays[2] },
            { num: '04', label: 'FINANCEZ', desc: 'plan + aides', x: 1620, y: 250, delay: stepDelays[3] },
          ].map((step, i) => {
            const op = interpolate(sp(step.delay), [0, 1], [0, 1]);
            const scale = sp(step.delay);
            return (
              <div key={i} style={{
                position: 'absolute',
                left: step.x - 130,
                top: step.y - 130,
                width: 260,
                opacity: op,
                transform: `scale(${scale})`,
                textAlign: 'center',
              }}>
                {/* Number badge */}
                <div style={{
                  width: 110,
                  height: 110,
                  borderRadius: '50%',
                  backgroundColor: '#1E2022',
                  border: '5px solid #FBF5A7',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  margin: '0 auto 15px',
                  boxShadow: '0 6px 0 #FBF5A7',
                }}>
                  <span style={{
                    fontFamily: 'Anton, sans-serif',
                    fontSize: 52,
                    color: '#F4CE14',
                    letterSpacing: 2,
                  }}>
                    {step.num}
                  </span>
                </div>
                {/* Label */}
                <div style={{
                  backgroundColor: '#FBF5A7',
                  padding: '10px 18px',
                  borderRadius: 10,
                  border: '3px solid #1E2022',
                  display: 'inline-block',
                }}>
                  <div style={{
                    fontFamily: 'Anton, sans-serif',
                    fontSize: 36,
                    color: '#1E2022',
                    letterSpacing: 1,
                    lineHeight: 1,
                  }}>
                    {step.label}
                  </div>
                  <div style={{
                    fontFamily: 'Montserrat, sans-serif',
                    fontSize: 22,
                    color: '#1E2022',
                    fontWeight: 600,
                    marginTop: 4,
                  }}>
                    {step.desc}
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        {/* Vignette */}
        <AbsoluteFill style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#1E2022',
          opacity: 0.5,
        }}>
          Matures Finance
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene59;
```

```tsx
import React from 'react';
Audio: "Ce moment sépare ceux qui subissent de ceux qui pilotent. Abonnez-vous à Matures Finance et dites-moi en commentaire votre stratégie. À très vite !"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene60',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene60: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big split text
  const leftOp = interpolate(sp(10), [0, 1], [0, 1]);
  const leftX = interpolate(sp(10), [0, 1], [-200, 0]);
  
  const rightOp = interpolate(sp(60), [0, 1], [0, 1]);
  const rightX = interpolate(sp(60), [0, 1], [200, 0]);

  // VS divider
  const dividerOp = interpolate(sp(120), [0, 1], [0, 1]);
  const dividerHeight = interpolate(sp(120), [0, 1], [0, 1]);

  // Subscribe button
  const subOp = interpolate(sp(180), [0, 1], [0, 1]);
  const subPulse = 1 + Math.sin(frame * 0.15) * 0.04;

  // Comment prompt
  const commOp = interpolate(sp(260), [0, 1], [0, 1]);

  // Final tagline
  const finalOp = interpolate(sp(340), [0, 1], [0, 1]);

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(251,245,167,0.5) 0%, transparent 60%)',
      }} />

      {/* Burst rays from center */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.08 }} viewBox="0 0 1920 1080">
        {Array.from({ length: 24 }).map((_, i) => {
          const angle = (i / 24) * 360 + frame * 0.3;
          const rad = (angle * Math.PI) / 180;
          const x2 = 960 + Math.cos(rad) * 1500;
          const y2 = 540 + Math.sin(rad) * 1500;
          return (
            <line key={i} x1="960" y1="540" x2={x2} y2={y2} stroke="#1E2022" strokeWidth="2" />
          );
        })}
      </svg>

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const seed = seededRandom(i + 1800);
        const size = 350 + seed * 400;
        const x = seededRandom(i * 6) * 1920;
        const y = seededRandom(i * 12) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `3px solid #1E2022`,
            transform: `rotate(${frame * 0.09 + seed * 360}deg) scale(${1 + Math.sin(frame * 0.03 + i) * 0.05})`,
            opacity: 0.06,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = seededRandom(i + 1900);
        const x = seededRandom(i * 5) * 1920;
        const y = (seededRandom(i * 11) * 1080 - frame * 0.7 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x + Math.sin(frame * 0.05 + i) * 12,
            top: y,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.3 + seed * 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>
        {/* Top split text */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 80,
        }}>
          {/* SUBISSENT */}
          <div style={{
            opacity: leftOp,
            transform: `translateX(${leftX}px)`,
            textAlign: 'right',
            width: 700,
          }}>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 28,
              color: '#1E2022',
              fontWeight: 600,
              letterSpacing: 4,
              opacity: 0.7,
              marginBottom: 5,
            }}>
              CEUX QUI
            </div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 130,
              color: '#1E2022',
              letterSpacing: 2,
              lineHeight: 0.95,
              textDecoration: 'line-through',
              textDecorationColor: '#ff0000',
              textDecorationThickness: 6,
            }}>
              SUBISSENT
            </div>
          </div>

          {/* DIVIDER */}
          <div style={{
            width: 6,
            height: dividerHeight * 220,
            backgroundColor: '#1E2022',
            opacity: dividerOp,
          }} />

          {/* PILOTENT */}
          <div style={{
            opacity: rightOp,
            transform: `translateX(${rightX}px)`,
            textAlign: 'left',
            width: 700,
          }}>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 28,
              color: '#1E2022',
              fontWeight: 600,
              letterSpacing: 4,
              opacity: 0.7,
              marginBottom: 5,
            }}>
              CEUX QUI
            </div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 130,
              color: '#1E2022',
              letterSpacing: 2,
              lineHeight: 0.95,
              backgroundColor: '#1E2022',
              color: '#F4CE14',
              padding: '5px 25px',
              display: 'inline-block',
              borderRadius: 12,
            }}>
              PILOTENT
            </div>
          </div>
        </div>

        {/* Subscribe Button */}
        <div style={{
          position: 'absolute',
          top: 440,
          left: '50%',
          transform: `translateX(-50%) scale(${subOp * subPulse})`,
          opacity: subOp,
        }}>
          <div style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 25,
            backgroundColor: '#ff0000',
            padding: '30px 70px',
            borderRadius: 80,
            border: '6px solid #1E2022',
            boxShadow: '0 10px 0 #1E2022',
          }}>
            <svg width="70" height="70" viewBox="0 0 70 70">
              <rect x="5" y="15" width="60" height="40" rx="10" fill="#FBF5A7" stroke="#1E2022" strokeWidth="3" />
              <polygon points="28,25 28,45 48,35" fill="#1E2022" />
            </svg>
            <span style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 72,
              color: '#FBF5A7',
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}>
              ABONNEZ-VOUS
            </span>
          </div>
        </div>

        {/* Brand name */}
        <div style={{
          position: 'absolute',
          top: 615,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: subOp,
        }}>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 38,
            color: '#1E2022',
            fontWeight: 700,
            letterSpacing: 3,
          }}>
            MATURES FINANCE
          </div>
        </div>

        {/* Comment prompt */}
        <div style={{
          position: 'absolute',
          top: 720,
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: commOp,
          backgroundColor: '#1E2022',
          padding: '18px 40px',
          borderRadius: 50,
          display: 'flex',
          alignItems: 'center',
          gap: 20,
          border: '3px solid #FBF5A7',
        }}>
          <svg width="40" height="40" viewBox="0 0 40 40">
            <path d="M 8 8 L 32 8 L 32 26 L 22 26 L 16 32 L 16 26 L 8 26 Z" fill="none" stroke="#F4CE14" strokeWidth="3" strokeLinejoin="round" />
            <circle cx="15" cy="17" r="1.5" fill="#F4CE14" />
            <circle cx="20" cy="17" r="1.5" fill="#F4CE14" />
            <circle cx="25" cy="17" r="1.5" fill="#F4CE14" />
          </svg>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 36,
            color: '#FBF5A7',
            letterSpacing: 1,
          }}>
            Votre stratégie en commentaire
          </span>
        </div>

        {/* Final tagline */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: finalOp,
        }}>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 48,
            color: '#1E2022',
            fontWeight: 700,
            fontStyle: 'italic',
          }}>
            À très vite —
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.32) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#1E2022',
          opacity: 0.5,
        }}>
          Matures Finance
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene60;
```

