```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene51',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene51: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const gradShift = interpolate(frame, [0, durationInFrames], [0, 100]);
  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.05]);

  const titleIn = sp(8);
  const ageBoxIn = sp(25);
  const arrowIn = sp(55);
  const switchIn = sp(80);
  const reverseIn = sp(140);

  const pulse = 1 + Math.sin(frame * 0.12) * 0.03;
  const arrowDash = (frame * 4) % 40;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic Gradient Background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at ${30 + gradShift * 0.4}% ${40 + gradShift * 0.2}%, #B8BAFF 0%, #9FA1FF 45%, #7A7DE8 100%)`,
        }}
      />

      {/* Camera Zoom Wrapper */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center center' }}>

        {/* Shape Layer — Drifting Geometric Forms */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          <circle
            cx={200 + Math.sin(frame * 0.015) * 40}
            cy={250 + Math.cos(frame * 0.02) * 30}
            r={180}
            fill="#FEEC41"
            opacity={0.06}
            transform={`rotate(${frame * 0.2} 200 250)`}
          />
          <rect
            x={1500 + Math.cos(frame * 0.018) * 35}
            y={780 + Math.sin(frame * 0.022) * 25}
            width={280}
            height={280}
            fill="#000000"
            opacity={0.05}
            transform={`rotate(${frame * 0.15} 1640 920)`}
          />
          <polygon
            points="960,80 1080,280 840,280"
            fill="#FEEC41"
            opacity={0.04}
            transform={`translate(${Math.sin(frame * 0.01) * 20}, ${Math.cos(frame * 0.012) * 15}) rotate(${frame * 0.1} 960 200)`}
          />
          <circle
            cx={1700}
            cy={180}
            r={90 * pulse}
            fill="none"
            stroke="#000000"
            strokeWidth={3}
            opacity={0.08}
          />
        </svg>

        {/* Motion Texture — Diagonal Grid Lines */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.08 }}>
          {Array.from({ length: 20 }).map((_, i) => (
            <line
              key={i}
              x1={-200 + i * 120 + (frame * 0.5) % 120}
              y1={0}
              x2={100 + i * 120 + (frame * 0.5) % 120}
              y2={1080}
              stroke="#000000"
              strokeWidth={1.5}
            />
          ))}
        </svg>

        {/* Floating Particles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {Array.from({ length: 24 }).map((_, i) => {
            const seed = seededRandom(i + 1);
            const x = seed * 1920;
            const y = (seededRandom(i + 50) * 1080 + frame * (0.5 + seed)) % 1080;
            const size = 3 + seed * 5;
            return (
              <circle
                key={i}
                cx={x}
                cy={y}
                r={size}
                fill={i % 2 === 0 ? '#FEEC41' : '#000000'}
                opacity={0.35}
              />
            );
          })}
        </svg>

        {/* MAIN CONTENT */}

        {/* Top Headline */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(titleIn, [0, 1], [-50, 0])}px)`,
            opacity: titleIn,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div
            style={{
              background: '#000000',
              padding: '14px 36px',
              borderRadius: 8,
              transform: 'skewX(-6deg)',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 64,
                fontWeight: 900,
                color: '#FEEC41',
                letterSpacing: 2,
                transform: 'skewX(6deg)',
              }}
            >
              THE AGE 70 SWITCH
            </div>
          </div>
        </div>

        {/* Age Timeline — Left Box (Age 62) */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            left: 140,
            opacity: ageBoxIn,
            transform: `scale(${ageBoxIn}) translateY(${interpolate(ageBoxIn, [0, 1], [40, 0])}px)`,
          }}
        >
          <div
            style={{
              background: '#FEEC41',
              border: '6px solid #000000',
              borderRadius: 20,
              padding: '40px 50px',
              boxShadow: '12px 12px 0 #000000',
              width: 420,
            }}
          >
            <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 700, color: '#000000', opacity: 0.7 }}>
              START HERE
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 180,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 0.95,
                marginTop: 10,
              }}
            >
              62
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 30, fontWeight: 700, color: '#000000', marginTop: 8 }}>
              EX'S SPOUSAL CHECK
            </div>
            {/* Mini icon — wallet */}
            <svg width="60" height="60" style={{ marginTop: 14 }} viewBox="0 0 60 60">
              <rect x="6" y="14" width="48" height="36" rx="4" fill="#000000" />
              <rect x="6" y="14" width="48" height="10" fill="#000000" />
              <circle cx="44" cy="32" r="4" fill="#FEEC41" />
            </svg>
          </div>
        </div>

        {/* Animated Arrow — Center */}
        <div
          style={{
            position: 'absolute',
            top: 450,
            left: '50%',
            transform: `translateX(-50%) scale(${arrowIn})`,
            opacity: arrowIn,
          }}
        >
          <svg width="280" height="100" viewBox="0 0 280 100">
            <defs>
              <marker id="arrowHead51" markerWidth="14" markerHeight="14" refX="6" refY="6" orient="auto">
                <polygon points="0 0, 12 6, 0 12" fill="#000000" />
              </marker>
            </defs>
            <line
              x1="10"
              y1="50"
              x2="240"
              y2="50"
              stroke="#000000"
              strokeWidth="10"
              strokeDasharray="20 8"
              strokeDashoffset={-arrowDash}
              markerEnd="url(#arrowHead51)"
            />
            <text
              x="125"
              y="32"
              textAnchor="middle"
              fontFamily="Inter, Poppins"
              fontSize="26"
              fontWeight="900"
              fill="#000000"
            >
              SWITCH AT
            </text>
          </svg>
        </div>

        {/* Age Timeline — Right Box (Age 70) */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            right: 140,
            opacity: switchIn,
            transform: `scale(${switchIn}) translateY(${interpolate(switchIn, [0, 1], [40, 0])}px)`,
          }}
        >
          <div
            style={{
              background: '#000000',
              border: '6px solid #FEEC41',
              borderRadius: 20,
              padding: '40px 50px',
              boxShadow: '12px 12px 0 #FEEC41',
              width: 420,
              position: 'relative',
            }}
          >
            <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 700, color: '#FEEC41', opacity: 0.8 }}>
              MAXIMIZED
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 180,
                fontWeight: 900,
                color: '#FEEC41',
                lineHeight: 0.95,
                marginTop: 10,
                textShadow: `0 0 ${20 + Math.sin(frame * 0.15) * 8}px rgba(254, 236, 65, 0.6)`,
              }}
            >
              70
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 30, fontWeight: 700, color: '#FEEC41', marginTop: 8 }}>
              YOUR OWN CHECK
            </div>
            {/* Mini icon — trophy / growth */}
            <svg width="60" height="60" style={{ marginTop: 14 }} viewBox="0 0 60 60">
              <rect x="8" y="40" width="44" height="8" fill="#FEEC41" />
              <rect x="20" y="30" width="20" height="12" fill="#FEEC41" />
              <path d="M14 10 L14 26 Q14 32 30 32 Q46 32 46 26 L46 10 Z" fill="#FEEC41" />
              <text x="30" y="24" textAnchor="middle" fontFamily="Inter" fontSize="18" fontWeight="900" fill="#000000">8%</text>
            </svg>
          </div>
        </div>

        {/* Bottom Reverse Strategy Card */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(reverseIn, [0, 1], [80, 0])}px)`,
            opacity: reverseIn,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            background: 'rgba(0,0,0,0.85)',
            padding: '24px 48px',
            borderRadius: 16,
            border: '4px solid #FEEC41',
          }}
        >
          {/* Reverse arrows icon */}
          <svg width="70" height="70" viewBox="0 0 70 70">
            <path d="M10 22 L50 22 L44 14 M50 22 L44 30" stroke="#FEEC41" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            <path d="M60 48 L20 48 L26 40 M20 48 L26 56" stroke="#FEEC41" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 44,
              fontWeight: 800,
              color: '#FEEC41',
              letterSpacing: 1,
            }}
          >
            OR DO THE <span style={{ color: '#FFFFFF', textDecoration: 'underline' }}>EXACT REVERSE</span>
          </div>
        </div>

        {/* Vignette Overlay */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.35) 100%)',
            pointerEvents: 'none',
          }}
        />
      </AbsoluteFill>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene51;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene52',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene52: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [-15, 15]);

  const titleIn = sp(6);
  const smallBarIn = sp(28);
  const arrowSwitchIn = sp(60);
  const bigBarIn = sp(85);
  const cpuIn = sp(140);

  // Bar growing animations
  const smallBarH = interpolate(smallBarIn, [0, 1], [0, 180]);
  const bigBarH = interpolate(bigBarIn, [0, 1], [0, 440]);
  const cpuPulse = 1 + Math.sin(frame * 0.18) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic Gradient Background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #9FA1FF 0%, #ADB0FF 50%, #8B8EF0 100%)',
        }}
      />

      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>

        {/* Shape Layer */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          <polygon
            points="100,100 350,80 320,340 80,360"
            fill="#FEEC41"
            opacity={0.05}
            transform={`rotate(${frame * 0.18} 200 220)`}
          />
          <circle
            cx={1750 + Math.sin(frame * 0.02) * 20}
            cy={900}
            r={250}
            fill="#000000"
            opacity={0.04}
          />
          <rect
            x={50}
            y={750}
            width={220}
            height={220}
            fill="#FEEC41"
            opacity={0.06}
            transform={`rotate(${-frame * 0.12} 160 860)`}
          />
        </svg>

        {/* Motion Texture — Concentric Circles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.07 }}>
          {Array.from({ length: 8 }).map((_, i) => (
            <circle
              key={i}
              cx={960}
              cy={540}
              r={100 + i * 90 + Math.sin(frame * 0.05 + i) * 8}
              fill="none"
              stroke="#000000"
              strokeWidth={2}
            />
          ))}
        </svg>

        {/* Floating Particles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {Array.from({ length: 22 }).map((_, i) => {
            const seed = seededRandom(i + 30);
            const x = (seed * 1920 + frame * (0.3 + seed * 0.5)) % 1920;
            const y = seededRandom(i + 70) * 1080;
            return (
              <rect
                key={i}
                x={x}
                y={y}
                width={4 + seed * 4}
                height={4 + seed * 4}
                fill={i % 3 === 0 ? '#000000' : '#FEEC41'}
                opacity={0.4}
                transform={`rotate(${frame + i * 30} ${x} ${y})`}
              />
            );
          })}
        </svg>

        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: '50%',
            transform: `translateX(-50%) scale(${titleIn})`,
            opacity: titleIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 70,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 2,
              lineHeight: 1,
            }}
          >
            REVERSE STRATEGY
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.65,
              marginTop: 8,
              letterSpacing: 6,
            }}
          >
            SMALL NOW — BIG LATER
          </div>
        </div>

        {/* BAR CHART — LEFT (Small Now) */}
        <div
          style={{
            position: 'absolute',
            bottom: 200,
            left: 280,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              width: 180,
              height: smallBarH,
              background: '#000000',
              borderRadius: '12px 12px 0 0',
              border: '4px solid #000000',
              position: 'relative',
            }}
          >
            <div
              style={{
                position: 'absolute',
                top: -60,
                left: '50%',
                transform: 'translateX(-50%)',
                fontFamily: 'Inter',
                fontSize: 44,
                fontWeight: 900,
                color: '#000000',
                opacity: smallBarIn,
              }}
            >
              $1,200
            </div>
          </div>
          <div
            style={{
              marginTop: 16,
              fontFamily: 'Inter',
              fontSize: 36,
              fontWeight: 800,
              color: '#000000',
              opacity: smallBarIn,
            }}
          >
            YOUR CHECK
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              fontWeight: 600,
              color: '#000000',
              opacity: smallBarIn * 0.7,
            }}
          >
            (TAKE EARLY)
          </div>
        </div>

        {/* SWITCH ARROW — Center */}
        <div
          style={{
            position: 'absolute',
            top: 540,
            left: '50%',
            transform: `translate(-50%, -50%) scale(${arrowSwitchIn})`,
            opacity: arrowSwitchIn,
          }}
        >
          <svg width="220" height="160" viewBox="0 0 220 160">
            <path
              d={`M 30 100 Q 110 ${20 + Math.sin(frame * 0.1) * 5} 190 60`}
              stroke="#000000"
              strokeWidth="10"
              fill="none"
              strokeLinecap="round"
            />
            <polygon points="180,40 200,60 175,75" fill="#000000" />
            <text
              x="110"
              y="135"
              textAnchor="middle"
              fontFamily="Inter, Poppins"
              fontSize="34"
              fontWeight="900"
              fill="#FEEC41"
              stroke="#000000"
              strokeWidth="1.5"
            >
              SWITCH
            </text>
          </svg>
        </div>

        {/* BAR CHART — RIGHT (Big Later) */}
        <div
          style={{
            position: 'absolute',
            bottom: 200,
            right: 280,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              width: 220,
              height: bigBarH,
              background: '#FEEC41',
              borderRadius: '12px 12px 0 0',
              border: '6px solid #000000',
              boxShadow: '8px 8px 0 #000000',
              position: 'relative',
            }}
          >
            <div
              style={{
                position: 'absolute',
                top: -70,
                left: '50%',
                transform: 'translateX(-50%)',
                fontFamily: 'Inter',
                fontSize: 56,
                fontWeight: 900,
                color: '#000000',
                opacity: bigBarIn,
                whiteSpace: 'nowrap',
              }}
            >
              $3,000
            </div>
            {/* Inner growth lines */}
            {bigBarIn > 0.5 && Array.from({ length: 5 }).map((_, i) => (
              <div
                key={i}
                style={{
                  position: 'absolute',
                  top: i * (bigBarH / 5),
                  left: 0,
                  right: 0,
                  height: 2,
                  background: '#000000',
                  opacity: 0.2,
                }}
              />
            ))}
          </div>
          <div
            style={{
              marginTop: 16,
              fontFamily: 'Inter',
              fontSize: 36,
              fontWeight: 800,
              color: '#000000',
              opacity: bigBarIn,
            }}
          >
            EX'S BENEFIT
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              fontWeight: 600,
              color: '#000000',
              opacity: bigBarIn * 0.8,
            }}
          >
            (SWITCH LATER)
          </div>
        </div>

        {/* SSA CPU Badge — Bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 50,
            left: '50%',
            transform: `translateX(-50%) scale(${cpuIn * cpuPulse})`,
            opacity: cpuIn,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            background: '#000000',
            padding: '16px 36px',
            borderRadius: 50,
            border: '3px solid #FEEC41',
          }}
        >
          {/* CPU icon */}
          <svg width="44" height="44" viewBox="0 0 44 44">
            <rect x="8" y="8" width="28" height="28" rx="3" fill="#FEEC41" />
            <rect x="14" y="14" width="16" height="16" fill="#000000" />
            <line x1="14" y1="4" x2="14" y2="8" stroke="#FEEC41" strokeWidth="2" />
            <line x1="22" y1="4" x2="22" y2="8" stroke="#FEEC41" strokeWidth="2" />
            <line x1="30" y1="4" x2="30" y2="8" stroke="#FEEC41" strokeWidth="2" />
            <line x1="14" y1="36" x2="14" y2="40" stroke="#FEEC41" strokeWidth="2" />
            <line x1="22" y1="36" x2="22" y2="40" stroke="#FEEC41" strokeWidth="2" />
            <line x1="30" y1="36" x2="30" y2="40" stroke="#FEEC41" strokeWidth="2" />
            <line x1="4" y1="14" x2="8" y2="14" stroke="#FEEC41" strokeWidth="2" />
            <line x1="4" y1="22" x2="8" y2="22" stroke="#FEEC41" strokeWidth="2" />
            <line x1="36" y1="14" x2="40" y2="14" stroke="#FEEC41" strokeWidth="2" />
            <line x1="36" y1="22" x2="40" y2="22" stroke="#FEEC41" strokeWidth="2" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 800,
              color: '#FEEC41',
              letterSpacing: 1,
            }}
          >
            SSA AUTO-CALCULATES YOUR MAX
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
            pointerEvents: 'none',
          }}
        />
      </AbsoluteFill>

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene52;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene53',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene53: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.02, 1.0]);

  const headerIn = sp(5);
  const card1In = sp(30);
  const card2In = sp(50);
  const card3In = sp(70);
  const xMarkIn = sp(110);
  const warningIn = sp(160);

  // Shake on warning
  const shakeFrame = frame - (5 + 160 / fps * fps);
  const warnShake = shakeFrame > 0 && shakeFrame < 20 ? Math.sin(shakeFrame * 1.5) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: `radial-gradient(ellipse at 50% 100%, #8285E8 0%, #9FA1FF 60%, #B3B5FF 100%)`,
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>

        {/* Shape Layer */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          <circle cx={300} cy={900} r={200} fill="#FEEC41" opacity={0.05} />
          <rect
            x={1500}
            y={100}
            width={350}
            height={200}
            fill="#000000"
            opacity={0.04}
            transform={`rotate(${frame * 0.1} 1675 200)`}
          />
          <polygon
            points="960,1000 1100,1080 820,1080"
            fill="#FEEC41"
            opacity={0.05}
          />
        </svg>

        {/* Motion Texture — Dots Grid */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.1 }}>
          {Array.from({ length: 14 }).map((_, row) =>
            Array.from({ length: 24 }).map((_, col) => (
              <circle
                key={`${row}-${col}`}
                cx={col * 85 + 40}
                cy={row * 85 + 40 + Math.sin(frame * 0.05 + col * 0.3) * 4}
                r={3}
                fill="#000000"
              />
            ))
          )}
        </svg>

        {/* Particles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {Array.from({ length: 20 }).map((_, i) => {
            const seed = seededRandom(i + 100);
            const x = seed * 1920;
            const y = (seededRandom(i + 200) * 1080 - frame * (0.4 + seed * 0.5)) % 1080;
            const adjY = y < 0 ? y + 1080 : y;
            return (
              <circle
                key={i}
                cx={x}
                cy={adjY}
                r={2 + seed * 3}
                fill="#FEEC41"
                opacity={0.45}
              />
            );
          })}
        </svg>

        {/* HEADER */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(headerIn, [0, 1], [-40, 0])}px)`,
            opacity: headerIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              background: '#000000',
              padding: '12px 40px',
              display: 'inline-block',
              borderRadius: 10,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 30,
                fontWeight: 700,
                color: '#FEEC41',
                letterSpacing: 4,
              }}
            >
              CRITICAL RULE
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 72,
              fontWeight: 900,
              color: '#000000',
              marginTop: 14,
              letterSpacing: 1,
            }}
          >
            TELL THEM <span style={{ color: '#FEEC41', WebkitTextStroke: '3px #000000' }}>EVERYTHING</span>
          </div>
        </div>

        {/* THREE EX-SPOUSE CARDS */}
        {[
          { delay: card1In, label: 'EX #1', sub: '12 YEARS', x: 220, color: '#FEEC41', check: true },
          { delay: card2In, label: 'EX #2', sub: '15 YEARS', x: 760, color: '#FEEC41', check: true },
          { delay: card3In, label: 'EX #3', sub: '11 YEARS', x: 1300, color: '#FEEC41', check: true },
        ].map((card, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              top: 380,
              left: card.x,
              opacity: card.delay,
              transform: `scale(${card.delay}) translateY(${interpolate(card.delay, [0, 1], [60, 0])}px)`,
              width: 400,
            }}
          >
            <div
              style={{
                background: card.color,
                border: '6px solid #000000',
                borderRadius: 20,
                padding: '30px 36px',
                boxShadow: '10px 10px 0 #000000',
              }}
            >
              {/* Document Icon */}
              <svg width="70" height="80" viewBox="0 0 70 80">
                <path d="M10 5 L50 5 L65 20 L65 75 L10 75 Z" fill="#000000" />
                <path d="M50 5 L50 20 L65 20" fill="none" stroke="#FEEC41" strokeWidth="2" />
                <line x1="20" y1="35" x2="55" y2="35" stroke="#FEEC41" strokeWidth="3" />
                <line x1="20" y1="48" x2="55" y2="48" stroke="#FEEC41" strokeWidth="3" />
                <line x1="20" y1="61" x2="45" y2="61" stroke="#FEEC41" strokeWidth="3" />
              </svg>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 64,
                  fontWeight: 900,
                  color: '#000000',
                  marginTop: 8,
                  lineHeight: 1,
                }}
              >
                {card.label}
              </div>
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 32,
                  fontWeight: 700,
                  color: '#000000',
                  opacity: 0.75,
                  marginTop: 4,
                }}
              >
                {card.sub}
              </div>
              {/* Check badge */}
              <div
                style={{
                  marginTop: 14,
                  display: 'inline-flex',
                  alignItems: 'center',
                  gap: 8,
                  background: '#000000',
                  padding: '6px 16px',
                  borderRadius: 30,
                }}
              >
                <svg width="22" height="22" viewBox="0 0 22 22">
                  <polyline points="4,12 9,17 18,6" fill="none" stroke="#FEEC41" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
                <span style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 800, color: '#FEEC41' }}>
                  DISCLOSED
                </span>
              </div>
            </div>
          </div>
        ))}

        {/* Warning Card — Bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: '50%',
            transform: `translateX(-50%) translateX(${warnShake}px) scale(${warningIn})`,
            opacity: warningIn,
            display: 'flex',
            alignItems: 'center',
            gap: 28,
            background: '#000000',
            padding: '24px 50px',
            borderRadius: 16,
            border: '5px solid #FEEC41',
          }}
        >
          <svg width="70" height="70" viewBox="0 0 70 70">
            <polygon points="35,8 65,60 5,60" fill="#FEEC41" stroke="#FEEC41" strokeWidth="3" strokeLinejoin="round" />
            <line x1="35" y1="24" x2="35" y2="44" stroke="#000000" strokeWidth="6" strokeLinecap="round" />
            <circle cx="35" cy="52" r="3.5" fill="#000000" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 40,
              fontWeight: 800,
              color: '#FFFFFF',
              letterSpacing: 1,
              lineHeight: 1.15,
            }}
          >
            MENTION <span style={{ color: '#FEEC41' }}>ONLY ONE</span> = LOSE THE REST
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
            pointerEvents: 'none',
          }}
        />
      </AbsoluteFill>

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene53;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene54',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene54: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camTilt = interpolate(frame, [0, durationInFrames], [-1, 1]);

  const titleIn = sp(5);
  const briefcaseIn = sp(28);
  const doc1In = sp(55);
  const doc2In = sp(75);
  const doc3In = sp(95);
  const checklistIn = sp(140);

  const briefcaseFloat = Math.sin(frame * 0.06) * 8;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(160deg, #9FA1FF 0%, #C2C4FF 100%)',
        }}
      />

      <AbsoluteFill style={{ transform: `rotate(${camTilt}deg)`, transformOrigin: 'center' }}>

        {/* Shape Layer */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          <circle
            cx={1700}
            cy={300}
            r={220 + Math.sin(frame * 0.05) * 10}
            fill="#FEEC41"
            opacity={0.06}
          />
          <rect
            x={80}
            y={650}
            width={300}
            height={300}
            fill="#000000"
            opacity={0.04}
            transform={`rotate(${frame * 0.1 + 20} 230 800)`}
          />
          <polygon
            points="1500,800 1750,750 1820,1000 1550,1050"
            fill="#FEEC41"
            opacity={0.05}
          />
        </svg>

        {/* Motion Texture — Vertical Lines */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.06 }}>
          {Array.from({ length: 30 }).map((_, i) => (
            <line
              key={i}
              x1={i * 65 + ((frame * 0.6) % 65)}
              y1={0}
              x2={i * 65 + ((frame * 0.6) % 65)}
              y2={1080}
              stroke="#000000"
              strokeWidth={1.5}
            />
          ))}
        </svg>

        {/* Particles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {Array.from({ length: 18 }).map((_, i) => {
            const seed = seededRandom(i + 300);
            const x = (seed * 1920 + Math.sin(frame * 0.02 + i) * 30) % 1920;
            const y = (seededRandom(i + 400) * 1080 + frame * (0.4 + seed * 0.4)) % 1080;
            return (
              <polygon
                key={i}
                points={`${x},${y - 6} ${x + 6},${y} ${x},${y + 6} ${x - 6},${y}`}
                fill={i % 2 === 0 ? '#FEEC41' : '#000000'}
                opacity={0.4}
              />
            );
          })}
        </svg>

        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: '50%',
            transform: `translateX(-50%) scale(${titleIn})`,
            opacity: titleIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 80,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 1,
              lineHeight: 1,
            }}
          >
            BRING <span style={{ background: '#FEEC41', padding: '0 18px', borderRadius: 8 }}>EVERY DOCUMENT</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.65,
              marginTop: 12,
              letterSpacing: 5,
            }}
          >
            SSA APPOINTMENT CHECKLIST
          </div>
        </div>

        {/* BRIEFCASE — Center Left */}
        <div
          style={{
            position: 'absolute',
            top: 340,
            left: 200,
            transform: `translateY(${briefcaseFloat}px) scale(${briefcaseIn})`,
            opacity: briefcaseIn,
          }}
        >
          <svg width="380" height="320" viewBox="0 0 380 320">
            {/* Handle */}
            <path d="M 130 30 Q 130 10 190 10 Q 250 10 250 30 L 250 70" fill="none" stroke="#000000" strokeWidth="10" />
            {/* Body */}
            <rect x="40" y="60" width="300" height="220" rx="14" fill="#000000" />
            <rect x="50" y="70" width="280" height="200" rx="10" fill="#FEEC41" />
            {/* Lock */}
            <rect x="170" y="130" width="40" height="50" rx="4" fill="#000000" />
            <circle cx="190" cy="148" r="6" fill="#FEEC41" />
            {/* Divider line */}
            <line x1="50" y1="160" x2="330" y2="160" stroke="#000000" strokeWidth="3" />
          </svg>
        </div>

        {/* DOCUMENTS — Stacked Right */}
        {[
          { delay: doc1In, top: 280, rot: -4, label: 'MARRIAGE\nCERTIFICATE' },
          { delay: doc2In, top: 460, rot: 3, label: 'DIVORCE\nDECREE' },
          { delay: doc3In, top: 640, rot: -2, label: 'SSA\nFORM SS-5' },
        ].map((doc, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              top: doc.top,
              left: 780,
              opacity: doc.delay,
              transform: `translateX(${interpolate(doc.delay, [0, 1], [200, 0])}px) rotate(${doc.rot}deg) scale(${doc.delay})`,
            }}
          >
            <div
              style={{
                background: '#FFFFFF',
                border: '5px solid #000000',
                borderRadius: 12,
                padding: '20px 28px',
                boxShadow: '8px 8px 0 #000000',
                display: 'flex',
                alignItems: 'center',
                gap: 24,
                width: 480,
              }}
            >
              {/* Document Icon */}
              <svg width="80" height="100" viewBox="0 0 80 100">
                <path d="M10 5 L55 5 L75 25 L75 95 L10 95 Z" fill="#FEEC41" stroke="#000000" strokeWidth="4" />
                <path d="M55 5 L55 25 L75 25" fill="none" stroke="#000000" strokeWidth="4" />
                <line x1="20" y1="42" x2="65" y2="42" stroke="#000000" strokeWidth="3" />
                <line x1="20" y1="55" x2="65" y2="55" stroke="#000000" strokeWidth="3" />
                <line x1="20" y1="68" x2="55" y2="68" stroke="#000000" strokeWidth="3" />
                {/* Seal */}
                <circle cx="58" cy="82" r="9" fill="#000000" />
                <text x="58" y="86" textAnchor="middle" fontSize="10" fontWeight="900" fill="#FEEC41">OK</text>
              </svg>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 38,
                  fontWeight: 900,
                  color: '#000000',
                  lineHeight: 1.05,
                  whiteSpace: 'pre-line',
                }}
              >
                {doc.label}
              </div>
            </div>
          </div>
        ))}

        {/* Bottom Strip */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(checklistIn, [0, 1], [60, 0])}px)`,
            opacity: checklistIn,
            background: '#000000',
            padding: '20px 50px',
            borderRadius: 100,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
          }}
        >
          <svg width="40" height="40" viewBox="0 0 40 40">
            <circle cx="20" cy="20" r="18" fill="#FEEC41" />
            <polyline points="10,21 17,28 30,12" fill="none" stroke="#000000" strokeWidth="5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 800,
              color: '#FEEC41',
              letterSpacing: 1,
            }}
          >
            GIVE THEM THE FULL LIST — EVERY MARRIAGE
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
            pointerEvents: 'none',
          }}
        />
      </AbsoluteFill>

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene54;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene55',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene55: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.04]);

  const titleIn = sp(5);
  const keyIn = sp(35);
  const card1In = sp(75);
  const card2In = sp(130);

  const keySpin = interpolate(frame, [35, 95], [-180, 0], { extrapolateRight: 'clamp' });
  const keyGlow = 0.4 + Math.sin(frame * 0.12) * 0.2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 50% 50%, #B5B7FF 0%, #9FA1FF 50%, #7F82E5 100%)`,
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>

        {/* Shape Layer */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          <circle cx={960} cy={540} r={500} fill="none" stroke="#FEEC41" strokeWidth={4} opacity={0.07} />
          <circle cx={960} cy={540} r={650} fill="none" stroke="#000000" strokeWidth={3} opacity={0.05} />
          <polygon
            points="100,100 250,80 270,250 120,270"
            fill="#FEEC41"
            opacity={0.06}
            transform={`rotate(${frame * 0.2} 185 175)`}
          />
          <rect
            x={1600}
            y={830}
            width={280}
            height={280}
            fill="#000000"
            opacity={0.04}
            transform={`rotate(${-frame * 0.15} 1740 970)`}
          />
        </svg>

        {/* Motion Texture — Radial Spokes */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.06 }}>
          {Array.from({ length: 24 }).map((_, i) => {
            const angle = (i * 15 + frame * 0.3) * (Math.PI / 180);
            const x1 = 960 + Math.cos(angle) * 200;
            const y1 = 540 + Math.sin(angle) * 200;
            const x2 = 960 + Math.cos(angle) * 1200;
            const y2 = 540 + Math.sin(angle) * 1200;
            return (
              <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#000000" strokeWidth={1.5} />
            );
          })}
        </svg>

        {/* Particles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {Array.from({ length: 24 }).map((_, i) => {
            const seed = seededRandom(i + 500);
            const angle = seed * Math.PI * 2 + frame * 0.01;
            const dist = 300 + seededRandom(i + 600) * 500;
            const x = 960 + Math.cos(angle) * dist;
            const y = 540 + Math.sin(angle) * dist;
            return (
              <circle key={i} cx={x} cy={y} r={2 + seed * 4} fill="#FEEC41" opacity={0.5} />
            );
          })}
        </svg>

        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 50,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(titleIn, [0, 1], [-50, 0])}px)`,
            opacity: titleIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              background: '#000000',
              padding: '10px 36px',
              display: 'inline-block',
              borderRadius: 8,
            }}
          >
            <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#FEEC41', letterSpacing: 6 }}>
              RECAP — 4 LOOPHOLES
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 70,
              fontWeight: 900,
              color: '#000000',
              marginTop: 12,
              letterSpacing: 1,
            }}
          >
            BRINGING IT TOGETHER
          </div>
        </div>

        {/* GIANT KEY — Center */}
        <div
          style={{
            position: 'absolute',
            top: 290,
            left: '50%',
            transform: `translateX(-50%) rotate(${keySpin}deg) scale(${keyIn})`,
            opacity: keyIn,
            filter: `drop-shadow(0 0 ${30 * keyGlow}px rgba(254, 236, 65, ${keyGlow}))`,
          }}
        >
          <svg width="280" height="280" viewBox="0 0 280 280">
            {/* Key head circle */}
            <circle cx="80" cy="140" r="60" fill="#FEEC41" stroke="#000000" strokeWidth="8" />
            <circle cx="80" cy="140" r="22" fill="#9FA1FF" stroke="#000000" strokeWidth="6" />
            {/* Key shaft */}
            <rect x="140" y="128" width="120" height="24" fill="#FEEC41" stroke="#000000" strokeWidth="6" />
            {/* Key teeth */}
            <rect x="220" y="152" width="14" height="22" fill="#FEEC41" stroke="#000000" strokeWidth="6" />
            <rect x="244" y="152" width="14" height="32" fill="#FEEC41" stroke="#000000" strokeWidth="6" />
            {/* Text on key head */}
            <text x="80" y="148" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#000000">10</text>
            <text x="80" y="168" textAnchor="middle" fontFamily="Inter" fontSize="11" fontWeight="800" fill="#000000">YEARS</text>
          </svg>
        </div>

        {/* LEFT CARD — 10 YEAR RULE */}
        <div
          style={{
            position: 'absolute',
            top: 600,
            left: 140,
            opacity: card1In,
            transform: `translateY(${interpolate(card1In, [0, 1], [80, 0])}px) scale(${card1In})`,
            width: 720,
          }}
        >
          <div
            style={{
              background: '#FEEC41',
              border: '6px solid #000000',
              borderRadius: 18,
              padding: '26px 36px',
              boxShadow: '10px 10px 0 #000000',
            }}
          >
            <div style={{ display: 'flex', alignItems: 'center', gap: 18 }}>
              <div
                style={{
                  background: '#000000',
                  width: 70,
                  height: 70,
                  borderRadius: 14,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'Inter',
                  fontSize: 42,
                  fontWeight: 900,
                  color: '#FEEC41',
                }}
              >
                1
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 46,
                  fontWeight: 900,
                  color: '#000000',
                  lineHeight: 1,
                }}
              >
                10-YEAR RULE
              </div>
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 28,
                fontWeight: 700,
                color: '#000000',
                marginTop: 14,
                opacity: 0.85,
                lineHeight: 1.25,
              }}
            >
              The golden key that unlocks every benefit
            </div>
          </div>
        </div>

        {/* RIGHT CARD — 50% SPOUSAL KICKER */}
        <div
          style={{
            position: 'absolute',
            top: 600,
            right: 140,
            opacity: card2In,
            transform: `translateY(${interpolate(card2In, [0, 1], [80, 0])}px) scale(${card2In})`,
            width: 720,
          }}
        >
          <div
            style={{
              background: '#000000',
              border: '6px solid #FEEC41',
              borderRadius: 18,
              padding: '26px 36px',
              boxShadow: '10px 10px 0 #FEEC41',
            }}
          >
            <div style={{ display: 'flex', alignItems: 'center', gap: 18 }}>
              <div
                style={{
                  background: '#FEEC41',
                  width: 70,
                  height: 70,
                  borderRadius: 14,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'Inter',
                  fontSize: 42,
                  fontWeight: 900,
                  color: '#000000',
                }}
              >
                2
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 46,
                  fontWeight: 900,
                  color: '#FEEC41',
                  lineHeight: 1,
                }}
              >
                50% KICKER
              </div>
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 28,
                fontWeight: 700,
                color: '#FFFFFF',
                marginTop: 14,
                opacity: 0.9,
                lineHeight: 1.25,
              }}
            >
              Half of ex's check — no permission needed
            </div>
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.35) 100%)',
            pointerEvents: 'none',
          }}
        />
      </AbsoluteFill>

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene55;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene56',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene56: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [20, -20]);

  const titleIn = sp(5);
  const card3In = sp(30);
  const card4In = sp(100);
  const shieldPulse = 1 + Math.sin(frame * 0.15) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(45deg, #8285E8 0%, #9FA1FF 50%, #B5B7FF 100%)',
        }}
      />

      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>

        {/* Shape Layer */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          <circle
            cx={150}
            cy={500}
            r={280}
            fill="#FEEC41"
            opacity={0.05}
            transform={`scale(${1 + Math.sin(frame * 0.04) * 0.05})`}
            style={{ transformOrigin: '150px 500px' }}
          />
          <rect
            x={1500}
            y={150}
            width={350}
            height={350}
            fill="#000000"
            opacity={0.05}
            transform={`rotate(${frame * 0.12 + 30} 1675 325)`}
          />
          <polygon
            points="800,950 1120,950 960,1080"
            fill="#FEEC41"
            opacity={0.06}
          />
        </svg>

        {/* Motion Texture — Hex Grid */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.07 }}>
          {Array.from({ length: 8 }).map((_, row) =>
            Array.from({ length: 14 }).map((_, col) => {
              const x = col * 145 + (row % 2) * 72;
              const y = row * 130 + 50;
              const size = 30;
              return (
                <polygon
                  key={`${row}-${col}`}
                  points={`${x},${y - size} ${x + size * 0.87},${y - size / 2} ${x + size * 0.87},${y + size / 2} ${x},${y + size} ${x - size * 0.87},${y + size / 2} ${x - size * 0.87},${y - size / 2}`}
                  fill="none"
                  stroke="#000000"
                  strokeWidth={1.5}
                />
              );
            })
          )}
        </svg>

        {/* Particles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {Array.from({ length: 22 }).map((_, i) => {
            const seed = seededRandom(i + 700);
            const x = (seed * 1920 + frame * (0.3 + seed)) % 1920;
            const y = seededRandom(i + 800) * 1080;
            return (
              <circle
                key={i}
                cx={x}
                cy={y + Math.sin(frame * 0.05 + i) * 10}
                r={3 + seed * 3}
                fill={i % 2 === 0 ? '#FEEC41' : '#000000'}
                opacity={0.45}
              />
            );
          })}
        </svg>

        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 50,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(titleIn, [0, 1], [-40, 0])}px)`,
            opacity: titleIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 72,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 1,
            }}
          >
            LOOPHOLES <span style={{ background: '#FEEC41', padding: '0 20px', borderRadius: 8 }}>3 & 4</span>
          </div>
        </div>

        {/* CARD 3 — 2-YEAR SHORTCUT (LEFT) */}
        <div
          style={{
            position: 'absolute',
            top: 220,
            left: 100,
            opacity: card3In,
            transform: `translateX(${interpolate(card3In, [0, 1], [-150, 0])}px) scale(${card3In})`,
            width: 820,
          }}
        >
          <div
            style={{
              background: '#FEEC41',
              border: '7px solid #000000',
              borderRadius: 20,
              padding: '36px 40px',
              boxShadow: '12px 12px 0 #000000',
              position: 'relative',
            }}
          >
            {/* Number badge */}
            <div
              style={{
                position: 'absolute',
                top: -30,
                left: -30,
                background: '#000000',
                color: '#FEEC41',
                width: 90,
                height: 90,
                borderRadius: '50%',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontFamily: 'Inter',
                fontSize: 56,
                fontWeight: 900,
                border: '5px solid #FEEC41',
              }}
            >
              3
            </div>

            {/* Hourglass icon */}
            <svg width="100" height="120" viewBox="0 0 100 120" style={{ marginLeft: 60 }}>
              <path d="M15 10 L85 10 L85 25 L55 55 L85 85 L85 110 L15 110 L15 85 L45 55 L15 25 Z" fill="#000000" />
              <path
                d={`M25 18 L75 18 L75 28 L50 ${50 + interpolate(card3In, [0, 1], [-15, 5])} L25 28 Z`}
                fill="#FEEC41"
                opacity={0.7}
              />
              <path
                d={`M25 102 L75 102 L75 ${90 - interpolate(card3In, [0, 1], [-15, 5])} L50 ${75 - interpolate(card3In, [0, 1], [-10, 0])} Z`}
                fill="#FEEC41"
                opacity={0.7}
              />
            </svg>

            <div
              style={{
                position: 'absolute',
                top: 36,
                left: 220,
                fontFamily: 'Inter, Poppins',
                fontSize: 52,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1,
              }}
            >
              2-YEAR<br/>SHORTCUT
            </div>
            <div
              style={{
                marginTop: 16,
                marginLeft: 60,
                fontFamily: 'Inter',
                fontSize: 28,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.85,
                lineHeight: 1.25,
              }}
            >
              File for benefits even if your ex hasn't filed yet
            </div>
          </div>
        </div>

        {/* CARD 4 — SURVIVOR SHIELD (RIGHT/BOTTOM) */}
        <div
          style={{
            position: 'absolute',
            top: 580,
            right: 100,
            opacity: card4In,
            transform: `translateX(${interpolate(card4In, [0, 1], [150, 0])}px) scale(${card4In})`,
            width: 820,
          }}
        >
          <div
            style={{
              background: '#000000',
              border: '7px solid #FEEC41',
              borderRadius: 20,
              padding: '36px 40px',
              boxShadow: '12px 12px 0 #FEEC41',
              position: 'relative',
            }}
          >
            {/* Number badge */}
            <div
              style={{
                position: 'absolute',
                top: -30,
                right: -30,
                background: '#FEEC41',
                color: '#000000',
                width: 90,
                height: 90,
                borderRadius: '50%',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontFamily: 'Inter',
                fontSize: 56,
                fontWeight: 900,
                border: '5px solid #000000',
              }}
            >
              4
            </div>

            {/* Shield icon */}
            <div style={{ display: 'flex', alignItems: 'center', gap: 30 }}>
              <svg width="120" height="140" viewBox="0 0 120 140" style={{ transform: `scale(${shieldPulse})`, transformOrigin: 'center' }}>
                <path
                  d="M60 5 L110 25 L110 70 Q110 110 60 135 Q10 110 10 70 L10 25 Z"
                  fill="#FEEC41"
                  stroke="#FEEC41"
                  strokeWidth="3"
                />
                <text x="60" y="65" textAnchor="middle" fontFamily="Inter" fontSize="32" fontWeight="900" fill="#000000">100</text>
                <text x="60" y="90" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#000000">%</text>
              </svg>
              <div>
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 52,
                    fontWeight: 900,
                    color: '#FEEC41',
                    lineHeight: 1,
                  }}
                >
                  SURVIVOR<br/>SHIELD
                </div>
                <div
                  style={{
                    marginTop: 16,
                    fontFamily: 'Inter',
                    fontSize: 28,
                    fontWeight: 700,
                    color: '#FFFFFF',
                    opacity: 0.9,
                    lineHeight: 1.25,
                  }}
                >
                  Claim 100% of their check if they pass away
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
            pointerEvents: 'none',
          }}
        />
      </AbsoluteFill>

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene56;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene57',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene57: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(5);
  const bar1In = sp(30);
  const bar2In = sp(48);
  const bar3In = sp(66);
  const highlightIn = sp(105);
  const warningIn = sp(155);

  const highlightPulse = 1 + Math.sin(frame * 0.18) * 0.04;
  const warnShake = frame > 165 && frame < 185 ? Math.sin(frame * 1.6) * 5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at top, #B0B3FF 0%, #9FA1FF 60%, #7E81E5 100%)',
        }}
      />

      <AbsoluteFill>

        {/* Shape Layer */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          <circle cx={1700} cy={200} r={180} fill="#FEEC41" opacity={0.06} />
          <circle cx={200} cy={950} r={240} fill="#000000" opacity={0.04} />
          <rect
            x={1450}
            y={800}
            width={250}
            height={250}
            fill="#FEEC41"
            opacity={0.04}
            transform={`rotate(${frame * 0.12} 1575 925)`}
          />
        </svg>

        {/* Motion Texture — Diagonal Stripes */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.06 }}>
          {Array.from({ length: 30 }).map((_, i) => (
            <line
              key={i}
              x1={-200 + i * 100 - ((frame * 0.6) % 100)}
              y1={1080}
              x2={400 + i * 100 - ((frame * 0.6) % 100)}
              y2={0}
              stroke="#000000"
              strokeWidth={1.5}
            />
          ))}
        </svg>

        {/* Particles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {Array.from({ length: 20 }).map((_, i) => {
            const seed = seededRandom(i + 900);
            const x = (seed * 1920 + Math.sin(frame * 0.02 + i) * 25) % 1920;
            const y = (seededRandom(i + 1000) * 1080 + frame * (0.4 + seed * 0.4)) % 1080;
            return (
              <polygon
                key={i}
                points={`${x},${y - 5} ${x + 5},${y} ${x},${y + 5} ${x - 5},${y}`}
                fill="#FEEC41"
                opacity={0.5}
              />
            );
          })}
        </svg>

        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: '50%',
            transform: `translateX(-50%) scale(${titleIn})`,
            opacity: titleIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              background: '#000000',
              padding: '10px 32px',
              display: 'inline-block',
              borderRadius: 8,
            }}
          >
            <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#FEEC41', letterSpacing: 5 }}>
              LOOPHOLE #4
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 76,
              fontWeight: 900,
              color: '#000000',
              marginTop: 14,
              letterSpacing: 1,
            }}
          >
            CHERRY-PICK <span style={{ color: '#FEEC41', WebkitTextStroke: '3px #000000' }}>THE HIGHEST</span>
          </div>
        </div>

        {/* COMPARISON BARS — 3 Exes */}
        <div
          style={{
            position: 'absolute',
            bottom: 280,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            gap: 80,
            alignItems: 'flex-end',
          }}
        >
          {[
            { delay: bar1In, height: 200, label: 'EX #1', value: '$2,100', isHighest: false },
            { delay: bar2In, height: 360, label: 'EX #2', value: '$3,400', isHighest: true },
            { delay: bar3In, height: 240, label: 'EX #3', value: '$2,500', isHighest: false },
          ].map((bar, i) => (
            <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
              {/* Value above */}
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: bar.isHighest ? 56 : 42,
                  fontWeight: 900,
                  color: bar.isHighest ? '#000000' : '#000000',
                  opacity: bar.delay,
                  marginBottom: 12,
                  transform: bar.isHighest ? `scale(${highlightPulse})` : 'none',
                }}
              >
                {bar.value}
              </div>
              {/* Bar */}
              <div
                style={{
                  width: bar.isHighest ? 220 : 180,
                  height: bar.height * bar.delay,
                  background: bar.isHighest ? '#FEEC41' : '#000000',
                  border: bar.isHighest ? '7px solid #000000' : '4px solid #000000',
                  borderRadius: '14px 14px 0 0',
                  boxShadow: bar.isHighest ? '8px 8px 0 #000000' : 'none',
                  position: 'relative',
                  overflow: 'hidden',
                }}
              >
                {/* Crown for highest */}
                {bar.isHighest && bar.delay > 0.8 && (
                  <div
                    style={{
                      position: 'absolute',
                      top: -75,
                      left: '50%',
                      transform: `translateX(-50%) scale(${highlightIn * highlightPulse})`,
                      opacity: highlightIn,
                    }}
                  >
                    <svg width="80" height="60" viewBox="0 0 80 60">
                      <path d="M5 50 L15 15 L30 35 L40 5 L50 35 L65 15 L75 50 Z" fill="#FEEC41" stroke="#000000" strokeWidth="4" strokeLinejoin="round" />
                      <circle cx="15" cy="13" r="4" fill="#000000" />
                      <circle cx="40" cy="3" r="4" fill="#000000" />
                      <circle cx="65" cy="13" r="4" fill="#000000" />
                    </svg>
                  </div>
                )}
              </div>
              {/* Label below */}
              <div
                style={{
                  marginTop: 14,
                  background: bar.isHighest ? '#FEEC41' : 'transparent',
                  border: bar.isHighest ? '4px solid #000000' : 'none',
                  padding: bar.isHighest ? '6px 18px' : '0',
                  borderRadius: 8,
                  fontFamily: 'Inter',
                  fontSize: bar.isHighest ? 38 : 32,
                  fontWeight: 900,
                  color: '#000000',
                  opacity: bar.delay,
                }}
              >
                {bar.label}
              </div>
            </div>
          ))}
        </div>

        {/* Warning Bar — Bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: '50%',
            transform: `translateX(-50%) translateX(${warnShake}px) translateY(${interpolate(warningIn, [0, 1], [80, 0])}px)`,
            opacity: warningIn,
            background: '#000000',
            padding: '20px 44px',
            borderRadius: 14,
            border: '4px solid #FEEC41',
            display: 'flex',
            alignItems: 'center',
            gap: 22,
          }}
        >
          <svg width="50" height="50" viewBox="0 0 50 50">
            <circle cx="25" cy="25" r="22" fill="#FEEC41" stroke="#FEEC41" strokeWidth="2" />
            <line x1="25" y1="14" x2="25" y2="30" stroke="#000000" strokeWidth="5" strokeLinecap="round" />
            <circle cx="25" cy="38" r="3" fill="#000000" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 800,
              color: '#FFFFFF',
              letterSpacing: 1,
            }}
          >
            NONE OF THIS HAPPENS <span style={{ color: '#FEEC41' }}>AUTOMATICALLY</span>
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
            pointerEvents: 'none',
          }}
        />
      </AbsoluteFill>

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene57;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene58',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene58: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.06]);

  const titleIn = sp(5);
  const buildingIn = sp(28);
  const stepOneIn = sp(70);
  const stepTwoIn = sp(105);
  const stepThreeIn = sp(140);
  const finalIn = sp(180);

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, #7B7EE0 0%, #9FA1FF 50%, #B5B7FF 100%)',
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>

        {/* Shape Layer */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          <circle cx={1750} cy={300} r={200} fill="#FEEC41" opacity={0.05} />
          <rect x={70} y={120} width={300} height={300} fill="#000000" opacity={0.04} transform={`rotate(${frame * 0.1} 220 270)`} />
          <polygon points="900,950 1080,1050 720,1050" fill="#FEEC41" opacity={0.06} />
        </svg>

        {/* Motion Texture — Grid */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.07 }}>
          {Array.from({ length: 12 }).map((_, i) => (
            <line key={`h${i}`} x1={0} y1={i * 90 + ((frame * 0.4) % 90)} x2={1920} y2={i * 90 + ((frame * 0.4) % 90)} stroke="#000000" strokeWidth={1} />
          ))}
          {Array.from({ length: 20 }).map((_, i) => (
            <line key={`v${i}`} x1={i * 100} y1={0} x2={i * 100} y2={1080} stroke="#000000" strokeWidth={1} />
          ))}
        </svg>

        {/* Particles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {Array.from({ length: 20 }).map((_, i) => {
            const seed = seededRandom(i + 1100);
            const x = seed * 1920;
            const y = (seededRandom(i + 1200) * 1080 - frame * (0.3 + seed * 0.3)) % 1080;
            const adjY = y < 0 ? y + 1080 : y;
            return (
              <circle key={i} cx={x} cy={adjY} r={3 + seed * 3} fill={i % 2 === 0 ? '#FEEC41' : '#000000'} opacity={0.4} />
            );
          })}
        </svg>

        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(titleIn, [0, 1], [-40, 0])}px)`,
            opacity: titleIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 78,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 1,
              lineHeight: 1,
            }}
          >
            SSA WON'T <span style={{ background: '#FEEC41', padding: '0 18px', borderRadius: 8 }}>CALL YOU</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.7,
              marginTop: 12,
              letterSpacing: 4,
            }}
          >
            YOU MUST TAKE ACTION
          </div>
        </div>

        {/* GOVERNMENT BUILDING — Left */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            left: 120,
            opacity: buildingIn,
            transform: `scale(${buildingIn}) translateY(${interpolate(buildingIn, [0, 1], [50, 0])}px)`,
          }}
        >
          <svg width="420" height="380" viewBox="0 0 420 380">
            {/* Base */}
            <rect x="20" y="320" width="380" height="50" fill="#000000" />
            {/* Steps */}
            <rect x="50" y="290" width="320" height="35" fill="#000000" />
            {/* Pillars */}
            {[80, 145, 210, 275, 340].map((x, i) => (
              <rect key={i} x={x - 18} y={150} width={36} height={140} fill="#FEEC41" stroke="#000000" strokeWidth={4} />
            ))}
            {/* Pediment */}
            <polygon points="20,150 400,150 210,40" fill="#FEEC41" stroke="#000000" strokeWidth={6} strokeLinejoin="round" />
            {/* SSA Text on pediment */}
            <text x="210" y="115" textAnchor="middle" fontFamily="Inter" fontSize="32" fontWeight="900" fill="#000000">SSA</text>
            {/* Roof line */}
            <rect x="10" y="140" width="400" height="14" fill="#000000" />
            {/* Closed door — X mark */}
            <rect x="180" y="220" width="60" height="70" fill="#000000" />
            <line x1="188" y1="228" x2="232" y2="282" stroke="#FEEC41" strokeWidth="5" />
            <line x1="232" y1="228" x2="188" y2="282" stroke="#FEEC41" strokeWidth="5" />
          </svg>
          <div
            style={{
              marginTop: 14,
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 800,
              color: '#000000',
              textAlign: 'center',
              opacity: 0.85,
            }}
          >
            THEY WAIT — THEY DON'T CHASE
          </div>
        </div>

        {/* THREE ACTION STEPS — Right */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            right: 100,
            width: 780,
            display: 'flex',
            flexDirection: 'column',
            gap: 22,
          }}
        >
          {[
            { delay: stepOneIn, n: '01', text: 'KNOW THE RULES EXIST', icon: 'book' },
            { delay: stepTwoIn, n: '02', text: 'BRING ALL MARRIAGE DATES', icon: 'calendar' },
            { delay: stepThreeIn, n: '03', text: 'MAKE THE CLAIM YOURSELF', icon: 'hand' },
          ].map((step, i) => (
            <div
              key={i}
              style={{
                opacity: step.delay,
                transform: `translateX(${interpolate(step.delay, [0, 1], [120, 0])}px)`,
                background: i % 2 === 0 ? '#FEEC41' : '#000000',
                border: i % 2 === 0 ? '5px solid #000000' : '5px solid #FEEC41',
                borderRadius: 16,
                padding: '20px 30px',
                display: 'flex',
                alignItems: 'center',
                gap: 24,
                boxShadow: i % 2 === 0 ? '8px 8px 0 #000000' : '8px 8px 0 #FEEC41',
              }}
            >
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 72,
                  fontWeight: 900,
                  color: i % 2 === 0 ? '#000000' : '#FEEC41',
                  lineHeight: 1,
                  opacity: 0.4,
                  width: 110,
                }}
              >
                {step.n}
              </div>
              {/* Icon */}
              <svg width="70" height="70" viewBox="0 0 70 70">
                {step.icon === 'book' && (
                  <>
                    <rect x="10" y="12" width="50" height="46" fill={i % 2 === 0 ? '#000000' : '#FEEC41'} />
                    <line x1="35" y1="12" x2="35" y2="58" stroke={i % 2 === 0 ? '#FEEC41' : '#000000'} strokeWidth="3" />
                    <line x1="18" y1="24" x2="30" y2="24" stroke={i % 2 === 0 ? '#FEEC41' : '#000000'} strokeWidth="2" />
                    <line x1="18" y1="32" x2="30" y2="32" stroke={i % 2 === 0 ? '#FEEC41' : '#000000'} strokeWidth="2" />
                    <line x1="40" y1="24" x2="52" y2="24" stroke={i % 2 === 0 ? '#FEEC41' : '#000000'} strokeWidth="2" />
                    <line x1="40" y1="32" x2="52" y2="32" stroke={i % 2 === 0 ? '#FEEC41' : '#000000'} strokeWidth="2" />
                  </>
                )}
                {step.icon === 'calendar' && (
                  <>
                    <rect x="8" y="14" width="54" height="48" rx="4" fill={i % 2 === 0 ? '#000000' : '#FEEC41'} />
                    <rect x="8" y="14" width="54" height="14" fill={i % 2 === 0 ? '#000000' : '#FEEC41'} />
                    <rect x="14" y="34" width="10" height="10" fill={i % 2 === 0 ? '#FEEC41' : '#000000'} />
                    <rect x="30" y="34" width="10" height="10" fill={i % 2 === 0 ? '#FEEC41' : '#000000'} />
                    <rect x="46" y="34" width="10" height="10" fill={i % 2 === 0 ? '#FEEC41' : '#000000'} />
                    <line x1="18" y1="8" x2="18" y2="18" stroke={i % 2 === 0 ? '#000000' : '#FEEC41'} strokeWidth="3" />
                    <line x1="52" y1="8" x2="52" y2="18" stroke={i % 2 === 0 ? '#000000' : '#FEEC41'} strokeWidth="3" />
                  </>
                )}
                {step.icon === 'hand' && (
                  <>
                    <path d="M22 14 L22 38 M28 10 L28 38 M34 12 L34 38 M40 16 L40 38" stroke={i % 2 === 0 ? '#000000' : '#FEEC41'} strokeWidth="6" strokeLinecap="round" />
                    <path d="M16 36 Q14 50 22 58 L46 58 Q54 54 54 44 L54 30 Q54 26 50 26 Q46 26 46 30 L46 38" fill={i % 2 === 0 ? '#000000' : '#FEEC41'} />
                  </>
                )}
              </svg>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 40,
                  fontWeight: 900,
                  color: i % 2 === 0 ? '#000000' : '#FEEC41',
                  lineHeight: 1.05,
                  flex: 1,
                }}
              >
                {step.text}
              </div>
            </div>
          ))}
        </div>

        {/* Bottom Tag */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            left: '50%',
            transform: `translateX(-50%) scale(${finalIn})`,
            opacity: finalIn,
            fontFamily: 'Inter, Poppins',
            fontSize: 30,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 4,
          }}
        >
          THE CHOICE IS YOURS
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
            pointerEvents: 'none',
          }}
        />
      </AbsoluteFill>

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene58;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene59',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene59: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [-10, 10]);

  const titleIn = sp(5);
  const moneyIn = sp(35);
  const bridgeIn = sp(70);
  const personIn = sp(100);
  const groceryIn = sp(140);
  const heatIn = sp(165);

  const moneyFloat = Math.sin(frame * 0.07) * 6;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(170deg, #9FA1FF 0%, #C5C7FF 100%)',
        }}
      />

      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>

        {/* Shape Layer */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          <circle cx={960} cy={550} r={400} fill="none" stroke="#FEEC41" strokeWidth={3} opacity={0.08} />
          <circle cx={300} cy={250} r={170} fill="#FEEC41" opacity={0.06} />
          <rect x={1500} y={780} width={300} height={300} fill="#000000" opacity={0.04} transform={`rotate(${frame * 0.1} 1650 930)`} />
        </svg>

        {/* Motion Texture — Wavy Lines */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.07 }}>
          {Array.from({ length: 10 }).map((_, i) => (
            <path
              key={i}
              d={`M 0 ${i * 110 + 50} Q 480 ${i * 110 + 50 + Math.sin(frame * 0.05 + i) * 25} 960 ${i * 110 + 50} T 1920 ${i * 110 + 50}`}
              fill="none"
              stroke="#000000"
              strokeWidth={2}
            />
          ))}
        </svg>

        {/* Particles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {Array.from({ length: 22 }).map((_, i) => {
            const seed = seededRandom(i + 1300);
            const x = (seed * 1920 + Math.sin(frame * 0.02 + i * 0.5) * 30) % 1920;
            const y = (seededRandom(i + 1400) * 1080 - frame * (0.3 + seed * 0.5)) % 1080;
            const adjY = y < 0 ? y + 1080 : y;
            return (
              <circle key={i} cx={x} cy={adjY} r={2 + seed * 4} fill={i % 2 === 0 ? '#FEEC41' : '#000000'} opacity={0.4} />
            );
          })}
        </svg>

        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(titleIn, [0, 1], [-40, 0])}px)`,
            opacity: titleIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 70,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 1,
              lineHeight: 1.05,
            }}
          >
            KNOWLEDGE = <span style={{ background: '#FEEC41', padding: '0 16px', borderRadius: 8 }}>YOUR MONEY</span>
          </div>
        </div>

        {/* BRIDGE METAPHOR — Center */}
        {/* Left side: Person */}
        <div
          style={{
            position: 'absolute',
            top: 420,
            left: 180,
            opacity: personIn,
            transform: `scale(${personIn})`,
          }}
        >
          {/* Knowledge book / brain */}
          <svg width="200" height="240" viewBox="0 0 200 240">
            <circle cx="100" cy="60" r="42" fill="#FEEC41" stroke="#000000" strokeWidth="6" />
            {/* Lightbulb in head */}
            <circle cx="100" cy="55" r="14" fill="#000000" />
            <text x="100" y="62" textAnchor="middle" fontFamily="Inter" fontSize="18" fontWeight="900" fill="#FEEC41">i</text>
            {/* Body */}
            <rect x="60" y="110" width="80" height="100" rx="10" fill="#000000" />
            {/* Arms */}
            <rect x="30" y="120" width="30" height="70" rx="8" fill="#000000" />
            <rect x="140" y="120" width="30" height="70" rx="8" fill="#000000" />
          </svg>
          <div
            style={{
              marginTop: 16,
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 900,
              color: '#000000',
              textAlign: 'center',
            }}
          >
            YOU
          </div>
        </div>

        {/* Bridge — Animated dashes */}
        <div
          style={{
            position: 'absolute',
            top: 510,
            left: 410,
            opacity: bridgeIn,
            transform: `scaleX(${bridgeIn})`,
            transformOrigin: 'left',
            width: 1100,
          }}
        >
          <svg width="1100" height="100" viewBox="0 0 1100 100">
            <line
              x1="0"
              y1="50"
              x2="1050"
              y2="50"
              stroke="#000000"
              strokeWidth="14"
              strokeDasharray="40 14"
              strokeDashoffset={-(frame * 3) % 54}
            />
            <polygon points="1040,30 1080,50 1040,70" fill="#000000" />
            <text
              x="525"
              y="32"
              textAnchor="middle"
              fontFamily="Inter, Poppins"
              fontSize="34"
              fontWeight="900"
              fill="#000000"
            >
              YOUR EARNED $$$
            </text>
          </svg>
        </div>

        {/* Right side: Money Vault */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            right: 180,
            opacity: moneyIn,
            transform: `translateY(${moneyFloat}px) scale(${moneyIn})`,
          }}
        >
          <svg width="280" height="280" viewBox="0 0 280 280">
            {/* Vault body */}
            <rect x="20" y="40" width="240" height="200" rx="16" fill="#000000" />
            <rect x="35" y="55" width="210" height="170" rx="10" fill="#FEEC41" />
            {/* Vault dial */}
            <circle cx="140" cy="140" r="55" fill="#000000" />
            <circle cx="140" cy="140" r="42" fill="#FEEC41" />
            <circle cx="140" cy="140" r="8" fill="#000000" />
            {/* Dial marks */}
            {Array.from({ length: 8 }).map((_, i) => {
              const angle = (i * 45) * (Math.PI / 180);
              const x1 = 140 + Math.cos(angle) * 36;
              const y1 = 140 + Math.sin(angle) * 36;
              const x2 = 140 + Math.cos(angle) * 44;
              const y2 = 140 + Math.sin(angle) * 44;
              return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#000000" strokeWidth="3" />;
            })}
            {/* Pointer */}
            <line
              x1="140"
              y1="140"
              x2={140 + Math.cos(frame * 0.08) * 30}
              y2={140 + Math.sin(frame * 0.08) * 30}
              stroke="#000000"
              strokeWidth="4"
              strokeLinecap="round"
            />
            {/* Dollar sign */}
            <text x="140" y="225" textAnchor="middle" fontFamily="Inter" fontSize="38" fontWeight="900" fill="#000000">$</text>
          </svg>
          <div
            style={{
              marginTop: 4,
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 900,
              color: '#000000',
              textAlign: 'center',
            }}
          >
            BENEFITS
          </div>
        </div>

        {/* Bottom: Worry items NOT to worry about */}
        <div
          style={{
            position: 'absolute',
            bottom: 50,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            gap: 30,
            alignItems: 'center',
          }}
        >
          {/* Groceries icon */}
          <div
            style={{
              opacity: groceryIn,
              transform: `scale(${groceryIn})`,
              background: '#000000',
              borderRadius: 14,
              padding: '14px 28px',
              display: 'flex',
              alignItems: 'center',
              gap: 14,
              border: '3px solid #FEEC41',
            }}
          >
            <svg width="48" height="48" viewBox="0 0 48 48">
              <path d="M8 14 L40 14 L36 38 L12 38 Z" fill="#FEEC41" stroke="#FEEC41" strokeWidth="2" strokeLinejoin="round" />
              <path d="M14 14 L18 6 L30 6 L34 14" fill="none" stroke="#FEEC41" strokeWidth="3" />
            </svg>
            <span style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 800, color: '#FEEC41' }}>GROCERIES</span>
          </div>

          {/* X mark */}
          <div
            style={{
              opacity: Math.min(groceryIn, heatIn),
              fontFamily: 'Inter',
              fontSize: 56,
              fontWeight: 900,
              color: '#000000',
            }}
          >
            NO MORE WORRY
          </div>

          {/* Heat icon */}
          <div
            style={{
              opacity: heatIn,
              transform: `scale(${heatIn})`,
              background: '#000000',
              borderRadius: 14,
              padding: '14px 28px',
              display: 'flex',
              alignItems: 'center',
              gap: 14,
              border: '3px solid #FEEC41',
            }}
          >
            <svg width="48" height="48" viewBox="0 0 48 48">
              <path d="M24 4 Q14 18 14 26 Q14 38 24 42 Q34 38 34 26 Q34 18 24 4 Z" fill="#FEEC41" />
              <path d="M24 18 Q19 24 19 30 Q19 36 24 38 Q29 36 29 30 Q29 24 24 18 Z" fill="#000000" />
            </svg>
            <span style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 800, color: '#FEEC41' }}>HEAT BILLS</span>
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
            pointerEvents: 'none',
          }}
        />
      </AbsoluteFill>

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene59;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene60',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene60: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.08]);

  const useThemIn = sp(5);
  const subBtnIn = sp(50);
  const shareIn = sp(95);
  const sisterIn = sp(130);
  const brotherIn = sp(145);
  const friendIn = sp(160);
  const neighborIn = sp(175);
  const finalLineIn = sp(210);

  const btnPulse = 1 + Math.sin(frame * 0.18) * 0.04;
  const ringPulse = interpolate(frame % 60, [0, 60], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 50% 50%, #C0C2FF 0%, #9FA1FF 50%, #7A7DE0 100%)`,
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>

        {/* Shape Layer */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          <circle cx={960} cy={540} r={350 + ringPulse * 100} fill="none" stroke="#FEEC41" strokeWidth={3} opacity={(1 - ringPulse) * 0.5} />
          <circle cx={960} cy={540} r={250 + ringPulse * 80} fill="none" stroke="#FEEC41" strokeWidth={2} opacity={(1 - ringPulse) * 0.4} />
          <polygon points="100,80 280,60 300,260 120,280" fill="#FEEC41" opacity={0.05} transform={`rotate(${frame * 0.15} 200 170)`} />
          <rect x={1550} y={830} width={280} height={280} fill="#000000" opacity={0.04} transform={`rotate(${-frame * 0.12} 1690 970)`} />
        </svg>

        {/* Motion Texture — Stars/Sparkles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.1 }}>
          {Array.from({ length: 30 }).map((_, i) => {
            const seed = seededRandom(i + 2000);
            const x = seed * 1920;
            const y = seededRandom(i + 2100) * 1080;
            const size = 4 + Math.sin(frame * 0.1 + i) * 3;
            return (
              <g key={i} transform={`translate(${x} ${y}) rotate(${frame + i * 30})`}>
                <line x1={-size} y1={0} x2={size} y2={0} stroke="#000000" strokeWidth={1.5} />
                <line x1={0} y1={-size} x2={0} y2={size} stroke="#000000" strokeWidth={1.5} />
              </g>
            );
          })}
        </svg>

        {/* Particles */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {Array.from({ length: 26 }).map((_, i) => {
            const seed = seededRandom(i + 2200);
            const x = (seed * 1920 + Math.sin(frame * 0.02 + i) * 40) % 1920;
            const y = (seededRandom(i + 2300) * 1080 - frame * (0.4 + seed * 0.6)) % 1080;
            const adjY = y < 0 ? y + 1080 : y;
            return (
              <circle key={i} cx={x} cy={adjY} r={2 + seed * 4} fill={i % 2 === 0 ? '#FEEC41' : '#000000'} opacity={0.5} />
            );
          })}
        </svg>

        {/* HEADLINE — USE THEM */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: '50%',
            transform: `translateX(-50%) scale(${useThemIn})`,
            opacity: useThemIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 130,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 2,
              lineHeight: 1,
            }}
          >
            USE <span style={{ color: '#FEEC41', WebkitTextStroke: '4px #000000' }}>THEM</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.7,
              marginTop: 8,
              letterSpacing: 5,
            }}
          >
            BUILT BY LAW — FOR YOU
          </div>
        </div>

        {/* SUBSCRIBE BUTTON — Center */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            left: '50%',
            transform: `translateX(-50%) scale(${subBtnIn * btnPulse})`,
            opacity: subBtnIn,
          }}
        >
          <div
            style={{
              background: '#000000',
              border: '7px solid #FEEC41',
              borderRadius: 70,
              padding: '24px 70px',
              boxShadow: '10px 10px 0 #FEEC41',
              display: 'flex',
              alignItems: 'center',
              gap: 24,
            }}
          >
            {/* Bell icon */}
            <svg width="60" height="60" viewBox="0 0 60 60">
              <path
                d="M30 8 Q18 8 18 24 L18 38 L12 44 L48 44 L42 38 L42 24 Q42 8 30 8 Z"
                fill="#FEEC41"
                transform={`rotate(${Math.sin(frame * 0.2) * 8} 30 30)`}
              />
              <circle cx="30" cy="50" r="4" fill="#FEEC41" />
            </svg>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 64,
                fontWeight: 900,
                color: '#FEEC41',
                letterSpacing: 2,
              }}
            >
              SUBSCRIBE
            </div>
          </div>
        </div>

        {/* SHARE LABEL */}
        <div
          style={{
            position: 'absolute',
            top: 540,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(shareIn, [0, 1], [40, 0])}px)`,
            opacity: shareIn,
            fontFamily: 'Inter',
            fontSize: 34,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 4,
          }}
        >
          PASS IT TO →
        </div>

        {/* Four people pills */}
        <div
          style={{
            position: 'absolute',
            top: 620,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            gap: 28,
          }}
        >
          {[
            { delay: sisterIn, label: 'SISTER', color: '#FEEC41' },
            { delay: brotherIn, label: 'BROTHER', color: '#000000' },
            { delay: friendIn, label: 'FRIEND', color: '#FEEC41' },
            { delay: neighborIn, label: 'NEIGHBOR', color: '#000000' },
          ].map((person, i) => (
            <div
              key={i}
              style={{
                opacity: person.delay,
                transform: `translateY(${interpolate(person.delay, [0, 1], [40, 0])}px) scale(${person.delay})`,
                background: person.color,
                border: person.color === '#FEEC41' ? '5px solid #000000' : '5px solid #FEEC41',
                borderRadius: 100,
                padding: '20px 36px',
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                gap: 8,
                boxShadow: person.color === '#FEEC41' ? '6px 6px 0 #000000' : '6px 6px 0 #FEEC41',
              }}
            >
              {/* Person silhouette */}
              <svg width="50" height="50" viewBox="0 0 50 50">
                <circle cx="25" cy="16" r="10" fill={person.color === '#FEEC41' ? '#000000' : '#FEEC41'} />
                <path d="M8 46 Q8 30 25 30 Q42 30 42 46 Z" fill={person.color === '#FEEC41' ? '#000000' : '#FEEC41'} />
              </svg>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 30,
                  fontWeight: 900,
                  color: person.color === '#FEEC41' ? '#000000' : '#FEEC41',
                  letterSpacing: 1,
                }}
              >
                {person.label}
              </div>
            </div>
          ))}
        </div>

        {/* Final line */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: '50%',
            transform: `translateX(-50%) scale(${finalLineIn})`,
            opacity: finalLineIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              background: '#000000',
              padding: '14px 40px',
              borderRadius: 12,
              display: 'inline-block',
              border: '3px solid #FEEC41',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 36,
                fontWeight: 800,
                color: '#FEEC41',
                letterSpacing: 2,
              }}
            >
              SEE YOU IN THE NEXT ONE
            </div>
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.32) 100%)',
            pointerEvents: 'none',
          }}
        />
      </AbsoluteFill>

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene60;
```

