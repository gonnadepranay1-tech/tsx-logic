```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene51_RevenueMachine',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene51_RevenueMachine: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slow camera zoom-in
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.07]);

  // Flowing dollar-orbs: YOUR STRESS -> THEIR REVENUE
  const orbs = Array.from({ length: 8 }, (_, i) => {
    const t = ((frame - i * 22) / 90) % 1;
    const active = frame > 60 + i * 22 && t >= 0;
    const x = interpolate(t, [0, 1], [480, 1440]);
    const y = 560 + Math.sin(t * Math.PI) * -90;
    const op = active ? Math.sin(Math.min(1, Math.max(0, t)) * Math.PI) : 0;
    return { x, y, op, key: i };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#12302B', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse 120% 90% at ${50 + Math.sin(frame / 90) * 8}% 40%, #408175 0%, #1E4A42 45%, #0E2622 100%)`,
        transform: `scale(${camZoom})`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: '2px solid #EBEDE3', opacity: 0.05,
        left: -250 + Math.sin(frame / 120) * 30, top: -300,
        transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame / 80) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 600, height: 600,
        border: '2px solid #FFF449', opacity: 0.04,
        right: -180, bottom: -220,
        transform: `rotate(${45 + frame * 0.08}deg)`,
      }} />

      {/* Motion texture: diagonal lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 24 }, (_, i) => (
          <line key={i} x1={i * 90 - ((frame * 0.5) % 90)} y1={0} x2={i * 90 - 400 - ((frame * 0.5) % 90)} y2={1080}
            stroke="#EBEDE3" strokeWidth={1} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 20 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i) * 1920,
          top: ((seededRandom(i + 50) * 1080) - frame * (0.3 + seededRandom(i) * 0.5) + 2160) % 1080,
          width: 3 + seededRandom(i + 9) * 4, height: 3 + seededRandom(i + 9) * 4,
          borderRadius: '50%', backgroundColor: i % 4 === 0 ? '#FFF449' : '#EBEDE3',
          opacity: 0.12 + seededRandom(i + 3) * 0.15,
        }} />
      ))}

      {/* Headline */}
      <div style={{
        position: 'absolute', top: 130, width: '100%', textAlign: 'center',
        transform: `translateY(${(1 - sp(10)) * -80}px)`, opacity: sp(10),
      }}>
        <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 100, fontWeight: 900, color: '#EBEDE3', letterSpacing: -2 }}>
          YOUR STRESS IS
        </div>
        <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 132, fontWeight: 900, color: '#FFF449', letterSpacing: -3, marginTop: -10 }}>
          THEIR REVENUE
        </div>
      </div>

      {/* Two nodes with flow */}
      <div style={{
        position: 'absolute', left: 260, top: 480, width: 340, padding: '34px 20px',
        background: 'rgba(14,38,34,0.85)', border: '2px solid #EBEDE3', borderRadius: 20,
        textAlign: 'center', transform: `scale(${sp(45)})`, opacity: sp(45),
      }}>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 800, color: '#EBEDE3' }}>YOUR STRESS</div>
        <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#7FD1C0', marginTop: 8 }}>debt + fees + interest</div>
      </div>
      <div style={{
        position: 'absolute', right: 260, top: 480, width: 340, padding: '34px 20px',
        background: '#FFF449', borderRadius: 20, textAlign: 'center',
        transform: `scale(${sp(65)})`, opacity: sp(65),
        boxShadow: `0 0 ${30 + Math.sin(frame / 8) * 15}px rgba(255,244,73,0.35)`,
      }}>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 900, color: '#12302B' }}>THEIR REVENUE</div>
        <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#1E4A42', marginTop: 8 }}>+21% APR / $35 fees</div>
      </div>

      {/* Flowing dollar orbs */}
      {orbs.map(o => (
        <div key={o.key} style={{
          position: 'absolute', left: o.x, top: o.y, width: 46, height: 46, borderRadius: '50%',
          background: '#FFF449', opacity: o.op * 0.9, display: 'flex', alignItems: 'center', justifyContent: 'center',
          fontFamily: 'JetBrains Mono, monospace', fontSize: 26, fontWeight: 800, color: '#12302B',
        }}>$</div>
      ))}

      {/* Payoff line */}
      <div style={{
        position: 'absolute', bottom: 170, width: '100%', textAlign: 'center',
        opacity: interpolate(frame, [400, 430], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        transform: `translateY(${interpolate(frame, [400, 430], [40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
      }}>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 52, fontWeight: 700, color: '#EBEDE3' }}>
          Know the rules. <span style={{ color: '#FFF449' }}>Play differently.</span>
        </div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(8,20,18,0.75) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene51_RevenueMachine;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene52_LifestyleInflation',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene52_LifestyleInflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan left -> right
  const camX = interpolate(frame, [0, durationInFrames], [-30, 30]);

  // Dual bar race: INCOME vs SPENDING (spending catches up = the trap)
  const incomeH = interpolate(frame, [70, 200], [80, 420], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const spendH = interpolate(frame, [110, 300], [80, 415], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const drainItems = ['BIGGER APARTMENT', 'NEWER CAR', 'PREMIUM SUBSCRIPTIONS'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#0F2A26', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${160 + Math.sin(frame / 100) * 10}deg, #357064 0%, #1B443D 50%, #0C211E 100%)`,
        transform: `translateX(${camX}px) scale(1.06)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 760, height: 760, borderRadius: 60,
        border: '2px solid #FFF449', opacity: 0.04,
        left: 1250, top: -260, transform: `rotate(${frame * -0.07}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: '50%',
        background: '#408175', opacity: 0.07,
        left: -160, bottom: -180, transform: `scale(${1 + Math.sin(frame / 70) * 0.05})`,
      }} />

      {/* Motion texture: dot grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 14 }, (_, r) =>
          Array.from({ length: 24 }, (_, c) => (
            <circle key={`${r}-${c}`} cx={c * 84 + 30} cy={r * 84 + 30 + Math.sin((frame + c * 8) / 40) * 4} r={2} fill="#EBEDE3" />
          ))
        )}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 16 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 20) * 1920 + frame * (0.2 + seededRandom(i) * 0.3)) % 1920,
          top: seededRandom(i + 70) * 1080,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: i % 5 === 0 ? '#FFF449' : '#EBEDE3',
          opacity: 0.15,
        }} />
      ))}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 100, left: 120,
        transform: `translateX(${(1 - sp(8)) * -120}px)`, opacity: sp(8),
      }}>
        <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 108, fontWeight: 900, color: '#EBEDE3', lineHeight: 1 }}>
          LIFESTYLE
        </div>
        <div style={{
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 108, fontWeight: 900, color: '#FFF449', lineHeight: 1,
          transform: `scale(${1 + Math.sin(frame / 20) * 0.015})`, transformOrigin: 'left',
        }}>
          INFLATION
        </div>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 42, color: '#9FCFC4', marginTop: 16 }}>
          The silent wealth killer
        </div>
      </div>

      {/* Bar chart: income vs spending */}
      <div style={{ position: 'absolute', left: 220, bottom: 200, display: 'flex', gap: 90, alignItems: 'flex-end' }}>
        <div style={{ textAlign: 'center' }}>
          <div style={{
            width: 170, height: incomeH, background: 'linear-gradient(180deg, #7FD1C0, #408175)',
            borderRadius: '14px 14px 0 0', opacity: sp(60),
          }} />
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 32, color: '#EBEDE3', marginTop: 14, fontWeight: 700 }}>INCOME</div>
        </div>
        <div style={{ textAlign: 'center' }}>
          <div style={{
            width: 170, height: spendH, background: 'linear-gradient(180deg, #FFF449, #B3A81F)',
            borderRadius: '14px 14px 0 0', opacity: sp(90),
          }} />
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 32, color: '#FFF449', marginTop: 14, fontWeight: 700 }}>SPENDING</div>
        </div>
      </div>

      {/* Drain list — staggered reveal */}
      <div style={{ position: 'absolute', right: 150, top: 460 }}>
        {drainItems.map((item, i) => {
          const d = 330 + i * 45;
          return (
            <div key={i} style={{
              display: 'flex', alignItems: 'center', gap: 24, marginBottom: 34,
              transform: `translateX(${(1 - sp(d)) * 150}px)`, opacity: sp(d),
            }}>
              <svg width="46" height="46" viewBox="0 0 46 46">
                <path d="M23 6 L23 34 M13 26 L23 36 L33 26" stroke="#FFF449" strokeWidth="4" fill="none" strokeLinecap="round" />
              </svg>
              <div style={{
                fontFamily: 'Inter, sans-serif', fontSize: 46, fontWeight: 800, color: '#EBEDE3',
                borderBottom: '3px solid rgba(255,244,73,0.4)', paddingBottom: 6,
              }}>{item}</div>
            </div>
          );
        })}
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 38, color: '#9FCFC4', marginTop: 12,
          opacity: interpolate(frame, [500, 530], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          Every raise, silently transferred away.
        </div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(6,18,16,0.78) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene52_LifestyleInflation;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene53_FreeMoney',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene53_FreeMoney: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow orbit tilt
  const camRot = Math.sin(frame / 150) * 0.6;

  // Coin stacks: YOU vs EMPLOYER MATCH — coins drop one by one
  const coinCount = 6;
  const coinDrop = (i: number, base: number) =>
    spring({ frame: Math.max(0, frame - (base + i * 18)), fps, config: { damping: 10, mass: 0.5, stiffness: 140 } });

  // Impact shake when MATCH stack completes
  const shakeFrame = 250;
  const shake = frame > shakeFrame && frame < shakeFrame + 12
    ? Math.sin((frame - shakeFrame) * 3) * (12 - (frame - shakeFrame)) : 0;

  const routes = ['EMERGENCY SAVINGS', 'INVESTMENTS', 'RETIREMENT'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#102D28', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 30% ${60 + Math.sin(frame / 80) * 6}%, #408175 0%, #20504A 40%, #0B211D 100%)`,
        transform: `rotate(${camRot}deg) scale(1.08) translateX(${shake}px)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 850, height: 850, borderRadius: '50%',
        border: '3px dashed #EBEDE3', opacity: 0.045,
        right: -300, top: -280, transform: `rotate(${frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 0, height: 0, left: 80, bottom: 60, opacity: 0.05,
        borderLeft: '260px solid transparent', borderRight: '260px solid transparent',
        borderBottom: '440px solid #FFF449',
        transform: `rotate(${-10 + Math.sin(frame / 90) * 4}deg)`,
      }} />

      {/* Motion texture: concentric circles */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 7 }, (_, i) => (
          <circle key={i} cx={960} cy={540} r={120 + i * 110 + Math.sin(frame / 50) * 8} stroke="#EBEDE3" strokeWidth={1} fill="none" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 5) * 1920,
          top: ((seededRandom(i + 40) * 1080) + frame * (0.25 + seededRandom(i) * 0.4)) % 1080,
          width: 3 + seededRandom(i + 2) * 3, height: 3 + seededRandom(i + 2) * 3,
          borderRadius: '50%', backgroundColor: i % 3 === 0 ? '#FFF449' : '#7FD1C0',
          opacity: 0.16,
        }} />
      ))}

      {/* Hero typography */}
      <div style={{
        position: 'absolute', top: 110, width: '100%', textAlign: 'center',
        transform: `scale(${sp(5)}) translateX(${shake * 0.6}px)`, opacity: sp(5),
      }}>
        <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 150, fontWeight: 900, color: '#FFF449', letterSpacing: -4, lineHeight: 0.95 }}>
          FREE MONEY
        </div>
        <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 78, fontWeight: 900, color: '#EBEDE3', letterSpacing: 6 }}>
          ON THE TABLE
        </div>
      </div>

      {/* The "table" line */}
      <div style={{
        position: 'absolute', left: 460, right: 460, top: 760, height: 8,
        background: '#EBEDE3', borderRadius: 4, opacity: sp(40) * 0.9,
        transform: `scaleX(${sp(40)})`,
      }} />

      {/* Coin stack: YOU */}
      <div style={{ position: 'absolute', left: 640, top: 0, width: 180 }}>
        {Array.from({ length: coinCount }, (_, i) => (
          <div key={i} style={{
            position: 'absolute', top: 740 - i * 26 - (1 - coinDrop(i, 70)) * 300, left: 0,
            width: 180, height: 22, borderRadius: 11,
            background: 'linear-gradient(180deg, #7FD1C0, #408175)',
            border: '2px solid #0B211D', opacity: coinDrop(i, 70),
          }} />
        ))}
        <div style={{ position: 'absolute', top: 790, width: 180, textAlign: 'center', fontFamily: 'JetBrains Mono, monospace', fontSize: 32, fontWeight: 700, color: '#EBEDE3' }}>YOU</div>
      </div>

      {/* Coin stack: EMPLOYER MATCH */}
      <div style={{ position: 'absolute', left: 1100, top: 0, width: 180 }}>
        {Array.from({ length: coinCount }, (_, i) => (
          <div key={i} style={{
            position: 'absolute', top: 740 - i * 26 - (1 - coinDrop(i, 160)) * 300, left: 0,
            width: 180, height: 22, borderRadius: 11,
            background: 'linear-gradient(180deg, #FFF449, #C7BC25)',
            border: '2px solid #0B211D', opacity: coinDrop(i, 160),
            boxShadow: frame > shakeFrame ? '0 0 24px rgba(255,244,73,0.45)' : 'none',
          }} />
        ))}
        <div style={{ position: 'absolute', top: 790, width: 240, marginLeft: -30, textAlign: 'center', fontFamily: 'JetBrains Mono, monospace', fontSize: 32, fontWeight: 700, color: '#FFF449' }}>+ MATCH</div>
      </div>

      {/* Route chips */}
      <div style={{ position: 'absolute', bottom: 110, width: '100%', display: 'flex', justifyContent: 'center', gap: 40 }}>
        {routes.map((r, i) => {
          const d = 380 + i * 40;
          return (
            <div key={i} style={{
              padding: '18px 40px', borderRadius: 50, border: '2px solid #7FD1C0',
              background: 'rgba(11,33,29,0.8)', fontFamily: 'Inter, sans-serif',
              fontSize: 36, fontWeight: 800, color: '#EBEDE3',
              transform: `translateY(${(1 - sp(d)) * 80}px)`, opacity: sp(d),
            }}>{r}</div>
          );
        })}
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(6,18,16,0.8) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene53_FreeMoney;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene54_CompoundCurve',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene54_CompoundCurve: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow zoom out then settle
  const camZoom = interpolate(frame, [0, 120, durationInFrames], [1.12, 1.02, 1.0], { easing: Easing.out(Easing.quad) });

  // Compound curve draw progress
  const draw = interpolate(frame, [150, 420], [0, 1], { easing: Easing.inOut(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Counter: $120 -> $37,000
  const counterVal = Math.floor(interpolate(frame, [150, 420], [120, 37000], { easing: Easing.in(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Exponential curve points (SVG)
  const pts: string[] = [];
  const N = 100;
  for (let i = 0; i <= N * draw; i++) {
    const t = i / N;
    const x = 320 + t * 1180;
    const y = 840 - Math.pow(t, 2.4) * 520;
    pts.push(`${x},${y}`);
  }

  return (
    <AbsoluteFill style={{ backgroundColor: '#0E2823', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${20 + Math.sin(frame / 110) * 8}deg, #0B1F1B 0%, #2A5B52 55%, #408175 120%)`,
        transform: `scale(${camZoom})`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 700, height: 700, borderRadius: '50%',
        background: '#FFF449', opacity: 0.03,
        right: -220, top: -240, transform: `scale(${1 + Math.sin(frame / 65) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', width: 460, height: 460, border: '2px solid #EBEDE3',
        opacity: 0.05, left: -140, top: 240, borderRadius: 40,
        transform: `rotate(${frame * 0.09}deg)`,
      }} />

      {/* Motion texture: graph grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 12 }, (_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 95} x2={1920} y2={i * 95} stroke="#EBEDE3" strokeWidth={0.6} />
        ))}
        {Array.from({ length: 21 }, (_, i) => (
          <line key={`v${i}`} x1={i * 96} y1={0} x2={i * 96} y2={1080} stroke="#EBEDE3" strokeWidth={0.6} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 15 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 11) * 1920,
          top: ((seededRandom(i + 33) * 1080) - frame * 0.4 + 2160) % 1080,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: '#FFF449', opacity: 0.14,
        }} />
      ))}

      {/* Setup: $120/month chip */}
      <div style={{
        position: 'absolute', top: 110, left: 150,
        transform: `translateY(${(1 - sp(10)) * -60}px)`, opacity: sp(10),
      }}>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, color: '#9FCFC4', fontWeight: 600 }}>CANCELED OVERLAPPING SUBSCRIPTIONS</div>
        <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 120, fontWeight: 900, color: '#EBEDE3', lineHeight: 1 }}>
          $120<span style={{ fontSize: 56, color: '#9FCFC4' }}>/MONTH</span>
        </div>
      </div>

      {/* Compound curve */}
      <svg width="1920" height="1080" style={{ position: 'absolute' }}>
        <polyline points={pts.join(' ')} fill="none" stroke="#FFF449" strokeWidth={9} strokeLinecap="round"
          style={{ filter: 'drop-shadow(0 0 14px rgba(255,244,73,0.5))' }} />
        {/* Baseline axis */}
        <line x1={320} y1={840} x2={1500} y2={840} stroke="#EBEDE3" strokeWidth={3} opacity={0.5} />
        <line x1={320} y1={840} x2={320} y2={280} stroke="#EBEDE3" strokeWidth={3} opacity={0.5} />
        {/* Moving tip dot */}
        {draw > 0 && (
          <circle
            cx={320 + draw * 1180}
            cy={840 - Math.pow(draw, 2.4) * 520}
            r={16 + Math.sin(frame / 6) * 3}
            fill="#FFF449"
          />
        )}
      </svg>

      {/* Year labels */}
      <div style={{ position: 'absolute', left: 320, top: 870, width: 1180, display: 'flex', justifyContent: 'space-between', opacity: sp(140) }}>
        {['YEAR 0', 'YEAR 5', 'YEAR 10', 'YEAR 15'].map((y, i) => (
          <div key={i} style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#9FCFC4', fontWeight: 700 }}>{y}</div>
        ))}
      </div>

      {/* Big compounding counter */}
      <div style={{
        position: 'absolute', right: 140, top: 300, textAlign: 'right',
        opacity: interpolate(frame, [150, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 130, fontWeight: 800, color: '#FFF449', letterSpacing: -3 }}>
          ${counterVal.toLocaleString('en-US')}
        </div>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, color: '#EBEDE3', fontWeight: 700 }}>AT 7% OVER 15 YEARS</div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(5,16,14,0.8) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene54_CompoundCurve;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene55_NinetyDays',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene55_NinetyDays: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: gentle orbit
  const camX = Math.sin(frame / 130) * 20;
  const camY = Math.cos(frame / 150) * 12;

  // Radial progress ring 0 -> 100%
  const ringProg = interpolate(frame, [90, 330], [0, 1], { easing: Easing.inOut(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const R = 250;
  const CIRC = 2 * Math.PI * R;

  // +12% counter
  const pct = interpolate(frame, [90, 330], [0, 12], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Day ticker
  const day = Math.min(90, Math.floor(interpolate(frame, [90, 330], [0, 90], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })));

  return (
    <AbsoluteFill style={{ backgroundColor: '#0F2B27', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${62 + Math.sin(frame / 95) * 5}% 45%, #408175 0%, #234F48 45%, #0A1F1B 100%)`,
        transform: `translate(${camX}px, ${camY}px) scale(1.08)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 980, height: 980, borderRadius: '50%',
        border: '1.5px solid #FFF449', opacity: 0.05,
        left: 780, top: -110, transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame / 85) * 0.03})`,
      }} />
      <div style={{
        position: 'absolute', width: 420, height: 420, borderRadius: 30,
        background: '#EBEDE3', opacity: 0.03,
        left: -120, bottom: -100, transform: `rotate(${frame * -0.06}deg)`,
      }} />

      {/* Motion texture: radial ticks around ring */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.14 }}>
        {Array.from({ length: 60 }, (_, i) => {
          const a = (i / 60) * Math.PI * 2 + frame / 300;
          return (
            <line key={i}
              x1={1270 + Math.cos(a) * 320} y1={470 + Math.sin(a) * 320}
              x2={1270 + Math.cos(a) * 338} y2={470 + Math.sin(a) * 338}
              stroke="#7FD1C0" strokeWidth={2} />
          );
        })}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 61) * 1920,
          top: ((seededRandom(i + 17) * 1080) - frame * (0.2 + seededRandom(i) * 0.35) + 2160) % 1080,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: i % 4 === 0 ? '#FFF449' : '#EBEDE3', opacity: 0.18,
        }} />
      ))}

      {/* Left typography block */}
      <div style={{ position: 'absolute', left: 140, top: 230 }}>
        <div style={{
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 96, fontWeight: 900, color: '#EBEDE3', lineHeight: 1.02,
          transform: `translateX(${(1 - sp(8)) * -140}px)`, opacity: sp(8),
        }}>
          MINUTES<br />TO SET UP.
        </div>
        <div style={{
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 96, fontWeight: 900, color: '#FFF449', lineHeight: 1.02, marginTop: 20,
          transform: `translateX(${(1 - sp(40)) * -140}px)`, opacity: sp(40),
        }}>
          90 DAYS<br />TO PROOF.
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 42, color: '#9FCFC4', marginTop: 34, fontWeight: 600,
          opacity: interpolate(frame, [340, 370], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          AI precision + your discipline
        </div>
      </div>

      {/* Radial progress ring */}
      <svg width="1920" height="1080" style={{ position: 'absolute' }}>
        <circle cx={1270} cy={470} r={R} stroke="rgba(235,237,227,0.12)" strokeWidth={26} fill="none" />
        <circle cx={1270} cy={470} r={R} stroke="#FFF449" strokeWidth={26} fill="none"
          strokeLinecap="round"
          strokeDasharray={CIRC}
          strokeDashoffset={CIRC * (1 - ringProg)}
          transform="rotate(-90 1270 470)"
          style={{ filter: 'drop-shadow(0 0 16px rgba(255,244,73,0.45))' }} />
      </svg>

      {/* Ring center content */}
      <div style={{ position: 'absolute', left: 1270 - 220, top: 470 - 130, width: 440, textAlign: 'center', transform: `scale(${sp(80)})` }}>
        <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 128, fontWeight: 800, color: '#EBEDE3', lineHeight: 1 }}>
          +{pct.toFixed(1)}%
        </div>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 38, fontWeight: 700, color: '#7FD1C0', marginTop: 8 }}>SAVINGS UPLIFT</div>
        <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 32, color: '#FFF449', marginTop: 16 }}>DAY {day} / 90</div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 54%, rgba(5,15,13,0.8) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene55_NinetyDays;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene56_SweetSpot',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene56_SweetSpot: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera push-in with slight tilt
  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.09]);
  const camTilt = Math.sin(frame / 170) * 0.5;

  // Slider marker: travels then LOCKS at "slightly behind" with micro-jitter
  const markerX = interpolate(frame, [70, 200], [260, 1080], { easing: Easing.inOut(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const jitter = frame > 200 ? Math.sin(frame / 3.5) * 4 : 0;

  // Lock pulse after marker settles
  const lockPulse = frame > 210 ? 1 + Math.sin((frame - 210) / 9) * 0.05 : 1;

  const loops = ['CARRY THE BALANCE', 'ROLL OVER THE LOAN', 'PAY THE MINIMUM'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D2622', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${185 + Math.sin(frame / 120) * 6}deg, #315F56 0%, #17362F 55%, #081713 100%)`,
        transform: `scale(${camZoom}) rotate(${camTilt}deg)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 1100, height: 1100, borderRadius: '50%',
        border: '2px solid #EBEDE3', opacity: 0.035,
        left: 410 + Math.sin(frame / 100) * 25, top: -420,
        transform: `rotate(${frame * 0.06}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 380, height: 380,
        background: '#FFF449', opacity: 0.035, borderRadius: 24,
        right: -110, top: 480, transform: `rotate(${frame * -0.1}deg)`,
      }} />

      {/* Motion texture: horizontal scan lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 36 }, (_, i) => (
          <line key={i} x1={0} y1={i * 30 + ((frame * 0.3) % 30)} x2={1920} y2={i * 30 + ((frame * 0.3) % 30)}
            stroke="#7FD1C0" strokeWidth={0.8} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 16 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: ((seededRandom(i + 27) * 1920) + frame * (0.3 + seededRandom(i) * 0.3)) % 1920,
          top: seededRandom(i + 81) * 1080,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: i % 5 === 0 ? '#FFF449' : '#EBEDE3', opacity: 0.14,
        }} />
      ))}

      {/* Headline */}
      <div style={{
        position: 'absolute', top: 120, width: '100%', textAlign: 'center',
        transform: `translateY(${(1 - sp(6)) * -70}px)`, opacity: sp(6),
      }}>
        <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 92, fontWeight: 900, color: '#EBEDE3', letterSpacing: -2 }}>
          THE INDUSTRY'S <span style={{ color: '#FFF449' }}>SWEET SPOT</span>
        </div>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 44, color: '#9FCFC4', marginTop: 10, fontWeight: 600 }}>
          Not broke. Not free. Slightly behind.
        </div>
      </div>

      {/* The spectrum slider */}
      <div style={{ position: 'absolute', left: 260, right: 260, top: 480, opacity: sp(40) }}>
        <div style={{
          height: 16, borderRadius: 8,
          background: 'linear-gradient(90deg, #1E4A42 0%, #FFF449 60%, #7FD1C0 100%)',
          opacity: 0.85,
        }} />
        <div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 20 }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 34, fontWeight: 700, color: '#9FCFC4' }}>BROKE</div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 34, fontWeight: 700, color: '#FFF449' }}>SLIGHTLY BEHIND</div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 34, fontWeight: 700, color: '#7FD1C0' }}>FREE</div>
        </div>
      </div>

      {/* Locked marker */}
      <div style={{
        position: 'absolute', left: markerX + jitter, top: 425,
        transform: `translateX(-50%) scale(${lockPulse})`, opacity: sp(70),
        textAlign: 'center',
      }}>
        <svg width="70" height="86" viewBox="0 0 70 86">
          {/* padlock icon */}
          <rect x="12" y="38" width="46" height="40" rx="8" fill="#FFF449" />
          <path d="M22 38 V26 a13 13 0 0 1 26 0 V38" stroke="#FFF449" strokeWidth="7" fill="none" />
          <circle cx="35" cy="56" r="6" fill="#12302B" />
        </svg>
      </div>

      {/* The loop chips */}
      <div style={{ position: 'absolute', bottom: 150, width: '100%', display: 'flex', justifyContent: 'center', gap: 34 }}>
        {loops.map((l, i) => {
          const d = 260 + i * 45;
          return (
            <div key={i} style={{
              display: 'flex', alignItems: 'center', gap: 18,
              padding: '20px 36px', borderRadius: 16,
              background: 'rgba(10,26,22,0.85)', border: '2px solid rgba(255,244,73,0.5)',
              transform: `translateY(${(1 - sp(d)) * 90}px) scale(${sp(d)})`, opacity: sp(d),
            }}>
              <svg width="38" height="38" viewBox="0 0 38 38" style={{ transform: `rotate(${frame * 2}deg)` }}>
                <path d="M19 4 a15 15 0 1 1 -10.6 4.4 M8.4 8.4 L8.4 2 M8.4 8.4 L2 8.4"
                  stroke="#FFF449" strokeWidth="3.4" fill="none" strokeLinecap="round" />
              </svg>
              <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 36, fontWeight: 800, color: '#EBEDE3' }}>{l}</div>
            </div>
          );
        })}
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(4,13,11,0.82) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene56_SweetSpot;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene57_LifelineLeash',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene57_LifelineLeash: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on the strike-through impact
  const strikeAt = 150;
  const shake = frame > strikeAt && frame < strikeAt + 14
    ? Math.sin((frame - strikeAt) * 2.6) * (14 - (frame - strikeAt)) * 1.4 : 0;

  // Strike-through draw
  const strike = interpolate(frame, [strikeAt, strikeAt + 18], [0, 1], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // LEASH slam
  const leashIn = sp(strikeAt + 22, { damping: 9, stiffness: 160 });

  // Phone slides in, cancel button press at ~430
  const phoneIn = sp(300);
  const pressAt = 430;
  const press = frame > pressAt && frame < pressAt + 10 ? 0.92 : 1;
  const canceled = frame > pressAt + 10;

  return (
    <AbsoluteFill style={{ backgroundColor: '#0E2924', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 25% ${40 + Math.sin(frame / 85) * 7}%, #3B7669 0%, #1D453D 50%, #091B17 100%)`,
        transform: `translate(${shake}px, ${shake * 0.5}px) scale(1.07)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 820, height: 820, borderRadius: '50%',
        border: '2px solid #FFF449', opacity: 0.04,
        left: -260, bottom: -320, transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame / 75) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 520, height: 520, borderRadius: 40,
        border: '2px solid #EBEDE3', opacity: 0.045,
        right: -140, top: -160, transform: `rotate(${frame * -0.09}deg)`,
      }} />

      {/* Motion texture: chain-link dashes */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 10 }, (_, i) => (
          <line key={i} x1={0} y1={i * 120 + 40} x2={1920} y2={i * 120 + 40}
            stroke="#EBEDE3" strokeWidth={2} strokeDasharray="26 30"
            strokeDashoffset={-frame * 0.8} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 14 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 44) * 1920,
          top: ((seededRandom(i + 91) * 1080) - frame * 0.35 + 2160) % 1080,
          width: 3 + seededRandom(i) * 3, height: 3 + seededRandom(i) * 3,
          borderRadius: '50%', backgroundColor: i % 4 === 0 ? '#FFF449' : '#7FD1C0', opacity: 0.15,
        }} />
      ))}

      {/* Typographic beat: LIFELINE -> struck -> LEASH */}
      <div style={{ position: 'absolute', left: 140, top: 220 }}>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 42, color: '#9FCFC4', fontWeight: 700, opacity: sp(5), letterSpacing: 4 }}>
          THE MINIMUM PAYMENT IS NOT A
        </div>
        <div style={{ position: 'relative', display: 'inline-block', marginTop: 6 }}>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 138, fontWeight: 900, color: '#EBEDE3',
            opacity: sp(30) * (canceled ? 1 : 1), lineHeight: 1,
          }}>
            LIFELINE
          </div>
          {/* strike-through bar */}
          <div style={{
            position: 'absolute', left: -12, top: '52%', height: 14, borderRadius: 7,
            width: `${strike * 106}%`, background: '#FFF449',
            boxShadow: '0 0 18px rgba(255,244,73,0.6)',
          }} />
        </div>
        <div style={{
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 176, fontWeight: 900, color: '#FFF449', lineHeight: 1, marginTop: 8,
          transform: `scale(${leashIn}) rotate(${(1 - leashIn) * -6}deg)`, transformOrigin: 'left center', opacity: leashIn,
          textShadow: '0 0 40px rgba(255,244,73,0.3)',
        }}>
          IT'S A LEASH
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 44, color: '#EBEDE3', fontWeight: 600, marginTop: 40, maxWidth: 760,
          opacity: interpolate(frame, [260, 290], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          Engineered to maximize the interest you pay — for years.
        </div>
      </div>

      {/* Phone mockup: find one charge, cancel it */}
      <div style={{
        position: 'absolute', right: 170, top: 200, width: 380, height: 700,
        borderRadius: 44, background: '#0A1D19', border: '3px solid #408175',
        transform: `translateY(${(1 - phoneIn) * 500}px) rotate(${(1 - phoneIn) * 8}deg)`,
        opacity: phoneIn, padding: 28, boxSizing: 'border-box',
      }}>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 800, color: '#EBEDE3', marginBottom: 22 }}>RECURRING CHARGES</div>
        {['STREAMING PLUS — $14.99', 'CLOUD EXTRA — $9.99', 'FORGOTTEN APP — $7.99'].map((row, i) => (
          <div key={i} style={{
            padding: '16px 14px', borderRadius: 12, marginBottom: 14,
            background: i === 2 ? (canceled ? 'rgba(255,244,73,0.12)' : 'rgba(255,244,73,0.06)') : 'rgba(235,237,227,0.05)',
            border: i === 2 ? '2px solid #FFF449' : '2px solid rgba(235,237,227,0.12)',
            fontFamily: 'JetBrains Mono, monospace', fontSize: 22, color: '#EBEDE3',
            textDecoration: i === 2 && canceled ? 'line-through' : 'none',
            opacity: sp(330 + i * 20),
          }}>{row}</div>
        ))}
        <div style={{
          marginTop: 26, padding: '20px 0', borderRadius: 14, textAlign: 'center',
          background: canceled ? '#7FD1C0' : '#FFF449',
          fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 900, color: '#0A1D19',
          transform: `scale(${press * sp(400)})`,
        }}>
          {canceled ? 'CANCELED' : 'CANCEL IT'}
        </div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 53%, rgba(4,13,11,0.82) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene57_LifelineLeash;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene58_OneBeatsHundred',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene58_OneBeatsHundred: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: pan up as fortress builds
  const camY = interpolate(frame, [0, durationInFrames], [20, -25]);

  // Fortress bricks stack (5 rows x 6 cols), one per beat
  const rows = 5;
  const cols = 6;
  const brickW = 116;
  const brickH = 52;
  const fortX = 1170;
  const fortBase = 880;

  return (
    <AbsoluteFill style={{ backgroundColor: '#0E2A25', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${200 + Math.sin(frame / 105) * 8}deg, #408175 -10%, #1E4A42 45%, #081713 100%)`,
        transform: `translateY(${camY}px) scale(1.09)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: '2px solid #EBEDE3', opacity: 0.04,
        right: -300, bottom: -380, transform: `rotate(${frame * 0.07}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 440, height: 440, borderRadius: 36,
        background: '#FFF449', opacity: 0.03,
        left: -130, top: -110, transform: `rotate(${frame * -0.05}deg) scale(${1 + Math.sin(frame / 90) * 0.05})`,
      }} />

      {/* Motion texture: vertical lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 26 }, (_, i) => (
          <line key={i} x1={i * 78} y1={0} x2={i * 78} y2={1080} stroke="#7FD1C0" strokeWidth={0.8} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 3) * 1920,
          top: ((seededRandom(i + 66) * 1080) - frame * (0.25 + seededRandom(i) * 0.4) + 2160) % 1080,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: i % 4 === 0 ? '#FFF449' : '#EBEDE3', opacity: 0.15,
        }} />
      ))}

      {/* Typographic hero: 1 > 100 */}
      <div style={{ position: 'absolute', left: 130, top: 180 }}>
        <div style={{ display: 'flex', alignItems: 'baseline', gap: 34 }}>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 220, fontWeight: 900, color: '#FFF449', lineHeight: 1,
            transform: `scale(${sp(10, { damping: 8, stiffness: 150 })})`, transformOrigin: 'left bottom',
            textShadow: '0 0 50px rgba(255,244,73,0.3)',
          }}>1</div>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 110, fontWeight: 900, color: '#EBEDE3',
            transform: `scale(${sp(50)})`,
          }}>&gt;</div>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 220, fontWeight: 900, color: 'rgba(235,237,227,0.35)', lineHeight: 1,
            transform: `scale(${sp(70)})`, transformOrigin: 'left bottom',
          }}>100</div>
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 50, fontWeight: 800, color: '#EBEDE3', marginTop: 26,
          opacity: sp(110),
        }}>
          ONE ACTION <span style={{ color: '#FFF449' }}>TODAY</span>
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 50, fontWeight: 800, color: 'rgba(235,237,227,0.45)', marginTop: 6,
          opacity: sp(135),
        }}>
          BEATS 100 INTENTIONS TOMORROW
        </div>

        {/* Transfer pill */}
        <div style={{
          display: 'inline-flex', alignItems: 'center', gap: 22, marginTop: 54,
          padding: '20px 38px', borderRadius: 60, border: '2px solid #7FD1C0', background: 'rgba(9,25,21,0.85)',
          transform: `translateY(${(1 - sp(190)) * 70}px)`, opacity: sp(190),
        }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 36, fontWeight: 700, color: '#EBEDE3' }}>$14.99/mo</div>
          <svg width="60" height="30" viewBox="0 0 60 30">
            <path d={`M4 15 H${34 + Math.sin(frame / 7) * 8} M38 6 L50 15 L38 24`} stroke="#FFF449" strokeWidth="4" fill="none" strokeLinecap="round" />
          </svg>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 36, fontWeight: 900, color: '#FFF449' }}>EMERGENCY FUND</div>
        </div>
      </div>

      {/* Fortress bricks stacking */}
      {Array.from({ length: rows * cols }, (_, i) => {
        const row = Math.floor(i / cols);
        const col = i % cols;
        const drop = spring({ frame: Math.max(0, frame - (120 + i * 12)), fps, config: { damping: 11, mass: 0.5, stiffness: 150 } });
        const offset = row % 2 === 0 ? 0 : brickW / 2;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: fortX + col * (brickW + 6) + offset,
            top: fortBase - row * (brickH + 6) - (1 - drop) * 260,
            width: brickW, height: brickH, borderRadius: 8,
            background: row === rows - 1 ? '#FFF449' : 'linear-gradient(180deg, #46897B, #2A5B50)',
            border: '2px solid rgba(8,23,19,0.8)', opacity: drop,
          }} />
        );
      })}
      <div style={{
        position: 'absolute', left: fortX, top: fortBase + 70, width: cols * (brickW + 6),
        textAlign: 'center', fontFamily: 'JetBrains Mono, monospace', fontSize: 32, fontWeight: 700, color: '#7FD1C0',
        opacity: interpolate(frame, [480, 510], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        BUILT BRICK BY BRICK
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 53%, rgba(4,13,11,0.82) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene58_OneBeatsHundred;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene59_AIPartnership',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene59_AIPartnership: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: subtle breathing zoom
  const camZoom = 1.04 + Math.sin(frame / 140) * 0.02;

  // Two halves converge to center
  const leftX = interpolate(sp(30), [0, 1], [-420, 0]);
  const rightX = interpolate(sp(30), [0, 1], [420, 0]);

  // Center amplifier pulse
  const pulse = 1 + Math.sin(frame / 10) * 0.06;
  const ampIn = sp(200, { damping: 9, stiffness: 150 });

  // Streaming numbers on AI side
  const numRows = 9;

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D2823', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(90deg, #16352E 0%, #408175 ${48 + Math.sin(frame / 90) * 4}%, #16352E 100%)`,
        transform: `scale(${camZoom})`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 760, height: 760, borderRadius: '50%',
        border: '2px solid #FFF449', opacity: 0.045,
        left: 580, top: 160, transform: `scale(${1 + Math.sin(frame / 70) * 0.05}) rotate(${frame * 0.06}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: 30,
        border: '2px solid #EBEDE3', opacity: 0.04,
        left: 710, top: 290, transform: `rotate(${45 + frame * -0.08}deg)`,
      }} />

      {/* Motion texture: center seam glow */}
      <div style={{
        position: 'absolute', left: 955, top: 0, width: 10, height: 1080,
        background: 'linear-gradient(180deg, transparent, rgba(255,244,73,0.35), transparent)',
        opacity: 0.5 + Math.sin(frame / 12) * 0.2,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 20 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 55) * 1920,
          top: ((seededRandom(i + 12) * 1080) - frame * (0.3 + seededRandom(i) * 0.3) + 2160) % 1080,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: i % 3 === 0 ? '#FFF449' : '#7FD1C0', opacity: 0.18,
        }} />
      ))}

      {/* LEFT: AI HANDLES THE NUMBERS */}
      <div style={{ position: 'absolute', left: 110, top: 200, width: 700, transform: `translateX(${leftX}px)`, opacity: sp(30) }}>
        <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 84, fontWeight: 900, color: '#7FD1C0', lineHeight: 1 }}>
          AI HANDLES<br />THE NUMBERS
        </div>
        <div style={{ marginTop: 36, borderRadius: 18, background: 'rgba(8,22,18,0.8)', border: '2px solid rgba(127,209,192,0.4)', padding: '26px 30px' }}>
          {Array.from({ length: numRows }, (_, i) => {
            const v = (seededRandom(i + Math.floor(frame / 12)) * 900 + 10).toFixed(2);
            return (
              <div key={i} style={{
                fontFamily: 'JetBrains Mono, monospace', fontSize: 28, color: '#7FD1C0',
                opacity: 0.35 + seededRandom(i + Math.floor(frame / 10)) * 0.6,
                marginBottom: 8,
              }}>
                {i % 3 === 0 ? 'SCAN' : i % 3 === 1 ? 'FLAG' : 'OPTIMIZE'} :: ${v} :: {i % 2 === 0 ? 'OK' : 'LEAK FOUND'}
              </div>
            );
          })}
        </div>
      </div>

      {/* RIGHT: YOU MAKE THE DECISIONS */}
      <div style={{ position: 'absolute', right: 110, top: 200, width: 700, textAlign: 'right', transform: `translateX(${rightX}px)`, opacity: sp(30) }}>
        <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 84, fontWeight: 900, color: '#EBEDE3', lineHeight: 1 }}>
          YOU MAKE<br />THE DECISIONS
        </div>
        <div style={{ marginTop: 36 }}>
          {['SAVE FIRST', 'INVEST LONG-TERM', 'REFUSE LIFESTYLE INFLATION'].map((d, i) => (
            <div key={i} style={{
              display: 'inline-block', clear: 'both', marginBottom: 18,
              padding: '16px 32px', borderRadius: 14, background: 'rgba(235,237,227,0.08)',
              border: '2px solid rgba(235,237,227,0.35)',
              fontFamily: 'Inter, sans-serif', fontSize: 38, fontWeight: 800, color: '#EBEDE3',
              transform: `translateX(${(1 - sp(120 + i * 35)) * 200}px)`, opacity: sp(120 + i * 35),
            }}>{d}</div>
          ))}
        </div>
      </div>

      {/* Center amplifier node */}
      <div style={{
        position: 'absolute', left: 960 - 120, top: 760, width: 240, textAlign: 'center',
        transform: `scale(${ampIn * pulse})`, opacity: ampIn,
      }}>
        <svg width="150" height="150" viewBox="0 0 150 150" style={{ display: 'block', margin: '0 auto' }}>
          {/* amplify icon: two arrows expanding from core */}
          <circle cx="75" cy="75" r="30" fill="#FFF449" />
          <path d="M75 32 L75 8 M63 18 L75 6 L87 18" stroke="#FFF449" strokeWidth="6" fill="none" strokeLinecap="round" />
          <path d="M75 118 L75 142 M63 132 L75 144 L87 132" stroke="#FFF449" strokeWidth="6" fill="none" strokeLinecap="round" />
          <circle cx="75" cy="75" r={44 + Math.sin(frame / 8) * 6} stroke="rgba(255,244,73,0.5)" strokeWidth="3" fill="none" />
        </svg>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 900, color: '#FFF449', marginTop: 12 }}>AMPLIFY</div>
      </div>

      {/* Top kicker */}
      <div style={{
        position: 'absolute', top: 90, width: '100%', textAlign: 'center',
        fontFamily: 'Inter, sans-serif', fontSize: 46, fontWeight: 700, color: '#EBEDE3',
        opacity: sp(5), letterSpacing: 3,
      }}>
        AI CANNOT REPLACE DISCIPLINE — <span style={{ color: '#FFF449' }}>IT AMPLIFIES IT</span>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 54%, rgba(4,13,11,0.8) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene59_AIPartnership;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene60_Outro',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene60_Outro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow zoom-in toward brand
  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.1], { easing: Easing.inOut(Easing.quad) });

  const traps = ['BNPL SPIRAL', 'MINIMUM PAYMENT CYCLE', 'SUBSCRIPTION BLEED', 'PAYDAY LOAN TRAP'];

  // Brand pulse
  const brandIn = sp(300, { damping: 9, stiffness: 140 });
  const brandPulse = 1 + Math.sin(frame / 11) * 0.02;

  // Lever visual: understanding lifts leverage
  const leverAngle = interpolate(frame, [380, 470], [0, -14], { easing: Easing.inOut(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#0C2621', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 50% ${44 + Math.sin(frame / 100) * 5}%, #408175 0%, #1C453D 48%, #071511 100%)`,
        transform: `scale(${camZoom})`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 1200, height: 1200, borderRadius: '50%',
        border: '1.5px solid #EBEDE3', opacity: 0.04,
        left: 360, top: -60, transform: `rotate(${frame * 0.04}deg) scale(${1 + Math.sin(frame / 95) * 0.03})`,
      }} />
      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: '50%',
        border: '2px dashed #FFF449', opacity: 0.045,
        left: 560, top: 140, transform: `rotate(${frame * -0.09}deg)`,
      }} />

      {/* Motion texture: converging lines to center */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 18 }, (_, i) => {
          const a = (i / 18) * Math.PI * 2 + frame / 400;
          return (
            <line key={i} x1={960} y1={540}
              x2={960 + Math.cos(a) * 1400} y2={540 + Math.sin(a) * 1400}
              stroke="#7FD1C0" strokeWidth={1} />
          );
        })}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 24 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 7) * 1920,
          top: ((seededRandom(i + 29) * 1080) - frame * (0.25 + seededRandom(i) * 0.45) + 2160) % 1080,
          width: 3 + seededRandom(i + 4) * 4, height: 3 + seededRandom(i + 4) * 4,
          borderRadius: '50%', backgroundColor: i % 4 === 0 ? '#FFF449' : '#EBEDE3',
          opacity: 0.14 + seededRandom(i) * 0.12,
        }} />
      ))}

      {/* Question */}
      <div style={{
        position: 'absolute', top: 110, width: '100%', textAlign: 'center',
        transform: `translateY(${(1 - sp(6)) * -70}px)`, opacity: sp(6),
      }}>
        <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 100, fontWeight: 900, color: '#EBEDE3', letterSpacing: -2 }}>
          WHICH TRAP HIT <span style={{ color: '#FFF449' }}>YOU</span>?
        </div>
      </div>

      {/* Trap chips grid */}
      <div style={{
        position: 'absolute', top: 290, width: '100%', display: 'flex',
        justifyContent: 'center', gap: 28, flexWrap: 'wrap', padding: '0 200px', boxSizing: 'border-box',
      }}>
        {traps.map((t, i) => {
          const d = 60 + i * 35;
          const hover = 1 + Math.sin((frame + i * 20) / 16) * 0.02;
          return (
            <div key={i} style={{
              padding: '20px 42px', borderRadius: 60,
              background: 'rgba(8,21,17,0.85)', border: '2px solid rgba(255,244,73,0.55)',
              fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 800, color: '#EBEDE3',
              transform: `scale(${sp(d) * hover}) translateY(${(1 - sp(d)) * 60}px)`, opacity: sp(d),
            }}>{t}</div>
          );
        })}
      </div>

      {/* Lever icon: understanding = leverage */}
      <div style={{ position: 'absolute', left: 250, top: 620, opacity: sp(360) }}>
        <svg width="420" height="200" viewBox="0 0 420 200">
          {/* fulcrum */}
          <path d="M190 170 L230 170 L210 120 Z" fill="#7FD1C0" />
          {/* lever beam */}
          <g transform={`rotate(${leverAngle} 210 122)`}>
            <rect x="20" y="112" width="380" height="14" rx="7" fill="#EBEDE3" />
            {/* weight */}
            <rect x="330" y="66" width="60" height="46" rx="8" fill="#FFF449" />
          </g>
          <line x1="20" y1="188" x2="400" y2="188" stroke="rgba(235,237,227,0.3)" strokeWidth="3" />
        </svg>
        <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#7FD1C0', fontWeight: 700, textAlign: 'center', marginTop: 6 }}>
          UNDERSTANDING = LEVERAGE
        </div>
      </div>

      {/* Brand block */}
      <div style={{
        position: 'absolute', right: 220, top: 620, textAlign: 'center',
        transform: `scale(${brandIn * brandPulse})`, opacity: brandIn,
      }}>
        <div style={{
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 110, fontWeight: 900, color: '#FFF449',
          textShadow: '0 0 60px rgba(255,244,73,0.35)', letterSpacing: -2,
        }}>
          KuxAI.7x
        </div>
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 38, fontWeight: 700, color: '#EBEDE3', marginTop: 6 }}>
          FINANCIAL KNOWLEDGE. FOR EVERYONE.
        </div>
        <div style={{
          display: 'inline-block', marginTop: 30, padding: '18px 56px', borderRadius: 60,
          background: '#FFF449', fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 900, color: '#0C2621',
          transform: `scale(${1 + Math.sin(frame / 9) * 0.04})`,
        }}>
          SUBSCRIBE
        </div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(3,11,9,0.85) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene60_Outro;
```

