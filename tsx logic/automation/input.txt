```tsx
import React from 'react';
Audio: "Move five — take a fresh, ruthless pass at your fixed costs. Every recurring dollar you reclaim is worth more than last year's dollar."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene51_MoveFive_FixedCosts',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene51_MoveFive_FixedCosts: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom-in
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // Big label entrance
  const labelY = interpolate(sp(5), [0, 1], [-80, 0]);
  const labelOp = interpolate(sp(5), [0, 1], [0, 1]);

  // Headline reveal
  const headOp = interpolate(sp(20), [0, 1], [0, 1]);
  const headY = interpolate(sp(20), [0, 1], [60, 0]);

  // Scissor cut sweep (representing "ruthless pass")
  const cutProgress = interpolate(frame, [40, 110], [0, 1], { extrapolateRight: 'clamp' });

  // Bar chart "fixed costs" entrance with reduction animation
  const barsBaseOp = interpolate(sp(50), [0, 1], [0, 1]);
  const reductionFrame = 130;
  const reduce = interpolate(frame, [reductionFrame, reductionFrame + 40], [1, 0.45], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Subtitle bottom strip
  const subOp = interpolate(sp(180), [0, 1], [0, 1]);
  const subY = interpolate(sp(180), [0, 1], [40, 0]);

  // Dollar coin pulse
  const coinPulse = 1 + Math.sin(frame * 0.12) * 0.04;

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => {
    const seed = i + 1;
    const x = seededRandom(seed) * 1920;
    const y = seededRandom(seed + 10) * 1080;
    const drift = Math.sin(frame * 0.018 + seed) * 18;
    const size = 3 + seededRandom(seed + 20) * 4;
    return { x, y: y + drift, size, op: 0.15 + seededRandom(seed + 30) * 0.25 };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 30% 20%, rgba(255,87,34,0.10) 0%, rgba(161,188,152,0) 55%), radial-gradient(ellipse at 75% 85%, rgba(238,238,238,0.18) 0%, rgba(161,188,152,0) 60%)',
        }}
      />

      {/* Shape Layer — drifting geometry */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 1350,
            top: 120,
            width: 520,
            height: 520,
            border: '2px solid #FF5722',
            borderRadius: '50%',
            opacity: 0.06,
            transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: -150,
            top: 600,
            width: 600,
            height: 600,
            backgroundColor: '#EEEEEE',
            opacity: 0.05,
            transform: `rotate(${frame * 0.08}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 900,
            top: 780,
            width: 0,
            height: 0,
            borderLeft: '180px solid transparent',
            borderRight: '180px solid transparent',
            borderBottom: '320px solid #FF5722',
            opacity: 0.04,
            transform: `rotate(${-frame * 0.12}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — horizontal grid lines */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              top: i * 80 + (frame * 0.3) % 80,
              left: 0,
              width: '100%',
              height: 1,
              backgroundColor: '#2D2D2D',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: p.y,
              width: p.size,
              height: p.size,
              backgroundColor: '#FF5722',
              borderRadius: '50%',
              opacity: p.op,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Main content wrapped in camera transform */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* MOVE LABEL — top left */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 110,
            opacity: labelOp,
            transform: `translateY(${labelY}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 22,
          }}
        >
          <div
            style={{
              width: 110,
              height: 110,
              backgroundColor: '#FF5722',
              borderRadius: 18,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Nexa Bold, Mont Heavy, sans-serif',
              fontSize: 76,
              color: '#EEEEEE',
              fontWeight: 900,
              boxShadow: '0 8px 0 #2D2D2D',
            }}
          >
            5
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins, sans-serif',
              fontSize: 38,
              color: '#2D2D2D',
              fontWeight: 700,
              letterSpacing: 6,
              textTransform: 'uppercase',
            }}
          >
            MOVE / FIVE
          </div>
        </div>

        {/* HEADLINE */}
        <div
          style={{
            position: 'absolute',
            top: 250,
            left: 110,
            right: 110,
            opacity: headOp,
            transform: `translateY(${headY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 178,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 0.95,
              letterSpacing: -3,
            }}
          >
            CUT YOUR
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 178,
              fontWeight: 900,
              color: '#FF5722',
              lineHeight: 0.95,
              letterSpacing: -3,
              marginTop: 8,
              position: 'relative',
              display: 'inline-block',
            }}
          >
            FIXED COSTS
            {/* Scissor cut line */}
            <div
              style={{
                position: 'absolute',
                top: '52%',
                left: 0,
                width: `${cutProgress * 100}%`,
                height: 10,
                backgroundColor: '#EEEEEE',
                boxShadow: '0 0 18px rgba(238,238,238,0.7)',
              }}
            />
          </div>
        </div>

        {/* BAR CHART — Fixed costs being trimmed */}
        <div
          style={{
            position: 'absolute',
            bottom: 230,
            left: 110,
            opacity: barsBaseOp,
            display: 'flex',
            alignItems: 'flex-end',
            gap: 28,
            height: 280,
          }}
        >
          {[
            { label: 'INSURANCE', h: 240, color: '#FF5722' },
            { label: 'INTERNET', h: 180, color: '#2D2D2D' },
            { label: 'STREAMING', h: 150, color: '#FF5722' },
            { label: 'SUBS', h: 120, color: '#2D2D2D' },
            { label: 'LEAKS', h: 90, color: '#FF5722' },
          ].map((b, i) => {
            const indivOp = interpolate(sp(55 + i * 6), [0, 1], [0, 1]);
            const indivH = interpolate(sp(55 + i * 6), [0, 1], [0, b.h]);
            const reducedH = indivH * (frame > reductionFrame ? reduce : 1);
            return (
              <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 10 }}>
                <div
                  style={{
                    width: 90,
                    height: reducedH,
                    backgroundColor: b.color,
                    opacity: indivOp,
                    borderRadius: '8px 8px 0 0',
                    position: 'relative',
                  }}
                >
                  {frame > reductionFrame && (
                    <div
                      style={{
                        position: 'absolute',
                        top: -34,
                        left: '50%',
                        transform: 'translateX(-50%)',
                        fontFamily: 'JetBrains Mono, monospace',
                        fontSize: 22,
                        color: '#2D2D2D',
                        fontWeight: 700,
                      }}
                    >
                      -55%
                    </div>
                  )}
                </div>
                <div
                  style={{
                    fontFamily: 'Inter, sans-serif',
                    fontSize: 18,
                    color: '#2D2D2D',
                    fontWeight: 700,
                    letterSpacing: 1,
                  }}
                >
                  {b.label}
                </div>
              </div>
            );
          })}
        </div>

        {/* RECLAIM COIN — right side */}
        <div
          style={{
            position: 'absolute',
            bottom: 280,
            right: 160,
            opacity: interpolate(sp(90), [0, 1], [0, 1]),
            transform: `scale(${sp(90) * coinPulse})`,
            width: 280,
            height: 280,
            borderRadius: '50%',
            backgroundColor: '#FF5722',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            boxShadow: '0 0 0 8px rgba(255,87,34,0.18), 0 12px 0 #2D2D2D',
          }}
        >
          <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
            <path
              d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"
              stroke="#EEEEEE"
              strokeWidth="2.5"
              strokeLinecap="round"
            />
          </svg>
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 26,
              color: '#EEEEEE',
              fontWeight: 800,
              letterSpacing: 3,
              marginTop: 6,
              textTransform: 'uppercase',
            }}
          >
            RECLAIM
          </div>
        </div>

        {/* BOTTOM STRIP — subtitle */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 110,
            right: 110,
            opacity: subOp,
            transform: `translateY(${subY}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div style={{ width: 8, height: 70, backgroundColor: '#FF5722' }} />
          <div
            style={{
              fontFamily: 'Inter, Poppins, sans-serif',
              fontSize: 42,
              color: '#2D2D2D',
              fontWeight: 600,
              lineHeight: 1.2,
            }}
          >
            Every recurring dollar reclaimed is worth <span style={{ color: '#FF5722', fontWeight: 800 }}>more than last year's dollar.</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.45) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene51_MoveFive_FixedCosts;
```

```tsx
import React from 'react';
Audio: "Reshop your auto and home coverage. Renegotiate the internet bill. Kill the three streaming services you forgot existed."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene52_KillTheLeaks',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene52_KillTheLeaks: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera subtle pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -25]);

  // Title
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);
  const titleScale = sp(5);

  // Three cards stagger
  const cards = [
    { label: 'AUTO + HOME', sub: 'RESHOP', icon: 'shield', frame: 30 },
    { label: 'INTERNET', sub: 'RENEGOTIATE', icon: 'wifi', frame: 70 },
    { label: 'STREAMING', sub: 'KILL THE 3', icon: 'play', frame: 110 },
  ];

  // Strike-through animation per card
  const strikeStart = 170;

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => {
    const seed = i + 7;
    const x = seededRandom(seed) * 1920;
    const y = seededRandom(seed + 10) * 1080;
    const drift = Math.cos(frame * 0.02 + seed) * 22;
    return { x: x + drift, y, size: 2 + seededRandom(seed + 30) * 3, op: 0.18 + seededRandom(seed + 40) * 0.25 };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 50% 0%, rgba(255,87,34,0.12) 0%, rgba(161,188,152,0) 50%), radial-gradient(ellipse at 50% 100%, rgba(45,45,45,0.18) 0%, rgba(161,188,152,0) 55%)',
        }}
      />

      {/* Shape layer — diagonal rectangles */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: -100,
            top: 100,
            width: 500,
            height: 80,
            backgroundColor: '#FF5722',
            opacity: 0.05,
            transform: `rotate(${20 + Math.sin(frame * 0.02) * 3}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: -150,
            bottom: 200,
            width: 700,
            height: 110,
            backgroundColor: '#2D2D2D',
            opacity: 0.06,
            transform: `rotate(${-15 + Math.cos(frame * 0.018) * 3}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 1500,
            top: 80,
            width: 280,
            height: 280,
            border: '3px solid #EEEEEE',
            opacity: 0.08,
            transform: `rotate(${frame * 0.2}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — vertical dotted columns */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {Array.from({ length: 32 }).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: i * 60,
              top: 0,
              bottom: 0,
              width: 1,
              backgroundImage:
                'repeating-linear-gradient(to bottom, #2D2D2D 0 6px, transparent 6px 14px)',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: p.y,
              width: p.size,
              height: p.size,
              backgroundColor: '#EEEEEE',
              borderRadius: '50%',
              opacity: p.op,
            }}
          />
        ))}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `scale(${0.85 + titleScale * 0.15})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 32,
              color: '#FF5722',
              fontWeight: 800,
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            — THE QUICK AUDIT —
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 145,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 1,
              marginTop: 14,
              letterSpacing: -2,
            }}
          >
            STOP THE <span style={{ color: '#FF5722' }}>LEAKS</span>
          </div>
        </div>

        {/* THREE CARDS */}
        <div
          style={{
            position: 'absolute',
            top: 460,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 50,
          }}
        >
          {cards.map((c, i) => {
            const op = interpolate(sp(c.frame), [0, 1], [0, 1]);
            const y = interpolate(sp(c.frame), [0, 1], [120, 0]);
            const struck = frame > strikeStart + i * 20;
            const strikeW = interpolate(
              frame,
              [strikeStart + i * 20, strikeStart + i * 20 + 25],
              [0, 100],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            );
            return (
              <div
                key={i}
                style={{
                  width: 460,
                  height: 420,
                  backgroundColor: '#EEEEEE',
                  borderRadius: 18,
                  padding: 38,
                  display: 'flex',
                  flexDirection: 'column',
                  justifyContent: 'space-between',
                  opacity: op,
                  transform: `translateY(${y}px)`,
                  boxShadow: '0 14px 0 #2D2D2D',
                  position: 'relative',
                  overflow: 'hidden',
                }}
              >
                {/* Icon */}
                <div
                  style={{
                    width: 100,
                    height: 100,
                    backgroundColor: '#FF5722',
                    borderRadius: 14,
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                  }}
                >
                  {c.icon === 'shield' && (
                    <svg width="56" height="56" viewBox="0 0 24 24" fill="none">
                      <path d="M12 2L4 5v6c0 5 3.5 9.5 8 11 4.5-1.5 8-6 8-11V5l-8-3z" stroke="#EEEEEE" strokeWidth="2.2" strokeLinejoin="round"/>
                    </svg>
                  )}
                  {c.icon === 'wifi' && (
                    <svg width="56" height="56" viewBox="0 0 24 24" fill="none">
                      <path d="M2 8.5c5.5-5 14.5-5 20 0M5 12c4-3.5 10-3.5 14 0M8.5 15.5c2-1.8 5-1.8 7 0M12 19h.01" stroke="#EEEEEE" strokeWidth="2.4" strokeLinecap="round"/>
                    </svg>
                  )}
                  {c.icon === 'play' && (
                    <svg width="56" height="56" viewBox="0 0 24 24" fill="none">
                      <rect x="3" y="5" width="18" height="14" rx="2" stroke="#EEEEEE" strokeWidth="2.2"/>
                      <path d="M10 9l5 3-5 3V9z" fill="#EEEEEE"/>
                    </svg>
                  )}
                </div>

                <div>
                  <div
                    style={{
                      fontFamily: 'Mont Heavy, sans-serif',
                      fontSize: 52,
                      fontWeight: 900,
                      color: '#2D2D2D',
                      lineHeight: 1,
                      letterSpacing: -1,
                      position: 'relative',
                      display: 'inline-block',
                    }}
                  >
                    {c.label}
                    {struck && (
                      <div
                        style={{
                          position: 'absolute',
                          top: '50%',
                          left: 0,
                          width: `${strikeW}%`,
                          height: 6,
                          backgroundColor: '#FF5722',
                        }}
                      />
                    )}
                  </div>
                  <div
                    style={{
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: 30,
                      color: '#FF5722',
                      fontWeight: 700,
                      marginTop: 14,
                      letterSpacing: 2,
                    }}
                  >
                    → {c.sub}
                  </div>
                </div>

                {/* Card number */}
                <div
                  style={{
                    position: 'absolute',
                    top: 26,
                    right: 30,
                    fontFamily: 'Nexa Bold, sans-serif',
                    fontSize: 60,
                    color: '#A1BC98',
                    fontWeight: 900,
                  }}
                >
                  0{i + 1}
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom marquee */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(200), [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 36,
              color: '#2D2D2D',
              fontWeight: 700,
              letterSpacing: 2,
            }}
          >
            COMB <span style={{ color: '#FF5722' }}>90 DAYS</span> OF CARD STATEMENTS — KILL THE AUTOPILOT
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.45) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene52_KillTheLeaks;
```

```tsx
import React from 'react';
Audio: "Move six — this dislocation is precisely where generational wealth gets quietly built."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene53_MoveSix_Generational',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene53_MoveSix_Generational: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow orbit
  const camRot = Math.sin(frame * 0.008) * 0.6;
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08]);

  // Move label
  const labelOp = interpolate(sp(5), [0, 1], [0, 1]);
  const labelX = interpolate(sp(5), [0, 1], [-100, 0]);

  // Headline
  const headOp = interpolate(sp(25), [0, 1], [0, 1]);

  // Growth curve animation
  const curveProgress = interpolate(frame, [60, 220], [0, 1], { extrapolateRight: 'clamp' });

  // Stat reveal
  const statOp = interpolate(sp(180), [0, 1], [0, 1]);
  const statScale = sp(180);

  // Build a curve path — exponential growth feel
  const pathPoints: { x: number; y: number }[] = [];
  for (let i = 0; i <= 50; i++) {
    const t = i / 50;
    const x = 100 + t * 1200;
    const y = 400 - Math.pow(t, 2.2) * 360;
    pathPoints.push({ x, y });
  }
  const visibleCount = Math.floor(pathPoints.length * curveProgress);
  const pathD = pathPoints
    .slice(0, visibleCount + 1)
    .map((p, i) => `${i === 0 ? 'M' : 'L'} ${p.x} ${p.y}`)
    .join(' ');

  // Tip of curve
  const tip = pathPoints[Math.max(0, visibleCount - 1)] || pathPoints[0];

  // Particles
  const particles = Array.from({ length: 28 }, (_, i) => {
    const seed = i + 11;
    const x = seededRandom(seed) * 1920;
    const yBase = seededRandom(seed + 10) * 1080;
    const drift = (frame * (0.4 + seededRandom(seed + 20) * 0.6)) % 1080;
    return {
      x,
      y: (yBase + drift) % 1080,
      size: 2 + seededRandom(seed + 30) * 3,
      op: 0.2 + seededRandom(seed + 40) * 0.3,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 80% 30%, rgba(255,87,34,0.14) 0%, rgba(161,188,152,0) 55%), radial-gradient(ellipse at 20% 90%, rgba(45,45,45,0.20) 0%, rgba(161,188,152,0) 60%)',
        }}
      />

      {/* Shape layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 1500,
            top: 700,
            width: 460,
            height: 460,
            border: '2px solid #FF5722',
            borderRadius: '50%',
            opacity: 0.06,
            transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 100,
            top: 80,
            width: 320,
            height: 320,
            backgroundColor: '#FF5722',
            opacity: 0.05,
            transform: `rotate(${frame * 0.1}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 200,
            top: 200,
            width: 0,
            height: 0,
            borderLeft: '120px solid transparent',
            borderRight: '120px solid transparent',
            borderBottom: '220px solid #EEEEEE',
            opacity: 0.05,
            transform: `rotate(${15 + Math.cos(frame * 0.02) * 5}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Texture — concentric circles */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 7 }).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: '50%',
              top: '50%',
              width: 200 + i * 200,
              height: 200 + i * 200,
              border: '1px solid #2D2D2D',
              borderRadius: '50%',
              transform: 'translate(-50%, -50%)',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: p.y,
              width: p.size,
              height: p.size,
              backgroundColor: '#FF5722',
              borderRadius: '50%',
              opacity: p.op,
            }}
          />
        ))}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>
        {/* MOVE LABEL */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 110,
            opacity: labelOp,
            transform: `translateX(${labelX}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 22,
          }}
        >
          <div
            style={{
              width: 110,
              height: 110,
              backgroundColor: '#FF5722',
              borderRadius: 18,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 76,
              color: '#EEEEEE',
              fontWeight: 900,
              boxShadow: '0 8px 0 #2D2D2D',
            }}
          >
            6
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 38,
              color: '#2D2D2D',
              fontWeight: 700,
              letterSpacing: 6,
              textTransform: 'uppercase',
            }}
          >
            MOVE / SIX
          </div>
        </div>

        {/* HEADLINE */}
        <div
          style={{
            position: 'absolute',
            top: 240,
            left: 110,
            right: 110,
            opacity: headOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 130,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 0.95,
              letterSpacing: -2,
            }}
          >
            THIS IS WHERE
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 170,
              fontWeight: 900,
              color: '#FF5722',
              lineHeight: 0.95,
              letterSpacing: -3,
              marginTop: 6,
            }}
          >
            GENERATIONAL
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 170,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 0.95,
              letterSpacing: -3,
            }}
          >
            WEALTH IS BUILT.
          </div>
        </div>

        {/* GROWTH CURVE — bottom right */}
        <div
          style={{
            position: 'absolute',
            right: 80,
            bottom: 100,
            width: 1300,
            height: 450,
            opacity: interpolate(sp(55), [0, 1], [0, 1]),
          }}
        >
          <svg width="1300" height="450" viewBox="0 0 1300 450">
            {/* Axes */}
            <line x1="100" y1="400" x2="1300" y2="400" stroke="#2D2D2D" strokeWidth="2" opacity="0.4" />
            <line x1="100" y1="40" x2="100" y2="400" stroke="#2D2D2D" strokeWidth="2" opacity="0.4" />

            {/* Grid */}
            {[1, 2, 3].map((g) => (
              <line
                key={g}
                x1="100"
                y1={400 - g * 90}
                x2="1300"
                y2={400 - g * 90}
                stroke="#2D2D2D"
                strokeWidth="1"
                strokeDasharray="4 6"
                opacity="0.2"
              />
            ))}

            {/* Growth path */}
            <path d={pathD} stroke="#FF5722" strokeWidth="8" fill="none" strokeLinecap="round" strokeLinejoin="round" />

            {/* Tip glow */}
            <circle cx={tip.x} cy={tip.y} r="14" fill="#FF5722" opacity="0.3" />
            <circle cx={tip.x} cy={tip.y} r="8" fill="#EEEEEE" />

            {/* Axis labels */}
            <text x="110" y="430" fontFamily="JetBrains Mono, monospace" fontSize="22" fill="#2D2D2D" fontWeight="700">
              DISLOCATION
            </text>
            <text x="1200" y="430" fontFamily="JetBrains Mono, monospace" fontSize="22" fill="#2D2D2D" fontWeight="700">
              DECADES
            </text>
          </svg>
        </div>

        {/* STAT BADGE */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            right: 130,
            opacity: statOp,
            transform: `scale(${0.6 + statScale * 0.4}) rotate(-6deg)`,
            backgroundColor: '#2D2D2D',
            padding: '28px 44px',
            borderRadius: 14,
            boxShadow: '0 10px 0 #FF5722',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 20,
              color: '#A1BC98',
              fontWeight: 700,
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}
          >
            BUILT QUIETLY
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 76,
              color: '#EEEEEE',
              fontWeight: 900,
              lineHeight: 1,
              marginTop: 4,
            }}
          >
            WHEN OTHERS
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 76,
              color: '#FF5722',
              fontWeight: 900,
              lineHeight: 1,
            }}
          >
            PANIC.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(28,28,28,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene53_MoveSix_Generational;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32_BerkshireLoss',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32_BerkshireLoss: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter animation for 1.5B loss
  const counter15 = interpolate(frame, [40, 130], [0, 1.5], { extrapolateRight: 'clamp', easing: Easing.bezier(0.25, 0.1, 0.25, 1) });
  // Counter for 1.9B writedown
  const counter19 = interpolate(frame, [180, 270], [0, 1.9], { extrapolateRight: 'clamp', easing: Easing.bezier(0.25, 0.1, 0.25, 1) });

  const title = sp(8);
  const box1 = sp(35);
  const box2 = sp(170);
  const divider = interpolate(frame, [100, 170], [0, 1], { extrapolateRight: 'clamp' });

  // Screen shake on impact
  const shake1 = frame > 125 && frame < 145 ? Math.sin(frame * 3) * 6 : 0;
  const shake2 = frame > 265 && frame < 285 ? Math.sin(frame * 3) * 8 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp, transform: `translateX(${shake1 + shake2}px)` }}>
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,87,34,0.06) 0%, rgba(161,188,152,0) 50%, rgba(45,45,45,0.18) 100%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: -120, top: 200, width: 500, height: 500,
          backgroundColor: '#FF5722', opacity: 0.04, borderRadius: 30,
          transform: `rotate(${15 + frame * 0.1}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: -80, top: 100, width: 400, height: 400,
          border: '5px solid #2D2D2D', opacity: 0.05, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute', left: '50%', bottom: -200, width: 700, height: 700,
          border: '3px solid #EEEEEE', opacity: 0.06,
          transform: `translateX(-50%) rotate(${frame * 0.15}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture - horizontal lines */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: `${(i / 14) * 100}%`, left: 0, right: 0,
            height: 1, backgroundColor: '#2D2D2D',
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 18 }).map((_, i) => {
          const r = seededRandom(i + 7);
          const r2 = seededRandom(i + 30);
          const drift = Math.cos(frame * 0.025 + i) * 22;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: `${r * 100}%`,
              top: `${r2 * 100}%`,
              width: 3 + r * 5,
              height: 3 + r * 5,
              backgroundColor: '#FF5722',
              borderRadius: '50%',
              opacity: 0.2 + r * 0.3,
              transform: `translateY(${drift}px)`,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Title bar */}
      <div style={{
        position: 'absolute', top: 60, left: 80, right: 80,
        transform: `translateY(${(1 - title) * -30}px)`, opacity: title,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace', fontSize: 24,
          color: '#2D2D2D', letterSpacing: 5, fontWeight: 700, marginBottom: 12,
        }}>
          BERKSHIRE HATHAWAY // POSITION P&L
        </div>
        <div style={{
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 88,
          color: '#2D2D2D', fontWeight: 900, letterSpacing: -2, lineHeight: 1.0,
        }}>
          THE DAMAGE REPORT.
        </div>
      </div>

      {/* Box 1: Total Loss */}
      <div style={{
        position: 'absolute', top: 360, left: 100, width: 820,
        transform: `translateY(${(1 - box1) * 60}px) scale(${0.85 + box1 * 0.15})`, opacity: box1,
      }}>
        <div style={{
          backgroundColor: '#2D2D2D', padding: '40px 50px',
          border: `4px solid ${frame > 125 ? '#FF5722' : '#2D2D2D'}`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 28, color: '#A1BC98',
            letterSpacing: 4, fontWeight: 700, marginBottom: 18,
          }}>
            TOTAL LOSS ABSORBED
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif', fontSize: 180,
            color: '#FF5722', fontWeight: 900, letterSpacing: -4, lineHeight: 1,
          }}>
            ${counter15.toFixed(1)}B
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace', fontSize: 28,
            color: '#EEEEEE', marginTop: 16, fontWeight: 600,
          }}>
            ↓ POSITION CLOSED AT LOSS
          </div>
        </div>
      </div>

      {/* Divider arrow */}
      <div style={{
        position: 'absolute', top: 540, left: 940, width: 80,
        opacity: divider,
      }}>
        <svg width="80" height="60">
          <path d="M 10 30 L 65 30 M 55 18 L 70 30 L 55 42"
            stroke="#2D2D2D" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace', fontSize: 20,
          color: '#2D2D2D', marginTop: 6, letterSpacing: 2, fontWeight: 700,
        }}>DRIVEN BY</div>
      </div>

      {/* Box 2: Writedown */}
      <div style={{
        position: 'absolute', top: 360, right: 100, width: 820,
        transform: `translateY(${(1 - box2) * 60}px) scale(${0.85 + box2 * 0.15})`, opacity: box2,
      }}>
        <div style={{
          backgroundColor: '#FF5722', padding: '40px 50px',
          border: `4px solid ${frame > 265 ? '#2D2D2D' : '#FF5722'}`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 28, color: '#2D2D2D',
            letterSpacing: 4, fontWeight: 700, marginBottom: 18,
          }}>
            STOCK WRITE-DOWN
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif', fontSize: 180,
            color: '#EEEEEE', fontWeight: 900, letterSpacing: -4, lineHeight: 1,
          }}>
            ${counter19.toFixed(1)}B
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace', fontSize: 28,
            color: '#2D2D2D', marginTop: 16, fontWeight: 600,
          }}>
            ↓ CONOCOPHILLIPS // COP
          </div>
        </div>
      </div>

      {/* Bottom takeaway */}
      <div style={{
        position: 'absolute', bottom: 90, left: 80, right: 80, textAlign: 'center',
        opacity: sp(240),
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 38, color: '#2D2D2D',
          fontWeight: 700, letterSpacing: 1,
        }}>
          Even the <span style={{ color: '#FF5722', fontWeight: 900 }}>greatest investor alive</span> got torched.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#2D2D2D', opacity: 0.4, letterSpacing: 1,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene32_BerkshireLoss;
```

```tsx
import React from 'react';
Audio: "Buffett's stepping back — Greg Abel takes the top job as of 2026. Even the all-time greats plan a baton pass instead of timing their exit."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene55_BatonPass_Buffett',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene55_BatonPass_Buffett: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  // Title strip
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);
  const titleY = interpolate(sp(5), [0, 1], [-60, 0]);

  // Left figure (Buffett)
  const leftOp = interpolate(sp(30), [0, 1], [0, 1]);
  const leftX = interpolate(sp(30), [0, 1], [-150, 0]);

  // Right figure (Abel)
  const rightOp = interpolate(sp(50), [0, 1], [0, 1]);
  const rightX = interpolate(sp(50), [0, 1], [150, 0]);

  // Baton animation
  const batonStart = 110;
  const batonEnd = 180;
  const batonProgress = interpolate(frame, [batonStart, batonEnd], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });
  const batonX = interpolate(batonProgress, [0, 1], [620, 1300]);
  const batonRot = interpolate(batonProgress, [0, 1], [-20, 20]);
  const batonY = 540 - Math.sin(batonProgress * Math.PI) * 80;

  // Year tag
  const yearOp = interpolate(sp(200), [0, 1], [0, 1]);
  const yearScale = sp(200);

  // Particles
  const particles = Array.from({ length: 20 }, (_, i) => {
    const seed = i + 21;
    return {
      x: seededRandom(seed) * 1920,
      y: seededRandom(seed + 10) * 1080,
      size: 2 + seededRandom(seed + 30) * 3,
      op: 0.18 + seededRandom(seed + 40) * 0.22,
      drift: Math.sin(frame * 0.02 + seed) * 15,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 50% 60%, rgba(255,87,34,0.14) 0%, rgba(161,188,152,0) 60%), radial-gradient(ellipse at 50% 100%, rgba(45,45,45,0.2) 0%, rgba(161,188,152,0) 55%)',
        }}
      />

      {/* Shape layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 300,
            top: 100,
            width: 380,
            height: 380,
            border: '2px solid #FF5722',
            borderRadius: '50%',
            opacity: 0.07,
            transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 250,
            bottom: 80,
            width: 320,
            height: 320,
            backgroundColor: '#EEEEEE',
            opacity: 0.05,
            transform: `rotate(${frame * 0.1}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Texture — diagonal lines */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              top: 0,
              left: -200 + i * 100,
              width: 1,
              height: 1400,
              backgroundColor: '#2D2D2D',
              transform: 'rotate(20deg)',
              transformOrigin: 'top left',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x + p.drift,
              top: p.y,
              width: p.size,
              height: p.size,
              backgroundColor: '#FF5722',
              borderRadius: '50%',
              opacity: p.op,
            }}
          />
        ))}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `translateY(${titleY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 30,
              color: '#FF5722',
              fontWeight: 800,
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            THE ORACLE'S EXIT PLAN
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 145,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 1,
              marginTop: 14,
              letterSpacing: -2,
            }}
          >
            THE BATON <span style={{ color: '#FF5722' }}>PASS.</span>
          </div>
        </div>

        {/* LEFT — Buffett */}
        <div
          style={{
            position: 'absolute',
            top: 460,
            left: 220,
            opacity: leftOp,
            transform: `translateX(${leftX}px)`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          {/* Portrait box */}
          <div
            style={{
              width: 320,
              height: 320,
              backgroundColor: '#EEEEEE',
              borderRadius: '50%',
              border: '8px solid #FF5722',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: '0 10px 0 #2D2D2D',
              position: 'relative',
            }}
          >
            <svg width="200" height="200" viewBox="0 0 100 100" fill="none">
              <circle cx="50" cy="38" r="22" fill="#2D2D2D" />
              <path d="M15 95 Q15 60 50 60 Q85 60 85 95 Z" fill="#2D2D2D" />
              <rect x="30" y="34" width="40" height="10" rx="2" stroke="#FF5722" strokeWidth="2.5" fill="none" />
              <line x1="40" y1="39" x2="60" y2="39" stroke="#FF5722" strokeWidth="2" />
            </svg>
            <div
              style={{
                position: 'absolute',
                bottom: -22,
                left: '50%',
                transform: 'translateX(-50%)',
                backgroundColor: '#2D2D2D',
                color: '#EEEEEE',
                padding: '8px 22px',
                borderRadius: 8,
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 18,
                fontWeight: 700,
                letterSpacing: 2,
              }}
            >
              STEPS BACK
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 64,
              color: '#2D2D2D',
              fontWeight: 900,
              marginTop: 50,
              letterSpacing: -1,
            }}
          >
            BUFFETT
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 26,
              color: '#FF5722',
              fontWeight: 700,
              letterSpacing: 3,
              marginTop: 4,
            }}
          >
            THE ORACLE
          </div>
        </div>

        {/* BATON in motion */}
        <div
          style={{
            position: 'absolute',
            left: batonX,
            top: batonY,
            transform: `rotate(${batonRot}deg)`,
            width: 180,
            height: 28,
            backgroundColor: '#FF5722',
            borderRadius: 14,
            boxShadow: '0 0 30px rgba(255,87,34,0.6)',
          }}
        />

        {/* RIGHT — Abel */}
        <div
          style={{
            position: 'absolute',
            top: 460,
            right: 220,
            opacity: rightOp,
            transform: `translateX(${rightX}px)`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              width: 320,
              height: 320,
              backgroundColor: '#2D2D2D',
              borderRadius: '50%',
              border: '8px solid #FF5722',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: '0 10px 0 #EEEEEE',
              position: 'relative',
            }}
          >
            <svg width="200" height="200" viewBox="0 0 100 100" fill="none">
              <circle cx="50" cy="38" r="22" fill="#EEEEEE" />
              <path d="M15 95 Q15 60 50 60 Q85 60 85 95 Z" fill="#EEEEEE" />
              <rect x="35" y="58" width="30" height="8" fill="#FF5722" />
            </svg>
            <div
              style={{
                position: 'absolute',
                bottom: -22,
                left: '50%',
                transform: 'translateX(-50%)',
                backgroundColor: '#FF5722',
                color: '#EEEEEE',
                padding: '8px 22px',
                borderRadius: 8,
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 18,
                fontWeight: 700,
                letterSpacing: 2,
              }}
            >
              TAKES OVER
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 64,
              color: '#2D2D2D',
              fontWeight: 900,
              marginTop: 50,
              letterSpacing: -1,
            }}
          >
            GREG ABEL
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 26,
              color: '#FF5722',
              fontWeight: 700,
              letterSpacing: 3,
              marginTop: 4,
            }}
          >
            THE SUCCESSOR
          </div>
        </div>

        {/* YEAR STAMP */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: '50%',
            transform: `translateX(-50%) scale(${0.7 + yearScale * 0.3})`,
            opacity: yearOp,
            backgroundColor: '#FF5722',
            padding: '18px 60px',
            borderRadius: 12,
            boxShadow: '0 10px 0 #2D2D2D',
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 56,
            color: '#EEEEEE',
            fontWeight: 900,
            letterSpacing: 4,
          }}
        >
          EFFECTIVE — JAN 2026
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene55_BatonPass_Buffett;
```

```tsx
import React from 'react';
Audio: "Your portfolio deserves that same calm. I'm Kunal — this is just my read after too many midnight sessions inside the data."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene56_MidnightSessions',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene56_MidnightSessions: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = Math.sin(frame * 0.01) * 0.4;

  // Calm headline
  const calmOp = interpolate(sp(5), [0, 1], [0, 1]);
  const calmY = interpolate(sp(5), [0, 1], [50, 0]);

  // Underline sweep
  const underlineW = interpolate(frame, [40, 90], [0, 100], { extrapolateRight: 'clamp' });

  // Name tag
  const nameOp = interpolate(sp(70), [0, 1], [0, 1]);
  const nameX = interpolate(sp(70), [0, 1], [-100, 0]);

  // Data terminal
  const termOp = interpolate(sp(100), [0, 1], [0, 1]);
  const termY = interpolate(sp(100), [0, 1], [60, 0]);

  // Clock hands
  const clockTick = Math.floor(frame / 8);
  const minuteAngle = (clockTick * 6) % 360;
  const hourAngle = (clockTick * 0.5) % 360;

  // Typing data lines
  const dataLines = [
    '> loading inflation tables...',
    '> parsing cb transcripts...',
    '> scanning crude futures...',
    '> cross-ref Q3 reports...',
    '> compile findings...',
  ];
  const lineRevealStart = 130;

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => {
    const seed = i + 28;
    return {
      x: seededRandom(seed) * 1920,
      y: seededRandom(seed + 10) * 1080,
      size: 2 + seededRandom(seed + 30) * 3,
      op: 0.15 + seededRandom(seed + 40) * 0.25,
      drift: Math.cos(frame * 0.02 + seed) * 12,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 70% 30%, rgba(255,87,34,0.10) 0%, rgba(161,188,152,0) 55%), radial-gradient(ellipse at 30% 80%, rgba(45,45,45,0.20) 0%, rgba(161,188,152,0) 60%)',
        }}
      />

      {/* Shape layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: -120,
            top: 200,
            width: 480,
            height: 480,
            border: '2px solid #2D2D2D',
            borderRadius: '50%',
            opacity: 0.07,
            transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 100,
            bottom: 150,
            width: 280,
            height: 280,
            backgroundColor: '#FF5722',
            opacity: 0.05,
            transform: `rotate(${frame * 0.12}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Texture — fine grid */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <div key={`v${i}`} style={{ position: 'absolute', left: i * 80, top: 0, bottom: 0, width: 1, backgroundColor: '#2D2D2D' }} />
        ))}
        {Array.from({ length: 14 }).map((_, i) => (
          <div key={`h${i}`} style={{ position: 'absolute', top: i * 80, left: 0, right: 0, height: 1, backgroundColor: '#2D2D2D' }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x + p.drift,
              top: p.y,
              width: p.size,
              height: p.size,
              backgroundColor: '#EEEEEE',
              borderRadius: '50%',
              opacity: p.op,
            }}
          />
        ))}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)` }}>
        {/* CALM HEADLINE */}
        <div
          style={{
            position: 'absolute',
            top: 130,
            left: 110,
            right: 110,
            opacity: calmOp,
            transform: `translateY(${calmY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 145,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 0.95,
              letterSpacing: -2,
            }}
          >
            YOUR PORTFOLIO
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 145,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 0.95,
              letterSpacing: -2,
            }}
          >
            DESERVES THAT
          </div>
          <div
            style={{
              display: 'inline-block',
              position: 'relative',
              marginTop: 8,
            }}
          >
            <div
              style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 180,
                fontWeight: 900,
                color: '#FF5722',
                lineHeight: 0.95,
                letterSpacing: -3,
              }}
            >
              SAME CALM.
            </div>
            <div
              style={{
                position: 'absolute',
                bottom: 8,
                left: 0,
                width: `${underlineW}%`,
                height: 14,
                backgroundColor: '#2D2D2D',
              }}
            />
          </div>
        </div>

        {/* NAME TAG bottom left */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: 110,
            opacity: nameOp,
            transform: `translateX(${nameX}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div
            style={{
              width: 90,
              height: 90,
              borderRadius: '50%',
              backgroundColor: '#2D2D2D',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 44,
              color: '#FF5722',
              fontWeight: 900,
              border: '4px solid #FF5722',
            }}
          >
            K
          </div>
          <div>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 22,
                color: '#FF5722',
                fontWeight: 700,
                letterSpacing: 3,
                textTransform: 'uppercase',
              }}
            >
              SIGNED
            </div>
            <div
              style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 52,
                color: '#2D2D2D',
                fontWeight: 900,
                letterSpacing: -1,
              }}
            >
              — KUNAL
            </div>
          </div>
        </div>

        {/* DATA TERMINAL — right side */}
        <div
          style={{
            position: 'absolute',
            top: 260,
            right: 110,
            width: 540,
            height: 420,
            opacity: termOp,
            transform: `translateY(${termY}px)`,
            backgroundColor: '#2D2D2D',
            borderRadius: 14,
            padding: 26,
            boxShadow: '0 10px 0 #FF5722',
            display: 'flex',
            flexDirection: 'column',
          }}
        >
          {/* Header bar */}
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 18 }}>
            <div style={{ display: 'flex', gap: 8 }}>
              <div style={{ width: 14, height: 14, borderRadius: '50%', backgroundColor: '#FF5722' }} />
              <div style={{ width: 14, height: 14, borderRadius: '50%', backgroundColor: '#A1BC98' }} />
              <div style={{ width: 14, height: 14, borderRadius: '50%', backgroundColor: '#EEEEEE' }} />
            </div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 18, color: '#EEEEEE', fontWeight: 600, letterSpacing: 2 }}>
              midnight_session.log
            </div>
          </div>

          {/* Terminal lines */}
          <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 14 }}>
            {dataLines.map((line, i) => {
              const lineOp = interpolate(
                frame,
                [lineRevealStart + i * 22, lineRevealStart + i * 22 + 10],
                [0, 1],
                { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
              );
              return (
                <div
                  key={i}
                  style={{
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 26,
                    color: i === dataLines.length - 1 ? '#FF5722' : '#EEEEEE',
                    fontWeight: 600,
                    opacity: lineOp,
                  }}
                >
                  {line}
                </div>
              );
            })}

            {/* Blinking cursor */}
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 26,
                color: '#FF5722',
                fontWeight: 700,
                opacity: Math.floor(frame / 15) % 2 === 0 ? 1 : 0,
              }}
            >
              _
            </div>
          </div>

          {/* Clock at bottom */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginTop: 14 }}>
            <svg width="50" height="50" viewBox="0 0 50 50">
              <circle cx="25" cy="25" r="22" stroke="#EEEEEE" strokeWidth="2" fill="none" />
              <line
                x1="25"
                y1="25"
                x2={25 + Math.sin((hourAngle * Math.PI) / 180) * 10}
                y2={25 - Math.cos((hourAngle * Math.PI) / 180) * 10}
                stroke="#FF5722"
                strokeWidth="3"
                strokeLinecap="round"
              />
              <line
                x1="25"
                y1="25"
                x2={25 + Math.sin((minuteAngle * Math.PI) / 180) * 16}
                y2={25 - Math.cos((minuteAngle * Math.PI) / 180) * 16}
                stroke="#EEEEEE"
                strokeWidth="2"
                strokeLinecap="round"
              />
            </svg>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 22, color: '#A1BC98', fontWeight: 700, letterSpacing: 2 }}>
              03:47 AM
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene56_MidnightSessions;
```

```tsx
import React from 'react';
Audio: "None of it is tailored advice for your exact life — sit down with a qualified planner before rearranging anything serious in a market this jumpy."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene57_NotAdvice_Planner',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene57_NotAdvice_Planner: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera shake on impact at frame 0 then settle
  const shakeX = frame < 12 ? Math.sin(frame * 2) * 6 : 0;
  const shakeY = frame < 12 ? Math.cos(frame * 2) * 4 : 0;

  // Warning stamp
  const stampOp = interpolate(sp(5), [0, 1], [0, 1]);
  const stampScale = sp(5, { damping: 8 });
  const stampRot = interpolate(sp(5), [0, 1], [-15, -4]);

  // Big headline
  const headOp = interpolate(sp(35), [0, 1], [0, 1]);
  const headY = interpolate(sp(35), [0, 1], [50, 0]);

  // Volatility waveform
  const waveProgress = interpolate(frame, [70, durationInFrames], [0, 1]);
  const wavePoints: { x: number; y: number }[] = [];
  for (let i = 0; i <= 80; i++) {
    const t = i / 80;
    const baseY = 90;
    const noise = Math.sin(t * 25 + frame * 0.08) * 35 + Math.sin(t * 13) * 22;
    wavePoints.push({ x: t * 1700, y: baseY + noise });
  }
  const waveCount = Math.floor(wavePoints.length * waveProgress);
  const waveD = wavePoints
    .slice(0, waveCount + 1)
    .map((p, i) => `${i === 0 ? 'M' : 'L'} ${p.x} ${p.y}`)
    .join(' ');

  // Planner badge
  const plannerOp = interpolate(sp(140), [0, 1], [0, 1]);
  const plannerY = interpolate(sp(140), [0, 1], [80, 0]);

  // Particles
  const particles = Array.from({ length: 26 }, (_, i) => {
    const seed = i + 33;
    return {
      x: seededRandom(seed) * 1920,
      y: seededRandom(seed + 10) * 1080,
      size: 2 + seededRandom(seed + 30) * 3,
      op: 0.15 + seededRandom(seed + 40) * 0.25,
      drift: Math.sin(frame * 0.025 + seed) * 18,
    };
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#A1BC98',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `translate(${shakeX}px, ${shakeY}px)`,
      }}
    >
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 50% 50%, rgba(255,87,34,0.15) 0%, rgba(161,188,152,0) 60%), linear-gradient(180deg, rgba(45,45,45,0.10) 0%, transparent 100%)',
        }}
      />

      {/* Shape layer — warning triangles drifting */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 1500,
            top: 100,
            width: 0,
            height: 0,
            borderLeft: '140px solid transparent',
            borderRight: '140px solid transparent',
            borderBottom: '240px solid #FF5722',
            opacity: 0.06,
            transform: `rotate(${frame * 0.15}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 80,
            bottom: 100,
            width: 320,
            height: 320,
            border: '3px solid #2D2D2D',
            opacity: 0.07,
            transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />
      </AbsoluteFill>

      {/* Texture — caution stripes top and bottom */}
      <div
        style={{
          position: 'absolute',
          top: 0,
          left: 0,
          right: 0,
          height: 28,
          background: 'repeating-linear-gradient(45deg, #2D2D2D 0 18px, #FF5722 18px 36px)',
          opacity: 0.7,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 0,
          left: 0,
          right: 0,
          height: 28,
          background: 'repeating-linear-gradient(45deg, #2D2D2D 0 18px, #FF5722 18px 36px)',
          opacity: 0.7,
        }}
      />

      {/* Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x + p.drift,
              top: p.y,
              width: p.size,
              height: p.size,
              backgroundColor: '#FF5722',
              borderRadius: '50%',
              opacity: p.op,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* WARNING STAMP — top right */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          right: 130,
          opacity: stampOp,
          transform: `scale(${0.6 + stampScale * 0.4}) rotate(${stampRot}deg)`,
          padding: '20px 38px',
          border: '6px solid #FF5722',
          backgroundColor: 'transparent',
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 42,
          fontWeight: 900,
          color: '#FF5722',
          letterSpacing: 5,
        }}
      >
        NOT ADVICE
      </div>

      {/* MAIN HEADLINE */}
      <div
        style={{
          position: 'absolute',
          top: 240,
          left: 110,
          right: 110,
          opacity: headOp,
          transform: `translateY(${headY}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 32,
            color: '#2D2D2D',
            fontWeight: 800,
            letterSpacing: 6,
            textTransform: 'uppercase',
          }}
        >
          BEFORE YOU MOVE A DOLLAR —
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 155,
            fontWeight: 900,
            color: '#2D2D2D',
            lineHeight: 0.95,
            letterSpacing: -2,
            marginTop: 12,
          }}
        >
          SIT DOWN WITH
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 170,
            fontWeight: 900,
            color: '#FF5722',
            lineHeight: 0.95,
            letterSpacing: -2,
          }}
        >
          A QUALIFIED PLANNER.
        </div>
      </div>

      {/* VOLATILITY WAVEFORM — bottom */}
      <div
        style={{
          position: 'absolute',
          bottom: 220,
          left: 110,
          width: 1700,
          height: 220,
        }}
      >
        <svg width="1700" height="220" viewBox="0 0 1700 220">
          <path d={waveD} stroke="#2D2D2D" strokeWidth="4" fill="none" strokeLinecap="round" />
        </svg>
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 30,
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#2D2D2D',
            fontWeight: 700,
            letterSpacing: 3,
          }}
        >
          MARKET = JUMPY
        </div>
      </div>

      {/* PLANNER BADGE — bottom left */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: 110,
          opacity: plannerOp,
          transform: `translateY(${plannerY}px)`,
          display: 'flex',
          alignItems: 'center',
          gap: 22,
          backgroundColor: '#2D2D2D',
          padding: '20px 36px',
          borderRadius: 14,
          boxShadow: '0 8px 0 #FF5722',
        }}
      >
        <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
          <path d="M12 2L4 5v6c0 5 3.5 9.5 8 11 4.5-1.5 8-6 8-11V5l-8-3z" stroke="#FF5722" strokeWidth="2.4" strokeLinejoin="round" />
          <path d="M9 12l2 2 4-4" stroke="#FF5722" strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
        <div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 20,
              color: '#A1BC98',
              fontWeight: 700,
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}
          >
            FIDUCIARY · LICENSED · ACCOUNTABLE
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 38,
              color: '#EEEEEE',
              fontWeight: 900,
              letterSpacing: -0.5,
            }}
          >
            LOOP THEM IN FIRST.
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(28,28,28,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene57_NotAdvice_Planner;
```

```tsx
import React from 'react';
Audio: "Tell me below what's actually keeping you up — pump prices, grocery bill, your job, your retirement clock?"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene58_KeepsYouUp_Comments',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene58_KeepsYouUp_Comments: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan up
  const camY = interpolate(frame, [0, durationInFrames], [20, -10]);

  // Question
  const qOp = interpolate(sp(5), [0, 1], [0, 1]);
  const qY = interpolate(sp(5), [0, 1], [-60, 0]);

  // Four option cards
  const options = [
    { label: 'PUMP PRICES', icon: 'fuel' },
    { label: 'GROCERY BILL', icon: 'cart' },
    { label: 'YOUR JOB', icon: 'briefcase' },
    { label: 'RETIREMENT', icon: 'clock' },
  ];

  // Comment box bottom
  const commentOp = interpolate(sp(180), [0, 1], [0, 1]);
  const commentScale = sp(180);

  // Cursor blink
  const cursorOn = Math.floor(frame / 15) % 2 === 0;

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => {
    const seed = i + 41;
    return {
      x: seededRandom(seed) * 1920,
      y: seededRandom(seed + 10) * 1080,
      size: 2 + seededRandom(seed + 30) * 3,
      op: 0.18 + seededRandom(seed + 40) * 0.22,
      drift: Math.cos(frame * 0.022 + seed) * 15,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 50% 30%, rgba(255,87,34,0.13) 0%, rgba(161,188,152,0) 55%), radial-gradient(ellipse at 50% 100%, rgba(45,45,45,0.18) 0%, rgba(161,188,152,0) 60%)',
        }}
      />

      {/* Shape layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: -120,
            top: 100,
            width: 360,
            height: 360,
            border: '3px solid #FF5722',
            opacity: 0.06,
            transform: `rotate(${frame * 0.12}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: -80,
            bottom: 200,
            width: 420,
            height: 420,
            backgroundColor: '#EEEEEE',
            borderRadius: '50%',
            opacity: 0.06,
            transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
          }}
        />
      </AbsoluteFill>

      {/* Texture — dotted grid */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {Array.from({ length: 22 }).map((_, i) =>
          Array.from({ length: 12 }).map((_, j) => (
            <div
              key={`${i}-${j}`}
              style={{
                position: 'absolute',
                left: i * 90 + 40,
                top: j * 90 + 40,
                width: 3,
                height: 3,
                backgroundColor: '#2D2D2D',
                borderRadius: '50%',
              }}
            />
          ))
        )}
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x + p.drift,
              top: p.y,
              width: p.size,
              height: p.size,
              backgroundColor: '#FF5722',
              borderRadius: '50%',
              opacity: p.op,
            }}
          />
        ))}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* QUESTION */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: qOp,
            transform: `translateY(${qY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 32,
              color: '#FF5722',
              fontWeight: 800,
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            DROP IT IN THE COMMENTS —
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 140,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 0.95,
              marginTop: 12,
              letterSpacing: -2,
            }}
          >
            WHAT'S KEEPING
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 165,
              fontWeight: 900,
              color: '#FF5722',
              lineHeight: 0.95,
              letterSpacing: -3,
            }}
          >
            YOU UP?
          </div>
        </div>

        {/* OPTIONS GRID */}
        <div
          style={{
            position: 'absolute',
            top: 510,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 28,
            padding: '0 110px',
          }}
        >
          {options.map((opt, i) => {
            const op = interpolate(sp(40 + i * 18), [0, 1], [0, 1]);
            const y = interpolate(sp(40 + i * 18), [0, 1], [100, 0]);
            const hoverBob = Math.sin(frame * 0.06 + i) * 4;
            return (
              <div
                key={i}
                style={{
                  width: 380,
                  height: 280,
                  backgroundColor: '#EEEEEE',
                  borderRadius: 16,
                  padding: 28,
                  display: 'flex',
                  flexDirection: 'column',
                  justifyContent: 'space-between',
                  opacity: op,
                  transform: `translateY(${y + hoverBob}px)`,
                  boxShadow: '0 12px 0 #2D2D2D',
                  position: 'relative',
                }}
              >
                <div
                  style={{
                    width: 78,
                    height: 78,
                    backgroundColor: '#FF5722',
                    borderRadius: 12,
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                  }}
                >
                  {opt.icon === 'fuel' && (
                    <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                      <path d="M3 21V5a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v16M3 21h12M3 13h12M15 9l3 3v6a2 2 0 0 0 2 2 2 2 0 0 0 2-2v-8l-4-4" stroke="#EEEEEE" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
                    </svg>
                  )}
                  {opt.icon === 'cart' && (
                    <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                      <circle cx="9" cy="20" r="1.5" fill="#EEEEEE"/>
                      <circle cx="18" cy="20" r="1.5" fill="#EEEEEE"/>
                      <path d="M2 3h3l2.5 12h12l2.5-9H6" stroke="#EEEEEE" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"/>
                    </svg>
                  )}
                  {opt.icon === 'briefcase' && (
                    <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                      <rect x="2" y="7" width="20" height="14" rx="2" stroke="#EEEEEE" strokeWidth="2.2"/>
                      <path d="M8 7V4h8v3" stroke="#EEEEEE" strokeWidth="2.2" strokeLinecap="round"/>
                    </svg>
                  )}
                  {opt.icon === 'clock' && (
                    <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                      <circle cx="12" cy="12" r="10" stroke="#EEEEEE" strokeWidth="2.2"/>
                      <path d="M12 6v6l4 2" stroke="#EEEEEE" strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round"/>
                    </svg>
                  )}
                </div>

                <div>
                  <div
                    style={{
                      fontFamily: 'Mont Heavy, sans-serif',
                      fontSize: 44,
                      fontWeight: 900,
                      color: '#2D2D2D',
                      lineHeight: 1,
                      letterSpacing: -1,
                    }}
                  >
                    {opt.label}
                  </div>
                  <div
                    style={{
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: 24,
                      color: '#FF5722',
                      fontWeight: 700,
                      marginTop: 10,
                      letterSpacing: 2,
                    }}
                  >
                    0{i + 1} ▸
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        {/* COMMENT INPUT BAR */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 110,
            right: 110,
            opacity: commentOp,
            transform: `scale(${0.92 + commentScale * 0.08})`,
            height: 100,
            backgroundColor: '#2D2D2D',
            borderRadius: 50,
            padding: '0 36px',
            display: 'flex',
            alignItems: 'center',
            gap: 22,
            boxShadow: '0 10px 0 #FF5722',
          }}
        >
          <svg width="42" height="42" viewBox="0 0 24 24" fill="none">
            <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z" stroke="#FF5722" strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 36,
              color: '#EEEEEE',
              fontWeight: 600,
              flex: 1,
              letterSpacing: 0.5,
            }}
          >
            Type your answer here{cursorOn ? '|' : ' '}
          </div>
          <div
            style={{
              backgroundColor: '#FF5722',
              color: '#EEEEEE',
              padding: '14px 32px',
              borderRadius: 40,
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 28,
              fontWeight: 900,
              letterSpacing: 2,
            }}
          >
            POST
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.45) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene58_KeepsYouUp_Comments;
```

```tsx
import React from 'react';
Audio: "If this earned a spot on your list — the single best thing you can do is forward it to one person quietly stressed about money right now."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene59_ForwardToOne',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene59_ForwardToOne: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.06]);

  // Headline
  const headOp = interpolate(sp(5), [0, 1], [0, 1]);
  const headY = interpolate(sp(5), [0, 1], [-60, 0]);

  // Sender node
  const senderOp = interpolate(sp(40), [0, 1], [0, 1]);
  const senderScale = sp(40);

  // Receiver node
  const receiverOp = interpolate(sp(60), [0, 1], [0, 1]);
  const receiverScale = sp(60);

  // Arrow/message in motion
  const sendStart = 110;
  const sendEnd = 200;
  const sendProgress = interpolate(frame, [sendStart, sendEnd], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });
  const msgX = interpolate(sendProgress, [0, 1], [460, 1320]);
  const msgY = 590 - Math.sin(sendProgress * Math.PI) * 60;
  const msgScale = interpolate(sendProgress, [0, 0.5, 1], [0.7, 1.1, 0.9]);

  // Ripple on arrival
  const arrived = frame > sendEnd;
  const rippleSize = arrived ? interpolate(frame, [sendEnd, sendEnd + 35], [0, 280], { extrapolateRight: 'clamp' }) : 0;
  const rippleOp = arrived ? interpolate(frame, [sendEnd, sendEnd + 35], [0.7, 0], { extrapolateRight: 'clamp' }) : 0;

  // CTA bottom
  const ctaOp = interpolate(sp(230), [0, 1], [0, 1]);
  const ctaY = interpolate(sp(230), [0, 1], [50, 0]);

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => {
    const seed = i + 47;
    return {
      x: seededRandom(seed) * 1920,
      y: seededRandom(seed + 10) * 1080,
      size: 2 + seededRandom(seed + 30) * 3,
      op: 0.18 + seededRandom(seed + 40) * 0.22,
      drift: Math.sin(frame * 0.02 + seed) * 16,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 50% 60%, rgba(255,87,34,0.14) 0%, rgba(161,188,152,0) 60%), radial-gradient(ellipse at 80% 20%, rgba(238,238,238,0.18) 0%, rgba(161,188,152,0) 55%)',
        }}
      />

      {/* Shape layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 200,
            top: 80,
            width: 340,
            height: 340,
            backgroundColor: '#FF5722',
            opacity: 0.06,
            borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 100,
            bottom: 120,
            width: 360,
            height: 360,
            border: '3px solid #2D2D2D',
            opacity: 0.06,
            transform: `rotate(${frame * 0.1}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Texture — concentric arcs */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: '50%',
              top: '50%',
              width: 300 + i * 160,
              height: 300 + i * 160,
              border: '1px dashed #2D2D2D',
              borderRadius: '50%',
              transform: 'translate(-50%, -50%)',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x + p.drift,
              top: p.y,
              width: p.size,
              height: p.size,
              backgroundColor: '#EEEEEE',
              borderRadius: '50%',
              opacity: p.op,
            }}
          />
        ))}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* HEADLINE */}
        <div
          style={{
            position: 'absolute',
            top: 100,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headOp,
            transform: `translateY(${headY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 32,
              color: '#FF5722',
              fontWeight: 800,
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            THE SINGLE BEST THING YOU CAN DO —
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 160,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 0.95,
              marginTop: 14,
              letterSpacing: -2,
            }}
          >
            FORWARD THIS TO
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 180,
              fontWeight: 900,
              color: '#FF5722',
              lineHeight: 0.95,
              letterSpacing: -3,
            }}
          >
            ONE PERSON.
          </div>
        </div>

        {/* SENDER NODE — left */}
        <div
          style={{
            position: 'absolute',
            top: 570,
            left: 220,
            opacity: senderOp,
            transform: `scale(${senderScale})`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 14,
          }}
        >
          <div
            style={{
              width: 180,
              height: 180,
              borderRadius: '50%',
              backgroundColor: '#2D2D2D',
              border: '6px solid #FF5722',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: '0 8px 0 #FF5722',
            }}
          >
            <svg width="100" height="100" viewBox="0 0 100 100" fill="none">
              <circle cx="50" cy="38" r="20" fill="#EEEEEE" />
              <path d="M18 92 Q18 60 50 60 Q82 60 82 92 Z" fill="#EEEEEE" />
            </svg>
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#2D2D2D',
              fontWeight: 800,
              letterSpacing: 2,
            }}
          >
            YOU
          </div>
        </div>

        {/* MESSAGE in motion */}
        <div
          style={{
            position: 'absolute',
            left: msgX,
            top: msgY,
            transform: `scale(${msgScale})`,
            width: 120,
            height: 90,
            backgroundColor: '#FF5722',
            borderRadius: 16,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            boxShadow: '0 0 40px rgba(255,87,34,0.5)',
          }}
        >
          <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
            <path d="M22 2L11 13M22 2l-7 20-4-9-9-4 20-7z" stroke="#EEEEEE" strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* RECEIVER NODE — right */}
        <div
          style={{
            position: 'absolute',
            top: 570,
            right: 220,
            opacity: receiverOp,
            transform: `scale(${receiverScale})`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 14,
          }}
        >
          <div style={{ position: 'relative' }}>
            {/* Ripple */}
            {arrived && (
              <div
                style={{
                  position: 'absolute',
                  left: '50%',
                  top: '50%',
                  width: rippleSize,
                  height: rippleSize,
                  border: '4px solid #FF5722',
                  borderRadius: '50%',
                  transform: 'translate(-50%, -50%)',
                  opacity: rippleOp,
                }}
              />
            )}
            <div
              style={{
                width: 180,
                height: 180,
                borderRadius: '50%',
                backgroundColor: '#EEEEEE',
                border: '6px solid #FF5722',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                boxShadow: '0 8px 0 #2D2D2D',
              }}
            >
              <svg width="100" height="100" viewBox="0 0 100 100" fill="none">
                <circle cx="50" cy="38" r="20" fill="#2D2D2D" />
                <path d="M18 92 Q18 60 50 60 Q82 60 82 92 Z" fill="#2D2D2D" />
              </svg>
            </div>
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#2D2D2D',
              fontWeight: 800,
              letterSpacing: 2,
              textAlign: 'center',
              maxWidth: 280,
              lineHeight: 1.2,
            }}
          >
            SOMEONE QUIETLY<br />STRESSED
          </div>
        </div>

        {/* CTA BAR */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: '50%',
            transform: `translateX(-50%) translateY(${ctaY}px)`,
            opacity: ctaOp,
            backgroundColor: '#2D2D2D',
            padding: '22px 60px',
            borderRadius: 14,
            boxShadow: '0 10px 0 #FF5722',
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 50,
            color: '#EEEEEE',
            fontWeight: 900,
            letterSpacing: -0.5,
          }}
        >
          ONE SHARE = ONE LIFELINE.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene59_ForwardToOne;
```

```tsx
import React from 'react';
Audio: "Regular households don't survive a setup like this by panicking in isolation. They survive by passing real information hand-to-hand."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene60_HandToHand_Close',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene60_HandToHand_Close: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow push
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // Opening line — "panic in isolation" crossed out
  const op1 = interpolate(sp(5), [0, 1], [0, 1]);
  const strikeW = interpolate(frame, [50, 110], [0, 100], { extrapolateRight: 'clamp' });

  // Main statement
  const op2 = interpolate(sp(120), [0, 1], [0, 1]);
  const y2 = interpolate(sp(120), [0, 1], [50, 0]);

  // Chain of nodes — hand-to-hand
  const chainStart = 170;
  const nodeCount = 5;

  // Brand signoff
  const brandOp = interpolate(sp(290), [0, 1], [0, 1]);
  const brandScale = sp(290);

  // Particles
  const particles = Array.from({ length: 28 }, (_, i) => {
    const seed = i + 55;
    return {
      x: seededRandom(seed) * 1920,
      y: seededRandom(seed + 10) * 1080,
      size: 2 + seededRandom(seed + 30) * 3,
      op: 0.18 + seededRandom(seed + 40) * 0.22,
      drift: Math.cos(frame * 0.022 + seed) * 16,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 50% 50%, rgba(255,87,34,0.15) 0%, rgba(161,188,152,0) 60%), radial-gradient(ellipse at 50% 100%, rgba(45,45,45,0.22) 0%, rgba(161,188,152,0) 60%)',
        }}
      />

      {/* Shape layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: -150,
            top: 80,
            width: 460,
            height: 460,
            border: '3px solid #FF5722',
            borderRadius: '50%',
            opacity: 0.06,
            transform: `scale(${1 + Math.sin(frame * 0.022) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: -120,
            top: 600,
            width: 480,
            height: 480,
            backgroundColor: '#EEEEEE',
            opacity: 0.06,
            borderRadius: '50%',
            transform: `scale(${1 + Math.cos(frame * 0.02) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 800,
            top: 100,
            width: 0,
            height: 0,
            borderLeft: '100px solid transparent',
            borderRight: '100px solid transparent',
            borderBottom: '180px solid #FF5722',
            opacity: 0.05,
            transform: `rotate(${frame * 0.1}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Texture — diagonal stripes */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: -200 + i * 120,
              top: 0,
              width: 1,
              height: 1400,
              backgroundColor: '#2D2D2D',
              transform: 'rotate(25deg)',
              transformOrigin: 'top left',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x + p.drift,
              top: p.y,
              width: p.size,
              height: p.size,
              backgroundColor: '#FF5722',
              borderRadius: '50%',
              opacity: p.op,
            }}
          />
        ))}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* OPENING LINE — crossed out */}
        <div
          style={{
            position: 'absolute',
            top: 110,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: op1,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              position: 'relative',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 76,
              fontWeight: 900,
              color: '#2D2D2D',
              letterSpacing: -1,
            }}
          >
            NOT BY PANICKING IN ISOLATION.
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                width: `${strikeW}%`,
                height: 8,
                backgroundColor: '#FF5722',
              }}
            />
          </div>
        </div>

        {/* MAIN STATEMENT */}
        <div
          style={{
            position: 'absolute',
            top: 270,
            left: 110,
            right: 110,
            textAlign: 'center',
            opacity: op2,
            transform: `translateY(${y2}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 130,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 0.95,
              letterSpacing: -2,
            }}
          >
            THEY SURVIVE BY PASSING
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 175,
              fontWeight: 900,
              color: '#FF5722',
              lineHeight: 0.95,
              letterSpacing: -3,
              marginTop: 10,
            }}
          >
            HAND — TO — HAND.
          </div>
        </div>

        {/* CHAIN OF NODES */}
        <div
          style={{
            position: 'absolute',
            bottom: 240,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 30,
          }}
        >
          {Array.from({ length: nodeCount }).map((_, i) => {
            const nodeOp = interpolate(
              frame,
              [chainStart + i * 20, chainStart + i * 20 + 15],
              [0, 1],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            );
            const nodeScale = interpolate(
              frame,
              [chainStart + i * 20, chainStart + i * 20 + 15],
              [0, 1],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            );
            const pulse = 1 + Math.sin(frame * 0.1 + i * 0.6) * 0.04;
            return (
              <React.Fragment key={i}>
                <div
                  style={{
                    width: 110,
                    height: 110,
                    borderRadius: '50%',
                    backgroundColor: i % 2 === 0 ? '#2D2D2D' : '#FF5722',
                    border: '5px solid #EEEEEE',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    opacity: nodeOp,
                    transform: `scale(${nodeScale * pulse})`,
                    boxShadow: '0 6px 0 #2D2D2D',
                  }}
                >
                  <svg width="50" height="50" viewBox="0 0 100 100" fill="none">
                    <circle cx="50" cy="36" r="18" fill="#EEEEEE" />
                    <path d="M20 92 Q20 60 50 60 Q80 60 80 92 Z" fill="#EEEEEE" />
                  </svg>
                </div>
                {i < nodeCount - 1 && (
                  <div
                    style={{
                      width: 80,
                      height: 6,
                      backgroundColor: '#2D2D2D',
                      borderRadius: 3,
                      opacity: interpolate(
                        frame,
                        [chainStart + i * 20 + 8, chainStart + i * 20 + 22],
                        [0, 1],
                        { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
                      ),
                    }}
                  />
                )}
              </React.Fragment>
            );
          })}
        </div>

        {/* BRAND SIGNOFF */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: brandOp,
            transform: `scale(${0.85 + brandScale * 0.15})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 26,
              color: '#FF5722',
              fontWeight: 700,
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            — FAILED FINANCES —
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 62,
              color: '#2D2D2D',
              fontWeight: 900,
              marginTop: 4,
              letterSpacing: -1,
            }}
          >
            REAL INFO. NO SEDATION.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(28,28,28,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene60_HandToHand_Close;
```

